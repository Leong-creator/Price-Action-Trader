# Golden Case Fixtures

本目录存放 M8B knowledge alignment 的最小 synthetic fixtures 说明。

约束：

- fixture 只用于验证 knowledge boundary、source_refs、explanation 与保守输出。
- fixture 必须显式视为 synthetic，不得包装成真实历史行情、真实回测或收益结论。
- 若后续新增 CSV/JSON，仅代表本地 fixture，不代表 curated real-history dataset。
