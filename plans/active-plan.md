# 当前执行计划

## 目标

把 M15 长桥模拟账户行情固定为官方 `longbridge==4.5.0` SDK 的单一长期 WebSocket 连接。先证明147只生产池的盘中五分钟K线完整、及时且可审计，再恢复模拟账户新开仓。策略、仓位、止损、目标和策略名单在行情验收完成前冻结。

## 唯一现行方案

1. 行情仅由一个官方 SDK `QuoteContext` 连接提供，同一连接订阅147只标的的 `Quote` 和 `Trade` 推送。
2. 回调先注册再订阅；回调只规范化数据并写入有界内存队列，五分钟聚合、策略、风控、订单和审计在回调外顺序执行。
3. 启动时通过同一个行情连接读取60根日线和一次初始快照。日线与快照只负责初始化，不得触发开仓。
4. 账户、持仓和订单使用独立的官方 SDK 交易上下文，只允许 `lb_papertrading`。
5. `longbridge serve`、行情CLI、快照轮询、第二行情连接、自动重试、自动重连和行情回退全部退出生产路径。
6. 任一订阅缺失、回调溢出、行情静默、K线边界缺失或行情进程退出，立即进入 `fault_halted`，停止新开仓并保留证据。修复原因后才允许人工重新启动。

## 当前证据

- 2026-08-28 官方 SDK 147只实机只读探针连续三次完成147/147订阅；最近一次30秒收到104条报价和104条逐笔成交，覆盖38个当时活跃标的，订阅缺失为0。
- 正式运行入口只读冒烟完成8820/8820条日线、147/147初始快照、模拟账户验证和新鲜账户快照。
- 聚焦 M15 回归共517项通过；官方 SDK 行情传输、行情门禁、看护、人工停止状态和清仓辅助路径均有独立单测。
- 行情整改、看护阶段判断、人工停止状态和完整日线缓存复用修复均已分支提交、推送并合并到 `main`；部署清单、正式配置和源码哈希验证无漂移。
- 正式只读运行已完成147/147订阅、8820/8820日线、147/147初始快照和9/9已有持仓额外监控；完整且为最新交易日的官方 SDK 日线缓存可以复用，不完整缓存立即停机；派发未请求，未创建订单。
- 实机冒烟发生在非完整交易时段，不能替代完整交易日K线验收。
- 长桥账户当前已有持仓未在本轮修改中处理；本轮没有制造、提交或撤销任何订单。

## 下一验收

1. 下一完整美股交易日只读运行，要求147/147订阅、78/78个五分钟边界、11466/11466根实时K线、盘后补录0、行情重启0、账户快照小于45秒、信号处理95分位小于1秒。
2. 一个完整交易时段通过后，下一交易日恢复长桥模拟账户新开仓。
3. 连续三个完整交易日通过后，才宣布行情层稳定并在独立分支开始300只股票池只读验收。

## 禁止事项

- 禁止使用行情CLI、`longbridge serve`、快照轮询、历史补录或本地模拟数据触发长桥订单。
- 禁止在行情失败后自动切换传输方式、自动重试、自动重连或使用旧值继续开仓。
- 禁止第二个行情连接、实盘、真实资金、期权、融资和未批准的策略改动。
- 禁止交易时段编辑生产源码；故障时先停止新开仓并保存证据。
- 禁止直接在 `main` 开发；每个主题必须从最新 `main` 建分支、推送远端、测试后合并。

## 完成标准

只有完整交易日验收产物通过，才能恢复模拟新开仓；只有连续三个完整交易日通过，才能宣布本轮行情整改稳定。
