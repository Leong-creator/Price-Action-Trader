# Price Action Trader Active Plan（V2 可执行版）

> 本文件日期统一使用项目运行时区 `Asia/Shanghai`（北京时间）。

## 1. 当前状态

- 2026-08-12：盘中巡检继续收口 SDK 行情子进程恢复。此前主进程只要累计重建达到 3 次，后续即使行情已连续稳定，也会永久采用最长 `60s` 无进度期限，导致新的原生行情请求卡住时恢复过慢。现在只按最近 `120s` 的真实重建突发决定是否临时放宽；替换后的行情进程连续完成 3 轮快速、完整的 `300/300` 快照后立即结束突发退避并恢复 `8s` 切断。看护器不再把“快照后备已经返回明确进度、账户快照仍健康”的恢复过程误判为主进程死亡；只有完全无行情进度或状态/账户停止推进才替换主进程。巡检 `--check` 在同配置生产进程切换期间只报告连接等待，不再建立第二个行情上下文与生产链路竞争。今天早先缺失的 4 个五分钟边界及源码指纹触发的受控重载继续如实保留，因此本交易日不能计为视觉策略完整影子日；8 条正式合同仍按各自连续上下文独立恢复，不修改策略、仓位或风控。

- 2026-08-11：完成 M15 行情完整性与慢统计稳定性收口。SDK 行情工作进程的完整K线、账户快照、就绪状态等关键消息在队列拥塞时不再静默丢弃；只有可被下一次更新覆盖的心跳和进度消息允许丢弃。新增纽约常规交易时段五分钟覆盖事实源，按当前交易池逐边界核对，300只完整交易日必须严格达到 `78/78` 个边界和 `23400` 根真实 SDK K线，恢复用 `context_only` 行不计入。缺口继续按策略合同隔离：PA002五分钟做多需要末尾23根连续K线，PA002/PA013做空需要3根，PA012与PA011只要求完整开盘区间和当前3根触发上下文，日线合同不因无关五分钟缺口停用。SDK推送订阅覆盖与快照后备的实际行情覆盖在看板分开显示。历史订单和盈亏分析超时现在只把统计标为过期并复用可信历史缓存，新的模拟账户快照和交易热路径继续运行；没有可信缓存时仍明确不可计算，不伪造数值。视觉影子已增加真实规模 `300×78` 回归，但仍必须取得一晚无缺口实时会话及人工 `10/10/5` 样例后才能申请纸面下单。

- 2026-08-11：纽约交易日 `2026-08-10` 复盘确认长桥实际 `41` 张成交订单，其中 `19` 张是 8 月 6 日故障残余仓位的授权清理、`21` 张是新策略开仓、`1` 张是旧故障样本的策略退出；授权清理成交现明确保留审计但不计入策略成交漏斗、胜率或盈亏。新开仓收盘浮盈合计约 `+138.28 USD`，长桥 App 同口径账户当日盈亏为 `-68.71 USD`，两者分别代表“新仓收盘浮盈”和“全账户当日变化”，禁止混算。订单热路径中位数约 `534ms`、P95 约 `3.876s`、最大约 `4.636s`，`21/21` 均在 `5s` 内；但全场五分钟行情只有 `65/78` 个完整边界，缺 `13` 个边界，因此昨晚只能证明 SDK 下单和归因链路可用，不能作为完整策略交易日。跨纽约午夜的 App 盈亏行情快照已修复；日线上下文子进程新增连接与单标的退避重试，每批改为原子返回“数据+完成状态”，失败标的保持合并重试，并把单连接批量从 `10` 只提高到安全上限 `25` 只，避免瞬时行情端连接失败或子进程退出消息丢失导致长期停在不完整状态。

- 2026-08-11：盘中全策略复核发现，人工直接执行 SDK `--daemon` 会绕过开机脚本中的进程级 `.cn` DNS 注入，导致行情端 `openapi.longbridge.cn` 经用户代理连接超时；交易账户连接不受影响。SDK 守护进程启动函数现统一自行准备并注入已完成 TLS 验证的子进程 DNS 环境，保留用户代理变量且不修改系统 DNS、hosts 或代理。正确重载后行情恢复 `300/300`、日线 `18000/18000`、账户快照健康并重新武装模拟下单。正式 8 条合同均进入检测；PA002 五分钟做多因本轮行情缺口按冻结规则等待重新形成 23 根连续K线，三条做空尚无合格结构；三条视觉策略继续只读，未完成的人工样例与完整影子日不得冒充已验收。

- 2026-08-10：用户已授权在美股常规交易时段先清理 `PA001日线（M10-PA-001-1d）` 与 `FTD长回调保护确认版（M12-FTD-001-pullback-guard-confirm-1d）` 的 8 月 6 日故障残余持仓。清仓计划按资金池、运行单元、合同哈希和标的汇总为 `19` 张模拟账户市价退出单；盘前不提交，开盘后逐单取得长桥订单号并确认成交，清仓完成前全局停止新开仓，完成后自动为这两个资金池建立新基线并恢复 8 条正式合同。SDK 网络路径恢复为已验证的“中国行情端 + 全球交易账户端”；域名解析修复只作用于 M15 子进程，不修改系统 DNS、hosts 或用户代理。当前 SDK `300/300`、日线 `18000/18000`、账户快照健康，开盘验收和周一验收均为零失败的“等待授权清仓”状态。三条视觉影子已通过 300 只历史回放、无未来数据和重启一致性，仍缺每条 10 个正例、10 个反例、5 个边界例的人工复核及至少 1 个完整实时影子交易日。

- 2026-08-09：账户统计与逐策略漏斗整改完成。长桥 App 当日盈亏固定为 `longbridge_app_asset_daily_pnl_v1`，实时 SDK 唯一行情进程输出原子行情快照，慢统计只用长桥持仓、成交、最新价和昨收价计算；纽约交易日收益分析不再允许替代。8条正式合同已统一展示“检测、候选、路由、执行、订单号、实际成交”漏斗，3条视觉影子分别展示历史回放、无未来数据、重启一致性、人工样例和完整实时影子日；条件不完整时不得显示为可下单。五分钟策略统一拒绝跨断档上下文。正式进程已在非交易日安全重载，SDK `300/300`、日线 `18000/18000`、模拟账户快照与后台看护健康。

- 2026-08-08：全策略盘中验收完成。合同版长桥 SDK、看护和盘后视觉调度均为单实例，模拟账户、300/300行情、18000/18000日线、账户快照及14/14开盘验收正常；最新完整五分钟批次300/300、热路径约639ms。8条正式合同全部加载，PA004-MBF-QC新增NOW 5股真实模拟成交；PA002/PA013做空已恢复完整检测，PA002做多按23根连续K线恢复，PA012做多和PA011开盘区间做空等待下一完整交易日。PA001和FTD因修复前旧重复占仓超限继续只允许退出，不擅自清仓或放宽上限。三条视觉策略自动盘后调度存活，历史回放证明齐全但实时完整影子日仍为0。全策略相关回归341项通过。

- 2026-08-07：行情稳定性补充收口已完成。正式 300 只快照退路从一次大请求改为每批100只、批间隔0.12秒，实机每轮约0.44秒；SDK子进程控制台噪音被隔离，10MB以上运行日志自动压缩，正常重载不再产生终止堆栈。五分钟数据缺口由各策略检测器按实际结构窗口独立恢复：PA002/PA013做空需3根、PA002做多需23根，PA012做多和PA011开盘区间做空需下一完整会话。正式进程重载后行情300/300、日线18000/18000、模拟账户快照和下单武装均恢复；原生行情请求偶发卡住时只重建行情子进程，主进程、K线状态和订单上下文不重启，16:25、16:35、16:40、16:45边界均完整派发300/300，热路径约606ms、548ms、502ms、544ms；16:45同时确认PA002/PA013做空在3根连续K线后自动恢复到300/300。达到3次重建后的恢复期限按主进程累计计数保持，不再因滚动计数下降反复进入重建风暴。SDK、做空、实时路由、执行和持仓管理回归303项通过。三条视觉策略历史300只逐根回放、无未来数据和重启一致性已通过，但完整实时影子交易日仍为0，继续保持只读影子，不进入订单通道。

- 2026-08-07：盘中巡检确认合同版 SDK、模拟账户、`300/300` 行情、`18000/18000` 日线、账户快照、订单连接和成交归因均正常，前 `15` 个完整五分钟批次全部覆盖 `300` 只，热路径 P95 约 `893ms`。同时发现 SDK `quote(300)` 偶发卡住会触发密集行情子进程重建；恢复策略改为两分钟内少于3次时保持 `8s` 快速切断，达到3次后在非五分钟边界使用最多 `60s` 恢复窗口，边界前后仍收紧为 `8s`，减少 OAuth/行情连接风暴而不放松K线边界。另修复“订阅请求返回300/300但没有真实报价回调”的假健康：常规交易时段8秒没有任一真实报价即终止该订阅进程并顺序切换到唯一完整快照进程，工作心跳不能冒充行情进度。盘中受控重载后，纽约 `11:05` 第一根干净五分钟K线已完整派发 `300/300`，热路径 `611ms`。

- 2026-08-07：完成重复占仓与账户对账稳定性收口。实时执行器现在从请求已发起开始持续保留资金池敞口，覆盖“券商快照尚未出现订单”“订单已成交但成交归因尚未刷新”“部分成交后终态”三个交接窗口；只有零成交终态或精确归因确认已平仓才释放。日线信号改用可恢复的“结构生命周期”编号：结构连续成立时，后续五分钟轮询继续使用同一编号；只有本轮最新行情完成检测并明确判定结构不成立后才解除，之后同一交易日再次形成的新结构可获得新编号。行情缺失、过期事件和未参与本轮检测不得解除旧结构。历史虚拟批次通过真实长桥订单证据完成 `33` 条不计策略成绩的账户对账调整，当前 `20` 个长桥实际持仓与虚拟归因逐标的数量差异为 `0`、归因异常为 `0`。合同版 SDK 三件套是唯一生产入口；正式测试标记激活后统一写 `blocks_new_entries=false`，超限资金池仍由各自 `6000 USD` 上限单独冻结。

- 2026-08-07：生产实测确认行情轮询子进程即使在五分钟边界附近被重建，也不会再丢弃已经闭合的K线；纽约 `13:25` 与 `13:30` 两个边界均派发 `300/300`，完整热路径分别约 `608ms`、`595ms`。SDK 慢速统计现从正式测试标记读取 `test_started_at` 并应用显式故障日配置，已归档 `42` 笔基线前成交，纽约 `2026-08-06` 的系统故障样本不再参与正常策略成绩。`PSKY` 修复前跨策略退出错配已用真实 PA012 开仓订单和长桥卖出订单做不计策略成绩的精确账户对账，当前标的归因冻结解除。

- 2026-08-07：修复纽约交易日 `2026-08-06` 的 SDK 五分钟行情断档和正式基线统计污染。交易时段重启不再永久继承上次 `sdk_snapshot_poll`，会先尝试唯一 SDK 推送；推送无回执时的轮询后备改为一次请求完整 `300` 只，取消三批请求和批间等待。健康整批请求实测约 `68~84ms`，原生调用超过 `8s` 无进度即重建，避免旧 `30s` 等待跨过五分钟边界。即使轮询子进程在半轮后重建，父进程也会先派发已经完成的五分钟K线，不再因 `poll_cycle_id` 切换静默清空整片边界。正式策略成绩现在按测试编号和 `test_started_at` 双重过滤，基线前成交转入审计归档；`2026-08-06` 因行情缺口及修复前超额敞口标为系统故障日，长桥账户真实盈亏仍完整保留。已成交的 PA001、PA012、FTD 超额仓位继续允许策略退出但禁止新增，不自动清仓。

- 2026-08-06：根据最新人工决定，纽约交易日 `2026-08-06` 不再重复执行一晚独立验收后清仓。长桥模拟账户当前持仓、挂单和待确认订单均为 `0`；今晚常规交易时段直接激活正式编号 `m15-sdk-contract-v1-20260806` / `m15-sdk-contract-v1-short-20260806`，8 条已冻结执行合同从第一笔长桥实际成交开始正式计分。当前生产配置不携带 `validation_business_date` 或 `validation_end_at`，因此不会在纽约 `15:45` 触发全账户验收清仓；各策略仍按自己的止损、目标、五分钟日内退出和日线最长持有规则管理仓位。`PA004-long / PA007 / PA008` 三条视觉合同继续只读影子运行，今晚完整会话证据不足不会阻断8条正式策略，也绝不生成订单。
- 2026-08-06：完成纽约交易日 `2026-08-05` 的长桥实际成交复盘和退出归因修复。长桥事实源为 `109` 张订单、`104` 张成交、`5` 张取消；其中新合同验收段为 `75` 张订单、`72` 张成交、`3` 张取消。PA002 的 10 笔市价止损均取得真实长桥订单号并成交，纽约 `15:46` 左右的 20 笔账户级市价退出也全部成交，当前账户持仓、挂单和待确认订单均为 `0`。账户级清仓和同策略汇总退出现在只在测试编号、资金池、运行单元、方向、合同哈希、标的和实际成交数量精确一致时，按真实订单号/成交号回填虚拟批次；任何差额继续保留为未归因异常，不允许按标的猜测。昨晚合同验收的 42 个完整开仓批次毛盈亏约 `-114.00 USD`；保守费用模型后的 `-257.88 USD` 只作项目估算，不冒充长桥实际费用或 App 当日盈亏。昨晚验收编号已归档，今晚正式编号重新计分，不混入昨晚数据。
- 2026-08-06：修复 SDK 非交易时段行情工作进程反复进行无效 20 秒订阅探测的问题。系统已验证当前账户的 `sdk_snapshot_poll` 可稳定取得 `300/300` 行情后，非交易时段只做一次完整 SDK 快照探针并持续心跳，不再反复启动会卡住的订阅调用；交易时段仍优先使用唯一 SDK 实时连接，快照仅作明确的安全后备。当前 M15 与后台看护均为单实例，配置指纹一致，账户快照和 300 只行情健康。
- 2026-08-06（已被上方最新决定替代）：曾计划使用独立验收编号运行一晚并在纽约 `15:45` 清仓；该安排未作为当前生产配置继续执行，相关代码能力仅保留为未来显式验收工具。
- 2026-08-06：修复三条受限做空合同长期被错误阻断的字段口径。长桥官方把 `estimate_max_purchase_quantity(side=Sell)` 定义为美股卖空查询；当前空账户对 AAPL、NVDA、AMD、TSLA 等返回非零 `cash_max_qty`、`margin_max_qty=0`，证明此前只读取 `margin_max_qty` 会把有效的现金担保卖空容量误判为零。SDK 和兼容诊断现只采用 `cash_max_qty`，继续禁止融资买入，也不使用 `margin_max_qty` 放大做空；每个真实候选仍逐标的向券商复核并受原短仓风险上限约束。
- 2026-08-06：视觉影子验收改用冻结的 `300×60=18000` 条日线回放基线，避免滚动日线窗口让旧审计与新窗口错位；盘中重启专用的 `context_only` 行不再污染真实会话完整性。历史逐根回放、无未来数据、重启一致性、状态和摘要一致性现均通过；今晚盘后调度器仍必须取得 `300×78=23400` 条真实常规时段五分钟K线，才把本次计为第一晚完整影子会话。
- 2026-08-06：完成 M15 唯一 SDK 行情连接根因修复。实机排他测试确认同一 `QuoteContext` 一次订阅 `300` 只只需 `0.866s`；此前盘中历史五分钟补齐连接和分批实时订阅会竞争同一行情通道，导致推送订阅超时并反复切到快照。现在交易时段重启不再打开历史行情连接，直接使用缓存并只延后缺上下文的五分钟运行单元；实时进程一次订阅 `300/300`，连续四分钟实测工作进程重建 `0` 次、账户快照 `0~15s`。快照仅保留为顺序故障后备，按 `100` 只分批并间隔 `0.5s`，子进程重建不得重置主进程K线边界。
- 2026-08-06（上一轮纽约交易日 `2026-08-05` 的验证记录）：上一轮自然信号验证结束后的 `20` 个长桥模拟持仓已由 SDK 市价清仓单全部成交，长桥事实源当前持仓 `0`、挂单 `0`。上一轮验收已归档；今晚纽约交易日 `2026-08-06` 直接使用新的正式编号，不继承上一轮持仓或成绩。
- 2026-08-06：完成合同版 SDK 冷启动稳定性收口。主进程会在开始耗时上下文恢复前立即写入实际 PID、配置指纹和 `starting_context_restore`；后台看护在有界启动宽限内只等待，不再每分钟误杀仍在补齐五分钟上下文的健康进程。实机证明盘中并发历史K线请求会与唯一实时行情连接竞争，因此启动跨过五分钟边界时不再盘中追赶历史缺口：只关闭受影响的五分钟策略到下一次干净会话，日线策略、实时行情、账户快照和退出继续运行；任何历史上下文都不回放信号或订单。
- 2026-08-06：300 只 SDK 快照轮询间隔为 `3s`，行情工作进程无进度重建期限从过窄的 `5s` 调整为 `15s`。该调整只减少瞬时网络抖动造成的无意义子进程重连，不改变新行情到信号/风控/SDK 请求的 `1s` 延迟目标；行情没有新进度时不会生成或补交订单，超过 `15s` 仍会重建并停止新开仓。
- 2026-08-06：完成合同 v1 “无合格信号”根因修复，不降低任何全局利润、收益风险比、仓位或做空门槛。PA002/PA012 的第 2 根跟进确认不再被互斥条件永久堵死；PA012 改用突破K线结构低点止损，使 30 分钟开盘区间测量目标可以达到现有 `1.5R` 门槛。该变更只写入尚未开始正式统计的 M15 长桥合同，不修改本地 M14 影子运行单元。五分钟检测器只接受严格连续K线，PA012 还必须从纽约时间 09:35 第一根完整K线连续构建开盘区间。
- 2026-08-06：M15 SDK 盘中重启会从长桥 SDK 补齐当日最多 78 根已完成五分钟K线，只作为策略上下文，不进入活跃事件、不补交旧信号；当前未闭合K线始终排除。盘中日线确认使用五分钟K线完成边界内冻结的当日 OHLCV，不再被K线完成后的最新报价改写。8 条执行合同新增按合同哈希隔离的“检测尝试、未形成候选、候选、路由通过、阻断原因”诊断；订单号与成交继续只取长桥事实源。
- 2026-08-05：为验证合同 v1 的真实盘中链路，在长桥模拟账户持仓、挂单和待确认订单均为零后，开启仅限当日常规交易时段的自然信号验证会话。验证会话不制造订单、不放宽策略门槛；只允许实时行情自然产生的合格信号提交，并于纽约时间 `15:45` 自动停止新开仓、清理验证持仓，再等待 `2026-08-06T13:30:00Z` 建立正式统计基线。前四根完整五分钟周期已产生 `16` 条 `M10-PA-012-5m` 自然候选，其中 `12` 条收益风险比不足、`2` 条不足一股、`2` 条扣费后目标利润不足，故当前真实策略订单号和成交均为 `0`；这是正常风控结果，不得冒充漏单或为了验收降低门槛。
- 2026-08-05：修复合同基线等待期的隐藏路由异常。`pending_flatten` 期间此前仍会把空的测试开始时间传给做空路由器，触发“缺少独立做空测试编号开始时间”；现在等待期直接安全跳过新信号路由，验证会话结束后自动切回停止新开仓并进入账户清理。正式巡检必须使用 `m15_longbridge_sdk_runtime.contract_v1.json`、`m15_opening_trade_readiness.paper_contract_v1.json` 和 `m15_background_watchdog.contract_v1.json`，禁止用旧配置误报指纹漂移。
- 2026-08-05：合同 v1 旧仓自动清理发现并修复券商终态重试缺口。首轮 `32` 个持仓中 `30` 个正常清空，`WBD` 与 `UAL` 的市价卖单被长桥明确取消且零成交；旧逻辑因已经记录订单号而永久跳过。现在仅在模拟账户快照可信、无活动挂单、实际持仓仍存在、原订单为 `Canceled/Expired` 且成交量为零时，使用新的稳定请求编号自动重试一次；部分成交、未知状态和缺少订单号均不重试。两笔重试订单均取得长桥订单号并成交，当前长桥持仓、挂单和待确认订单均为零；综合验收已能识别“清仓完成、等待配置激活时间”为安全等待而非故障，新合同基线仍按计划等待 `2026-08-06T13:30:00Z` 激活。
- 2026-08-05：`PA004-long / PA007-1d / PA008-1d` 的只读视觉影子验收进入自动化阶段。M15 SDK `300` 只股票、每只 `60` 根日线已完成逐根回放，共生成 `54000` 条三策略审计事件；独立临时目录验证无未来数据前缀一致性与中途重启结果一致。盘后调度器新增非阻断视觉影子步骤：只有当天 `300` 只均具备 `78` 根完整常规时段五分钟 SDK K 线，才把该交易日计为一个完整影子会话。机器自动筛选正例、反例和边界例候选，但不得增加人工通过计数或自动批准下单。
- 2026-08-05：当前唯一推进主线切换为 M15 长桥 SDK 策略合同 v1。长桥新测试只允许 `8` 个独立运行单元：`PA001-1d`、`PA002-5m`、`PA012-5m`、`PA004-MBF-QC-1d`、`FTD-pullback-guard-confirm-1d` 和三条受限做空策略；每条策略一个独立虚拟资金池，代码、信号、订单、成交和看板必须携带同一 SHA-256 合同哈希。`PA004-long`、`PA007-1d`、`PA008-1d` 只处于规则草案/实时影子阶段，不允许下单。旧 `19` 运行单元及其成绩归档，不再作为新测试准入或统计口径。
- 2026-08-05：PA001、PA002、PA012 已接入独立完整实时检测器；PA002/PA012 五分钟仓在纽约时间 `15:55` 强制退出，PA001、PA004-MBF-QC 和 FTD 日线仓最长持有 `5` 个美股交易日并在到期日 `15:55` 市价退出。FTD 唯一合同为长回调保护加首次 1 至 2 根跟进确认，市场和更高周期背景仅审计。PA004-MBF-QC 明确是实验合同，不冒充 PA004 完整版。
- 2026-08-05：新基线固定为 `m15-sdk-contract-v1-20260806` / `m15-sdk-contract-v1-short-20260806`。合同版 SDK 已接管并进入 `pending_flatten`：当前只允许在美股常规交易时段清理长桥旧持仓，长桥实际持仓、挂单、待确认订单未全部归零前禁止新开仓；全部归零后仍须等到 `2026-08-06T13:30:00Z` 才能自动激活新基线。旧运行配置和旧隐藏保活入口均已停用。

- 2026-08-03（历史阶段，已归档）：曾新增 `M10-PA-002-5m-repaired-v1` 形成 19 运行单元双版本测试；该简化修复版不进入 2026-08-05 合同 v1 新基线。
- 2026-08-03 PA002 双版本节点检查已并入现有 M15 后台看护，每 `300` 秒随长桥实际成交归因刷新一次，不新增 Windows 弹窗或独立高频进程。两个版本各自满 `5` 个有效交易日时给技术检查提示；只有两个版本各自同时满 `15` 个有效交易日和 `100` 笔完整交易，才生成最终人工复核建议。最终门槛固定为扣费后净利润为正、盈利因子和平均盈亏比均不低于 `1.20`、最大回撤不高于 `400 USD`、单日/单标的盈利贡献均不超过 `50%` 且无成交归因异常；系统只提醒，不自动改策略或晋级。
- 2026-08-03 开盘前巡检修复 SDK 行情回调跨进程序列化缺口：长桥 PyO3 `TradeSession` 枚举会伪装成整数子类，但不能由 `multiprocessing.Queue` 序列化，导致部分报价推送在队列 feeder 中静默丢失。SDK 对象规范化现在只把原生标量直接透传，嵌套容器和 SDK 枚举递归转为普通 Python 值；新增不可序列化整数子类回归。重载后实际 SDK 进程为单实例，`300/300` 行情、`18000/18000` 日线上下文、模拟账户、账户快照和订单派发均健康，修复后日志新增区间没有同类错误，当前仅等待美股常规交易时段。
- 2026-08-03 完成 `M10-PA-002-5m` 同口径复核并冻结策略参数：当前长桥正式样本为 `61` 个非故障日完整交易，`21` 胜 `40` 负，扣保守费用后约 `-304.02 USD`，仅覆盖 `8` 个平仓交易日。扩展到 300 只前已有 `44` 笔、`14` 胜 `30` 负、约 `-246.36 USD`，因此扩池不是主要原因。用户此前看到的本地好成绩来自旧本地账户口径：`870` 笔、同时包含 `430` 笔看涨和 `440` 笔看跌、修复确认与连亏冷却，并混有旧/备用行情；它与长桥当前纯做多实时突破确认不是同一可执行合同。当前不为追逐短期结果修改门槛；先保持规则不变，继续收集到至少 `15` 个有效交易日且 `100` 个完整交易，再决定保留、重写为本地修复版同规则，或退出长桥单仓。
- 2026-07-31 修复 M15 盘中行情快照偶发抖动被误判为故障：300 只 SDK 快照通常约 `70~90ms`，但原配置把 `>1000ms` 连续三轮直接视为行情进程故障，子进程重建后又需五轮验收，导致看护和看板反复显示连接中。行情快照健康上限调整为 `2500ms`，仍低于 `3s` 轮询周期；信号生成到 SDK 请求的 `1s` 目标保持不变。后台看护对 `connecting/reconnecting` 增加最多 `20s` 的复查窗口，恢复后不再误报整套系统故障。看板在快照模式下明确展示实际行情覆盖，不再把 `0/300` 的直接订阅数冒充交易池无行情。
- 2026-07-31 完成长桥 SDK 行情、选股与分仓口径整改：五分钟成交量改为 Longbridge quote 累计 `volume` 的连续差值，累计量缺失、倒退或时间倒退会显式阻断；日线盘中确认直接使用同一 SDK quote 的当日累计 OHLCV，不再把稀疏五分钟推送相加成错误日成交量。正式交易池在已有 `300/300` 订阅、`18000/18000` 日线和一整场只读验收通过后，从固定 147 只晋级到独立 300 只池。实时路由新增不发起额外网络请求的轻量相对强度排序，半导体候选结合 `SPY/QQQ`、`SMH/SOXX` 和个股本轮强度排序，不放宽原有信号、利润、风险和仓位门槛。
- 2026-07-31 长桥单账户统计固定为“真实账户、精确虚拟批次、无法归因差额”三层：只有长桥实际 `Filled` 成交进入策略成绩，跨策略同标的按测试编号、资金池、运行单元、方向、开仓订单号和成交编号分别核销；未成交、取消、拒绝不计盈亏、胜率或回撤。长桥持仓缺少当前价时，市值和浮盈显示等待长桥数据，不再用零值或开仓价伪造账户差额。`PA004-MBF` 与 `PA004-MBF-QC` 的 28 个历史超额未平批次已生成精确模拟账户退出计划；清理完成前只阻断这两个仓的新开仓，退出不受阻，全部实际成交后分别建立新统计基线。
- 2026-07-30 完成 M15 SDK 热路径瘦身，不调整策略门槛、仓位或运行单元：信号与执行审计改为只追加，运行进程启动时加载一次完整审计并建立内存去重集合、当前交易日决策和已提交订单热状态；每个五分钟批次不再重读 `15.4MB` 信号流水和 `31.5MB` 执行流水。持仓退出的精确成交归因只在长桥订单、成交、持仓或本地订单归因元数据变化时重建，行情价格变化直接复用缓存。扩展 153 只仍只读，策略路由只处理固定 147 只。按当前真实 `12916` 行上下文、`6621` 条信号审计和 `6826` 条执行审计回放，优化后路由与持仓管理合计中位 `487.4ms`、最大 `540.6ms`，低于 1 秒目标；修复前同机回放约 `2.3~2.6s`。M15 相关 `429` 项回归通过，正式 SDK 进程配置指纹一致，模拟账户、`300/300` 订阅、交易池 `147/147`、`18000/18000` 日线和账户快照均健康。
- 2026-07-29 修复账户级清仓后同标的新仓被旧零持仓门禁污染的问题：账户对账调整不再要求标的当前净持仓必须为零，而是只清理人工核准的具体旧开仓订单批次，并在应用前验证“清理后的虚拟净持仓”必须与长桥当前实际净持仓完全一致。`LCID` 7 月 23 日账户清仓后的旧虚拟残量已清除，7 月 28 日 `M10-PA-004-long-1d` 新买入的 13 股保持独立归因；重算后成交归因异常和持仓数量不一致均为 `0`，策略指标恢复可信。
- 2026-07-29 修复会直接影响长桥模拟账户稳定性的运行缺口：交易 SDK 每 60 秒通过只读订单接口检查 OAuth/TradeContext，只有明确授权失效或上下文缺失才重建；普通接口限频只暂停新开仓 5 秒后复查，避免重复创建连接。重建完成前运行状态显示“交易连接恢复中”，无订单号的失败请求不算提交且不重放旧信号。做空开仓容量改为使用 Longbridge `Sell` 估算响应里的 `margin_max_qty`，同时保留 `cash_max_qty` 审计；该字段只用于借券卖出上限，普通买入仍禁止融资。分仓总敞口直接消费当前正式测试编号的精确未平成交批次成本，并叠加未完成券商订单剩余金额；跨日、重启和当日订单列表不含旧订单时也不会漏算。已超 `6000 USD` 的仓位显示剩余额度为零并阻断新增，但退出继续允许。退出执行固定为止损优先；失败退出只有在 900 秒冷却后、账户订单视图健康且券商侧确认不存在对应活动/成交订单时才允许生成新意图，防止响应超时造成重复卖出。
- 2026-07-28 盘中 SDK 行情恢复：确认长桥 `QuoteContext.subscribe` 冷启动会在部分批次长期无回执，而 SDK 整批 `quote(147)` 可在约 `0.06~0.83s` 返回。自动交易仍禁止 CLI 和第二条并发行情连接；订阅连续一轮无进度后，先终止旧订阅子进程，再由唯一的新 SDK 行情进程每秒读取原 147 只交易池。只有连续 5 轮 `147/147` 完整且每轮不超过 `1s` 才允许纸面订单热路径；连续 3 轮超时或缺标的会终止该进程并停止新开仓。该模式明确记为 `sdk_snapshot_poll`，不得冒充订阅完成，也不得用于 300 只扩展池验收。SDK 慢统计已移除第二个 `QuoteContext`，只读取账户、订单、成交和收益接口。
- 2026-07-28 完成长桥事实源与运行稳定性收口，不调整任何策略信号、止损、目标价、仓位或运行单元名单。策略成绩改为按长桥实际 `Filled` 成交重建完整开仓批次，只有对应批次全部退出后才计为一笔完整交易；分批退出次数单独统计，不能再冒充交易笔数。每笔完整交易按每侧订单 `1.99 USD`、每张卖单监管费 `0.02 USD` 的保守估算扣费，并同时保留毛盈亏、估算费用和扣费后盈亏。发生账户快照过期或账户通道异常的交易日单独列为系统故障日，不进入正常策略胜率、盈利因子和回撤判断，但完整保留审计。
- 2026-07-28 三条受限做空仓新增从“看跌候选、路由通过、券商可开数量校验、取得订单号到实际成交”的完整诊断漏斗。券商可开数量查询增加短时缓存，价格变化不超过 `2%` 且缓存未过期时复用成功结果，失败结果仅短暂缓存并继续安全阻断；该优化不降低任何做空结构或利润门槛。当前事实源显示 `2` 个做空候选中 `1` 个通过路由并完成券商数量检查，但因可开数量校验阻断，尚未取得订单号或成交。
- 2026-07-28 SDK 账户工作进程遇到超时或不可信结果时会终止重建，并保留上一份可信快照的原时间戳；连续异常达到阈值后熔断，绝不把错误刷新伪装成健康新快照。SDK 运行锁已迁移到用户级稳定目录，所有配置共用一把全局锁；订阅失败次数会在真实失败路径累计并在达到阈值后停止新开仓。300 只订阅池和原 147 只交易池使用独立文件及指纹，重排 300 只文件也不能改变可交易的 147 只。
- 2026-07-28 非交易时段已把 M15 唯一 SDK 行情连接从 147 只扩展为订阅 300 只，同时把可交易股票池硬锁定为原 147 只；新增 153 只只写行情审计和日线上下文，不进入策略路由、持仓管理或订单。运行锁已改为跨输出目录的全局单连接锁，避免独立扩展配置误开第二条长连接；开盘验收只要求原 147 只交易股票池完整，扩展标的失败不会阻断现有交易。新增 153 只需完成一个完整常规交易日、订阅 300/300、日线 18000/18000、账户快照健康且热路径 95 分位不超过 1 秒后，才允许另行评估是否加入交易股票池。
- 2026-07-27 周一开盘前复核确认三条受限做空仓自正式测试以来没有长桥订单或成交。实际五分钟行情重放显示 `M10-PA-002-5m-short` 曾有 `12` 条通过看跌结构和质量门槛的候选，但原 `500 USD` 单标的上限与 `10 USD` 风险上限、`2R` 目标、约 `4 USD` 双边费用和 `12 USD` 净利润门槛组合后，理论最高净利润仅约 `11 USD`；`M10-PA-011-ORB-R1-5m-short` 的理论上限约 `10.06 USD`，同样低于 `15 USD` 门槛，属于配置数学矛盾。三条短仓单标的名义金额上限已提高到 `750 USD`，每笔最大风险仍为 `10 USD`、每仓总敞口仍为 `2000 USD`，结构、质量分、盈亏比和净利润门槛均未降低；真实行情重放后 `12` 条候选中有 `2` 条可通过完整路由。新增按短仓正式测试编号累计的做空候选诊断，看板会直接展示候选数、可提交数和主要阻断原因，不再被下一轮空摘要覆盖。
- 2026-07-27 周一前巡检发现隐藏 WSL 保活连接在周五后退出，M15 SDK、后台看护和本地盘后调度器停止，但长桥模拟账户 SDK 端点仍健康。现已恢复隐藏长期保活，并通过当前用户 `schtasks.exe` 建立工作日北京时间 20:45 的无窗口兜底唤醒；冷启动日线装载期间继续安全关闭新开仓，完成 `8820/8820` 后自动武装。
- 2026-07-23 昨夜退出链路复盘与修复：正式测试清仓确实于 7 月 16 日完成；当前持仓全部是正式测试编号激活后重新成交并跨日保留的仓位，不能再称为“清仓前旧持仓”。7 月 22 日长桥实际成交 5 笔止损和 2 笔止盈，但盘中增量成交归因只恢复 67 个批次并把 7 个唯一异常重复累计为 105 条，导致 19 个当前持仓标的被冻结；另有 DDOG 止损事件生成后未进入执行流水，却被永久去重。现已让退出管理器优先采用更完整的成交归因基线、按开仓订单号补回止损/目标元数据、过滤非正式测试成交，并允许“从未进入执行器”的退出事件生成新重试编号；重复异常按唯一订单与成交编号去重。同轮退出改为止损优先于止盈，固定单轮名额不再延迟已核验止损。用户人工卖出的 `LCID` 和历史跨测试编号净额造成的 `GILD` 虚拟残量，已通过精确开仓订单号账户对账调整清理，并永久排除在策略成绩之外；盘中复核净持仓不一致标的为 `0`。聚焦回归通过，SDK 重载后首个完整五分钟周期处理耗时 `565ms`。
- 2026-07-22 盘中实机复核：M15 SDK、`147/147` 行情订阅、`8820/8820` 日线上下文、模拟账户、正式测试编号和单实例看护均正常；账户快照年龄 `9s`、刷新约 `0.51s`，无超时或熔断。纽约交易日当天长桥实际返回 `18` 张策略订单，`17` 张成交、`1` 张取消、未完成挂单 `0`；信号到 SDK 请求中位数约 `171ms`、最大 `684ms`。周一综合验收已修复把合法 `0s` 快照当作缺失值的误判，并增加零秒快照回归。
- 2026-07-22 长桥稳定性整改已完成代码与回归验证：跨策略同标的成交不再按标的、价格或数量猜测归属，正式测试统一按“测试编号 + 资金池 + 运行单元 + 方向 + 标的 + 长桥开仓订单号 + 长桥成交编号”维护独立剩余数量；部分成交和分批退出分别核销。无法精确匹配、跨测试编号或长桥净持仓与虚拟批次不一致时，只冻结该标的新开仓并在看板报警，不自动代替某条策略平仓。
- 2026-07-22 账户快照改为独立可终止工作进程：常规刷新硬期限 `8s`，首次 SDK 连接单独允许 `30s`；首次超时会终止并重建后在同一轮再试一次，连续超时才进入短暂熔断，保留最后可信快照但不延长其 `45s` 安全寿命。M15 运行层新增进程级单实例锁，已清理此前并发写同一状态文件的 5 个重复进程。实机复核为单实例、账户刷新约 `0.4~0.6s`、快照年龄低于 `15s`、超时 `0`、熔断关闭、配置指纹一致。SDK 慢统计已实际查询最近两天增量并与可信缓存合并；相关 M15 聚焦回归共 `236` 项通过，`git diff --check` 通过。
- 2026-07-21 电脑重启后复盘与稳定性修复完成：隐藏保活自动恢复为单实例，M15 SDK、后台看护和本地盘后调度器均存活。重启初期账户读取连接的持仓/订单请求连续超时，现已把账户轮询连接与订单执行连接彻底分离；账户只读连接超时后可在冷却期内重建并重试一次，不会阻塞或重建下单连接。盘后重启也会直接复用已验证包含最新完整交易日的 `147×60=8820` 条日线，不再清空后重复拉取。当前 `147/147`、`8820/8820`、账户快照、模拟账户、正式编号、配置指纹、看护和综合验收全部通过，只等待北京时间 `21:30` 常规交易时段。
- 2026-07-20 长桥实际交易复盘：当日 `63` 张模拟订单全部取得长桥订单号，`62` 张成交、`1` 张过期买单由系统撤销、当前挂单为 `0`。28 个实际平仓批次按成交价归因的毛盈亏为 `+148.09 USD`，10 盈利/18 亏损；但长桥 SDK 按纽约交易日查询的美股当日账户盈亏为 `-109.26 USD`，反映持仓日内变化和费用后整体现实结果。长桥累计美股收益分析当前为 `-51.98 USD`。63 次请求中 57 次信号到请求不超过 `1s`，其余 6 次为 `1.002~1.640s`，全部低于 `5s` 可接受线；95% 不超过 `1s` 的优化目标尚未完全达到。
- 2026-07-21 巡检发现 `PYPL` 买单超过15分钟仍未撤销，根因是长桥实际备注包含 `PAT-RT + signal_id + client_request_id`，SDK维护器错误地用整段备注匹配执行流水。现已改为从备注中解析可归因的实时信号编号，保持人工/未知订单不处理，并用真实备注格式补回归；运行层重载后将由原生SDK自动撤销该过期模拟买单。
- 2026-07-20 盘中真实订单巡检确认 SDK 已正常交易：当日长桥返回 30 张订单，其中 27 张已成交、3 张挂单；发现一次 SDK 账户查询瞬时失败把 8 条实时信号误判为非模拟账户。现已改为在账户通道未变化且上一份快照健康时保留最后可信快照，原始时间戳继续受 `45s` 门禁约束，连续失败仍停止新开仓；同时修复盘中 canonical readiness 状态被周一验收误判。重启后首个完整五分钟批次再次生成并提交 `M10-PA-008-1d / PYPL / 7股` 模拟订单，本地处理 `37ms`、SDK请求 `142ms`，账户和两层验收均正常。
- 2026-07-20 重启后巡检与修复完成：Windows 隐藏保活成功自动拉起 SDK、后台看护和本地盘后调度器；盘中账户快照已移除同步收益分析调用，收益分析超时不再把 15 秒账户刷新拖过 45 秒安全线。新进程连续快照年龄 `2~13s`，`147/147`、`8820/8820`、模拟账户、正式编号和配置指纹均通过；M15 全量 `329` 项测试通过，当前只等待北京时间 `21:30` 常规交易时段。
- 2026-07-18 最终代码复核补齐三项周一运行边界：持仓管理器现在分别保留做多正式编号和做空正式编号，做多编号过滤不再吞掉已确认空仓的回补；后台慢统计连续跳过时会持续保留上次成功时间，严格维持 `300s` 刷新间隔；SDK 慢统计在写入新时间戳前必须确认 SDK 进程真实存活、状态新鲜且配置指纹一致。周一综合验收的 `broker_connection` 也改为随 SDK 和模拟账户真实状态计算，不再硬编码。新增回归后 M15 全量测试通过，周末重启实测仍为 `147/147`、`8820/8820`、`lb_papertrading`、正式编号 active、周一验收 `12 pass / 1 waiting / 0 fail`。
- 2026-07-18 周一前完整验收已收敛到 SDK-only：当前 M15 SDK、模拟账户、`147/147` 订阅、`8820/8820` 日线、账户快照、正式测试编号、独立长桥看板和隐藏开机保活均通过；新的 `m15_monday_refresh_acceptance` 不再读取 M12/M13/M14 或旧 CLI 产物，当前结果为 `12 pass / 1 waiting / 0 fail`，唯一等待项是周末尚未进入美股常规交易时段。旧 readiness 路径改写为 canonical SDK 验收指针，不能再作为交易判断来源。
- 2026-07-18 后台稳定性补强：修复后台看护可能并发启动两个实例的问题，增加启动锁和全生命周期运行锁，当前只保留一个看护进程；正常健康流水从每分钟整条写入改为状态变化立即写、稳定状态每 5 分钟写一次。SDK 慢统计新增账户快照 `45s` 新鲜度门禁，主进程离线时不得生成看似最新的订单/盈亏统计。
- 2026-07-18 正式分仓统计新增跨测试编号退出保护：若平仓请求引用的开仓信号来自旧 `test_epoch_id`，长桥真实成交仍保留在账户总口径，但不再计入具体分仓、策略胜率或盈亏。当前完整对账识别出 `2` 笔此类历史成交并从策略成绩排除，避免旧 FTD/旧持仓归因污染正式基线。
- 2026-07-18 巡检确认 2026-07-17 整晚没有新信号或新订单请求，不是策略门槛问题，而是 M15 SDK 与后台看护在北京时间 15:18 随 WSL `init` 退出。长桥另有两张 2026-07-16 收盘瞬间遗留单在周五更新为成交，不属于周五新交易；完整区间 SDK 对账已避免按创建日期查询漏掉跨日成交。Windows 当前用户仍无权注册 21:10 计划任务；原登录启动器执行完 bootstrap 后也会退出 Windows 侧 `wsl.exe`，无法防止后续回收。现改为隐藏 `--keep-alive` 模式，保留一个长期 WSL 连接，并在 Linux 内每 60 秒幂等恢复 M15 SDK、后台看护和本地盘后调度器；不创建会弹窗的 Windows 周期任务，不修改代理、策略门槛或仓位。长桥看板慢统计也已改为 SDK-only 每 300 秒刷新，不再因 CLI 退役长期显示旧统计。
- 2026-07-17 已完成 2026-07-16 正式 SDK 交易日巡检与两项边界修复：长桥模拟账户实际返回 `55` 张订单、`53` 张成交、`2` 张收盘瞬间 `NotReported` 日内单。SDK 热路径现在严格禁止纽约时间 `16:00:00` 及之后提交订单，收盘行情只用于审计。持仓退出管理现在只读取当前正式 `test_epoch_id/test_started_at` 之后的执行归因，旧测试 LCID 买入记录不再能控制新基线的虚拟仓或生成跨仓卖单。旧订单和旧测试流水继续保留审计，不进入新策略成绩。
- 2026-07-16 盘中正式测试门禁已修复并恢复：16 个旧模拟持仓已通过长桥 SDK 清空，持仓、挂单和待确认订单均为零，正式编号 `m15-sdk-formal-single-strategy-20260716` 与短仓编号已激活。清仓完成路径此前只在 formal marker 写入 `test_started_at`，共享执行 epoch 仍为空且使用 `activated` 状态，导致正式信号被误判为清仓前旧信号。现在新写入统一为 `active`，运行层会用 formal marker 自动修复旧 epoch，执行器和路由器兼容历史 `activated + activated_at`，开盘验收直接核对实际 epoch 编号和开始时间；不一致时禁止新开仓。SDK 仍为唯一盘中交易引擎，模拟账户、147/147 订阅、8820 日线和账户快照均正常。
- 2026-07-16 最终开盘门禁保持为 `pending_flatten -> 市价清理旧持仓 -> 长桥确认持仓/挂单/待确认订单全零 -> 激活正式 epoch -> 接受新信号`，不得跳过清仓。M15 SDK、轻量看护和本地盘后调度均已由隐藏启动入口独立托管；M12.47/M12.37 和旧 CLI 自动链路未运行。
- 长桥配置有 `18` 个运行单元，其中 `13` 个当前可通过静态收益风险比门槛；`M10-PA-013-5m`、`M10-PA-011-ORB-R1-5m`、`M10-PA-001-1d`、`M10-PA-005-5m`、`M10-PA-012-5m` 保持阻断并进入后续目标/止损几何修复，不为今晚制造订单而降低门槛。
- 开盘前不再执行大规模历史目录清理或本地批处理重构。正式窗口后继续：压缩旧研究/回测归档、让本地盘后批处理真正按保留运行单元执行、完成 300 标的一个整场只读验收。
- 2026-07-16 开盘前复核已修复 Windows 隐藏启动器：改用无嵌套引号的 `wsl.exe --exec bash`，实测三个服务由 WSL `init` 托管并持续存活。当前 SDK、模拟账户、147 订阅、8820 日线、账户快照和配置指纹全部正常；唯一正式门禁仍是 16 个旧持仓等待北京时间 21:30 清仓。历史 CLI/M14 流水已继续压缩，订单、成交和分仓审计保留。
- 2026-07-16 正在实施“长桥 SDK 正式测试、旧持仓清仓与本地系统瘦身”：M15 SDK 是唯一盘中交易主系统；正式配置继续锁定 `147` 只标的和 `18` 个交易运行单元。当前 `16` 个旧验收持仓使正式基线保持 `pending_flatten`，允许新开仓数为 `0`；纽约常规交易时段开始后由常驻 SDK 自动提交模拟账户市价退出，只有明确拒绝且没有订单号时才允许一次基于两秒内行情的可成交限价退路。持仓、挂单和待确认订单全部归零后，当晚激活新的做多与做空正式测试编号。
- 本地 M12/M13/M14 已收拢为用户界面统一名称“本地研究与修复系统”：不再盘中常驻，只在纽约收盘 15 分钟后运行一次本地行情回放、账本和修复评估；失败最多重试 3 次且不影响 M15。开机脚本不再启动 M12.47/M12.37，M15 readiness 和 watchdog 也不再把 M12.47 当成门禁。
- 独立长桥看板只读取 SDK 状态、长桥模拟账户、实际订单/成交、分仓归因和正式测试编号；当前清晰展示父级交易策略 `24`、本地模拟运行单元 `30`、长桥交易运行单元 `18`、辅助/资料模块 `7`。M15 的 300 标的独立候选池已建立为“原 147 顺序不变 + 新增 153”，但完成 147 正式交易日和 300 只读验收前不会进入信号源。
- 2026-07-16 昨晚 SDK 验收交易已完成但授权清仓未完成：长桥实际历史确认 `13` 张订单、`10` 张成交、`3` 张取消，实时热路径 P95 `579ms` 且全部样本低于 `1s`；清仓脚本在停止交易进程后的报价重连发生 `connect timeout`，未提交任何清仓单，当前仍有 `16` 个长桥持仓标的。系统现已进入 `pending_flatten`，正式测试编号和执行/路由配置已统一；下个美股常规交易窗口只处理实际长桥持仓退出，不允许新开仓。只有持仓和挂单全部归零后，才激活新的做多和做空正式测试基线。清仓连接已加入有界只读重试与成本价保守退化，运行层和后台看护也不再把死亡进程误报为健康。
- 2026-07-15 M15 SDK 全量切换已完成上线前只读预检：M15 自动交易、账户快照和后台看护均已改走 Longbridge SDK，自动路径禁止调用 CLI，也不读取 M12/M13/M14 或本地模拟账本。实测 SDK `QuoteContext/TradeContext/PortfolioContext`、模拟账户、`147/147` 订阅、`147×60` 日线缓存和账户快照均正常。五分钟 K 线在 `150ms` 内合并后才触发一次路由/风控，日线与五分钟输入独立保留；日线获取独立于订阅进程并有超时回收，不能再拖慢实时推送。完整日线缓存现原子保存，运行进程重启只会复用覆盖完整且包含最近完整交易日的缓存；收盘后会重新刷新最终日线，避免次日沿用陈旧上下文。用户已批准在今晚用当时新产生的合格信号进行模拟订单验收，因此不再以“两天只读”阻断下单；每笔仍必须满足完整订阅、完整日线、健康账户快照、模拟账户和现有策略/风控门槛。今晚产生的成交仅归档为 SDK 执行验收记录；用户已批准在收盘前清仓，任务会先停止新开仓和看护、取消未完成订单，再按实际方向卖出多头或回补空头；仅当全部清仓成交确认后，明晚才建立新的正式测试基线并归零统计。旧 CLI 守护器、旧快速队列和本地模拟开平仓均不再是 M15 下单来源。
- 2026-07-15 盘中 SDK 订单验收已取得长桥实际证据：完整 `147/147` 五分钟批次生成 `7` 笔 `M10-PA-004-MBF-1d` 纸面买单并全部取得订单号，已有 `MSFT/ORCL/AMZN/AAPL` 实际成交；下一轮重复标的全部按同仓订单/持仓去重。`M10-PA-002-5m` 的 `LRCX` 退出从信号到 SDK 请求 `13ms`、响应 `139ms` 并成交，较早的 `M10-PA-002-1d` 对 `LI` 的止盈单经 SDK 原生改价后也已成交。当前代码把上一交易日完整日线和当日 SDK 五分钟聚合确认分开，补齐 `PA004-MBF/PA004-MBF-QC` 独立检测，并把配置、关联配置和实时源码全部纳入运行指纹。今晚剩余动作固定为继续自然信号验收和纽约时间 `15:45~15:55` 的授权清仓；清仓成功后自动建立 `m15-sdk-formal-single-strategy-20260716` 与对应短仓正式基线，明晚不得沿用今晚旧持仓、挂单或统计。

- 2026-07-13 已完成受限纸面做空测试仓实施与回归：在既有长桥单策略单仓做多测试不重置、不降仓、不提高 `6000 USD` 做多敞口上限的前提下，新增三条受限纸面做空单策略仓：`M10-PA-002-5m-short`、`M10-PA-013-5m-short`、`M10-PA-011-ORB-R1-5m-short`。三仓基线为 `m15-short-single-strategy-20260713`，每仓 `10000 USD` 虚拟资金、`2000 USD` 总做空敞口、`500 USD` 单标的上限、`10 USD` 单笔风险。它们只消费长桥实时五分钟行情和长桥账户状态，不读取本地模拟；只允许整股、常规交易时段、模拟账户、`sell` 开空和 `buy` 回补。每笔开空必须先核验 `max-qty --side sell`、无同标的多头或多头买单、严格的结构/质量/盈亏比/扣费利润门槛；回补必须按“资金池+运行单元+标的+原开空订单号”逐笔处理，同一原开空批次只要存在未终态回补单就持续阻断。只有券商确认该回补已撤销、拒绝或过期且原短仓仍未回补时，才允许基于当前持仓生成新的 `-retry-N` 回补意图；已成交、待确认和未终态回补不会重发。长桥成功回执但缺订单号时只等待后台对账，不重发同一信号。M12.29 当前日数据源已同步使用本次激活扫描范围，缓存覆盖按该范围计算，生产仍保留 `147` 只池。泛化做空、实盘、期权、碎股和融资买入继续禁用。所有短仓成绩只由长桥实际 `Filled` 成交按“资金池+运行单元+方向+标的+订单号”归因，至少 `20` 个实际交易日且 `30` 笔已完成空头交易后才给出保留、降级或回本地修复结论。
- 本计划用于替换初始化模板，并驱动后续全部实施。
- 当前已接受基线为 M0 基础设施初始化，基线提交为 `96259ad`。
- M1 已完成知识库 schema、KB 校验、wiki index、资料投放流程的最小闭环。
- M2 已完成测试数据、OHLCV schema、CSV/JSON 回放的最小闭环。
- M3 已完成 PA context、setup、signal 输出的 research-only 最小闭环。
- M4 已完成最小回测引擎与报告的 deterministic baseline。
- M5 已完成 paper-only 的模拟执行与风控闭环。
- M6 已完成新闻过滤与复盘整合，且保持新闻只作辅助因子。
- M7 已完成正式券商 API readiness assessment，当前冻结结论为 `no-go`。
- 当前分析基线固定为 `main`。
- `feature/m7-broker-api-assessment` 保留为历史阶段/里程碑分支，不再作为未来默认合并目标。
- `M8B` 已于 merge commit `0047100` 从 `integration/m8-reliability-validation` 整合进稳定基线 `feature/m7-broker-api-assessment`。
- M8 的基础离线可靠性红线与 shadow/paper 框架已完成并冻结，作为后续验证主线的前置基线。
- `M8C` 当前已切换为 `Long-Horizon & Intraday Paper Validation`：
  - `M8C.1：长周期日线验证` 已完成并整合进稳定基线
  - `M8C.2：单标的日内试点` 已完成并整合进 `main`
  - `M8C.2` 的第二标的扩展验证已完成并整合进 `main`
- `M8B.1` 已完成知识源接入诊断与最小补齐：补齐 transcript / Brooks PPT 的 `source` 页、rule-pack / index 接线，并修复默认 strategy bundle 读取 active rule pack 的缺口。
- `M8B.2a：Knowledge Atomization 基础层` 已完成，`M8B.2b：Knowledge Trace 接入` 已完成并整合进稳定基线。
- M8D.1：Artifact & Trace Unification 已完成，当前已把 `m8c1_long_horizon_daily_validation` 重算到 canonical trace contract，并统一 `actual hit / actual evidence / bundle support` 的 artifact 语义。
- M8D.2：Curated Promotion Minimal Expansion 已完成，当前已新增 `breakout_follow_through_failed_breakout` 与 `tight_channel_trend_resumption` 两个最小 promoted curated `rule` theme，并让 transcript / Brooks 通过这些 theme 的 evidence chain 进入更具体的 actual trace。
- M8D.3：Repository State Consistency 已完成，当前已把 README / status / active-plan / acceptance / decisions / roadmap / reliability docs / testing READMEs 对齐到 `main + M8 + M8 shadow/paper baseline + M8D.1/.2/.3` 的主线口径。
- M8E.1：Validation Gap Closure 已完成，当前已把 checked-in `summary.json` / `report.md` 的路径元数据统一为 repo-relative logical path，给 `m8c1_long_horizon_daily_validation` 增加 `sample_adequacy`，并补齐 golden catalog smoke test 与 artifact portability 回归。
- M8E.2：Longer-Window Daily Validation 已完成，当前已新增 `config/examples/public_history_backtest_longer_window.json`，并生成 checked-in run `reports/backtests/m8e2_longer_window_daily_validation/`，覆盖 `2018-01-01 ~ 2026-04-17`、`NVDA / TSLA / SPY`、`1d` 的更长窗口 daily validation。
- M8E.3 当前继续延后：在每个标的至少具备 `30` 个完整 regular session 之前，不启动 intraday 更长窗口扩展。
- M9 Strategy Lab / Strategy Factory 已降级为 legacy / historical baseline，不再作为后续策略提炼默认工作面。
- 已从 `main` 切出 `codex/m10-price-action-strategy-refresh` 作为 M10 新支线，目标是基于 Brooks v2 手工转录、方方土 YouTube transcript、方方土 notes 重新提炼策略目录与测试规划。
- M10 的来源优先级固定为：`brooks_v2_manual_transcript > fangfangtu_youtube_transcript > fangfangtu_notes`。
- M10 clean-room 提炼阶段禁止读取或参考旧 `PA-SC-*`、`SF-*`、旧 strategy cards、旧 specs、旧 triage 或旧 catalog；旧内容只允许在新 catalog 生成后做 comparison / legacy inventory。
- M10.1 已完成 catalog 复审与测试承接：当前冻结 `16` 条 `M10-PA-*` 策略/规则条目，并把策略分流为 `backtest_wave_a`、`backtest_wave_b_candidate`、`visual_golden_case_first`、`supporting_rule`、`research_only`。
- M10.2 已完成 Visual Golden Case Pack：`M10-PA-003/004/007/008/009/010/011` 均已生成 3 正例、1 反例、1 边界例的 Brooks v2 图例包；该状态只表示证据包完整，仍不代表策略有效或盈利。
- M10.3 已完成 Backtest Spec Freeze：为 Wave A 的 `M10-PA-001/002/005/012` 冻结可执行 backtest specs、event definition ledger、skip rule ledger、成本/样本门槛 policy。
- M10.4 已完成 Historical Backtest Pilot：使用本地 Longbridge daily 长窗口与 5m intraday cache 跑通 Wave A specs、dataset inventory、candidate events、skip/no-trade、source ledger、成本敏感性、per-symbol、per-regime 与 failure-mode 输出链路；该阶段只验证规格和测试链路，不证明盈利，也不输出 `retain/promote`。
- M10.5 已完成 Read-only Observation Plan：把 M10.4 Wave A pilot 转成只读观察队列、事件 schema、pilot quality review 与 M11 paper gate handoff；本阶段不启动实时观察 runner，不接 broker，不下单，不批准 paper trading。
- M10.6 已完成 Read-only Observation Input / Ledger Prototype：使用本地 cached OHLCV 做 recorded replay，只生成只读 observation ledger 与输入 manifest，不接 broker、不接真实账户、不做实时订阅、不生成执行或盈亏结论。
- M10.7 已完成 Business Metric Policy：冻结后续甲方报告的资金口径、资本模型、成本敏感性、必备指标与报告模板；本阶段只定义模拟测试成绩单口径，不输出策略收益结论或 paper trading 批准。
- M10.8 已完成 Wave A Capital Backtest：把 `M10-PA-001/002/005/012` 的 M10.4 candidate events 转成甲方可读模拟资金曲线、交易明细、胜率、回撤、分标的/分周期与成本压力成绩单；结果仍仅为 historical simulation，不批准 paper trading。
- M10.9 已完成 Definition Tightening：只对 `M10-PA-005` 做重复确认去重与日内 20-bar 冷却复测，触发密度下降但因 range geometry 字段缺失仍保留 `needs_definition_fix`。
- M10.10 已完成 Visual Wave B Gate：`M10-PA-003/008/009/011` 与既有 `M10-PA-013` 进入 Wave B queue；`M10-PA-004/007/010` 暂不进入自动回测。
- M10.11 已完成 Wave B Capital Backtest：对 `M10-PA-013/003/008/009/011` 输出第二批模拟资金曲线成绩单；视觉策略仍保留 OHLCV proxy / 人工图形复核边界。
- M10.12 已完成 All Strategy Scorecard：把 16 条 `M10-PA-*` 汇总为甲方状态矩阵、资金测试成绩和 portfolio proxy；该 proxy 只用于业务视角汇总，不是可执行组合回测，不批准 paper trading。
- M10.13 已完成 Read-only Observation Runbook：把通过筛选的 `M10-PA-001/002/012/008/009` 整理为只读观察队列、周报模板和暂停条件；本阶段不启动真实观察，不接 broker，不批准 paper trading。
- M11 已完成 Paper Gate 当前检查点：输出 paper gate report、候选策略清单和风险暂停规则；`M10-PA-001/002/012` 只是 Tier A 核心观察候选，`M10-PA-008/009` 只是 Tier B 视觉条件候选，当前 gate decision 为 `not_approved`，不批准 paper trading。
- M12.0 已完成 Longbridge Read-only Auth Preflight：确认本地 Longbridge CLI 可用且当前 token/quote/K 线只读探针可用；本阶段只允许 `check / quote / kline / subscriptions` 行情检查命令，不调用交易、账户、资产、持仓或订单命令，不批准 paper/live。
- M12.1 已完成 Longbridge Read-only Feed 原型：使用 Longbridge quote 作为健康检查、K 线轮询作为只读 bar-close 输入，输出 `16` 条 `SPY/QQQ/NVDA/TSLA x 1d/1h/15m/5m` feed ledger；该阶段只生成输入，不运行策略、不生成交易/账户字段、不输出盈亏结论。
- M12.2 已完成 Core Strategy Daily Observation：只消费 M12.1 feed ledger 与 M10.13/M11 Tier A 候选，生成 `32` 条只读观察记录；由于 M12.1 feed 只有单根 latest bar，本阶段诚实写入 `skip_no_trade` 而不伪造完整策略触发。
- M12.3 已完成 Visual Review Precheck：复用 M10.2 visual packs 与 M10.10 visual gate，整理 `7` 条策略、`30` 个图例 case 的预审包；Brooks 图片仍 local-only，当前通过旧 M10 worktree 解析且 checksum 全部匹配，仍不替代人工图形判断。
- M12.4 已完成 Definition Fix and Retest：`M10-PA-005` 复用 M10.9 definition tightening 的 before/after 复测指标；`M10-PA-004/007` 只沉淀可执行字段缺口和图例证据，不生成假交易结果；本阶段没有按收益曲线调参，也不批准 paper trading。
- M12.5 已完成 Liquid Universe Scanner：第一版高流动性 US 股票/ETF seed 共 `147` 只，自动 scanner 只启用 Tier A `M10-PA-001/002/012`；当前本地 cache 实际完成 `SPY / QQQ / NVDA / TSLA` 扫描并输出 `12` 条候选，其余 `143` 只缺少本地数据的 seed 全部 deferred，不补假候选。
- M12.6 已完成 Weekly Client Scorecard：把历史资金测试、每日只读观察、scanner 候选、图形预审和定义修正合成 `16` 条策略 dashboard、客户周报和下周行动计划；当前只作为模拟观察与测试管理材料，不作为交易批准。
- M11.5 已完成 Paper Gate Recheck：基于 M12.2-M12.6 的实际 artifact 复查 `M10-PA-001/002/012/008/009`，当前 gate decision 仍为 `not_approved`；核心阻塞是没有完成真实只读观察窗口、M12.2 没有 completed candidate events、`M10-PA-008/009` 人工图形复核未关闭、`M10-PA-005/004/007` definition blocker 未关闭或未正式降级、scanner universe cache 覆盖不完整、缺少人工业务审批。
- M12.7 已完成 Daily Trend Benchmark Reuse：把早期截图中的 `signal_bar_entry_placeholder` 日线逻辑固定为 `M12-BENCH-001 Daily Trend Momentum Baseline`，用本地 Longbridge `1d` 长窗口 `2010-06-29 ~ 2026-04-21` 重跑 `SPY / QQQ / NVDA / TSLA`；结果只作为 `scanner_factor_candidate` benchmark，不反向污染 M10 clean-room catalog，也不作为准入证据。
- M12.8 已完成 Universe Kline Cache Completion 的 coverage / deferred / fetch-plan 交付：M12.5 的 `147` 只 seed 全部入账为 `147 x 4` 个 timeframe rows；当前只有 `4` 只有任一 native cache，目标窗口完整覆盖标的为 `0`，`588` 个缺口全部进入 deferred/error ledger，`294` 个 native cache 请求进入只读 fetch plan；本阶段不伪造 K 线、不把局部缓存宣称为全 universe 可用，也不批量写入 `local_data/`。
- M12.9 已完成 Visual Review Closure overlay：复用 M10.2 visual packs、M10.10 visual gate 与 M12.3 precheck，覆盖 `M10-PA-008/009/003/011/004/007` 共 `6` 条策略、`30` 个 case；`M10-PA-008/009` 只完成 agent-side closure，仍需用户确认关键图例后才可讨论 gate evidence，`M10-PA-004/007` 仍保持 definition evidence only。
- M12.10 已完成 Definition Fix and Retest：`M10-PA-005` 已从本地 OHLCV 重新持久化 `34651` 条带 range 几何字段的事件记录，但复测后仍为 `reject_for_now_after_geometry_review`；`M10-PA-004/007` 因缺少可执行宽通道/第二腿标签，正式降级为 `visual_only_not_backtestable_without_manual_labels`，不再占用自动回测队列。
- M12.11 已完成 Read-only Trading Dashboard：生成本地只读 HTML 看板、dashboard JSON、snapshot 报告和 handoff，展示 scanner 候选、只读观察事件、hypothetical entry/stop/target、simulated metrics/equity curves、策略状态和 blocker；当前仍不接真实账户、订单、持仓或 live execution。
- M12.12 已完成 Daily Observation Loop 第一版：第一批 `50` 只股票/ETF 的 `1d` 长窗口与当前交易日 `5m` 只读 K 线已补齐，`M10-PA-001/002/012 + M12-FTD-001` 已进入每日只读测试链路，当前生成 `141` 条机会和中文看板；看板已把“候选”解释为未成交机会，并新增机会估算盈亏、早期日线资金曲线和中文状态；长历史 `5m` 全窗口仍未补齐，不得宣称两年日内历史完整。
- M12.14 已完成 Source Strategy Closure：`M12-FTD-001` 已从早期截图/benchmark 口径升级为多来源增强测试候选，来源补入方方土 notes、方方土 YouTube transcript、Brooks v2 手工转录与 Brooks/PPT 支撑页；已新增 `6` 条来源回看候选，并直接关闭 `M10-PA-008/009` 关键图例确认，`M10-PA-005/004/007` 的定义问题也已明确收口。
- M12.15-M12.20 已完成早期强日线策略 A/B 重测、6 条来源回看候选分流、每日只读测试连续记录、视觉策略严格观察、`M10-PA-004/007` 检测器原型与机器识别第一版；当前统一队列已把 M10/M12 策略合并管理，后续不再分开汇报“16 条”和“6 条”。
- M12.21 已完成 Detector Quality Review：对 M12.20 已保留的 `4801` 条机器识别候选做全量结构复核，结构复核未拒绝 `4801` 条，其中 `2785` 条高质量、`1645` 条中等、`371` 条建议抽样看图、自动拒绝 `0` 条；本阶段只回答检测器结构是否自洽，不输出盈利、胜率、回撤或准入结论。
- M12.21 的限制已明确：M12.20 检测器每个 strategy/symbol 最多保留最近 `50` 条事件，因此本次是对 checked-in retained candidates 的全量复核，不代表 raw detector 全历史分布；下一步若要进入历史测试，需补 raw/capped 分布审计与抽样看图。
- M12.22 已完成 Detector Sample Visual Review：对 M12.21 retained `4801` 条候选做全量严格复核，并生成 `467` 条标注图包，覆盖全部 `371` 条 needs_spot_check；结果为清晰像目标图形 `3275` 条、边界样例 `1357` 条、疑似误判 `169` 条，因此 `M10-PA-004/007` 不能直接进入完整历史回测，需先收紧检测器。
- M12.23 已完成 Detector Tightening Rerun：收紧 `M10-PA-004/007` 机器识别规则，并重跑检测、结构复核和严格图形复核；收紧后保留 `3990` 条候选，清晰像目标图形 `3917` 条、边界样例 `73` 条、疑似误判 `0` 条，较 M12.22 边界减少 `1284` 条、疑似误判减少 `169` 条。
- M12.24 已完成 PA004/PA007 Small Historical Pilot：对通过 M12.23 的 `M10-PA-004/007` 做 `1d` 小范围历史模拟；`M10-PA-004` 收益 `-0.9580%`、胜率 `34.72%`、最大回撤 `4.0220%`，结论为保留图形研究；`M10-PA-007` 收益 `0.6473%`、胜率 `41.15%`、最大回撤 `3.8796%`，结论为进入每日观察。
- M12.25 已完成 Daily Observation Continuity：每日测试主线继续保持 `M10-PA-001/002/012 + M12-FTD-001`，当前仍是 `141` 条机会、估算盈亏 `-8482.20`、连续记录 `1/10`；`M10-PA-007` 已加入观察队列但不进入模拟买卖准入，`M10-PA-004` 保留图形研究。
- M12.26 已完成 Cache Scanner Expansion：第一批 `50` 只日线与当日 `5m` 可用于只读 scanner，今日 scanner 候选 `141` 条、覆盖 `45` 只股票；长历史 `5m` 完整覆盖仍为 `0/50`，`147` 只扩展还有 `97` 只缺缓存，不得宣称全量可用。
- M12.27 已完成 PA004 Expanded Retest + Live Read-only Snapshot：美股开盘时段 Longbridge 只读快照已获取 `16` 条 `SPY/QQQ/NVDA/TSLA x 1d/1h/15m/5m` 记录且缺口 `0`；PA004 整体混合版本仍弱，但“只做多”分支转正，收益 `0.5530%`、胜率 `40.24%`、最大回撤 `2.0479%`、交易 `574` 笔，下一步改为 PA004 做多版观察规则，做空版暂不进入主线。
- M12.28 已完成 PA004 Long Dashboard Refresh：PA004 做多版已接入盘中只读模拟看板；本阶段使用 Longbridge 只读 quote 刷新第一批 `50` 只股票当前价格，当前看板显示 `160` 条机会/观察项，其中主线机会 `141` 条、PA004 做多观察 `19` 条，盘中模拟盈亏快照为 `-69888.72`；该盈亏只是当前只读报价覆盖，不是成交和实盘。
- M12.28 的限制已明确：主线候选日期仍为 `2026-04-27`，当前报价日期为 `2026-04-28`，因此本阶段是 `live_quote_overlay_on_prior_candidate_set`，还不是今日 50 只股票重新扫描；下一阶段优先把 M12.12/M12.26 扫描日期滚动到当前交易日，并继续补第一批 `50` 只长历史 `5m`。
- M12.29 已完成 Current-day Scan + Minute Read-only Dashboard：已把第一批 `50` 只股票滚动到当前美股交易日 `2026-04-28` 重新扫描，Longbridge 只读 quote 覆盖 `50` 只，日线与当日 `5m` 可用均为 `50/50`；当前今日新机会 `122` 条，PA004 做多观察 `19` 条，总可见机会 `141` 条，当前模拟盈亏快照为 `843.92`。
- M12.30-M12.31 已完成策略全量收口与图形/定义终局：`M10-PA-001/002/012/M12-FTD-001` 进入每日实时只读测试；`M10-PA-004` 仅保留做多观察，`M10-PA-007/008/009` 进入观察队列；`M10-PA-003` 作为过滤器/排名因子；`M10-PA-005/006/010/016` 为研究项；`M10-PA-014/015` 为辅助规则；不再挂“等待用户确认”作为主线阻塞。
- M12.32 已优化分钟级中文只读模拟看板：首屏改为共享模拟账户视角，展示当前模拟权益、今日机会、盘中模拟盈亏、模拟收益率、浮盈机会占比、最大回撤参考和可用策略数；第二层按单策略独立展示当前账本表现、胜率、最大回撤、盈利因子、今日模拟盈亏；第三层保留今日机会明细和单策略下钻数据。
- M12.33/M11.6 当前结论：已完成第 `1/10` 个真实交易日记录；模拟交易试运行准入仍未批准，原因是还没满连续 `10` 个交易日且仍需继续验证每日扫描稳定性。这里的未批准不是策略失败，而是观察天数还不够。
- M12.34-M12.39 当前已进入自动盘中测试与 Codex 观察员阶段：观察策略不再是空状态，已进入低准入每日测试 lane；看板已按 `1d / 1h / 15m / 5m` 分组；`M12-FTD-001` 已单独展示重点监控；自动运行器、Codex 观察员 inbox 与 Codex App 心跳自动化 `m12` 已落地，心跳只在北京时间美股交易相关窗口运行，21:30 前做开盘预热，开盘时刷新只读 K 线/报价/策略机会并中文汇报，非交易时段不再 24 小时轮询，仍只做只读行情、模拟盈亏和中文摘要。
- M12.40-M12.45 / M11.7 当前已完成新闻财报 sidecar 第一版：新闻/财报只作为标签、风险提示、优先级加分和复盘解释，不回写策略定义、不触发真实交易；Google/Alphabet 2026-04-29 财报盘后跳涨已被双 ticker 标注到 `GOOG/GOOGL`，但 2026-04-30 次日确认候选为 `M10-PA-012` 看跌信号，和财报利好方向相反，因此当前新闻影响结论为 `risk_filter_only`。
- M12.42 已完成 `M12-FTD-001` 新闻/风险 A/B 第一版：baseline 收益 `610.44%`、最大回撤 `48.65%`；`loss_streak_guard` 版本在当前历史交易序列上收益 `1136.15%`、最大回撤 `25.07%`、胜率 `39.48%`，作为下一轮 FTD001 增强候选，但仍需每日只读观察验证。
- M12.43 已完成财报跳空 / 盘后异动策略原型：Google 案例进入观察队列，当前仅说明“财报跳空后次日存在价格确认候选”，不批准模拟交易试运行。
- M12.45 已完成统一策略成绩单与组合代理视图：当前合并 `23` 条策略/候选状态，`4` 条每日测试、`4` 条观察测试；共用 `100,000 USD` 模拟本金组合代理当前模拟盈亏 `1031.71`、胜率 `46.67%`、最大回撤 `2.83%`。
- M11.7 当前复查仍为未批准：新闻/财报 sidecar、FTD001 优化和财报跳空原型已具备，但只读观察仍为约 `1/10` 个真实交易日，未满 10 天，不进入模拟交易试运行。
- M12.46 当前进行中：已从 `main` 切出 `codex/m12-46-accountized-testing`，把 M12.29/M12.37 从“机会当前价估值”升级为“20,000 USD 独立模拟账户”；当前已落下主线/实验账户分栏、`1d / 5m` 主视图、`M10-PA-004 long-only` 主线化、`M12-FTD-001 baseline / loss_streak_guard` 并行账户、纽约交易日累计修复，并已新增对应单测；下一步继续补回归、同步文档并把自动盘中测试正式切到新账户口径。
- M12.48 当前进行中：已从 `codex/m12-47-realtime-session-supervisor` 切出 `codex/m12-48-premarket-afterhours-monitor`，把 Longbridge `pre_market_quote / post_market_quote` 接入分钟级账户看板和 Codex 观察摘要；当前目标是补齐盘前 quote 刷新、盘后延时 quote 刷新、盘前/盘后异动榜，以及 AMD / 谷歌 / 半导体 / 存储芯片重点关注股提示，且继续保持只读/模拟边界。
- M12.49 已完成并推送运行产物归档：`codex/m12-49-dashboard-runtime-hardening` 已提交 `d6c730f m12.49: archive latest runtime artifacts`，保留最新 M12.12/M12.29/M12.46/M12.48 运行报告和 `2026-05-01` 至 `2026-05-07` 盘后新闻复盘证据。
- M13 当前已完成真实每日策略测试闭环接线目标：已从 `origin/main` 切出 `codex/m13-real-strategy-test-loop`，并合入 M12.49 作为前置基线；当前已新增 `M13 strategy_runtime_registry`、`daily_strategy_test_runner`、策略信号账本、账户操作账本、goal 状态和 AI-Trader 外部研究边界；M12.46 的 `today_signal_count / today_opened_count / today_closed_count` 已改为从当日交易账本统计；`M10-PA-005/007/008/009/011/013` 已接入最小 detector/adapter 进入实验账户账本，其中 `M10-PA-011` 只保留 `5m` 开盘反转账户，不用日线伪装测试。
- M14/M15 当前进行中：M14 已从策略级旧口径改成 `runtime_id` 级动作矩阵，`strategy_id` 只作为父策略分组；当前动作只允许 `advance_internal_sim`、`risk_limited_advance`、`repair_now`、`pause_runtime`、`auxiliary_module`、`paper_candidate`，高回撤和低胜率只产生风险提醒与仓位折扣，不再自动否掉盈利 runtime。M12/M14/M15 活跃输出不再展示或引用旧版历史利润类字段；未来历史表现必须基于 M13 account operation ledger 重新 mark-to-market。M15 长桥实时链路已和本地模拟完全隔离：实时行情采集、实时信号路由、实时账户状态、实时持仓退出、实时执行和实时会话守护器只消费长桥实时行情/账户自身状态/实时信号事件，不读取本地模拟交易账本、旧快速队列或本地平仓，也不把 M13/M14 完整重算作为每笔下单前置。快速队列和全策略预演只保留为本地审计/历史兼容产物。长桥实时链路以 `1` 秒内本地处理为优化目标，第一版 `5` 秒内可接受，超过 `5` 秒必须重新校验当前价格、风控和扣费后利润后再决定。
- M15 当前长桥模拟账户采用单策略单仓正式测试：券商侧仍只有一个 `lb_papertrading` 模拟账户，本地默认按 `test_epoch_id=m15-single-strategy-buckets-20260702` 把合格策略拆成独立 `10000 USD` 虚拟仓；每个单策略仓统一 `1.0` 测试，不再使用 `0.5 / 0.25 / 0.10` 倍率，风控只靠每仓总敞口 `6000 USD`、单标的 `1500 USD`、单笔风险 `20 USD`、现金保留 `4000 USD`，不设置所有单仓合计敞口上限。第一批单仓为 `M10-PA-004-long-1d`、`M10-PA-002-5m`、`M12-FTD-001-baseline-1d`、`M12-FTD-001-loss-streak-guard-1d`、`M10-PA-004-MBF-1d`、`M10-PA-004-MBF-QC-1d`、`M10-PA-013-5m`、`M10-PA-011-ORB-R1-5m`；统一实验仓继续低倍率筛选 `M10-PA-002-1d`、`M10-PA-013-1d`、`M10-PA-008-1d`、`M10-PA-005-1d/5m`、`M10-PA-012-5m`、`M10-PA-001-1d`。旧 `主力仓/实验仓/002专项主力仓` 三仓方案只作历史复盘，不再参与新单仓判断。修复策略、未批准影子变体和辅助模块继续只跑本地模拟，不进入长桥买入通道。当前仍只限长桥模拟账户，live token、真实资金、真正碎股订单、融券做空和期权保持禁用；但大于等于 `1` 股的小数建议数量必须向下取整为整股后重新验算，小于 `1` 股才阻断。
- Git 远端推送阻塞已处理：原始 M10.6 大 JSONL 通过 gzip archive manifest 保留可追溯，未把超过 GitHub 单文件限制的原始 blob 推送到远端；`main` 已可推送。
- 下一阶段主线是继续用 `scripts/run_m12_37_intraday_auto_loop.py --session` 在美股开盘相关时段自动预热并持续刷新看板和 Codex 摘要；该入口已自动运行 M13 并在盘后运行 M14 固化每日 ledger/challenge/gate。M12.47 守护器必须使用配置化美股休市日历，周一假日不得被当作下一交易日拉起 M12.37。M13 最新有效样本已显示所有 required 策略/模块都有有效账本状态且 `blocked_strategy_ids=[]`；M14.1 已修复 fallback close、stop/target 成交价、以及“旧持仓不在当天信号行就不更新报价”的全量持仓估值缺口。当前 M14 gate 已基于既有 fully-ready challenge 重算为 `10/10`，`M10-PA-004 / M10-PA-005 / M10-PA-008` 进入 `approved_internal_sim_only`，但仍只允许内部模拟，不接 broker、不下真实订单；M12.47 `--status` 会把该准入结果同步到券商式主面板。下一步继续按自动化积累新交易日，同时记录 `PA004 baseline / PA004-MBF / PA004-MBF-QC` 三条独立账户。`M10-PA-001/002/007/009/012/013/M12-FTD-001` 当前进入 `modify_candidate`，但 baseline 不静默覆盖，后续只能通过新变体 A/B 验证。
- Codex App 自动化已优化：`m14` 守护检查从每小时 cron 调整为每个美股交易日前北京时间 `21:10` 执行一次，避免反复新建“策略测试闭环守护”会话；盘中真实刷新继续由 M12.47 supervisor 和 M12.37 child session 承担。
- M14.1 看板当前补丁已修复 PA004 对照区与 M14 gate 的同步漏网：`strategy_accounts`、`pa004_comparison.rows`、自选股信号数/策略列表和空历史百分比显示现在保持一致，避免首屏出现同一策略两套准入结论。
- M14.2 / M15 预备方向：可以提前做模拟账户与未来券商接入的 readiness scaffold，但默认必须是 `paper_dry_run_only`、`live_disabled_by_default`、`broker_connection=false`、`real_orders=false`。允许准备接口、审批 gate、凭证隔离、订单预演日志、kill switch、回放一致性测试和人工审批清单；不允许接真实账户、不允许真实下单、不允许把 broker paper 或 live 开关默认打开。
- M14.2 当前已切出 `codex/m14-2-broker-readiness-scaffold`，目标是在不联网、不接账户、不下单的前提下，先完成 `BrokerReadinessConfig / BrokerOrderPreview / BrokerReadinessPlan`、dry-run plan builder、示例配置和测试；后续只允许接 M14 gate artifact 生成审计报告，不允许直接进入 broker paper/live。
- M14.2 已补上真实 artifact 生成器：`scripts/run_m14_2_broker_readiness_scaffold.py` 只读 M14 gate 与内部模拟执行账本，批量生成 `m14_2_broker_readiness/broker_readiness_plan.json` 和 `broker_readiness_audit.jsonl`。当前样本去重后显示 `8` 条内部模拟 `risk_check` 中 `5` 条 broker dry-run ready、`3` 条被风控阻断；M14 内部桥接已补 `close` 释放敞口，并把总敞口配置对齐账户化 runtime 资金口径，单笔风险 `100` 不变。下一步策略救援应优先减少低质量信号、超敞口、超单笔风险和连续亏损触发，不得为了提高通过率跳过风控。
- M14.2 broker blocker diagnostics 已落地：`scripts/run_m14_2_broker_blocker_diagnostics.py` 只读 broker readiness plan、M14 challenge 风控配置和内部模拟执行账本，生成 `broker_blocker_diagnostics.json/md`。当前 `3` 条 blocked dry-run rows 分布在 `M10-PA-005 / M10-PA-008`：`M10-PA-008` 是单笔风险略超 `100`，应影子测试 quantity cap；`M10-PA-005` 同时触发总敞口与连续亏损暂停，应影子测试组合曝险排序、同日 cooldown 和质量 veto。该诊断只用于救援优化，不改变 readiness status，不批准 broker/paper/live。
- M14.2 broker blocker shadow repair plan 已落地：`scripts/run_m14_2_broker_blocker_shadow_repair.py` 只读 blocker diagnostics，生成 `broker_blocker_shadow_repair.json/md`。当前 shadow plan 将 `M10-PA-008 / ADBE` 标为 quantity-cap A/B 候选，数量从 `5.2469` 降至 `5.2083` 后风险可从 `100.74` 压回 `100`；`M10-PA-005 / XLY` 只能延后等待敞口或由排序器让路，`M10-PA-005 / XLV` 必须保留连续亏损暂停并只做 cooldown/quality veto 影子测试。原 broker readiness rows 继续保持 blocked，不批准 broker/paper/live。
- M14 goal readiness 已接入 broker blocker shadow repair：`scripts/run_m14_goal_readiness_report.py` 现在把 quantity-cap、exposure deferral、cooldown halt 汇总进单一验收视图和 `next_actions`；当前总览显示 `1` 个 quantity-cap 候选、`1` 个敞口延后、`1` 个 cooldown 暂停，且 `readiness_status_mutation=false`、broker/live/real order 继续全禁用。
- M14.2 broker blocker shadow A/B prep 已落地：`scripts/run_m14_2_broker_blocker_shadow_ab_prep.py` 只读 `broker_blocker_shadow_repair.json`，生成 `broker_blocker_shadow_ab_prep.json/md`；当前把 `3` 条 repair rows 转成 `1` 个可后续评审登记的 `M10-PA-008` quantity-cap 影子 runtime 候选，以及 `2` 个 `M10-PA-005` rule-only 影子候选（敞口排序、cooldown/quality veto）。该 prep 不修改 M13 registry、不修改 M12 account specs、不改变 broker readiness blocked rows，也不允许 broker/live。
- M14.2 broker blocker rule-only shadow evidence 已落地：`scripts/run_m14_2_broker_blocker_rule_shadow_evidence.py` 只读 `broker_blocker_shadow_ab_prep.json`，把 `M10-PA-005` 的 `2` 个 rule-only 候选固化为 evidence rows（`portfolio_exposure_ranker=1`、`cooldown_quality_veto=1`）。该 artifact 只记录下一次内部模拟 fresh refresh 的对比契约，不创建 runtime、不改 M13 registry、不改 M12 account specs、不改变 broker readiness blocked rows，也不允许 broker/live。
- M14 rescue 当前已新增 `scripts/run_m14_strategy_rescue_plan.py`：`M10-PA-004 / M10-PA-005 / M10-PA-008` 继续内部模拟；`M10-PA-001/002/004-MBF/004-MBF-QC/007/009/012/013/M12-FTD-001` 进入新变体 A/B 救援；`M10-PA-011` 先重建 detector 再决定是否最终放弃；plugin/filter/research 策略继续做 A/B 或影子覆盖，不伪装成独立交易账户。AI-Trader 和 TradingAgents 只作为公开项目架构与影子信号研究参考。
- M14 rescue 首批变体已进入 M13 registry：所有已登记 rescue runtime 先以独立 `rescue` lane 进入每日 ledger，目标是让未盈利或待修复策略从“计划中要救”变成“每天有账本状态”。当前 `M10-PA-001-m14-modify-20260522`、`M10-PA-002-m14-modify-20260522`、`M10-PA-004-MBF-QC-m14-modify-20260522`、`M10-PA-007-m14-modify-20260522`、`M10-PA-008-broker-risk-cap-shadow`、`M10-PA-009-m14-modify-20260522`、`M10-PA-012-m14-modify-20260522`、`M10-PA-013-m14-modify-20260522`、`M12-FTD-001-m14-modify-20260522`、`M10-PA-011-ORB-R1` 已接入 M12 accountized `rescue` lane；其中 PA004-MBF-QC rescue 使用更严格的风险压缩 adapter，PA008 broker-risk-cap shadow 只在影子账户中测试单笔风险 `100.00` 的数量上限，ORB-R1 使用 5m 失败开盘区间突破后回测确认 adapter，其余复用父策略扫描器并增加质量过滤；下一步继续用 10 日 A/B 数据判断 `promote / modify / reject`，不得把已接线等同于已通过。
- M14 rescue runtime 覆盖审计已落地：`scripts/run_m14_rescue_runtime_coverage.py` 当前证明 M13 registry 中 `11/11` 条 rescue 策略、`12` 个 M12 account specs 输入流均已接线，M14 rescue plan 中 `10/10` 个 rescue/rebuild 动作已有 runtime 覆盖；该审计只证明输入覆盖，不代表策略通过或准入，所有 rescue runtime 仍必须继续累计 10 个交易日 A/B ledger 后再判断 `promote / modify / reject`。
- M14 goal readiness 总览已落地：`scripts/run_m14_goal_readiness_report.py` 当前把 M14 challenge、internal sim gate、rescue coverage 与 M14.2 broker dry-run 汇总成单一验收视图；结论是 10 日 challenge 已够，`M10-PA-004 / M10-PA-005 / M10-PA-008` 可继续内部模拟账户测试，弱策略救援变体已准备进入 10 日 A/B 取证，目标仍未完成，因为 rescue 变体尚未形成新 10 日证据且 broker/live 继续禁用。
- M14 internal simulated-account launch readiness 已落地：`scripts/run_m14_internal_sim_launch_readiness.py` 当前把 M14 paper gate、M13 runtime registry、M12 account input audit、M13 scorecard 与 M14.2 broker readiness 合成获批策略启动检查；最新结果为 `3/3` 个 approved internal-sim 策略、`4/4` 个 approved runtime 输入已接线，`hard_boundary_violation_count=0`。`M10-PA-004` 可直接继续内部模拟；`M10-PA-005 / M10-PA-008` 可继续内部模拟但需保留 broker dry-run blocker watch；`can_start_broker_paper=false`，broker/live/real order/paper approval 继续全禁用。
- M14 internal simulated-account next-session plan 已落地：`scripts/run_m14_internal_sim_next_session_plan.py` 当前把 internal-sim launch readiness、goal readiness、rescue next-refresh readiness 与 rescue A/B evidence 合成下一次 M12.47 fresh refresh 的执行/验收清单；最新结果为 `can_run_next_internal_sim_session=true`，`3/3` 个 approved 策略、`4/4` 个 approved runtime 输入可继续内部模拟，`13` 条 rescue watch rows 和 `2` 条 first-ledger watch 等待下一次 fresh refresh 后验收。该计划明确禁止手动 `M12.37 --once`，下一次刷新只能由 M12.47 守护器拉起，且 broker/live/real order/paper approval 继续全禁用。
- M14 project stage assessment 已接入 post-refresh outcome review、strategy evidence gap matrix、burndown 队列与 pre-refresh review packet：`scripts/run_m14_project_stage_assessment.py` 当前把 goal readiness、internal-sim next-session plan、rescue plan、rescue optimization backlog、post-refresh outcome、evidence gap matrix、evidence gap burndown、pre-refresh review packet 和 post-fresh checklist 合成单一项目阶段/策略路线图；最新结论为项目处于 `M14 stable strategy testing + M14.2 broker readiness dry-run scaffold`，10 日 challenge `10/10` 已满足，`M10-PA-004 / M10-PA-005 / M10-PA-008` 继续内部模拟，`8` 条救援路线继续收集 A/B 证据，`1` 条并行 A/B，`1` 条先重建 detector 后再判断，`7` 条仅保持 shadow/plugin/research 覆盖；post-refresh 当前 `fresh_refresh_observed=false`、`quote_source=fallback_quotes_only`、`13/13` watch rows 仍等待 M12.47 fresh refresh、`passed_count=0`、`failed_count=0`；burndown 当前把 `20` 条 open gap 排成 `P0/P1/P2=12/1/7`，其中 `3` 条 approved refresh、`2` 条 first-ledger watch、`10` 条 rescue A/B、`11` 条 shadow-review；pre-refresh review packet 当前有 `19` 条 review rows、`1` 条 held row，`12` 条仍依赖 fresh refresh、`7` 条只允许 artifact/source review，close/promote/discard/mutation allowed 均为 `0`。目标仍未完成，因为 rescue promotion 仍为 `0`、`2` 条 rescue runtime 还缺首个 M13 ledger，且 broker/live/real order/paper approval 继续全禁用。
- M14 rescue A/B evidence tracker 已落地：`scripts/run_m14_rescue_ab_evidence_tracker.py` 当前把 rescue coverage、M14 gate、M13 scorecard 与 M13 signal/account ledger 合成取证门禁；最新结果显示 rescue runtime coverage 完整，既有 9 条 rescue 变体已有 M13 ledger evidence，新接入的 PA008 broker-risk-cap shadow 与 PA012 target/stop 影子 runtime 尚待下一次 M12.47/M13 刷新生成首条 ledger，因此当前为 `9/11` observed、`2` 条 no-ledger，父策略历史不得折算为 rescue 变体通过证据，promotion allowed 固定为 `0`。
- M14 rescue optimization backlog 已落地：`scripts/run_m14_rescue_optimization_backlog.py` 当前把 rescue plan、rescue A/B evidence 与 M14.2 broker dry-run blocker 合成 10 日 A/B 未满前可提前做的优化队列；M13 已修正 M12 trade ledger 的交易日归因，显式 `trading_date` 优先，旧 open 记录回退到 `signal_time`，因此 `M10-PA-011-ORB-R1` 已从 signal-to-account no-op 变为 `open_count=2`、继续等待更多 A/B 证据。最新结果显示 `11` 条 rescue 变体均有 backlog 行，其中 `8` 条为已接线但零信号，`2` 条新影子 runtime（PA008 broker-risk-cap、PA012 target/stop）等待下一次 M12.47-owned fresh run 生成首条 M13 ledger，`0` 条为有信号但没有 account operation，另有 `M10-PA-005 / M10-PA-008` 的 `3` 个 broker dry-run 风控阻断需要继续用 sizing、exposure、cooldown 或 stop/target normalization 修复；该 backlog 不代表晋级，也不允许 broker/live。
- M14 rescue next-refresh readiness 已落地：`scripts/run_m14_rescue_next_refresh_readiness.py` 当前把 rescue backlog、zero-signal diagnostics、A/B evidence、PA012 target/stop shadow normalization 和 PA005 broker-rule evidence 合成下一次 M12.47 fresh refresh 后的验收矩阵；最新结果为 `13` 条 watch rows，其中 `7` 条 fresh quote 复核、`2` 条首个 M13 ledger watch、`2` 条 PA005 broker-rule shadow 复核、`1` 条 PA012 target/stop shadow 对比、`1` 条 PA013 parent-detector 同周期等待。当前 `parameter_change_allowed_now_count=0`，说明下一步先等 fresh evidence，再决定是否调参或继续重建 detector，不允许 broker/live。
- M14 rescue post-refresh outcome review 已落地：`scripts/run_m14_rescue_post_refresh_outcome_review.py` 当前只读 next-refresh readiness、M12 dashboard、M13 scorecard/ledger 和 M14.2 broker readiness，生成 refresh 后验收结果；最新快照因 `quote_source=fallback_quotes_only` 判定 `fresh_refresh_observed=false`，所以 `13/13` 条 watch rows 全部保持 `waiting_for_m12_47_fresh_refresh`，`passed_count=0`、`failed_count=0`。下一次 M12.47-owned fresh refresh 后，该评审器会直接判定 `2` 条首个 ledger、`7` 条 fresh quote、`2` 条 PA005 broker-rule、`1` 条 PA012 target/stop、`1` 条 PA013 parent-detector watch 是否通过；仍不允许手动 M12.37 once-mode、参数即时变更、registry/account-spec/broker readiness mutation 或 broker/live。
- M14 rescue external reference map 已落地：`scripts/run_m14_rescue_external_reference_map.py` 当前把 AI-Trader 的 agent-native signal sync / paper sandbox / experiment exposure tracking 思路，以及 TradingAgents 的 analyst / researcher / risk / portfolio-manager 分工和 persistent decision log 思路，映射到本地 `11` 条 rescue rows 与 `2` 条 broker-blocker rows；该 artifact 只输出本地 shadow scoreboard、技术证据复核、bull/bear objection、risk/portfolio review 与 decision-log audit 清单，不允许 copy trading、外部 agent 决策覆盖 M13/M14 gate、broker sync、真实订单、live execution、paper approval 或手动 M12.37 once-mode。该汇总已接入 M14 project stage assessment。
- M14 rescue parameter experiment queue 已落地：`scripts/run_m14_rescue_parameter_experiment_queue.py` 当前把 rescue backlog、zero-signal/target-stop diagnostics、next-refresh readiness、external reference map 和 project stage assessment 合成 `14` 条只读参数实验候选；其中 `11` 条等待下一次 M12.47 fresh refresh、`2` 条等待首个 M13 ledger、`1` 条继续 A/B 取证，覆盖 fresh quote gate、target/stop reward geometry、ledger path、PA013 parent detector、PA005 exposure/cooldown 和 PA008 quantity cap。当前 `allowed_now_count=0`，不改 M13 registry、不改 M12 account specs、不改 broker readiness，不允许 broker/live 或手动 M12.37 once-mode。该队列已接入 M14 project stage assessment。
- M14 rescue parameter activation gate 已落地：`scripts/run_m14_rescue_parameter_activation_gate.py` 当前把参数实验队列与 post-refresh outcome review 合成参数实验验收门；最新结果 `14` 条 gate rows 中 `13` 条仍等待 M12.47 fresh refresh、`1` 条继续 A/B 取证，`shadow_review_candidate_count=0`、`implementation_mutation_allowed_count=0`、`parameter_mutation_allowed_count=0`。该 gate 只说明 fresh evidence 通过后哪些参数家族可进入人工 M14 shadow-review，不直接改参数、不改 registry/account specs/broker readiness，也不允许 broker/live 或手动 M12.37 once-mode。该 gate 已接入 M14 project stage assessment。
- M14 rescue parameter shadow spec 已落地：`scripts/run_m14_rescue_parameter_shadow_spec.py` 当前把参数实验队列、activation gate、PA012 target/stop normalization、PA008/PA005 broker-blocker shadow prep/evidence 与外部参考映射合成 `14` 条只读影子规格和 `14` 个候选变体；其中 `12` 条仍等待 M12.47 fresh evidence，已固化 `1` 个 PA012 `risk_normalized_1_0r` target/stop 影子变体、`1` 个 PA008 quantity-cap 变体、`2` 个 PA005 broker-rule 变体，以及 fresh quote recheck、ledger mapping、parent detector 和继续 A/B 的验收口径。该 spec 只作为 review/spec artifact，不触发参数实现、不改 registry/account specs/broker readiness，不允许 broker/live/真实订单/paper approval 或手动 M12.37 once-mode，并已接入 M14 project stage assessment。
- M14 strategy decision ladder 已落地：`scripts/run_m14_strategy_decision_ladder.py` 当前把 goal readiness、next-session、rescue A/B evidence、parameter shadow spec、external reference map 与 objective execution plan 合成 `20` 条策略决策阶梯；最新结果为 `3` 条 approved 策略只能进入下一次 M12.47-supervised 内部模拟刷新，`10` 条策略继续 rescue/detector/参数影子取证，`7` 条保持 shadow/plugin/research coverage，`final_discard_allowed_count=0`、`promotion_candidate_count=0`。该阶梯明确最终放弃必须等 rescue route、shadow spec、首个 ledger、10 日 A/B 证据和人工 M14 review 全部完成，不允许 broker/live、真实订单、paper approval、参数 mutation、registry/account-spec/broker readiness mutation 或手动 M12.37 once-mode，并已接入 M14 project stage assessment。
- M14 objective completion audit 已补强：`scripts/run_m14_objective_completion_audit.py` 当前把用户目标拆成 `12` 项机器可读需求审计，并直接读取 parameter shadow spec、strategy decision ladder 与 strategy evidence gap matrix 作为弱策略救援/调参路径证据；其中 `4` 项 proven（项目阶段、10 日 challenge、approved internal sim、外部参考映射）、`3` 项 guardrail（内部模拟口径、broker/live 禁用、手动 M12.37 once-mode 禁用）、`2` 项 in_progress（弱策略救援、参数实验路径）、`3` 项 blocked（rescue promotion 证据、fresh refresh 前置、整体目标完成）。当前 `objective_complete=false`，原因是 rescue promotion 仍为 `0`、fresh refresh 尚未出现、parameter activation shadow-review candidate 仍为 `0`，且 strategy evidence gap matrix 仍有 `20` 条 open gap（`12` 条等 fresh refresh、`2` 条缺首个 rescue-specific M13 ledger、`10` 条缺 rescue 10 日 A/B、`11` 条缺 shadow-review）；审计已确认 `14` 个参数影子变体、`10` 条 rescue-continuation ladder rows、`final_discard_allowed_count=0`，并已接入 M14 project stage assessment，继续保持只读/模拟，不允许 broker/live、参数 mutation、registry/account-spec/broker readiness mutation 或手动 M12.37 once-mode。
- M14 objective execution plan 已落地：`scripts/run_m14_objective_execution_plan.py` 会把 objective completion audit 的 blocked/in_progress 项拆成 `7` 条执行动作；当前 `P0=5`，`5` 条动作仍需要 M12.47-owned fresh refresh，覆盖 approved internal-sim 下一次刷新、2 条 no-ledger rescue 首账本 watch、11 条 rescue A/B 继续取证、14 条参数 activation gate fresh-evidence 后 shadow-review、broker dry-run watch-only、外部参考 review-only 和目标完成复核；目标完成复核现在直接显示 `open evidence gaps=20`。该计划不触发手动 M12.37 once-mode、不改参数、不改 M13 registry/M12 account specs/broker readiness，不允许 broker/live/真实订单/paper approval。
- M14 post-fresh-refresh recompute checklist 已落地：`scripts/run_m14_post_fresh_refresh_recompute_checklist.py` 当前把下一次 M12.47 fresh refresh 后的重算顺序、验收门和边界固化为 `29` 步清单，其中 `28` 个只读 M14 script steps、`9` 个 acceptance gates，并明确 objective audit / objective execution / strategy decision ladder / strategy evidence gap matrix / strategy evidence gap burndown / source visual confirmation response gate / future source-reextract spec prep / objective blocker burndown / strategy next-step matrix / internal-sim trial acceptance gate / strategy pre-refresh review packet 需要稳定化后再刷新 project stage assessment。当前仍 `fresh_refresh_observed=false`、`quote_source=fallback_quotes_only`、`13` 条 post-refresh watch rows 等待，清单只定义 refresh 后怎么验收，不触发 M12.37 once-mode、不接 broker、不改参数、不改 registry/account specs/broker readiness；旧版历史利润类字段已经废弃，不再作为 display-only 指标或规划输入。
- M14 strategy evidence gap matrix 已落地：`scripts/run_m14_strategy_evidence_gap_matrix.py` 当前把 strategy decision ladder、rescue A/B evidence、parameter shadow spec、objective audit/execution 与 post-fresh checklist 合成 `20` 条策略证据缺口矩阵；最新结果为 `20/20` 条仍有 open evidence gap，`12` 条需要下一次 M12.47 fresh refresh，`2` 条缺首个 rescue-specific M13 ledger，`10` 条缺 rescue 10 日 A/B 证据，`11` 条缺 shadow-review 证据；`final_discard_allowed_count=0`、`promotion_candidate_count=0`、`parameter_mutation_allowed_count=0`。该矩阵已接入 M14 project stage assessment，继续保持只读/模拟，不允许 broker/live、真实订单、paper approval、参数 mutation、registry/account-spec/broker readiness mutation 或手动 M12.37 once-mode。
- M14 strategy evidence gap burndown 已落地：`scripts/run_m14_strategy_evidence_gap_burndown.py` 当前把 strategy evidence gap matrix、objective execution plan、parameter experiment queue、activation gate 与 external reference map 合成可执行的缺口消化队列；最新结果为 `20` 条 burndown rows，`P0/P1/P2=12/1/7`，lane 计数为 approved internal-sim refresh `3`、first rescue ledger `1`、rescue shadow parameter review `7`、detector rebuild A/B `1`、rescue A/B collection `1`、shadow/plugin research `7`，并汇总 `3` 条 approved refresh、`2` 条 first-ledger watch、`10` 条 rescue A/B、`11` 条 shadow-review、`19` 条 pre-refresh review available。该队列只排序证据收集和人工复核，不允许 promotion、final discard、参数 mutation、broker/live、真实订单、paper approval 或手动 M12.37 once-mode，并已接入 post-fresh checklist 与 M14 project stage assessment。
- M14 strategy pre-refresh review packet 已落地：`scripts/run_m14_strategy_pre_refresh_review_packet.py` 当前把 burndown 里的 `19` 条 pre-refresh-available rows 转成只读复核包，另有 `1` 条 `M10-PA-004-MBF` held row；review rows 的 `P0/P1/P2=12/0/7`，其中 `12` 条仍依赖 M12.47 fresh refresh，`7` 条只能做 artifact/source review，`11` 条带外部参考复核清单。该 packet 只能准备人工复核、影子参数验收和外部架构参考检查，不能 close gap、不能 promote、不能 discard、不能改参数、不能手动运行 M12.37 once-mode，也不能启用 broker/live；已接入 post-fresh checklist 与 M14 project stage assessment。
- M14 rescue zero-signal diagnostics 已落地并纠偏：`scripts/run_m14_rescue_zero_signal_diagnostics.py` 当前把 `8` 条零信号策略拆到 `9` 个 runtime 粒度；结果显示 `7` 个 runtime 主要受当前 recompute/audit 快照的非 fresh quote gate 影响，应等下一次 M12.47 fresh quote 后复核；`1` 个 runtime 是 `M10-PA-012` 的 reward/R 阻断，但 shadow-only `1.0R / 1.1R / 1.2R` 质量门计数均为 `0`，下一步应先检查 target/stop 生成而不是简单降 min-R；`1` 个 runtime 是 `M10-PA-013` 父策略同周期也零信号，需保持同周期映射并等待父 detector 证据，不跨周期硬映射。该诊断已进入 M14 goal readiness，用于决定先等 fresh 数据、做参数影子测试还是修 detector 映射，不改变 broker/live 边界。
- M14 rescue target/stop diagnostics 已落地：`scripts/run_m14_rescue_target_stop_diagnostics.py` 当前只读 M12 dashboard 与 zero-signal artifact，专门诊断 reward/R 阻断 runtime。最新结果只纳入 `M10-PA-012-m14-modify-20260522-5m`：父源行 `47` 条、非杠杆看涨有效行 `15` 条、风险门槛通过 `12` 条，但 `1.0R / 1.1R / 1.2R` 通过数均为 `0`，reward/R 区间为 `0.7159 ~ 0.9819`；结论是先影子测试 ORB target/stop 生成（如开盘区间高度、measured move 或 normalized 1.0R target），不要直接降低 frozen rescue runtime 阈值。该诊断已接入 M14 goal readiness，继续保持只读/模拟、无 broker/live。
- M14 rescue target/stop shadow normalization 已落地：`scripts/run_m14_rescue_target_stop_shadow_normalization.py` 当前对 PA012 reward/R 阻断源行做 shadow-only 目标价生成候选。最新结果显示 `risk_normalized_1_0r` 可让 `12/12` 条已过方向、杠杆、价格几何与风险门槛的 PA012 候选达到 `1.0R`，中位目标价调整约 `0.0410%`；`opening_range_height_30m` 仍无法突破原始低于 `1.0R` 的问题。当前已把该候选登记为独立 rescue shadow runtime `M10-PA-012-m14-modify-20260522-target-stop-risk_normalized_1_0r-shadow-5m`，父策略仍为 `M10-PA-012`，baseline 与 frozen rescue runtime 都不覆盖；该 runtime 仍需 M12.47 fresh quote 后生成首条 M13 ledger，并重新累计 10 个 rescue A/B 交易日证据后才可 M14 review，不允许 broker/live。

## 2. 执行总原则

- M15 SDK 稳定性当前执行项：完整 SDK 五分钟 K 线按区间完成时间而非最后报价时间判断时效；运行重启只恢复当天每标的最近 20 根 SDK 五分钟 K 线作为结构上下文，不重放订单，并抑制重启所在的第一根残缺区间；SDK 提交统一使用 `.US` 标的代码，单笔券商异常必须只记录为该订单失败，不能让运行层退出。日线策略必须使用上一完整日线加当前 SDK 五分钟聚合确认，运行指纹必须覆盖实时源码；单笔延迟必须按真正 SDK 请求时间计算，排队超过 `5s` 的新信号等待实时重建。SDK 挂单维护的最近实际动作不得被后续空检查覆盖。维持现有利润、盈亏比、仓位和纸面账户边界，不为增加订单而降低门槛。
- Source of Truth 固定为：`AGENTS.md`、`docs/requirements.md`、`docs/architecture.md`、`docs/data-sources.md`、`docs/pa-strategy-spec.md`、`docs/roadmap.md`、`docs/acceptance.md`、`docs/implement.md`、`docs/branching.md`、`docs/status.md`。
- 当前阶段必须从轻资产验证开始，不把正式券商 API、真实账户、实盘自动下单作为前置条件。
- 浏览器自动化只允许排在后续验证或只读观察路径，不得进入前置 milestone。
- 每个 milestone 完成条件固定为：范围符合 plan、验证已运行、reviewer 通过、qa 通过、`docs/status.md` 已同步、存在清晰回退点；高风险里程碑还必须标记人工复核。
- 回退边界固定为每个已验收 milestone 的分支终点；未合并分支直接废弃，已合并 milestone 通过 revert 该 milestone 合并提交回退。
- 当前初始回退点固定为 `96259ad`。

## 3. 分支策略

- 禁止直接在 `main`、`master` 或稳定主线上开发。
- 每个 milestone 开始前必须从最近一个已验收检查点创建新分支。
- milestone 分支命名固定为：
  - `feature/m1-kb-ingestion-index`
  - `feature/m2-data-schema-replay`
  - `feature/m3-pa-signal-prototype`
  - `feature/m4-backtest-report`
  - `feature/m5-papertrading-risk`
  - `feature/m6-news-review-integration`
  - `feature/m7-broker-api-assessment`
  - `integration/m8-reliability-validation`
- 当前与后续所有新的 `feature/*`、`fix/*`、`docs/*`、`test/*`、`integration/*` 分支都从 `main` 切出。
- 低/中风险阶段验收通过后的默认合并目标是 `main`。
- `feature/m7-broker-api-assessment` 仅保留为历史阶段基线/里程碑分支，不再作为未来默认长期合并目标。
- 并行写代码时优先使用独立 branch 或 worktree。
- 涉及 `src/execution/`、`src/risk/`、`src/broker/`、凭证、实盘开关的改动必须单独分支、单独测试、单独复核。

## 4. Subagent 策略

- 若 milestone 内存在 2 个以上独立子任务、同时涉及知识库与代码、同时涉及探索/实现/审查/测试、跨多个模块边界、或触及高风险目录，则必须显式创建 subagent。
- `planner`：用于 milestone 启动、依赖变化、阻塞后重排和阶段切换。
- `researcher`：用于只读摸底、现状差距定位、接口与风险点搜集。
- `data_engineer`：负责样本、schema、回放、时区与数据质量。
- `kb_curator`：负责 raw→wiki 整理、来源登记、source refs、`missing_visuals` 和样例页。
- `implementer`：负责有边界的实现与必要的最小文档改动。
- `reviewer`：负责合并前审查正确性、回归风险、分支规范和高风险边界。
- `qa`：负责里程碑验收、失败路径验证和验收清单核对。
- 所有 subagent 输出必须遵守 `docs/handoff-protocol.md`。

## 5. 防死循环与熔断

- 同一子任务连续失败 3 次，必须熔断。
- 同一 reviewer 连续打回 3 次，必须熔断。
- 熔断后必须输出 Failure Dossier。
- 熔断后暂停该任务，不得盲目重试。
- 只有在用户补充关键决策、权限、凭证或接受降级方案后，才能继续该任务。

## 6. Milestone 顺序

- M0 已完成 → M1 知识库 schema / KB 校验 / wiki index / 资料投放流程 → M2 测试数据 / 数据 schema / CSV-JSON 回放 → M3 PA context / setup / signal 原型 → M4 最小回测与报告 → M5 纸面交易 / 模拟执行 / 风控闭环 → M6 新闻过滤与复盘整合 → M7 正式券商 API 接入评估 → M8 可靠性验证。
- 第一实施波次固定为：M1 完成后进入 M2。
- 不得把浏览器自动化、正式券商 API、真实账户、实盘自动下单前置到 M1 或 M2。
- 在完成 M8 可靠性验证前，不重新评估真实 broker、真实账户、live execution 或付费 API。

## 7. M0 基础设施与规范基线

- 分支：已完成于 `feature/m0-infra-v2`
- 目标：完成仓库结构、Codex agent 配置、规则文档、知识库目录、测试样本、KB 脚本、GitHub 推送能力的初始化。
- 当前验收状态：视为已通过，可作为后续全部开发基线。
- 验证基线：
  - `python -m py_compile scripts/validate_kb.py scripts/build_kb_index.py`
  - `python scripts/validate_kb.py`
  - 空 wiki 校验
  - GitHub remote 与 push 已打通
- 回退点：`96259ad`

## 8. M1 知识库 schema、KB 校验、wiki index、资料投放流程

- 分支：`feature/m1-kb-ingestion-index`
- 当前状态：已完成
- 目标：把 `knowledge/raw/` → `knowledge/wiki/` → `knowledge/wiki_index.json` 的基础流转做成可重复执行、可校验、可回退的最小闭环。
- 交付内容：
  - 统一 `knowledge/schema/*` 与 KB 脚本的字段契约。
  - 补齐 wiki 页面类型与 frontmatter 约束。
  - 明确 raw 投放、source refs、`missing_visuals`、冲突记录与整理流程。
  - 让 `validate_kb.py` 与 `build_kb_index.py` 对空目录、代表性页面、字段缺失和 setup 页面必填项都能给出稳定结果。
- 关键接口：
  - wiki frontmatter 字段集以 `knowledge/schema/page-frontmatter-template.md` 为准。
  - 索引输出字段至少保持 `path`、`title`、`type`、`status`、`confidence`、`market`、`timeframes`、`direction`、`source_refs`、`pa_context`、`tags`、`open_questions`。
- 验证方式：
  - 空 wiki 与非空 wiki 两条路径都要跑通。
  - 至少覆盖 `concept`、`setup`、`source` 三类代表性页面。
  - `python scripts/validate_kb.py` 和 `python scripts/build_kb_index.py` 必须可重复执行。
  - 索引结果需与 frontmatter 一致。
- 验收条件：
  - raw 层不可变原则未被破坏。
  - wiki 页面可被校验与索引。
  - KB 入库流程文档清晰。
  - reviewer 确认 raw/wiki/schema 未混淆。
  - qa 确认脚本行为与 frontmatter 约束一致。
- 可并行子任务：
  - `kb_curator` 负责样例页面与来源规范。
  - `implementer` 负责脚本与模板对齐。
  - `researcher` 负责找出 schema 与脚本差距。
  - `qa` 负责空目录、缺字段、setup 特殊字段的验收。
- 依赖：只依赖 M0。
- 风险：
  - schema 与脚本再次漂移
  - 样例页字段不全
  - 将猜测写入 wiki
- 回退点：
  - 若 M1 未通过，直接废弃 `feature/m1-kb-ingestion-index`
  - 若已合并，回退到 M0 基线
- 实际完成摘要：
  - 已补齐 `concept`、`setup`、`source` 三类代表性 wiki 页面。
  - 已统一 `knowledge/schema/*`、frontmatter 模板、`validate_kb.py`、`build_kb_index.py` 的契约。
  - 已补充 M1 验收条目，并完成空目录、当前 wiki、代表性样本与负向校验。
  - reviewer 与 qa 已通过。

## 9. M2 测试数据、OHLCV schema、CSV/JSON 回放 adapter

- 分支：`feature/m2-data-schema-replay`
- 当前状态：已完成
- 目标：建立可供策略、回测和 QA 复用的本地数据最小闭环，优先围绕 `tests/test_data/` 和用户导出文件，不引入外部付费或浏览器依赖。
- 交付内容：
  - 明确 OHLCV 与新闻 JSON schema。
  - 实现本地 CSV/JSON 读取、字段归一化、timezone 解析、重复检查、价格关系校验、非法值处理和 deterministic bar replay。
  - 保留对后续 adapter 的抽象边界。
- 关键接口：
  - OHLCV 最小字段固定为 `symbol`、`market`、`timeframe`、`timestamp`、`timezone`、`open`、`high`、`low`、`close`、`volume`。
  - 新闻样本至少包含 `symbol`、`market`、`timestamp`、`source`、`event_type`、`headline`、`severity`、`notes`。
- 验证方式：
  - 以 `tests/test_data/ohlcv_sample_5m.csv` 与 `tests/test_data/news_sample.json` 为主样本。
  - 覆盖时间解析、排序、重复时间戳、高低价关系、空值或非法值。
  - 验证 replay 输出 bar 顺序稳定，不依赖外部网络。
- 验收条件：
  - 数据导入与 replay 可在本地独立运行。
  - 策略与回测可以消费统一结构。
  - reviewer 确认没有把浏览器或第三方 SDK 写入核心数据路径。
  - qa 确认异常样本能正确报错或拦截。
- 可并行子任务：
  - `data_engineer` 负责 schema、清洗规则和异常样本。
  - `implementer` 负责 replay adapter。
  - `researcher` 负责现有脚本与模块落点。
  - `qa` 负责样本矩阵。
- 依赖：
  - M1 的知识库字段约束应已稳定。
  - 代码层只依赖 M0。
- 风险：
  - 时间区与市场字段不一致
  - 样本过少导致回放接口设计偏差
- 回退点：
  - 若 M2 失败，保留 M1 产物不动，废弃 `feature/m2-data-schema-replay`
- 实际完成摘要：
  - 已新增 `src/data/schema.py`，固定 OHLCV、新闻事件、ValidationError、CleanedRecord 等最小稳定契约。
  - 已新增本地 CSV/JSON loader 与 deterministic replay，统一消费 schema 契约。
  - 已建立 `tests/unit/test_data_pipeline.py`，覆盖正向样本、重复键、非法 market、非法 severity、非法 timezone、aware timestamp 归一化和统一类型输出。
  - reviewer 与 qa 已通过，确认核心数据路径未引入浏览器或第三方 SDK。

## 10. M3 PA context、setup、signal 输出原型

- 分支：`feature/m3-pa-signal-prototype`
- 当前状态：已完成
- 目标：在不接入实盘的前提下，用规则化、可解释、可追溯的方式产出最小交易信号原型。
- 交付内容：
  - 建立 PA context、setup、signal 的内部表示。
  - 信号输出至少包含 `signal_id`、`symbol`、`market`、`timeframe`、`direction`、`setup_type`、`pa_context`、`entry_trigger`、`stop_rule`、`target_rule`、`invalidation`、`confidence`、`source_refs`、`explanation`、`risk_notes`。
  - 要求每个 setup 能回溯到知识库规则。
- 关键接口：
  - PA 字段以 `docs/pa-strategy-spec.md` 和 KB frontmatter 为准。
  - 信号只用于研究、回测和模拟，不得直连执行。
- 验证方式：
  - 使用 M1 的知识页与 M2 的 replay 数据。
  - 覆盖无信号、单信号、多信号、失效条件和缺 `source_refs` 的路径。
- 验收条件：
  - 信号对象结构稳定且可解释。
  - setup 与 `source_refs` 可追溯。
  - reviewer 确认没有把未经验证规则写成稳定盈利策略。
  - qa 确认输出字段完整。
- 可并行子任务：
  - `kb_curator` 负责把知识页整理成可引用规则。
  - `implementer` 负责信号原型。
  - `researcher` 负责策略输入输出界面梳理。
  - `qa` 负责解释性与字段完整性。
- 依赖：M1 和 M2。
- 风险：
  - 过早把知识库抽象成黑箱规则
  - 信号字段缺少解释性
- 回退点：回退到 M2 已验收检查点
- 实际完成摘要：
  - 已新增 `knowledge/wiki/rules/m3-research-reference-pack.md` 与 `knowledge/wiki/rules/m3_signal_reference_index.json`，形成 research-only 的最小知识引用层。
  - 已新增 `src/strategy/` 最小 contracts / knowledge / context / signal 原型，且 strategy 层只消费 M2 的 `OhlcvRow`、`NewsEvent` 与 `DeterministicReplay` 契约。
  - 已建立 `tests/unit/test_strategy_signal_pipeline.py`，覆盖无信号、单信号、traceability、placeholder 低置信度、news 只作风险说明、缺失 `source_refs` 早失败、invalidation、多信号稳定性。
  - reviewer 与 qa 已通过，确认信号字段完整、来源可追溯、research-only 边界清晰，且未越界到 execution / risk / broker / 外部 API。

## 11. M4 最小回测引擎与报告

- 分支：`feature/m4-backtest-report`
- 当前状态：已完成
- 目标：基于静态 OHLCV 与 M3 信号，生成可复现的最小回测结果与报告，不伪造收益结论。
- 交付内容：
  - 最小 trade lifecycle、交易记录、胜率、盈亏比、期望值、最大回撤、交易频率、滑点敏感性统计。
  - 输出复盘或回测摘要，明确数据不足和假设边界。
- 关键接口：
  - 回测输入固定为本地 replay 数据与结构化信号。
  - 输出需能被后续 review/simulation 消费。
- 验证方式：
  - 固定样本运行结果可重复。
  - 覆盖零交易、单交易、多交易、止损/目标命中、样本不足等路径。
  - 验证统计口径与报告字段一致。
- 验收条件：
  - 回测可重复运行，报告可解释。
  - 数据不足时不会给出伪确定性结论。
  - reviewer 确认没有暗含实盘假设。
  - qa 确认统计正确性与边界条件。
- 可并行子任务：
  - `data_engineer` 负责样本和统计口径。
  - `implementer` 负责引擎与报告。
  - `reviewer` 审查结果可信度。
  - `qa` 做回归样本验证。
- 依赖：M2、M3。
- 风险：
  - 信号与成交假设耦合过深
  - 回测统计口径不一致
- 回退点：回退到 M3 已验收检查点
- 实际完成摘要：
  - 已新增 `src/backtest/` 最小 contracts / engine / reporting，回测层只消费本地 bars / replay 与结构化 signal。
  - 已建立 `tests/unit/test_backtest_pipeline.py`，覆盖零交易、单交易、多交易、same-bar stop/target、end_of_data、news 不改收益、profit_factor 空值路径。
  - 已固定 deterministic baseline：next-bar-open entry、signal-bar extremum stop、固定 2R target、same-bar stop-first。
  - reviewer 与 qa 已通过，确认回测层未越界到 execution / risk / broker / 外部 API，且数据不足路径不会伪装成确定性收益统计。

## 12. M5 纸面交易、模拟执行与风控闭环

- 分支：`feature/m5-papertrading-risk`
- 当前状态：已完成
- 目标：在完全不触碰真实账户的前提下，完成信号 → 风控 → 建议订单 → 模拟成交 → 持仓状态 → 日志 的闭环。
- 交付内容：
  - `PaperBrokerAdapter` 最小实现。
  - 执行请求结构、风控校验链、连续亏损暂停、日内最大亏损、总仓位限制、集中度限制、止损规则、熔断与恢复条件。
  - 所有默认模式必须是 paper/simulated。
- 关键接口：
  - 执行层必须与 `src/execution/`、`src/risk/` 的高风险规则对齐。
  - 不得绕过风控直接下单。
  - 不得出现任何真实 broker 凭证路径。
- 验证方式：
  - 正常允许路径
  - 风控拦截路径
  - 模拟成交路径
  - 市场关闭路径
  - 重复信号路径
  - 连续亏损触发熔断路径
  - 恢复交易条件路径
- 验收条件：
  - 默认仍为模拟盘。
  - 风控失败会阻断执行。
  - 日志可用于复盘。
  - reviewer 确认未进入真实下单边界。
  - qa 覆盖高风险失败路径。
- 可并行子任务：
  - `implementer` 分离 paper execution 与 risk 模块。
  - `reviewer` 做高风险审查。
  - `qa` 执行阻断与异常路径。
  - `planner` 在高风险边界变更时重排。
- 依赖：M3、M4。
- 风险：
  - 执行层与风控接口耦合不清
  - 默认模式误入实盘语义
- 回退点：
  - M5 未通过不得合入
  - 若已合入，必须整体 revert M5 合并提交
- 实际完成摘要：
  - 已新增 `src/risk/` 最小 contracts / engine，固定 `RiskConfig`、`SessionRiskState`、`RiskDecision` 与 paper-only 风控状态流转。
  - 已新增 `src/execution/` 最小 contracts / paper adapter / state / logging，形成 signal -> risk decision -> suggested order -> simulated fill -> position state -> close-path audit log 的最小闭环。
  - 已补 request-binding 校验，显式阻断 stale / mismatched risk decision、duplicate signal、market closed、config_error、invalid_request 与风险超限路径。
  - 已建立 `tests/unit/test_paper_execution_pipeline.py`，覆盖 allow、risk_block、market_closed、duplicate_signal、loss-streak halt、manual recovery、config_error、invalid_request、mismatched / stale / direction-mismatch risk decision。
  - reviewer 与 qa 已通过，确认 paper-only 边界清晰、close-path 审计日志可复盘，且未越界到真实 broker / live execution。

## 13. M6 新闻事件过滤与复盘整合

- 分支：`feature/m6-news-review-integration`
- 当前状态：已完成
- 目标：把新闻/事件样本纳入解释和风险过滤链路，同时把 KB 规则引用、价格行为解释和结果统计纳入统一复盘输出。
- 交付内容：
  - 新闻只作为过滤、解释或风险提示因子。
  - 复盘结构中整合 signal 来源、KB 引用、PA 解释、新闻影响、入场/止损/目标、结果统计、错误原因和待改进项。
- 关键接口：
  - 新闻输入沿用 M2 JSON schema。
  - 输出不得把新闻情绪直接转成实盘订单。
- 验证方式：
  - 覆盖有新闻过滤、有新闻但不触发阻断、无新闻、高风险事件提示、复盘引用 KB `source_refs` 等场景。
- 验收条件：
  - 新闻不会直接触发下单。
  - 复盘可同时引用 KB、信号、结果和新闻。
  - reviewer 确认新闻仍处于辅助地位。
  - qa 确认复盘信息完整。
- 可并行子任务：
  - `kb_curator` 负责规则引用与复盘知识链接。
  - `implementer` 负责过滤和报告整合。
  - `qa` 负责新闻样本路径。
- 依赖：
  - M3、M4
  - 与 M5 只在输出接口稳定后可部分并行
- 风险：
  - 新闻模块越权成为主信号源
  - 复盘字段和回测字段分叉
- 回退点：回退到 M5 已验收检查点
- 实际完成摘要：
  - 已新增 `src/news/` 最小 `NewsMatch`、`NewsReviewNote`、`NewsFilterDecision` 与 `evaluate_news_context(...)`，固定新闻只作 filter / explanation / risk hint 的 research-only 辅助语义。
  - 已新增 `src/review/` 最小 `ReviewTradeOutcome`、`ReviewItem`、`ReviewReport` 与 `build_review_report(...)`，把 KB `source_refs`、PA explanation、news review notes、回测结果与执行阻断证据整合进统一复盘输出。
  - 已新增 `knowledge/wiki/rules/m6-news-review-evidence-pack.md`，并同步 `knowledge/wiki/index.md` 与 `knowledge/wiki/log.md`，登记 M6 的知识证据包与开放问题。
  - 已建立 `tests/unit/test_news_review_pipeline.py`，覆盖无新闻、caution、block、future-event leakage、防止缺失 `reference_timestamp`、review 中 filter / explanation / risk_hint 结构化透传、以及新闻不改写 signal 主字段。
  - reviewer 与 qa 已通过，确认新闻未越界到 signal / order / execution，future-event leakage 已关闭，且复盘输出保留结构化 `news_review_notes` 与可追溯的 `source_refs`。

## 14. M7 正式券商 API 接入评估

- 分支：`feature/m7-broker-api-assessment`
- 当前状态：已完成
- 目标：只做 readiness assessment，不做真实接入、不做真实下单、不做真实账户联通。
- 交付内容：
  - `FormalBrokerAdapter` 接口草案
  - 凭证隔离要求
  - 模拟验证前置条件
  - 测试策略
  - 人工审批清单
  - 是否进入下一阶段的 go/no-go 评估结论
- 关键接口：
  - 只允许定义 adapter contract、风险门禁与审批条件。
  - 不允许实现真实 broker 调用链。
- 验证方式：
  - reviewer 审查是否误入实盘边界。
  - qa 核对 go/no-go 清单是否覆盖凭证、模拟验证、人工批准、回退与审计要求。
- 验收条件：
  - 产出的是评估与门禁，而不是接入代码。
  - 正式 API 仍不是前置条件。
  - 若结论为 no-go，计划停留在模拟与研究阶段。
- 可并行子任务：
  - `planner` 负责阶段切换和决策门禁。
  - `researcher` 负责 API 能力调研。
  - `reviewer` 做高风险边界审查。
- 依赖：
  - M5 稳定闭环
  - M6 复盘整合完成
- 风险：
  - 评估阶段偷跑到接入实现
  - 凭证与审批门禁不足
- 回退点：
  - M7 仅产生评估文档与接口草案，若越界立即整体回退
- 实际完成摘要：
  - 已新增 `src/broker/` assessment-only contract draft，固定 `FormalBrokerAdapterDraft`、`BrokerCredentialPolicy`、`BrokerExecutionGateDependency`、`BrokerCapabilityRequirement` 与 `BrokerAssessmentEnvelope` 最小接口草案。
  - 已新增 `docs/broker-readiness-assessment.md` 与 `docs/broker-approval-checklist.md`，明确 capability matrix、credential isolation、approval gates、rollback boundary 与当前 `no-go` 结论。
  - 已新增 `tests/unit/test_broker_contract_assessment.py`，覆盖 contract shape、no-live invariant、risk / execution gate dependency 与无默认凭证字段。
  - reviewer 与 qa 已通过，确认 M7 仍停留在 readiness assessment，不含真实 broker SDK、外部网络调用、真实账户联通或 live execution 路径。
  - 当前阶段最终结论为 `no-go`；在用户明确批准外部权限、真实账户、付费服务或下一阶段评估前，系统继续停留在 paper / simulated。

## 15. M8 可靠性验证

- 分支：`integration/m8-reliability-validation`（M8A/M8B 已完成）；`M8B.2a` 已从 `feature/m8b2-knowledge-atomization-callable-access` 整合进稳定基线 `feature/m7-broker-api-assessment`
- 当前状态：进行中
- 当前子阶段：M8B.2a.1：Statement Quality Audit + Merge Gate（已完成）
- 目标：以行为可靠性而不是功能扩展为首目标，验证 M0-M7 既有交付物在知识约束、研究链可复现性、paper-only 安全性和真实输入下的保守稳定性。
- 总边界：
  - 默认运行边界仍为 `paper / simulated`
  - 不接真实资金
  - 不启用实盘自动下单
  - 不重开真实 broker、真实账户、live execution、付费 API 或浏览器生产化讨论
  - M8 完成前，不重新评估 broker / live
- 可接受输入边界：
  - P0：静态测试样本
  - P1：用户导出的真实历史 CSV/JSON
  - P2：免费公共数据源的本地快照
  - 实时只读输入：shadow / paper 观察路径
  - 明确不进入 P4 broker / live

### M8A：测试骨架与验收门禁落盘

- 当前状态：已完成
- 目标：
  - 将 M8 正式写入 `plans/active-plan.md`
  - 将 M8 验收条件写入 `docs/acceptance.md`
  - 将当前阶段切换写入 `docs/status.md`
  - 在 `docs/roadmap.md`、`docs/decisions.md` 中冻结 M8 位置与门禁
  - 新增 `docs/testing-reliability.md` 与 `docs/eval-rubric.md`
  - 落盘 `tests/golden_cases/`、`tests/integration/`、`tests/reliability/`、`reports/reliability/` 的 discoverable 骨架
  - 新增 `docs/test-dataset-curation.md`
  - 新增 `scripts/run_reliability_suite.py`，提供当前可靠性相关 suite 的本地统一入口
- 交付物类型：
  - 主线文档
  - 验收门禁
  - 评分规则
  - 阶段 runbook 总纲
  - 测试目录骨架
  - 数据集整理规范
  - 本地 suite 运行入口
- 验证方式：
  - reviewer 审查 M8 是否被定义为 M7 之后的当前下一阶段，而不是 broker 后续开发
  - qa 核对 `docs/status.md`、`docs/roadmap.md`、`docs/acceptance.md`、`docs/decisions.md` 是否同步
  - `python -m py_compile scripts/run_reliability_suite.py`
  - `python scripts/run_reliability_suite.py --list`
  - `python scripts/run_reliability_suite.py`
  - `python -m unittest discover -s tests/unit -v`
  - reviewer 与 qa 都把 “无越权到 broker/live” 作为硬门禁
- 验收条件：
  - `plans/active-plan.md` 已新增 M8 与 M8A/M8B/M8C/M8D
  - `docs/acceptance.md` 已新增阶段 8 验收
  - `docs/status.md` 已把下一步切到 M8，而不是继续 broker
  - `docs/roadmap.md` 已同步 M8 在 M7 之后的位置
  - `docs/decisions.md` 已新增 “先 M8、后 broker/live” 的冻结决策
  - `docs/testing-reliability.md` 与 `docs/eval-rubric.md` 只包含计划与门禁，不含实现细节
  - `docs/test-dataset-curation.md` 已说明可接受数据层级、样本元数据、脱敏与离线边界
  - `tests/golden_cases/`、`tests/integration/`、`tests/reliability/`、`reports/reliability/` 均已可发现且带 README/占位说明
  - `scripts/run_reliability_suite.py` 在无真实历史样本、无 M8B/M8C/M8D 测试文件时可安全运行，并显式输出 skipped / deferred 提示
- 可并行子任务：
  - `planner` 负责冻结主线与阶段切换
  - `researcher` 负责核对附件与现有 SoT 的一致性
  - `implementer` 负责骨架目录、README、数据集规范与 suite 入口落盘
  - `reviewer` 负责高风险边界审查
  - `qa` 负责门禁完整性复核
- 依赖：
  - M7 已完成且当前结论为 `no-go`
  - 当前分析基线固定为 `main`
- 风险：
  - 把 M8 写成收益优化或 broker 延续阶段
  - 在门禁文档或脚手架中混入 M8B/M8C/M8D 的具体实现细节
  - 未同步 `docs/roadmap.md` 导致 SoT 再次分叉
  - 在无真实历史样本时误报“真实稳健性已通过”
- 回退点：
  - 若 M8A 文档越界，整体回退 `integration/m8-reliability-validation` 上的文档改动，回到当时的稳定基线提交；该历史基线分支仍保留为 `feature/m7-broker-api-assessment`
- 实际完成摘要：
  - 已补齐 `tests/golden_cases/`、`tests/integration/`、`tests/reliability/`、`reports/reliability/` 的 README 骨架。
  - 已新增 `docs/test-dataset-curation.md`，冻结 M8 可接受数据层级、最小元数据、脱敏与离线边界。
  - 已新增 `scripts/run_reliability_suite.py`，默认运行当前稳定存在的 baseline unit suites，并对空目录或缺真实历史样本显式 skipped / deferred。
  - 已验证新增骨架不会破坏 `python -m unittest discover -s tests/unit -v`。

### M8B：知识库对齐测试

- 当前状态：已完成
- 目标：
  - 定义 golden-case 主线
  - 验证输出是否严格受知识库约束，而不是“看起来像遵守”
- 交付物类型：
  - KB alignment 测试计划
  - golden-case 输入约束
  - reviewer / qa 审查清单
- 验证方式：
  - 重点围绕 `source_refs` 真实性、适用性 / 不适用性、冲突显式化、资料不足时保守 `no-trade / wait`
  - reviewer 核对知识页引用边界
  - qa 核对保守输出与冲突场景要求
- 验收条件：
  - 明确要求 `source_refs` 必须真实存在
  - 明确禁止 hallucinated refs
  - 明确禁止越过 `not_applicable`
  - 冲突场景必须显式提示冲突
  - 资料不足时允许且鼓励 `no-trade / wait`
  - 整个子阶段不得引入新的策略规则扩展
- 可并行子任务：
  - `kb_curator` 负责 golden-case 引用边界
  - `researcher` 负责梳理知识冲突与资料不足场景
  - `qa` 负责 KB consistency 硬门禁清单
- 依赖：
  - M8A 已完成
  - M1/M3/M6 的知识与信号契约保持冻结
- 风险：
  - 将“知识不足”误写成必须给方向
  - 把冲突知识页简化成单一路径
  - 用实现细节代替门禁要求
- 回退点：
  - 如门禁定义越界或放宽边界，回退到 M8A 检查点
- 实际完成摘要：
  - 已新增 `src/strategy/alignment.py`，提供 M8B 的最小 knowledge alignment 评估入口。
  - 已把 `source_refs` 真值校验收紧到 knowledge load/validate 层，缺失或不存在的 wiki/raw refs 现在会硬失败。
  - 已在 `tests/golden_cases/cases/` 落盘 5 个最小 golden cases，覆盖 placeholder setup、news role conflict、insufficient evidence、not_applicable hard gate、missing/fake refs hard fail。
  - 已新增 `tests/reliability/test_kb_alignment.py`、`tests/reliability/test_no_hallucinated_kb_refs.py`、`tests/reliability/test_no_trade_when_insufficient_evidence.py`。
  - 已验证 explanation 必须存在且回链到 setup/rule/source，且 `wait / no-trade` 在证据不足场景下视为合格结果。
  - 已于 2026-04-17 通过 merge commit `0047100` 整合进稳定基线 `feature/m7-broker-api-assessment`。
  - 已完成 M8B.1：定位 transcript / Brooks PPT 先前缺席是因为 raw-only、无 `source` 页、未入 active rule pack，且默认 strategy bundle 未加载 rule pack；现已补齐最小 traceability 接线并验证“能接入时出现、未接入时不编造”。

### M8B.2a：Knowledge Atomization 基础层

- 当前状态：已完成
- 目标：
  - 为 in-scope source 建立 machine-readable source registry
  - 为可解析资料建立 chunk registry
  - 为 chunk 与 curated wiki 页面建立 evidence-backed knowledge atoms
  - 建立可按 source/type/context/status 查询的 callable index
- 交付物类型：
  - source / chunk / atom / callable schema
  - source / chunk / atom / callable indices
  - builders / validators
  - reliability tests
- 验证方式：
  - 运行 `test_kb_coverage.py`、`test_knowledge_atoms.py`、`test_callable_access.py`
  - 运行全量 `tests/reliability`
  - reviewer 核对 evidence-backed 原则与 callable 层边界
  - qa 核对熔断条件与“本轮不进入 2b”的边界
- 验收条件：
  - `M8B.2` 已在 SoT 中拆成 `2a / 2b`
  - 10 个 in-scope source 全部进入 source registry
  - `Zone.Identifier` 被过滤且显式记录
  - `statement` atom 已落盘，且具备 `atom_id / source_ref / raw_locator / evidence_chunk_ids / status / confidence / callable_tags`
  - 无证据不产出 `statement`
  - transcript / Brooks / 全部方方土笔记在 callable index 层可检索
  - 关键 curated atoms 已形成 evidence-backed atom
  - 本轮不修改 strategy / explanation / review / report 接线，不修改 trigger
- 熔断条件：
  - 10 个 in-scope source 中 `blocked >= 4`
  - 关键 curated atoms 无法形成稳定 evidence-backed atom
  - `statement` 抽取无法稳定回溯证据
- 可并行子任务：
  - `source_inventory / kb_auditor` 负责 coverage baseline 与 source-page 缺口
  - `kb_structurer` 负责 schema、builders、validators、indices
  - `reviewer / qa` 负责 contract、熔断判定与回归
- 依赖：
  - M8B.1 已完成 source traceability 最小接线
  - 当前仍保持 `paper / simulated`
- 风险：
  - 把 source-level 或 statement-level atom 误包装成 executable rule
  - PDF 解析失败导致 coverage 统计与 evidence atom 不稳定
  - 在 2a 中偷跑 strategy 接线
- 回退点：
  - 如命中熔断，停在 `M8B.2a`，更新状态并输出 Failure Dossier，不进入 `2b`
- 实际完成摘要：
  - 已补齐 10 个 in-scope source 的 source registry 覆盖，并把 `Price_Action方方土.pdf:Zone.Identifier` 显式过滤到 `coverage_summary.filtered_files`。
  - 已新增 5 份方方土笔记 source page 与 2 份 Brooks PPT per-file source page；现有 `al-brooks-price-action-ppt.md` 继续保留为 family overview。
  - 已用 `pypdf` 构建 `source_manifest.json`、`chunk_manifest.jsonl`、`knowledge_atoms.jsonl` 与 `knowledge_callable_index.json`。
  - 当前 source coverage 结果为 `parsed=9 / partial=1 / blocked=0`，partial 来源是 `方方土视频笔记 - 楔形.pdf`，原因为 `1 page(s) produced no stable text`。
  - 已落盘 `statement` callable atom，且保持 `draft`、evidence-backed、无 `strategy_candidate`；当前 atom 统计为 `statement=11171`、`source_note=5492`、`open_question=24`、`concept=1`、`setup=1`、`rule=1`。
  - 关键 curated atoms `market-cycle-overview`、`signal-bar-entry-placeholder` 与 `m3-research-reference-pack` 已形成 evidence-backed atom。
  - 已通过 `python scripts/validate_kb_coverage.py`、`python scripts/validate_knowledge_atoms.py`、`python -m unittest discover -s tests/reliability -v` 与 `python -m unittest discover -s tests/unit -v`。
  - M8B.2a.1 已完成 statement 质量审计，结论为 `pass_with_small_fixes`；最小修复仅限于收紧 statement 提取条件、去除明显页眉页脚 / 时间轴 / 起始标点 / 未完成碎片，并对同一 source 内的明显重复做保守去重。
  - 审计后 statement 分布为：`al_brooks_ppt=11042`、`fangfangtu_transcript=88`、`fangfangtu_notes=41`；重复/噪音摘要为：`exact_dup_extra=13`、`normalized_dup_extra=16`、`trailing_open=0`、`datey=0`、`start_punct=0`。
  - 已于 2026-04-17 通过 merge commit `23755c0` 从 `feature/m8b2-knowledge-atomization-callable-access` 整合进稳定基线 `feature/m7-broker-api-assessment`。
  - 当前未触发熔断；`M8B.2a` 已先整合进稳定基线，后续 `M8B.2b` 由最新稳定基线独立分支启动。

### M8B.2b：Callable 接入 Strategy / Explanation / Review / Report

- 当前状态：已完成并整合进稳定基线
- 启动前提：
  - `M8B.2a` 全部测试通过
  - 未触发熔断
  - reviewer 通过
  - qa 通过
- 当前约束：
  - trigger 逻辑保持不变
  - `statement` / `source_note` / `contradiction` / `open_question` 只进入 trace，不进入 trigger
  - 继续保持 `paper / simulated`
- 实际完成摘要：
  - 已新增 `src/strategy/knowledge_access.py`，为 callable atom 提供 query、trace resolve 与 legacy `source_refs` 兼容 helper。
  - `Signal` 已新增 `knowledge_trace`，`ReviewItem` 已新增 `kb_trace`。
  - public demo 已新增 `knowledge_trace.json`；Markdown `report.md` 仅展示每笔最多 3 条 trace 摘要。
  - 已实现 source family 失衡保护：curated atom 优先、statement/source-note 去重与限量、source family 多样性控制、禁止把 atom 数量当作 confidence/trigger proxy。
  - 已通过 `tests/reliability/test_strategy_atom_trace.py`、`tests/unit/test_strategy_signal_pipeline.py`、`tests/unit/test_news_review_pipeline.py`、`tests/unit/test_public_backtest_demo.py`、`python -m unittest discover -s tests/reliability -v` 与 `python -m unittest discover -s tests/unit -v`。
  - 已从 `feature/m8b2b-knowledge-trace-integration` 整合回稳定基线 `feature/m7-broker-api-assessment`；trigger 逻辑未改变，`statement` 仍未进入 trigger。

### M8 基础离线可靠性门禁

- 当前状态：已完成
- 目标：
  - 围绕当前最小闭环定义离线集成可靠性红线
  - 固化无 future leakage、可复现、风险先于成交、审计与复盘可追溯
- 交付物类型：
  - offline E2E 测试总纲
  - 红线清单
  - forbidden path 清单
- 验证方式：
  - 以现有 `data -> strategy -> backtest -> risk -> execution -> news -> review` 最小闭环为对象
  - reviewer 审查 forbidden path 与 leak path 定义
  - qa 核对 determinism、future leakage、audit traceability 门禁
- 验收条件：
  - 明确禁止 future leakage
  - 明确要求相同输入必须 deterministic
  - 明确要求 `risk_block` 永远早于 simulated fill
  - 明确要求 review / audit 字段可追溯
  - 明确 forbidden paths，不得越过到 broker/live
- 可并行子任务：
  - `researcher` 负责现有闭环输入输出依赖梳理
  - `planner` 负责红线优先级与阶段排序
  - `qa` 负责离线可靠性验收条目
- 依赖：
  - M8B 已冻结 KB alignment 门禁
  - M2/M4/M5/M6 的既有契约保持冻结
- 风险：
  - 只测收益，不测行为正确性
  - future leakage 定义模糊
  - 将 paper-only 执行红线降级为质量项
- 回退点：
  - 如离线可靠性门禁表述模糊，回退到 M8B 检查点
- 实际完成摘要：
  - 已新增 `tests/integration/test_offline_e2e_pipeline.py`，覆盖 `src/data -> src/strategy -> src/backtest -> src/risk -> src/execution -> src/news -> src/review` 的离线端到端链路。
  - 已新增 `tests/reliability/test_replay_determinism.py`、`tests/reliability/test_no_future_leakage.py`、`tests/reliability/test_audit_traceability.py`、`tests/reliability/test_forbidden_paths.py`。
  - 已覆盖 deterministic replay 一致性、bars / news future leakage fail-fast、forbidden paths、audit / review traceability、`end_of_data`、缺 bar gap 与重复 bar 的稳健性。
  - 已更新 `scripts/run_reliability_suite.py`，使 `integration` 与 `reliability` suites 可统一运行且继续保持 `real_historical_data=deferred`。
  - 已通过 `tests/reliability` 18 项、`tests/integration` 4 项、`tests/unit` 57 项与统一 reliability suite 验证。

### M8C：Long-Horizon & Intraday Paper Validation

- 当前状态：进行中
- 总目标：
  - 在不改 trigger 的前提下，把当前 short-window daily demo 扩展为更长周期、更分段、更可解释的 `paper / simulated` 验证套件。
  - 先完成长周期日线验证，再决定是否进入单标的日内试点。
  - `knowledge_trace` 与 legacy `source_refs` 继续兼容；`statement` / `source_note` 只作 trace 证据，不进入 trigger。

#### M8C.1：长周期日线验证

- 当前状态：已完成
- 范围与边界：
  - 只做 daily、equity/ETF、公有历史数据缓存驱动的长周期验证。
  - 不做期权、不做 intraday、不改 trigger、不改变 `knowledge_trace` 与 legacy `source_refs` 的兼容语义。
  - `knowledge_trace` 只用于解释、审计和覆盖率摘要，不得作为 trigger 或 score proxy。
- 实际完成摘要：
  - 已新增 `config/examples/public_history_backtest_long_horizon.json`，把 daily demo 扩展到 `2020-01-01 ~ 2025-12-31`，覆盖 `NVDA / TSLA / SPY` 三个 equity/ETF 标的。
  - 已新增 walk-forward 风格 split：`in_sample`、`validation`、`out_of_sample`。
  - 已新增 regime 分层窗口：`covid_crash_high_vol`、`liquidity_trend_up`、`macro_drawdown`、`recovery_rotation`、`ai_momentum_and_range`。
  - 已新增结构化产物：
    - `summary.json`
    - `split_summary.json`
    - `regime_breakdown.json`
    - `knowledge_trace_coverage.json`
    - `no_trade_wait.jsonl`
    - `trades.csv`
    - `knowledge_trace.json`
    - `report.md`
    - `equity_curve.png`
  - 已新增 `no-trade / wait` 结构化持久化，最小 reason class 包括：
    - `context_not_trend`
    - `duplicate_direction_suppressed`
    - `insufficient_evidence`
    - `risk_blocked_before_fill`
  - 已补齐 per-symbol breakdown、regime 摘要、blocked signals 汇总、knowledge trace 覆盖率摘要，以及 curated vs statement trace 占比摘要。
  - 已保持 curated atom 优先、statement 仅作补充证据；禁止使用 statement 数量作为 confidence 或 trigger proxy。
  - 已通过新增 `tests/reliability/test_long_horizon_daily_validation.py`、`tests/unit/test_public_backtest_demo.py` 回归，以及现有 `tests/reliability` / `tests/unit` 套件。
- 验收门槛：
  - 报告与 JSON 产物生成成功率为 `100%`。
  - 同一缓存输入重复运行结果保持 deterministic。
  - executed trades 的 `knowledge_trace` 覆盖率与 curated-first trace 约束稳定。
  - `no_trade / wait` 输出可重复生成，且保持 `paper / simulated`、不进入期权或 broker/live。
- 回退点：
  - 若长周期报告产物、trace 覆盖率摘要或 `no_trade / wait` 持久化不稳定，回退到 `M8B.2b` 检查点。

#### M8C.2：单标的日内试点

- 当前状态：已完成并整合进 `main`
- 启动前提：
  - `M8C.1` 验收通过并整合进稳定基线。
  - 继续保持 `paper / simulated`，不进入期权、broker/live/real-money。
- 计划边界：
  - 默认只选一个标的，优先 `SPY 15m`。
  - 必须补 session open/close、market hours / timezone、日内风险重置、slippage / fee 最小模型、duplicate signal protection、`no-trade / wait` 结构化输出。
  - 如实现被迫修改 `src/risk/` 或 `src/execution/` 核心语义，则停止自动合并并转高风险审批。
 - 实际完成摘要：
   - 已新增 `config/examples/intraday_pilot_spy_15m.json`、`scripts/intraday_pilot_lib.py`、`scripts/run_intraday_pilot.py`。
   - 已把首轮 intraday pilot 冻结为 `SPY / 15m / America/New_York / 2026-03-30 ~ 2026-04-16`，并把数据缓存到 `local_data/public_intraday/`。
   - 已新增结构化产物：
     - `summary.json`
     - `session_summary.json`
     - `session_quality.json`
     - `knowledge_trace.json`
     - `knowledge_trace_coverage.json`
     - `no_trade_wait.jsonl`
     - `trades.csv`
     - `report.md`
     - `equity_curve.png`
   - 已验证：
     - session open/close
     - market hours / timezone
     - 日内风险重置
     - duplicate signal protection
     - slippage / fee 最小可配置模型
     - `no-trade / wait` 结构化输出
     - intraday 下的 `knowledge_trace` 与 legacy `source_refs` 兼容
     - curated atom 优先、statement 仅作补充证据，且 source family 失衡保护继续生效
   - 已保持 trigger 逻辑不变；`statement` / `source_note` / `contradiction` / `open_question` 仍未进入 trigger。
 - 已通过新增 `tests/unit/test_intraday_pilot.py`、`tests/reliability/test_intraday_pilot_validation.py`，以及现有 `tests/reliability` / `tests/unit` / public demo smoke 回归。
  - 已完成第二标的扩展：新增 `config/examples/intraday_pilot_nvda_15m.json`，并生成 `reports/backtests/m8c2_intraday_pilot_nvda_15m/`。
  - `NVDA / 15m / 2026-03-30 ~ 2026-04-16` 当前录得：总收益率 `0.8793%`、最大回撤 `0.0000%`、交易 `2` 笔、blocked signals `27`、`no_trade_wait` `310`；仍保持 `paper / simulated`。
  - 第二标的扩展继续保持 session / risk reset / duplicate protection / slippage / fee / `knowledge_trace` 契约不变，且未改 trigger。

### M8 Shadow/Paper Baseline：真实历史数据稳健性 + 实时 shadow / paper 验证框架

- 当前状态：进行中
- 目标：
  - 在真实输入条件下验证系统仍然保守、稳定、可解释
  - 只建立 shadow / paper 验证框架，不进入真实 broker
- 交付物类型：
  - 真实历史数据验证计划
  - shadow / paper 验证 runbook 需求
  - 报告与人工抽检门禁
- 验证方式：
  - 允许真实历史 CSV/JSON、本地快照、实时只读输入
  - 明确仍为 shadow / paper，不连接真实 broker、不启用 live
  - reviewer 审查真实输入边界是否干净
  - qa 核对 shadow / paper 与 no-go 结论是否一致
- 验收条件：
  - 明确真实历史数据与实时只读输入的范围
  - 明确禁止真实 broker、真实账户、live execution
  - 明确要求输出仍为 shadow / paper 结果
  - 明确要求真实输入下仍保守 `wait / no-trade`，而不是强行给结论
  - 明确任何 broker/live 重新评估都排在 M8 完成之后
- 可并行子任务：
  - `researcher` 负责输入源与 regime 范围定义
  - `planner` 负责 shadow / paper 阶段门禁
  - `qa` 负责真实输入下的保守性与报告门禁
- 依赖：
  - M8C 已完成离线可靠性门禁定义
  - 当前数据源优先级仍以 `docs/data-sources.md` 为准
- 风险：
  - 用“实时验证”模糊掉 shadow / paper 边界
  - 将真实输入测试误解成真实 broker 恢复
  - 在未完成 M8 前重开 live 讨论
- 回退点：
  - 如出现任何真实 broker / live 暗示，整体回退到 M8C 检查点
- 实际完成摘要：
  - 已新增 `docs/shadow-mode-runbook.md`，固定 manifest、shadow/paper 命令、deferred 语义与运行边界。
  - 已新增 `scripts/run_shadow_session.py`，提供本地 manifest 驱动的只读 shadow/paper runner，默认不连接真实 broker、不触发 live execution。
  - 已新增 repo-safe 小样本 manifest：`tests/test_data/real_history_small/sample_us_5m_recorded_session/dataset.manifest.json`。
  - 已新增 `tests/reliability/test_regime_robustness.py`、`tests/reliability/test_shadow_paper_consistency.py`、`tests/reliability/test_dataset_manifest_contract.py`。
  - 已补齐 `docs/test-dataset-curation.md` 与 `reports/reliability/README.md`，固定 small/large dataset 约定、受控标签与报告最小字段。
  - 已新增 `scripts/download_public_history.py`、`scripts/run_public_backtest_demo.py`、`scripts/run_public_backtest_demo.sh`，打通历史数据下载缓存、用户可直接执行的一键回测入口与用户可读报告链路。
  - 已新增 `config/examples/public_history_backtest_demo.json` 与 `docs/user-backtest-guide.md`，并在当时的历史阶段接入了 provider-specific 的 public-history 入口配置。
  - 已完成一轮公共历史数据演示回测：早期历史 run 使用 `yfinance` 作为 research-only fallback，把数据缓存到 `local_data/public_history/`，并生成 `reports/backtests/demo_public_2024h1/`；当前 Strategy Factory 已改为通过 repo config 合同解析 `primary_provider`，不再把该历史 provider 名称写成长期固定口径。
  - 该示例 run 在当前 demo 风控参数下录得 `1.9923%` 总收益率、`1.5157%` 最大回撤、`16` 笔交易、`43.75%` 胜率；仍明确保持 `paper / simulated`，不代表实盘能力。
  - 当前更完整的真实历史样本、用户自有 CSV、录制型实时样本与 shadow/paper 延展验证仍可继续扩展，但本轮没有引入真实 broker、真实账户或 live execution。

## 16. 公共接口冻结点

- KB 层在 M1 后冻结 frontmatter 主字段与 setup 专属字段，`source_refs`、`missing_visuals`、`open_questions`、`pa_context` 等字段视为稳定约束。
- 数据层在 M2 后冻结 OHLCV 输入字段、新闻 JSON 最小字段、回放输出的 deterministic bar 序列结构。
- 策略层在 M3 后冻结结构化 signal 输出字段，下游模块不得依赖自由文本。
- 回测层在 M4 后冻结交易记录和核心统计字段，供复盘与模拟统一消费。
- 执行与风控层在 M5 仅允许 paper/simulated contract，真实 broker contract 只能在 M7 评估中定义，不落真实实现。

## 17. 总体验证策略

- 所有 milestone 都必须包含最小可重复验证命令、代表性样本、失败路径和空输入路径。
- M1 重点测 frontmatter 缺失、setup 特殊字段、空 wiki、索引字段一致性。
- M2 重点测 timestamp/timezone 解析、重复记录、非法价格、高低价关系、排序与 replay 稳定性。
- M3 重点测无信号、单信号、多信号、`source_refs` 缺失、失效条件与解释字段完整性。
- M4 重点测零交易、单交易、多交易、止损/目标命中、数据不足和统计可重复性。
- M5 重点测允许交易、风险拦截、模拟成交、重复信号、市场关闭、连续亏损熔断和恢复条件。
- M6 重点测新闻仅作过滤/解释、不直接下单、复盘字段完整和 KB 引用可追溯。
- M7 重点测门禁清单完整性、审批前置条件和“无真实接入”边界。
- M8A 重点测主线文档、验收门禁、状态切换、测试骨架与 suite 入口是否同步，且无 broker/live 越界表述。
- M8B 重点测知识库对齐、`source_refs` 真实性、`not_applicable` 约束、冲突显式化、explanation 回链与资料不足时的保守 `no-trade`。
- M8C 重点测无 future leakage、同输入 deterministic、risk-before-fill、audit / review traceability 与 forbidden paths。
- M8 shadow/paper baseline 重点测真实历史数据与实时只读输入下仍保持 shadow / paper、保守稳定、可解释，且不进入真实 broker / live execution。

## 18. 当前阶段与下一步

- 当前阶段：阶段 9：Price Action Strategy Lab / Strategy Factory（进行中）。
- 当前 milestone：`M9I.2 Wave3 Holdout / Walk-forward Robustness Validation`（当前分支完成）。
- 当前下一步：
  - 当前默认不自动启动新的 batch backtest，也不自动进入 `paper shadow`。
  - `SF-001 ~ SF-003` 已完成 `Wave3 robustness validation`，当前仍为 `modify_and_retest`；`SF-004` 当前为 `insufficient_sample`；`SF-005` 继续保持 `deferred_single_source_risk`。
  - 由于当前不存在 `strict post-freeze holdout`，本轮 `Wave3` 不允许给出 `retain_candidate`；下一步只能在 `paper shadow`、`再次修改并重测`、`继续补 SF-005 evidence` 三者中决策。
  - 当前不做全量 curated promotion，不做 statement/source_note 入 trigger，不改 `knowledge/raw`。
  - 保持当前 `no-go` 结论与 `paper / simulated` 边界，不继续 broker 开发。
  - 未经单独批准，不重新评估真实 broker、真实账户、live execution 或付费 API。

## 19. 假设

- 当前仓库基础设施已满足 M0 验收，且 GitHub push 工作流已打通。
- `python` 命令已可用，因此验证命令统一写为 `python ...`。
- 第一实施波次固定为 M1 后接 M2；浏览器验证与正式券商 API 评估都排在后续，不得前置。
- 若任一 milestone 在执行中触发熔断，则冻结该里程碑分支，更新 `docs/status.md`，输出 Failure Dossier，并等待用户决策。

## 20. M9：Price Action Strategy Lab

- 分支：`feature/m9-wave3-robustness-validation`
- 当前状态：进行中
- 目标：
  - 在保持 `paper / simulated`、`no-go`、raw 只读与 trigger 不变边界下，建立可恢复、可校验、以 `source_manifest` 为 SoT 的 Strategy Factory。
  - 在进入任何 batch backtest 之前，先完成 `Full Extraction Completeness Audit v4`，证明 text-extractable 范围内的提炼已闭环，并把 visual / partial 缺口诚实列账。
- 固定范围：
  - 必须先保存当前项目快照。
  - 必须优先盘点并使用 `fangfangtu_transcript`，其次 `al_brooks_ppt`，最后 `fangfangtu_notes`。
  - 已完成的 `PA-SC-*` 产物全部保留为 legacy / historical baseline。
  - 新一轮提炼必须从 `knowledge/indices/source_manifest.json` 覆盖的全部来源重新开始。
- 明确不做：
  - 不改 trigger 逻辑。
  - 不触碰 `src/risk/`、`src/execution/`、`src/broker/`。
  - 不进入真实 broker、真实账户、live execution、付费 API。
  - 不修改 `knowledge/raw/`。
- 子阶段：
  - `M9A`~`M9F`：已完成，但当前只作为 legacy / historical baseline 保留。
  - `M9G.0`：Contract Freeze & Legacy Boundary Reset（已完成）。
  - `M9G Audit v4`：`Full-Pass Chunk Adjudication`、`Cross-Chunk Synthesis Pass`、`Discovery Closure`、`Overmerge Review`、`Notes Per-Source Findings`、`Source Section / Unit / Theme Coverage Matrix`、`Cross-Source Corroboration Report`、`Saturation / Convergence Pass`、`Strategy Closure 与 Catalog Freeze`（已完成）。
  - `M9H`：`Controlled Batch Backtest + Strategy Triage`（已完成 wave2 扩样本回测）。
    - 首轮 wave 仅覆盖 `SPY / 5m` 的受控 baseline + limited diagnostics，当前只保留为历史参考。
    - wave2 已将 `SF-001 ~ SF-004` 扩展到 `SPY / QQQ / NVDA / TSLA`、`5m`、`2025-04-01 ~ 2026-04-21` 的多标的聚合回测。
    - `SF-005` 当前继续保持 `deferred_single_source_risk`，不得与 `SF-001 ~ SF-004` 无差别推进。
    - 当前 wave2 triage 结果固定为：`SF-001=modify_and_retest`、`SF-002=modify_and_retest`、`SF-003=modify_and_retest`、`SF-004=modify_and_retest`、`SF-005=deferred_single_source_risk`。
    - 当前 wave2 sample status 固定为：`SF-001 ~ SF-004 = robust_candidate`，但这只表示样本覆盖提高，不代表已具备实盘能力或稳定盈利结论。
  - `M9I.1`：`Freeze v0.2 Candidate Specs`（当前分支完成）。
    - 只允许基于 wave2 已被选中的 `quality_filter` 观察，生成 `SF-001 ~ SF-004` 的 `v0.2-candidate` spec。
    - `v0.2-candidate` 只作为候选包装层，不替代现有 `SF-001.yaml ~ SF-004.yaml` 的 `v0.1` 基线，也不改写 `strategy_catalog.json` 的 freeze 语义。
    - 每个 `v0.2-candidate` 必须绑定 wave2 证据路径、明确 `v0.1 -> v0.2-candidate` 的变更、预期改善的失败模式与残余风险。
    - `SF-005` 在 `M9I.1` 继续保持 `deferred_single_source_risk`，不得生成 `v0.2-candidate`。
  - `M9I.2`：`Wave3 Holdout / Walk-forward Robustness Validation`（当前分支完成）。
    - 只允许加载冻结后的 `SF-001 ~ SF-004 v0.2-candidate`，不得修改 specs，不得边测边调。
    - 当前 `Wave3` 实际公共窗口为 `2025-04-01 ~ 2026-04-21`、`SPY / QQQ / NVDA / TSLA`、`5m`；当前 `strict post-freeze holdout` 不可用，因此本轮不存在 `retain_candidate`。
    - 当前 `Wave3` triage 结果固定为：`SF-001=modify_and_retest`、`SF-002=modify_and_retest`、`SF-003=modify_and_retest`、`SF-004=insufficient_sample`。
    - 本轮新增 `reports/strategy_lab/wave3_robustness_summary.md`、`wave3_robustness_summary.json` 与各策略 `wave3/` artifact，用于 holdout / walk-forward / symbol / regime / time-of-day / cost-stress / conversion 审计。
  - 下一步不是自动扩大 batch backtest，而是根据 `Wave3` 结论在 `paper shadow`、`再修改`、`继续补 SF-005 evidence` 三者之间人工决策。
- 当前 Provider Contract：
  <!-- strategy_factory_provider_contract={"active_provider_config_path":"config/strategy_factory/active_provider_config.json","primary_provider_runtime_source":"source_order[0]"} -->
  - `primary_provider` 的唯一运行时来源是 `reports/strategy_lab/strategy_factory/run_state.json.active_provider_config_path` 指向的 repo 配置文件中的 `source_order[0]`。
  - `plans/active-plan.md`、`docs/status.md`、`docs/acceptance.md` 只描述这个 contract，不把 provider 名称写成长期固定口径。
  - 若 repo config 与上述文档的 contract block 不一致，`M9G.0` 必须失败并先修正文档/配置一致性。
- 当前 Legacy Boundary：
  - `knowledge/wiki/strategy_cards/` 继续保留，但只作为 legacy / historical baseline。
  - `PA-SC-*` 不得作为新 catalog 的 seed、family prior、默认 merge target 或 triage baseline。
  - `PA-SC-002` 只允许作为 historical benchmark / regression reference。
  - 新 catalog 的编号空间固定为 `SF-*`。
- 关键产物：
  - legacy baseline：
    - `knowledge/wiki/strategy_cards/`
    - `reports/strategy_lab/m9_source_inventory.md`
    - `reports/strategy_lab/m9_strategy_extraction_log.md`
    - `reports/strategy_lab/m9_strategy_test_plan_index.md`
    - `reports/strategy_lab/m9_strategy_lab_summary.md`
  - Strategy Factory：
    - `knowledge/wiki/strategy_factory/`
    - `reports/strategy_lab/strategy_factory/coverage_ledger.json`
    - `reports/strategy_lab/strategy_factory/extraction_queue.json`
    - `reports/strategy_lab/strategy_factory/catalog_ledger.json`
    - `reports/strategy_lab/strategy_factory/backtest_queue.json`
    - `reports/strategy_lab/strategy_factory/triage_ledger.json`
    - `reports/strategy_lab/strategy_factory/run_state.json`
    - `docs/strategy-factory.md`
    - `reports/strategy_lab/strategy_factory_plan.md`
    - `reports/strategy_lab/strategy_catalog.json`
    - `reports/strategy_lab/strategy_dedup_map.json`
    - `reports/strategy_lab/chunk_adjudication.jsonl`
    - `reports/strategy_lab/source_family_completeness_report.json`
    - `reports/strategy_lab/source_theme_coverage.json`
    - `reports/strategy_lab/cross_chunk_synthesis.json`
    - `reports/strategy_lab/cross_source_corroboration.json`
    - `reports/strategy_lab/cross_source_corroboration_final.json`
    - `reports/strategy_lab/overmerge_review.json`
    - `reports/strategy_lab/saturation_report.json`
    - `reports/strategy_lab/unresolved_strategy_extraction_gaps.json`
    - `reports/strategy_lab/full_extraction_audit.json`
    - `reports/strategy_lab/factory_summary.md`
    - `reports/strategy_lab/cards/`
    - `reports/strategy_lab/specs/`
    - `reports/strategy_lab/backtest_eligibility_matrix.json`
    - `reports/strategy_lab/executable_spec_queue.json`
    - `reports/strategy_lab/backtest_queue.json`
    - `reports/strategy_lab/backtest_batch_summary.json`
    - `reports/strategy_lab/strategy_triage_matrix.json`
    - `reports/strategy_lab/final_strategy_factory_report.md`
  - `reports/strategy_lab/m9_initial_project_snapshot.md`
- 验收前提：
  - 文档、KB schema、validator、index 与 `strategy_factory` 字段契约保持一致。
  - `knowledge/indices/source_manifest.json` 是唯一 coverage SoT；全部 10 个来源都必须进入 `coverage_ledger.json`，且零 silent skip。
  - `run_state.json`、provider contract config 与 contract docs 必须可通过 `python scripts/validate_strategy_factory_contract.py`。
  - 新提炼必须不读取 legacy `PA-SC-*` 作为先验；只允许用于 overlap / comparison / benchmark 字段。
  - `statement`、`source_note`、`bundle support` 仍不得进入 trigger。
  - 全部 parseable chunks 必须完成 full-pass 审计，且 `unresolved=0`、`unmapped=0`。
  - `Cross-Chunk Synthesis Pass`、`Overmerge Review`、`Notes Per-Source Findings`、`Source Section / Unit / Theme Coverage Matrix`、`Cross-Source Corroboration Report`、`Saturation / Convergence Pass` 必须全部落盘。
  - `saturation_report.json` 必须满足双轮连续零增量 closure。
  - `full_extraction_audit.json` 必须同时写出 `text_extractable_closure=true` 与 `full_source_closure` 结论；若 `full_source_closure=false`，则 `unresolved_strategy_extraction_gaps.json` 必须完整存在。
  - `M9G Audit v4` 本身不得启动任何 batch backtest。
  - `M9H` 若启动 batch backtest，必须限制在已冻结且 corroboration 足够的 `SF-*` family；不得把 deferred / parked family 偷渡进 queue。
  - `M9H` 结果不得夸大为实盘能力或稳定盈利结论。
- 可并行子任务：
  - `researcher`：coverage baseline、legacy boundary 与差异盘点。
  - `kb_curator`：legacy 标注、source mapping 与新 catalog wiki 目录整理。
  - `implementer`：schema、validator、index、provider contract、ledger / run_state 模板。
  - `reviewer`：真实性、边界与回归风险审查。
  - `qa`：KB/trace/reliability 回归与验收清单。
- 风险：
  - 把 legacy `PA-SC-*` 误当作新 catalog 的 family prior。
  - 把 `source_manifest` 以外的 legacy snapshot 当成 coverage SoT。
  - 把 provider-specific 历史事实误写成长期固定 provider 契约。
  - 因新字段与目录引入 KB 校验漂移。
- 回退点：
  - 本支线初始回退点固定为 `main@d9ef8a73ff8bdb4e08605b13cd64233d95ade6dc`。

## 21. M10：Price Action Strategy Refresh

- 分支：`codex/m10-price-action-strategy-refresh`
- 当前状态：进行中，已完成 source ingestion、clean-room catalog refresh、comparison artifacts、M10 test plan 初版、M10.1 catalog review / frozen catalog / test queue、M10.2 Visual Golden Case Pack、M10.3 Backtest Spec Freeze、M10.4 Historical Backtest Pilot、M10.5 Read-only Observation Plan，以及 M10.6 Read-only Observation Input / Ledger Prototype。
- 目标：
  - 从 `main` 启动新阶段，避免继续叠加 M9 杂项状态。
  - 先做 workspace / worktree / branch audit，并把旧 M8/M9、`PA-SC-*`、`SF-*`、旧 reports/specs/catalog 全部登记为 legacy-only。
  - 从 Brooks v2 manual transcript、方方土 YouTube transcript、方方土 notes 重新提炼 `M10-PA-*` 策略目录。
  - ChatGPT share 只作为 reference-only comparison，不作为策略 source of truth。
- 来源优先级：
  - `brooks_v2_manual_transcript`
  - `fangfangtu_youtube_transcript`
  - `fangfangtu_notes`
- 入选规则：
  - Brooks v2 单源支撑不得因缺少 YouTube/notes 交叉验证而自动拒绝。
  - FangFangTu YouTube 单源支撑不得因缺少 Brooks/notes 交叉验证而自动拒绝。
  - 多来源验证只提升 confidence、priority、visual review 顺序和测试顺序。
  - notes-only 策略只能进入 `research_only` 或 `needs_corroboration`，不得直接提升为高优先级回测候选。
- 关键产物：
  - `knowledge/raw/brooks/transcribed_v2/al_brooks_price_action_course_v2/`
  - `knowledge/wiki/sources/al-brooks-price-action-course-v2-manual-transcript.md`
  - `knowledge/wiki/sources/chatgpt-bpa-reference-share.md`
  - `knowledge/wiki/sources/codex-brooks-v2-transcription-thread.md`
  - `scripts/import_brooks_v2_source.py`
  - `scripts/generate_m10_strategy_refresh.py`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/strategy_catalog_m10.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/source_support_matrix_m10.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/chatgpt_bpa_comparison.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/legacy_comparison_m10.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/visual_gap_ledger.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/backtest_eligibility_matrix.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_test_plan.md`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_catalog_review.md`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/strategy_catalog_m10_frozen.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_strategy_test_queue.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/visual_golden_cases/`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_visual_golden_case_index.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_visual_case_selection_ledger.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_2_visual_review_summary.md`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_3_backtest_spec_handoff.md`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/backtest_specs/`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_backtest_spec_index.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_event_definition_ledger.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_skip_rule_ledger.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_cost_sample_gate_policy.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_3_backtest_spec_freeze_summary.md`
  - `config/examples/m10_wave_a_historical_pilot.json`
  - `scripts/m10_historical_pilot_lib.py`
  - `scripts/run_m10_historical_pilot.py`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/historical_pilot/m10_4_wave_a_pilot/`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/historical_pilot/m10_4_wave_a_pilot/m10_4_data_availability.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/historical_pilot/m10_4_wave_a_pilot/m10_4_dataset_inventory.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/historical_pilot/m10_4_wave_a_pilot/m10_4_wave_a_pilot_summary.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/historical_pilot/m10_4_wave_a_pilot/m10_4_wave_a_pilot_report.md`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_5_pilot_quality_review.md`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_5_observation_candidate_queue.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_5_read_only_observation_plan.md`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_5_observation_event_schema.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/m10_5_paper_gate_handoff.md`
  - `config/examples/m10_read_only_observation_replay.json`
  - `scripts/m10_read_only_observation_lib.py`
  - `scripts/run_m10_read_only_observation.py`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/read_only_observation/m10_6_replay/m10_6_input_manifest.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/read_only_observation/m10_6_replay/m10_6_observation_ledger.jsonl`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/read_only_observation/m10_6_replay/m10_6_observation_summary.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/read_only_observation/m10_6_replay/m10_6_deferred_inputs.json`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/read_only_observation/m10_6_replay/m10_6_observation_report.md`
  - `reports/strategy_lab/m10_price_action_strategy_refresh/workspace_audit_legacy_inventory_m10.md`
- 测试规划：
  - Daily、1h、15m、5m 是独立测试线；日线不是 5m 辅助过滤器。
  - `backtest_wave_a` 固定为 `M10-PA-001`、`M10-PA-002`、`M10-PA-005`、`M10-PA-012`；其中 `001/002/005` 测 `1d / 1h / 15m / 5m`，`012` 测 `15m / 5m`。
  - `M10-PA-013` 保留为 `backtest_wave_b_candidate`，不在 M10.1 执行。
  - `visual_golden_case_first` 固定为 `M10-PA-003`、`004`、`007`、`008`、`009`、`010`、`011`；Visual golden case 不是所有策略的统一门槛。
  - `M10-PA-014`、`M10-PA-015` 是 supporting rules，不得生成独立 entry trigger。
  - `M10-PA-006`、`M10-PA-016` 保持 research-only。
  - M10.2 图例包当前 `ready_count=7 / blocked_count=0`；`visual_pack_ready` 只表示 Brooks v2 evidence image logical path 与 checksum 完整，仍需人工 visual review。
  - M10.3 已冻结 Wave A backtest spec：`M10-PA-001/002/005` 测 `1d / 1h / 15m / 5m`，`M10-PA-012` 只测 `15m / 5m`；每份 spec 必须输出 candidate events、skip/no-trade ledger、source ledger、成本/滑点敏感性、per-symbol、per-regime 与 failure-mode notes。
  - M10.3 成本/样本门槛固定为 baseline `1 bps`、stress low `2 bps`、stress high `5 bps`；每个 strategy/timeframe 至少 `30` 个 candidate events 且 skip 后至少 `10` 个 executed trades 才允许解释测试质量。
  - M10.4 已执行 Wave A historical pilot：`M10-PA-001/002/005` 跑 `1d / 1h / 15m / 5m`，`M10-PA-012` 跑 `15m / 5m`，共 `14` 个 strategy/timeframe 组合。
  - M10.4 daily 数据窗口固定为 `2010-06-29 ~ 2026-04-21`；实际标的若晚于该日期才有数据，必须写入 `m10_4_dataset_inventory.json`。
  - M10.4 `5m` 使用 sibling main worktree 的本地 Longbridge intraday cache；`15m / 1h` 从 `5m` 聚合，并在 inventory 中记录 `derived_from_5m`。
  - M10.4 当前解析 `16` 个 dataset，`available_count=16 / deferred_count=0`；所有 strategy/timeframe 当前只允许维持 `continue_testing` 等 M10.3 允许 outcome，不得解释为盈利结论。
  - M10.5 已把 Wave A 转成只读观察候选队列：`M10-PA-001/002/005` 观察 `1d / 1h / 15m / 5m`，`M10-PA-012` 只观察 `15m / 5m`。
  - M10.5 明确 `1d` 只做收盘后观察，`1h / 15m / 5m` 只在 regular-session bar close 后记录；当前实时只读输入状态为 `observation_input_deferred`，不得补假事件。
  - M10.5 对 candidate density 设置 `100 events / 1000 bars` 的 definition breadth review 阈值；`M10-PA-005` 的 `1h / 15m / 5m` 已标记为 `definition_breadth_review`，但不因 PnL 自动拒绝。
  - M10.6 已把 M10.5 queue 转成 recorded replay ledger：输入只来自本地 cached OHLCV，当前 `deferred_input_count=0`，`lineage_counts={"native_cache": 8, "derived_from_5m": 8}`。
  - M10.6 当前输出 `108640` 条 observation ledger row，其中 `73619` 条 candidate event、`35021` 条 skip/no-trade；ledger 只记录 hypothetical entry/stop/target 与 review status，不生成执行、仓位、现金或盈亏结论。
  - M10.6 继续保持 `M10-PA-005` 的 `1h / 15m / 5m` 为 `needs_definition_fix` / `definition_breadth_review` 路径，不做自动升级。
  - 阶段顺序固定为 historical backtest -> realtime read-only observation -> paper trading -> live approval；M10 不进入真实下单。
- 后续承接：
  - 当前 M11 gate 继续关闭；只有完成真实只读观察周报闭环、`M10-PA-008/009` 人工图形语境复核、必要 definition review 和明确人工业务审批后，才允许重新评估 paper trading gate。
  - M12.10 的执行边界固定为：优先修复或正式降级 `M10-PA-005/004/007`，只复跑受影响策略/周期，输出 before/after 交易数、模拟收益、胜率、最大回撤和 blocker 状态；若缺少可执行几何字段或图形证据，必须保留 blocker 或降级，不得补假规则。
  - M12.11 的执行边界固定为：只读本地 dashboard，不引入真实账户、订单、持仓或 broker 字段；所有交易信息必须使用 `hypothetical_*` 或 `simulated_*` 语义。
  - M12.12 的执行边界固定为：日常循环只更新只读 cache/scanner/observation/dashboard artifacts，不启用真实交易连接，不生成真实委托、成交、账户或持仓字段。
- 当前边界：
  - 继续保持 `paper / simulated`。
  - 不接真实账户，不连真实 broker，不自动下单。
  - 不修改 `src/risk/`、`src/execution/`、`src/broker/` 的 live 行为。
- 验收前提：
  - `python scripts/validate_kb.py`
  - `python scripts/validate_kb_coverage.py`
  - `python scripts/validate_knowledge_atoms.py`
  - `python -m unittest tests/unit/test_m10_strategy_refresh.py -v`
  - `python -m unittest tests/unit/test_m10_backtest_spec_freeze.py -v`
  - `python -m unittest tests/unit/test_m10_historical_pilot.py -v`
  - `python -m unittest tests/unit/test_m10_read_only_observation_plan.py -v`
  - `python -m unittest tests/unit/test_m10_read_only_observation_replay.py -v`
  - `python -m unittest discover -s tests/unit -v`
  - `python -m unittest discover -s tests/reliability -v`
  - `git diff --check`

## 22. M12.15-M12.21：FTD 重测、候选安排、每日测试与图形检测器复核

- 分支节奏：
  - `feature/m12-15-ftd-v02-ab-retest`
  - `feature/m12-16-source-candidate-test-plan`
  - `feature/m12-17-daily-observation-continuity`
  - `feature/m12-18-visual-strategy-observation`
  - `feature/m12-19-visual-detector-prototypes`
  - `feature/m12-20-visual-detector-implementation`
  - `feature/m12-21-detector-quality-review`
- 当前目标：
  - 把早期强日线策略 `M12-FTD-001` 从“收益强但回撤大”推进到可每日观察的最佳版本。
  - 把 M12.14 回看来源后整理出的 `6` 条候选排清楚：哪些直接测、哪些做过滤器、哪些只观察。
  - 继续推进第一批 `50` 只股票/ETF 的每日只读测试，优先看今日机会、模拟盈亏、累计收益、胜率、最大回撤和策略状态。
  - `M10-PA-008/009` 不再等用户逐图确认，进入严格定义观察；`M10-PA-004/007` 改为图形检测器任务，不再拖每日测试主线。
  - 把 M10 的 16 条 clean-room 策略和 M12 的 6 条来源回看候选合并到一张统一策略队列；能每日测试的直接进看板，不能直接测的明确为过滤器、严格观察、检测器、辅助或研究。
- M12.15 当前结果：
  - 已跑 `baseline / pullback_guard / follow_through_confirm / context_signal_quality / full_v02` 共 `5` 个版本。
  - baseline：收益 `745.13%`、胜率 `36.74%`、最大回撤 `49.04%`、交易 `41030` 笔、最大连续亏损 `65`。
  - `pullback_guard`：收益 `610.44%`、胜率 `37.36%`、最大回撤 `48.38%`、交易 `22184` 笔、最大连续亏损 `56`。
  - `follow_through_confirm`：收益 `-179.50%`、胜率 `36.46%`、最大回撤 `154.82%`、交易 `20747` 笔、最大连续亏损 `67`；不适合作为 FTD 主版本。
  - `context_signal_quality`：收益 `288.22%`、胜率 `36.66%`、最大回撤 `64.08%`、交易 `14445` 笔、最大连续亏损 `58`；净利仍为正，但回撤变差。
  - `full_v02`：收益 `-76.03%`、胜率 `35.17%`、最大回撤 `99.94%`、交易 `3182` 笔、最大连续亏损 `32`；说明多来源条件不能直接全叠加成一个 trigger。
  - 当前最佳折中版本为 `pullback_guard`，进入 M12.17 每日只读测试候选；这不是实盘批准，也不是模拟买卖试运行批准。
- M12.16 当前结果：
  - `M12-SRC-001` 接 M12.15 最佳版本。
  - `M12-SRC-002/003` 保持每日主线；`M12-SRC-003` 同时作为 FTD 确认过滤器候选。
  - `M12-SRC-004/005` 只做排名/风险过滤器，不伪装成独立交易策略。
  - `M12-SRC-006` 与 `M10-PA-008` 合并进入反转观察队列。
- 下一步 M12.17：
  - 每日主线固定为 `M10-PA-001`、`M10-PA-002`、`M10-PA-012` 与 `M12-FTD-001 pullback_guard`。
  - 输出中文看板快照、每日机会、假设入场/止损/目标、当前价、模拟盈亏、累计收益、胜率、最大回撤和暂停原因。
- M12.17 当前结果：
  - 已生成第 `1/10` 个连续交易日记录；今日机会 `141` 条，今日估算盈亏 `-8482.20`，今日估算收益率 `-8.48%`。
  - 中文看板快照首页已把今日机会、模拟盈亏、收益率、FTD增强版收益/胜率/最大回撤和连续记录天数放在最前面。
  - 当前还差 `9` 个交易日连续记录，不能进入模拟买卖试运行复查。
- 下一步 M12.18：
  - `M10-PA-008` 主要趋势反转和 `M10-PA-009` 楔形反转只进入严格观察队列。
  - 输出观察事件数量、分标的机会、典型成功/失败样例和是否值得进入下一轮历史回测。
- M12.18 当前结果：
  - `M10-PA-008/009` 已进入严格观察队列，不再等人工逐图确认。
  - 当前整理出 `8631` 条观察候选：`M10-PA-008` `4375` 条，`M10-PA-009` `4256` 条。
  - 这些事件不是自动买卖信号，只用于后续观察样例和是否值得重跑历史测试的判断。
- 下一步 M12.19：
  - `M10-PA-004` 宽通道边界反转和 `M10-PA-007` 第二腿陷阱反转不补假图例，改做自动检测器原型。
  - 输出候选图形列表、自动判定理由、关键样例图包和是否值得进入后续回测。
- M12.19 当前结果：
  - `M10-PA-004/007` 已从“等待确认”改为“检测器原型任务”，不再拖每日测试主线。
  - 当前整理出 `10` 个候选图例：`M10-PA-004` `5` 个、`M10-PA-007` `5` 个。
  - 两条策略当前都是 `detector_prototype_ready_not_backtest_ready`：知道机器要识别什么，但还不能拿来跑盈利回测。
- M12.20 当前结果：
  - 已真正启动机器识别检测器，不再停留在“后续要做”的计划层。
  - 使用第一批 `50` 只股票/ETF 的 `1d` 长窗口缓存运行检测器，`daily_cache_ready_symbols=50`，`deferred_input_count=0`。
  - 共输出 `4801` 个候选图形：`M10-PA-004` 宽通道边界反转 `2301` 个，`M10-PA-007` 第二腿陷阱反转 `2500` 个。
  - 检测器事件已带 `event_id`、`source_cache_path`、`source_checksum`、`source_lineage`、`source_refs` 和 `spec_ref`，可重复追溯。
  - 已新增统一策略队列：每日测试为 `M10-PA-001/002/012 + M12-FTD-001 pullback_guard`；`M10-PA-003` 和 `M12-FTD-001-filter` 为过滤器；`M10-PA-008/009` 为严格观察；`M10-PA-004/007` 为机器检测器观察。
- 后续收口：
  - 每日只读测试继续累计到 `10` 个交易日后，才进入 M11.6 模拟买卖试运行复查。
  - `M10-PA-004/007` 下一步不是补图例，而是抽样检查机器识别出来的候选图形是否稳定；稳定后再决定是否进入历史回测，不稳定则正式降级为图形研究。
- M12.21 当前结果：
  - 已对 M12.20 已保留的 `4801` 条机器识别候选做全量结构复核，而不是只抽样。
  - 结构复核未拒绝 `4801` 条，通过率 `100.00%`；其中 `2785` 条为高质量、`1645` 条为中等质量、`371` 条建议抽样看图、自动拒绝 `0` 条。
  - 分策略结果：`M10-PA-004` 高质量 `780` 条、中等 `1165` 条、建议看图 `356` 条；`M10-PA-007` 高质量 `2005` 条、中等 `480` 条、建议看图 `15` 条。
  - 已输出 `m12_21_detector_quality_summary.json`、`m12_21_full_quality_ledger.csv/jsonl`、`m12_21_review_sample.csv`、`m12_21_review_packet.md/html`、`m12_21_next_action_plan.md`。
  - 限制说明：M12.20 每个 strategy/symbol 最多保留最近 `50` 条事件，因此 M12.21 是对 retained candidates 的全量复核；若要进入历史回测，还需要补 raw/capped 分布审计，并先看抽样图形包是否误判过多。
- 验收边界：
  - 所有报告继续只允许 `readonly_*`、`hypothetical_*`、`simulated_*` 语义。
  - 不接真实账户、不下真实订单、不输出真实持仓、真实现金、真实成交或 live-ready 结论。
  - 每阶段完成后必须运行新增单测、相关回归、KB validator 和 `git diff --check`，再提交。
- [x] 删除 147 个订阅批次全部确认后的 `quote.subscriptions()` 冗余查询和第二次回调替换，避免确认阶段卡死后误降级到轮询；以逐批成功/失败结果作为订阅覆盖事实，并用单次回调注册加内存开关过滤初始快照。
- [x] SDK 快照退路改为 3 秒低压轮询，五分钟 K 线状态留在父进程跨行情子进程重建保存；连续失败进入停止新开仓的 15 秒冷却并自动重连，不再永久退出 M15。
- [x] `2026-07-28T16:10Z` 跨行情子进程重建验收通过：父进程保留 `147` 个当前五分钟柱，完整收线后生成 `19` 条实时信号，`2` 条通过风控并在 `1295ms` 内发起 SDK 请求；两笔均取得长桥订单号并实际成交。
- [x] 修复后台看护“命令退出码为 0 但业务状态已阻断仍报健康”的问题；SDK 运行层连接中、账户快照不健康、开盘验收阻断或综合验收阻断时，看护现在必须输出 `needs_attention`，但不会误杀仍在自愈且状态持续更新的主进程。
- [x] OAuth 访问令牌过期造成的 `29` 次无订单号拒绝已定位并恢复；订单执行上下文遇到明确 OAuth 刷新失败后立即作废，只允许为下一条新实时信号重建，禁止重放旧信号。
# 2026-08-06 M15 盘中稳定性收口

- [x] 将 `m15-sdk-contract-v1-*` 纳入长桥实际成交精确归因，平仓必须绑定原始开仓订单号和成交批次。
- [x] 修复 SDK 行情快照子进程短暂重连导致只读统计和后台看护误报警的问题，同时保持纸面账户与 SDK 模式硬边界。
- [x] 盘中验证重复开仓已被已有持仓、单标的和资金池总敞口门禁阻断；精确止损退出仍可取得长桥模拟订单号。
- [x] 重启合同测试 SDK 运行层和合同测试后台看护，确认 `300/300` 行情、`18000/18000` 日线、账户快照新鲜、配置指纹一致。
- [ ] 后续仅按长桥实际成交逐步退出修复前超额持仓；三个超限做多仓在降回各自 `6000 USD` 以内前保持禁止新开仓，不手工改写历史归因。
- [x] 修复提交、券商快照和成交归因之间的敞口交接空窗；任何未确认释放的买入都持续占用对应资金池额度。
- [x] 日线策略按纽约交易日生成稳定结构实例编号；同一结构在后续行情轮询中不得重复开仓。
- [x] 用长桥真实订单证据结清 broker-flat 和旧测试编号残量，当前逐标的实际持仓与虚拟归因数量差异为 `0`，调整不进入策略成绩。

# 2026-08-10 周一开盘前收口

- [x] 复核 SDK 运行层、纸面账户、`300/300` 交易池、`18000/18000` 日线、账户快照、正式测试编号、配置指纹和后台看护。
- [x] 修复 `PA012-5m` 与 `PA011-ORB-R1-5m-short` 因无关盘中缺口被永久关闭的问题，仅保留开盘区间和当前触发上下文的连续性要求。
- [x] 修复周末/开盘前五分钟会话进度误报，不再产生虚假的全池行情缺口报警。
- [x] 使用 `2026-08-07` 的 300 只行情重放三条做空策略，确认修复后没有系统性漏检；当日零候选来自既有结构门槛未满足。
- [x] 完成 `182` 项聚焦单测、编译检查和差异检查。
- [ ] 周一开盘后核对第一轮真实五分钟收线、策略漏斗、账户快照、订单号与成交；无合格信号时只记录真实阻断原因，不制造订单。
- [ ] `PA001-1d` 与 `FTD pullback-guard-confirm` 在故障日遗留敞口降至各自上限前保持只退出；未经用户单独授权不主动清仓。

# 2026-08-11 M15 盘中巡检与清仓归因收口

- [x] 用户授权的 `PA001-1d` 与 `FTD pullback-guard-confirm` 故障日旧持仓已按长桥实际成交清理完成；`19/19` 张清仓订单均取得长桥订单号并成交，清理门禁已解除。
- [x] 修复账户级汇总退出元数据在长桥订单对账中丢失的问题，保留 `aggregate_strategy_exit` 与精确 `source_batch_ids`，不再把已成交清仓误报为 `exit_missing_source_batch`。
- [x] 汇总退出成交只允许分配给订单明确声明的开仓批次；普通退出仍必须精确绑定单一原始开仓批次，禁止按标的或数量猜测归因。
- [x] 重新生成长桥订单对账、成交归因和看板后，归因异常为 `0`，长桥实际 `8` 个持仓与本地 `9` 个虚拟批次精确一致；`DDOG` 的两个批次属于两个独立策略仓的合法重叠。
- [x] 盘中 SDK 链路保持 `300/300` 行情、`18000/18000` 日线、纸面账户、账户快照新鲜、正式测试编号 active、配置无漂移和看护器健康。
- [ ] 收盘后由既有调度器完成 `2026-08-10` 完整实时影子会话验收；`PA004`、`PA007`、`PA008` 在人工样例和完整影子证据未齐前继续只读，不进入下单合同。

# 2026-08-12 M15 五分钟会话完整性最终收口

- [x] 定位连续多个交易日不完整的共同根因：订阅阶段首批 SDK 回调被内存开关丢弃，运行层随后把“订阅已确认但无推送”误判为静默并切换到快照轮询；盘中源码指纹变化还会切断正在积累的会话。
- [x] quote 回调改为订阅前启用，继续由 builder 抑制启动时不完整K线；不向当前 `longbridge 4.3.3` Python SDK 传入其实际签名不支持的 `is_first_push` 参数。
- [x] 已完成五分钟K线改为写盘后 `flush + fsync`，重启恢复继续使用确定性事件编号并禁止历史信号/订单重放。
- [x] 新增盘后单连接缺口补齐：先回收实时行情子进程，再用单一 SDK 历史连接补取；补录行固定为 `context_only`，只形成“盘后数据可复核完整”，不冒充实时会话完整，也不能进入路由或执行。
- [x] 收盘后重载日线上下文时保留当日五分钟上下文，避免运行状态在 16:10 后人为归零。
- [x] 新增纽约时间 `09:25-16:05` 部署冻结；健康进程的代码/配置漂移盘中不热重启，真实故障恢复不受影响。
- [x] 视觉影子失败流水补齐期望K线、接受K线、缺失K线、不完整标的和缺口样例；长桥看板分开展示实时完整度与盘后补齐完整度。
- [x] 聚焦编译和 `184` 项 SDK runtime、视觉影子、长桥看板回归通过；账户快照、开盘验收、后台看护、实时路由、执行与持仓管理外围回归另有 `180` 项通过，`git diff --check` 通过。
- [ ] 下一完整美股交易日取得真实 `300 × 78 = 23400` 根实时K线，且主进程整场不因部署漂移重启；这是本轮唯一剩余的真实市场验收证据，盘后补录不能替代。
- [ ] 若下一完整交易日仍有实时缺口，必须使用新的缺口字段定位到具体边界、工作进程重启原因和来源模式，不再笼统写“行情不完整”。
