# 当前执行规则

## 开始任务

1. 读取 `plans/active-plan.md`、`docs/status.md`、`docs/architecture.md`。
2. 获取最新 `origin/main`，从它创建单一主题的 `codex/*` 分支或独立工作树。
3. 修改前保存用户工作区；禁止丢弃来源不明的改动和运行产物。
4. 交易时段禁止修改生产源码。故障时停止新开仓并保留诊断证据。

## 本轮清理与环境建立（2026-09-19）

- 先撤销旧自动启动、保留证据，再建立官方环境；校验新入口后恢复统一自动启动。不得并行保留第二套生产行情路径。
- 分支、暂存、未提交/忽略资料先在项目外归档并验证。删除前核对活动进程和远端最新提交；保留凭证、知识库原始资料、交易记录、待确认订单及防重复状态。归档不用于自动回退运行。
- 运行解释器、官方发布wheel和实际加载SDK文件均须校验，版本号或项目源码哈希不能替代依赖来源验证。来源不符禁止正式启动。
- 独立只读诊断先少量股票、再147只，串行执行；数据接收、处理、成K线分别留证。不因未知状态继续交易。

## M15 行情

- 生产只能使用官方 `longbridge==4.5.0` SDK 和一个长期 `AsyncQuoteContext`。官方异步I/O在同一个后台事件循环await；无同步SDK回退，不新建第二行情上下文。
- 回调必须在订阅前注册；同一连接按配置批次订阅147只标的的 `Quote` 和 `Trade`，完成后调用 SDK 订阅查询确认147/147。
- 同一连接在订阅前读取每个标的最近60根日线，共8820条；任一标的不足时整场停止，不允许部分运行。
- 订阅完成后只读取一次147只初始快照；快照只初始化价格状态，不能触发开仓。
- 回调只校验时间戳、规范化数据和入有界队列。K线聚合、策略、账户、订单、文件和看板均不得在回调内运行。
- `Trade` 推送按官方美股成交类型生成五分钟价格和成交量；仅计量成交不得污染高低价，其他无效类型不参与。乱序成交按事件时间确定开收价。
- 生产节假日不形成占位K线。边界2秒到达窗口内处理已收到的成交，5秒内未完成必须报警；已封口后迟到不静默改历史值。
- `Quote` 推送维护最新价格和行情健康，重复转发同一个回调时间不得延长存活资格。
- 同一行情连接盘后分步刷新日线，完整后才替换缓存；过期日线不能用于新开仓。不得为更新日线创建另一行情连接。
- 无成交标的可生成 `official_sdk_no_trade_carry_forward` 零成交平K线，但不得触发开仓。
- 行情CLI、`longbridge serve`、快照轮询、历史补录和本地模拟数据均无开仓资格。
- M15运行期间，本地盘后研究任务不得创建第二个长桥行情连接或调用长桥行情CLI。自动启动不再拉起本地盘后调度器；确需运行本地研究时必须先人工停止M15行情进程，完成后再按正式流程启动M15。

## 故障处理

- 项目不自动重试订阅、不重建行情连接、不切换端点、不回退快照或CLI。官方SDK内部自动重连不可由现有Python接口禁用；按用户2026-09-19确认，验收实际数据连续性、及时性和完整性，不要求证明零内部重连。不可观测事件保持未知，不能将未知视为健康；详见生命周期决策记录。
- 任一订阅失败、订阅覆盖不足、回调队列溢出、参考行情静默、K线边界不完整或行情子进程退出，状态立即写为 `fault_halted`。
- `fault_halted` 时新开仓关闭；看护器只报告故障，不重启行情或父进程。
- 必须先定位并修复明确原因，再由人工使用正式配置重新启动。
- 重启不得补交故障期间旧信号，不得重放历史买入、卖出草稿或本地账本事件。

## 账户与订单

- 2026-09-08日期限定模拟订单联调授权已到期，本轮不延续，不发单、不撤单、不清仓。一个完整日通过只提供恢复资格依据，恢复模拟订单仍须核对届时有效授权，不自动恢复交易。原有退出保护与待确认订单防重复状态必须保留，但已退出的交易进程不能宣称仍在自动监控持仓。

- 只允许 `lb_papertrading`；禁止实盘、真实资金、期权和融资。
- 账户、持仓、订单和成交使用独立官方 SDK 上下文，账户快照正常周期为15秒。
- 账户快照超过45秒、账户类型不明、持仓或订单读取失败时停止新开仓。
- 新开仓必须同时通过部署清单、配置和源码指纹、完整实时行情边界、日线缓存、账户快照、策略合同和既有风控。
- 未取得长桥订单号只能记为待确认，不得重发；成绩只统计长桥实际成交。
- 本地模拟数据不得进入长桥信号、订单、持仓、退出或盈亏计算。

## 清仓

- 清仓脚本只在用户明确授权的纽约交易日和美股常规交易时段运行。
- 清仓前单次确认模拟账户、持仓、挂单和官方 SDK 当前行情；任一数据缺失立即停止。
- 清仓只提交一次市价退出请求。市价单失败后不得自动改挂限价单，不得按成本价代替实时行情，不得重复提交。
- 只有持仓、挂单和待确认订单均为0，才能激活新的测试编号。

## 部署

- 自动启动只使用：
  - `config/m15_longbridge_marketdata.production.json`
  - `config/m15_opening_trade_readiness.production.json`
  - `config/m15_background_watchdog.production.json`
- 自动启动只拉起M15 SDK和M15看护器，不启动本地盘后研究调度器。
- 新开仓前必须在干净、已推送远端的提交上签发部署清单。
- 正式启动还须验证运行解释器和SDK原包来源；运行提交、依赖来源、配置哈希、源码哈希或部署清单不一致时不得取得新开仓资格。旧正常态仅可显示为故障前快照，不可据此给当前状态签发通过。
- 每个主题执行编译、聚焦单测、敏感信息扫描、大文件检查和 `git diff --check` 后才可合并。

## 验收

- 下一完整交易日只读要求：147/147订阅、8820条日线、78/78边界、11466/11466分类明确的实时K线、零盘后补录、无未处理的数据中断或缺口、账户快照小于45秒、信号处理95分位小于1秒。
- 一个完整交易时段通过仅提供恢复资格依据，不自动武装订单；连续三个完整交易时段通过才宣布稳定。SDK内部事件不可观测不得记录为零次。无成交延续K线不可开仓，没有合格信号允许零订单。
- 本轮不执行300只扩展；后续需稳定后另立计划和独立分支，只读验收，不得直接替换147只生产池。

历史执行规则只存放于 `docs/archive/` 与 `docs/decisions/`，不得作为当前生产依据。
