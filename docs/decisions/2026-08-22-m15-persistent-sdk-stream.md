# M15 官方 SDK 长连接决定

- 生产行情固定使用未修改官方 `longbridge==4.5.0` 的一个长期 `QuoteContext`。
- 生产股票池暂时冻结为147只，每批10只订阅 `Quote + Trade`；300只仍保持独立只读扩展配置。
- 回调必须在订阅前注册。报价回调只更新最新价格和连接健康，逐笔成交回调才生成五分钟 OHLCV。
- 订阅完成后查询一次服务端订阅集合，并拉取一次初始快照；此后不再轮询快照。
- `sdk_snapshot_poll`、历史K线和盘后补录永远不具备新开仓资格。行情断开时只允许已有持仓退出。
- 2026-08-22 非交易时段以官方 SDK 只读运行90秒，订阅与交易池覆盖均为 `147/147`，账户快照健康，派发关闭且未回退快照。
