# Windows监控修复及下一次开盘诊断（准备于2026-09-21）

当前结论：本地监控修复已交付新case018并完成独立复核；一次Windows测试已登记为北京时间9月22日21:25—21:45，最晚21:26启动。用户已手动校时，系统确认23:13:33成功同步；但随后Windows授时请求往返不确定性偏大，当前启动时间检查未通过，不能宣布测试完全就绪或行情恢复。新任务启动前会检查时间质量，未通过就拒绝连接。

## 已确认问题与修复范围

9月21日晚case013已收到真实行情，却因读取健康文件异常提前结束；历史记录没有保存具体异常子类型，不能追认它必然是权限或文件竞争。此次在原生Windows上复现并发读写时PermissionError，确认这一条本地缺陷可独立修复，不把它当作长桥服务端故障。

- 监控读取允许Windows文件共享删除/重命名，短暂读取或JSON解析失败只在原有5秒健康期限内继续检查；期限依据最后合法记录里的单调时间，重复读到旧快照不能续期。身份错误、明确失败、非法字段、时间倒退依旧立即停止。
- 原子写入只对Windows错误5/32/33有限重试，最多250ms且最多26次；使用相同临时文件和时间戳，持续失败仍退出。错误类型、errno、winerror、累计次数及恢复次数进入诊断记录。
- 正式行情时间、队列和静默阈值不变，官方SDK5.0.0未修改，147只仍通过同步QuoteContext一次订阅Quote/Trade；没有新增分批或替代源。

读写共享规则依据[微软CreateFileW文档](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilew)。这些变化属于项目监控修复，不是改写官方行情软件。

## 实测与复核

作者Windows原生验证77项通过（25监控、42行情处理/假SDK、10Job）；独立审查重新运行监控组合80项通过，其中13项为独立编写。时钟与任务外壳37项、guardian18项、OAuth工具14项离线验证通过。重复运行的重叠测试不相加包装成独立用例数量。

原生3秒、单读线程每10ms检查的顺序文件对照：原实现272次写失败、6次读失败；修后397次写入、145次读取，最终读写失败均0。内部仍有147次短暂replace异常、137次有限恢复，均被记录；更极端四读线程无等待负载可耗尽250ms并明确失败。因此不是宣称文件竞争消失，也不是实时行情连续性验收。压力测试使用的controller与最终冻结版仅一行官方文档URL注释不同，独审已重算排除该行后的校验值。

冻结源及完整日志在项目外 `/home/hgl/project-archives/Price-Action-Trader/20260922-windows-continuity/`，公开安全汇总见[证据](../evidence/20260922-windows-monitor-repair.json)。旧case013—017证据及清单不改写。

## 时间与认证

准备阶段两处只读NTP查询显示Windows约慢1.11—1.14秒，WSL约慢1.08—1.12秒；这不是21:26历史时刻的精确重建，不能据此完全归因当时的所有负时间差。普通权限调整WindowsTime收到拒绝访问；管理员调用未取得完成记录，仍待用户确认。

已准备一次管理员脚本 `C:\Users\hegua\AppData\Local\Temp\pat-time-repair-20260921\repair-windows-time.ps1`：保持微软同一授时服务器，把0x9固定特殊轮询改为0x8客户端轮询并执行resync/rediscover。执行前核对原配置，执行后留存结果，不改全局代理、DNS或SDK。此自动授时配置修复需要Windows管理员权限，**尚未执行，需人工确认并复核**；用户本次手动校时已经完成，两者不能混淆。根据[微软WindowsTime说明](https://learn.microsoft.com/en-us/windows-server/networking/windows-time-service/windows-time-service-tools-and-settings)，校正可能渐进发生，命令成功也不能替代校准结果检查。

用户随后手动校时，WindowsTime只读状态确认最后成功同步为北京时间2026-09-21 23:13:33。首轮Windows的Cloudflare偏差估计约2.3ms，WSL两源约26.4ms/9.1ms；Windows微软样本往返1.364秒，无法满足下述保守边界。针对测量不确定性做一次只读复查，Windows两源往返约1.454/1.688秒，仍无法证明边界；两轮原样保留，不择取较好样本宣称通过，也不把网络测量不足说成用户校时失败。配置仍为time.windows.com,0x9，管理员修复结果文件仍不存在；当前是手动校时已执行、自动授时修复未执行、启动时间检查未通过的三个不同状态。此后未继续重复查询。

新入口在复制认证、写入启动记录和连接SDK之前查询两个授时源，每份估计的 `abs(offset)+RTT/2` 均须不超过0.5秒；失败或未知则拒绝。这是本次诊断新增的主机时间前提，不是长桥官方要求，也没有放宽原行情2秒容差或5秒监控期限。网络时间估计不代表专业精度认证；网络查询失败也不归因于长桥行情。

原认证期限不足以覆盖明晚窗口，已按[长桥官方刷新接口](https://open.longbridge.com/zh-CN/docs/refresh-token-api)一次续期，HTTP200，同一client；新期限至2026-10-05T15:04:20Z。旧凭证私有归档只作证据，刷新后的认证已安装，新case采用独立校验清单，旧认证清单保留历史含义。没有行情、账户或订单连接。

## 新任务及验收

任务 `PAT-Official-Quote-Continuity-20260922-Once` 已注册并读回Ready；时间为9月22日北京时间21:25—21:45，21:26后拒绝启动。有限权限、登录用户、单次、不重试、不补跑、IgnoreNew、外部25分钟上限；电脑须在线且用户保持登录。任务manifest SHA256为 `04cffb85d17e451b408ed6e33e5813e11e92850488f97af28a1e7b13d5845b95`，绑定源码、官方运行文件、新认证清单及5份保护状态。

定时器通过WSL唤起看护，行情SDK实际运行在Windows原生环境；这不是新的WSL行情测试。WSL日线请求超时仍未解决，没有自动重排。先看Windows开盘持续接收是否通过，再安排能区分WSL请求问题的实验，不能反复相同条件重启。

下一步处理自动授时持续性，并核实网络授时测量能否提供足够小的不确定性；不再以用户未手动校时为阻断原因。明晚任务会重新核验时间和冻结文件。窗口结束读取正式结果、原始事件及监控诊断，检查是否覆盖开盘、参考流连续、147只实际覆盖和所有进程退出；失败按具体阶段定位。一个短窗不能替代策略接回及147×78=11,466条完整日记录，当前完整日通过0，正式故障锁及订单防重状态保留。
