# 9月25日开盘停止后的修复与盘中续测

## 事实与本轮目标

用户授权继续修复并实测，要求解释为何开盘没有完成测试以及是否可忽略本机时差。9月25日实际在北京时间21:15准时启动，21:15:42准备完成，21:30:02因消费端 `trade_source_delivery_age_exceeded` 停止；不是未安排或未启动。原失败目录和证据不覆盖。147只报价、144只成交覆盖、8820条日线上下文不构成盘中持续性通过；完整五分钟边界与策略判断均为0。

原事件55491的XLU成交时间13:30:00 UTC，Windows回调13:30:02.049167，消费端13:30:02.102520报错；其后监督器主动关闭控制管道。此时间差是本地回调时刻减成交秒标签，不能直接解释为精确网络延迟。官方v5.0.0从Unix整秒创建成交时间，没有毫秒精度；也没有据此证实本次成交一定在2秒内。小单I仍贡献成交量，不能删除这类成交规避检查。

## 修复边界

- 默认日常严格行情资格、原策略和风险参数、2秒/30秒等既有数值保持，不用放宽阈值宣布通过。
- K线归桶继续使用长桥成交时间；本地经过测量的时间用于接收年龄、处理积压与期限。最新成交时间不能当作当前标准时间，否则缓存/断流也可能看似新鲜。
- 质量不合格可以继续显式、有限、无账户订单的诊断采样；它不授予策略恢复或正式运行资格。诊断收流与策略验收分别留证。
- 新增独立盘中诊断窗口，使用维护中的官方Windows单连接和原WSL消费路径；窗口最长一小时，不超过当天收盘加5秒，不迟到补跑，不自动重试。全局排他、Windows Job、宿主退出证明、临时认证归属和文件来源校验保持。
- 显式诊断模式只允许在首个成交交付时效错误后冻结本次策略，继续验证原始流。其他损坏、积压、源时间不推进/静默、时间跳变或期限错误仍停止。原始首错保留；不能用producer心跳冒充SDK推送。
- 故障摘要优先展示本次运行身份匹配的实际消费错误；保护未知/敏感异常，不因展示缺字段称正常。

## 验证与部署

需针对盘中窗口、身份/清单、严格默认行为、错误后冻结、连续流/静默、尾序和验收降级补充测试，独立复核后才部署新实测。9月28/29原冻结任务在源码更新后须归档并重新准备、读回，不能留下失效旧清单。

实际测试、校时检查和最终部署结果将在本页收尾补充。未完成前不得称行情已恢复，完整日通过仍为0。

官方依据：[成交推送字段](https://open.longbridge.com/docs/quote/push/trade)；[v5.0.0成交时间转换](https://github.com/longbridge/openapi/blob/v5.0.0/rust/src/quote/types.rs#L167)。

## 本轮校时核查（北京时间00:14）

固定预算对照通过原0.5秒门槛：两源Windows测量界约0.351/0.340秒，估计慢约0.235/0.240秒；Windows与WSL相对差约0.087毫秒。微软stripchart同时结果一致。服务已运行，最近23:27同步成功；没有再次resync、没有改变系统时钟。

注册表确认MinPollInterval=10、MaxPollInterval=15，当前poll=14；动态0x8配置仍在，允许17分钟至9小时轮询。`SpecialPollInterval`残值在0x8下不被启用，不是旧运行路径恢复。仅把MaxPollInterval收紧到10的候选管理员脚本已外置备查，当前权限不能完成管理员有效配置检查，未执行变更；轮询过长是待处理项，不能说已经证实导致此次行情失败。

证据：`/home/hgl/project-archives/Price-Action-Trader/20260926-clock-followup/once-001`。

## 部署前验证

原严格消费与新诊断模式49项组合通过（18项新诊断、28项原消费、3项147股票集成，含147×78离线穿透）；运行外壳128项通过，独立整场scope拒绝4项通过。离线不计真实行情。独审发现诊断完成摘要核验偏宽，已补reason/last_error、四参考末次进展、首错身份、冻结计数及末水位等核验；反例复验通过。最终发布及新窗口结果在实测后追加。

独立终审已通过，12份源码/测试文件校验一致，无剩余阻塞。正式账户/订单恢复仍需人工复核，本轮授权只读实测不包括账户或订单操作。
