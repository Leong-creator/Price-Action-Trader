# 2026-09-08 开盘巡检失败记录

## 结论

北京时间21:35的一次性巡检未通过。生产行情在21:30:30已因参考行情静默停止，模拟新开仓关闭；不能把盘前连接、768项离线回归或当日授权当成开盘成功。当前没有可证实并已验证的根因修复，不盲目重启或补交旧信号。

本文件只记录脱敏事实，不部署运行代码，不改变策略、风控、代理、账户或测试编号。生产仍为 `a413cb86c97f08e7db7cda181d3b2bd7a6133213`。

## 证据时间线

以下时间均为北京时间2026-09-08：

- 20:45:00：开机审计保留的运行快照显示147/147订阅、8820条完整日线、模拟派发已武装、提交日志无待确认请求。实际异步交易客户端健康检查739毫秒，此数字不是下单延迟。
- 21:29:11.768941：最后记录的QQQ参考推送。
- 21:29:12.967030：最后记录的SPY参考推送。
- 21:30:27：订单维护0项计划、0项执行，未制造测试订单。
- 21:30:30.587839：`reference_market_data_stalled`，原始状态为 `fault_halted`。接收worker心跳年龄0.025秒，回调队列0，已处理通知累计97897条，reader errors为空。进程随后释放并退出。
- 21:35:28：SDK只读预检确认 `lb_papertrading`；资产、持仓、订单、成交、组合接口通过，无错误。当前持仓24条，当日订单0条；没有创建第二行情连接。
- 21:35:48：开盘检查和综合验收明确阻断；看护单实例存活并报告异常，不重启行情。
- 独立部署校验通过，工作区干净，源码/配置/提交/部署清单一致。

## 已知与未知

- 已确认参考推送没有及时到达父进程，首根五分钟边界及可交易K线均为0。不是策略没有合格信号导致今天无订单，而是行情链路先停止，策略未获得可执行新边界。
- worker仍报告心跳且队列为0，不支持“停机时回调大量积压”的解释；不能仅凭此排除SDK内部、网络、上游或尚未被日志记录的接收问题。
- 通知总数只有故障终值，缺少故障前逐秒变化及非参考标的原始回调时间。不能据此宣称147只全部断流，也不能归责为长桥服务故障。
- 官方SDK内部连接状态、重连事件及原生传输错误没有完整现场证据，当前无法准确区分SDK、网络和上游原因。
- 独立代码复核确认：通知数在项目队列出队时累计，worker心跳中的 `reader_errors` 当前固定为空；worker循环心跳不是SDK原生接收任务心跳。因此这些指标不足以证明原生接收正常，补观测是必要后续工作，但不能称为已经修好行情。
- `--status` 的展示会将死亡进程描述为 `stopped/runtime_process_not_alive`；原始JSON仍保存 `fault_halted/reference_market_data_stalled`，应以原始故障原因解释本次事件。
- 停机状态未保留全部正常态字段，readiness派生出日线0/8820、部署未验证、账户年龄缺失等提示。它们是故障后缺字段，不能证明日线被删除、部署漂移或账户授权失效。
- 当日请求日志为空、当前长桥订单0条，没有订单请求/响应延迟和成交样本；不得用历史8月执行记录解释为今日活动。

## 安全状态与已有持仓

- 新开仓保持停止，未清除故障锁，未重启SDK、未回退CLI、未创建行情探针、未改信号门槛。
- 原交易主进程已退出，24条原持仓的自动止损/止盈不能宣称仍受本系统监控。没有用户本次清仓授权，不自动卖出或回补。用户需要在长桥App关注这些模拟持仓；已有券商侧委托与本地自动退出不可混为一谈。
- 原始状态、只读预检、账户快照、订单维护和部署清单保存于本机权限700目录 `/tmp/pat-opening-failure-20260908-2135/`，不提交账户明细或凭证到远端。

## SDK安装来源漏检

21:40后两个代理复核及主代理重新检查发现：生产入口强制使用项目 `.venv`；其中 `longbridge-4.5.0.dist-info/direct_url.json` 指向8月的本地构建wheel，`.venv/.m15-longbridge-sdk-no-first-push.json` 标记为 `m15_confirmed_candlestick_trade_aggregation_and_load_shedding`。安装文件修改时间为8月20日03:58。只检查版本号4.5.0不能证明原包来源。

- 当前项目SDK二进制SHA256：`5507828948c72302f3b011c8988c6395a47c71435287a90dc470b459355a1952`。
- 本机两份先前标记为官方安装的独立环境二进制均为：`4af34d03b87402e680630e2e0f5e11612d84ce22ac12a8dcd3a18886d094d185`。正式更换仍须与官方分发wheel核验，不能仅凭临时目录名字认定来源。
- 部署清单覆盖项目源码而未覆盖SDK二进制，因此源码校验通过不等于依赖部署正确。此前“官方原包已部署”的结论不充分，必须更正。
- 故障进程已退出，未留存当时实际加载二进制哈希；不能单凭当前包差异认定此次断流因果关系。此次仅保存证据，不在盘中偷偷替换依赖或重启。
- 下一修复先将原包安装来源、运行解释器、模块路径、wheel及二进制哈希纳入独立部署门禁，再复核现场原生推送证据；纠正依赖来源与证明断流根因是两项验收，不能混淆。

## 下一步修复必须满足

1. 先纠正并校验上节SDK安装来源；在独立诊断分支补齐同一连接的原生错误、逐秒回调计数和参考/非参考最后回调证据；不得额外建立行情连接或用旧时间戳续命。
2. 保留正常状态的只读诊断副本，停机时明确标记为故障前快照，缺字段显示未知，不能输出虚假的日线0或漂移判断。新开仓仍必须阻断。
3. 针对证实原因修复并测试；没有确定原因就保持停止，不以一次重启恢复推送作为根因已修复的证明。
4. 经复核与干净部署后，重新运行只接受当时新产生信号，不补交断流期间信号。今日完整交易日已存在缺口，不签发78/78或零重启通过记录。

## 本次检查范围

执行生产SDK状态、只读账户检查、开盘验收、综合验收、看护状态、独立部署校验和进程扫描；未执行订单、撤单、清仓、行情重试或策略修改。两个独立只读审查任务检查回调/桥接和SDK/其他启动路径，未找到已证明可立即部署的根因时不得强行恢复。
