# 2026-09-25 日常行情与策略观察接回

用户已授权执行时间诊断、正式行情入口迁移、监控与来源核验、逐K线留证及整日验证计划。此次日常入口仅允许行情和原策略观察；不构造账户/订单上下文，不解除原交易故障或防重复状态。旧日期限定订单授权不延续。

## 实施顺序与责任

1. 时间模块：固定预算对照微软工具与原采样器，保存实际服务器IP、原始样本及Windows/WSL时差证明。保留原0.5秒门槛与失败历史；依据结果修复具体问题，不重复采样直到碰到通过。
2. 日常看护：把case023已验交接、单连接锁、双系统阻断、Windows Job和认证归属收尾纳入维护；每日不可变清单绑定交易日、窗口、来源与解释器，任务重复触发不建立第二连接。
3. 接回与监控：唯一启动入口改接Windows收流、WSL原聚合/router；日常监控不再刷新旧账户/旧行情产物，来源清单覆盖新程序。官方Windows环境使用稳定路径，来源来自已核官方发布文件。
4. 实时证据：每根分类K线、排除原因、原始时刻、边界处理及策略耗时落盘；另建完整日验收，保留诊断/行情资格/账户交易资格的区别。
5. 验证：离线反例、真实无SDK生命周期检查、独立审查后部署；盘中可验证持续输入。使用最终日常入口覆盖完整日，正常日147×78=11,466条分类记录，一日提供行情恢复资格，连续三个正常交易日通过才称稳定。

主代理负责计划、配置、旧入口退役、监控与部署集成；独立实现分支分别负责看护、时间、消费证据，独审后集成。高风险启动和认证生命周期改动需独立复核并标记人工复核；本次已授权只读行情部署无需重复索取许可。正式账户与订单恢复仍属后续单独阶段。

## 验证边界

- 长桥官方SDK、147只股票、原K线生成方式及策略/风险参数保持；不增加回退来源、重试或旧信号补交。
- 完整日必须由实时记录计算；半日市单独识别，本轮不得以78条正常日模板伪造。
- 一个策略完成判断不保证出现信号，背景不足如实记载；无成交延续K线不可作为开仓输入。
- 时间检查不代表行情网络延迟，Windows时间证明不自动代表WSL时间证明。
- 运行失败后留证停止；退出不明保留阻断及认证，不清文件制造恢复。
- 三日实测是未来实际时段的结果，不以任务登记或离线回放宣称完成。

## 当前进展

本版本将case023已实测链路接入维护：`run_m15_daily_feed.py prepare|launch|status`负责每日不可变清单、原生Windows任务分发及当前状态；登录入口委托同一原生动作，行情守护等待原宿主退出证明后才创建Windows运行进程。行情只使用官方Windows Longbridge 5.0.0 / CPython 3.14.3，WSL负责原聚合及策略判断。

已确认并处理的本地问题：

| 问题 | 修复与证据 |
| --- | --- |
| 新Windows环境被MSIX写入虚拟目录 | 已逐文件归档校验后迁至真实稳定目录，并核验原生路径、加载文件及写入位置；官方wheel全部8个非RECORD成员一致 |
| 时间测量未识别跨样本/阶段跳时 | 新增各自操作系统的原始单调时间锚点；跳时、缺锚点不通过，不重新采到通过 |
| 异常状态字段可让监控退出 | 校验状态类型和枚举，异常报告未知，不静默退出 |
| 收到旧行情可能被显示成当前正常；盘前易误报 | 收到消息与合格源时间分别显示；正常盘前等待，常规时段按原参考流时限判断；2秒交付上限不当作2秒静默限制 |
| 整日多GB日志可能耗尽归档内存 | 1MiB分块归档并校验；确认退出后先做终场时间测量和临时凭证清理，再归档大文件 |
| 外层收尾失败可能被终验报告覆盖 | 独立整日验收必须同时核查外层成功、行情通过和完整清理；部分窗口、缺口、重复、迟到或外层失败不通过 |

北京时间17:11的固定预算对照中，微软工具与项目采样结果相符。17:26:50—55在新正式解释器运行修复后检查：Windows界限0.2599/0.2405秒，WSL界限0.2601/0.2407秒，跨阶段时间连续，均通过原0.5秒标准。此标准是项目的时间可信度检查，不是长桥官方SDK要求，也不是行情传输延迟。本次没有修改系统时钟或网络设置；旧失败记录保留。启动、每30分钟和收尾继续固定检查。

最终集成验证为104项生命周期测试、70项消费/监控/部署测试通过；另23项时间测试和2项真实官方Linux wheel兼容/临时副本篡改测试通过。独立复核已关闭本轮发现的阻塞项，见[结构化证据](../evidence/20260925-daily-feed-integration.json)。

真实无SDK启动测试覆盖宿主退出后的152次心跳、30.69秒持续运行和正常EOF退出；147×78条原K线及78次原router离线穿透已通过。离线测试与时间点检查不计真实完整日，当前整场通过仍为0。

## 部署、清理与真实验收安排

- 稳定环境：`C:\Users\hegua\AppData\Local\PriceActionTrader\official-longbridge-5.0.0`。环境安装清单、独审、895文件旧Temp环境归档校验和退役记录均存于外部证据目录；旧行情日志保留，旧环境不再作为入口。
- 原9月24日两项一次任务已保存XML并停用；原认证及5份保护状态核对一致。当前主题分支合入主线后清理，不影响M7历史和M16研究分支。
- 正常整日计划：2026-09-25、09-28、09-29；北京时间21:15启动、21:16后拒绝新启动、次日04:00:05结束，覆盖21:30—04:00正常交易时段。逐日使用独立清单、单次预留和同一审核版本，不自动重试。
- 任务实际登记、Windows读回和开机入口核验保存在外部`startup/`与`deployment-result.json`。登记不等于启动或通过，运行状态以日常CLI及各日原始证据为准。
- 每日收尾自动输出`completion.json`、`clock-evidence.json`、`feed_session_acceptance.json`。正常日必须11466条分类K线、78次真实判断、完整时效和退出证据；没有合格策略信号允许零订单。
- 一个完整真实日提供行情恢复资格，连续三个正常交易日才称稳定。账户新鲜度、模拟订单恢复和WSL直接SDK连接仍未验收；本轮不启用账户或订单。

外部实施证据：`/home/hgl/project-archives/Price-Action-Trader/20260925-daily-feed`。逐日运行证据：`/home/hgl/project-archives/Price-Action-Trader/daily-feed-sessions/YYYY-MM-DD`。后续实际结果写入每日证据，不改冻结清单或覆盖失败历史。

交易日与半日市依据：[NYSE官方2026日历](https://www.nyse.com/publicdocs/nyse/ICE_NYSE_2026_Yearly_Trading_Calendar.pdf)。时间诊断依据：[微软Windows Time工具说明](https://learn.microsoft.com/en-us/windows-server/networking/windows-time-service/windows-time-service-tools-and-settings)。
