# 2026-09-21 盘前实时检查

结论：Windows官方隔离环境本次可以订阅147只并收到真实盘前报价和成交；尚不能称数据质量完全正常。本机时间落后外部授时约1.31秒，足以解释本轮大量源时间领先接收时间的现象。未改变系统时间或行情阈值，未启动正式系统、账户或订单，不能据此证明9月8日故障根因或WSL已恢复。

## 实测与分类

用户要求“现在先看一下盘前数据是否正常”后，在现有官方Longbridge5.0.0 / Windows CPython3.14.3隔离环境执行一次case015。使用官方默认Config、同步QuoteContext，147只一次Quote+Trade订阅，无分批、自定义端点、SDK修改或第二条行情连接。订阅于2026-09-21 08:49:22.040899 UTC返回（北京时间16:49:22），之后观察30.007秒。14项采样分类测试、12项看护测试及独立审查在实连前通过。

| 检查 | 实际结果 |
|---|---|
| 报价 | 409条，覆盖147只 |
| 逐笔成交 | 722条，覆盖51只；短窗无成交不能直接判定断流 |
| 会话 | 1131条全部为当日Pre |
| 按原0—2000ms规则及时 | 283条 |
| 源时间领先本机接收 | 712条，领先24—829ms；未按及时计入 |
| 较旧记录 | 136条，全部为首次Quote；不能把首快照年龄当作网络传输延迟 |

独立逐行重算与采样分类一致。XLY首快照价格为0，正确排除有效行情；源时间均整秒，同秒多笔成交不能直接当作重复故障。SPY与QQQ均收到实际盘前Quote/Trade，但SPY本轮4条Trade全部落入源时间领先，因此不能称参考流严格新鲜度通过，也不能用累计数量冒充连续性。

## 时钟证据与局限

08:55 UTC左右使用同一Windows原生Python标准库分别向time.windows.com、time.cloudflare.com发出只读NTP查询，没有设置/同步系统时钟或导入SDK。回包origin对应本次请求，LI=0、stratum分别4和3、mode=4。按标准四时刻公式得到服务器相对本机偏移+1.309304秒、+1.308532秒，往返0.634977秒、0.582918秒；两次估计相差不到1ms。

在非负单向网络延迟假设下，半往返误差区间仍显示本机落后约0.99—1.63秒。若采样至授时查询之间偏差稳定，这足以解释本轮最多0.829秒的领先。它支持“本机时钟偏慢会干扰行情新鲜度判断”，不证明长桥源端绝无误差，也不证明历史订阅超时由时钟造成。UDP授时未认证、未单独核验回包地址、未保存四个原时刻，不能称高精度授时或追溯修正原始行情。

下一步先按操作系统正规授时方式校准Windows，并核对WSL与独立时源；校准后复验偏差，再按已授权串行窗口验证真实行情。校准未完成前，今晚仍可能因时间偏差拒收；不通过放宽两秒门槛、容许未来时间、改写历史记录来消除异常。本轮仅检查，没有执行系统校时或额外行情重试。

## 收尾与今晚安排

本次连接已退出，Windows Job活动进程0，跨系统锁空闲、隔离标记已清；临时认证副本已归档清理，原认证及五份正式故障/防重状态哈希不变。case015的14个Windows文件逐件归档校验后删除，仅保留项目外证据；今晚共用的官方环境保持。

两项今晚任务均Ready，任务XML除保存换行表示规范化外一致，两个manifest字节不变，未消耗执行机会：北京时间Windows21:25—21:45、WSL21:50—22:20，依旧串行无订单。正式系统仍保留9月8日fault_halted状态；旧状态文件不是今天行情健康证据。完整交易日通过仍0，147×78及连续三日标准保持。

完整私密证据位于`/home/hgl/project-archives/Price-Action-Trader/20260921-premarket-check/`；包含原始事件、官方SDK日志、采样/看护源码及校验、运行终态、独立复核、NTP只读结果、清理清单和任务读回。公开安全摘要见[本轮证据](../evidence/20260921-premarket-check.json)。

## 用户校时后的复测（北京时间17:05，case016）

用户确认已经校准时间并要求再检查。先在Windows和WSL各向两个独立NTP时源做只读查询；四时刻公式独立复算一致，偏移估计分别为Windows +15.695/+18.095ms、WSL +15.948/+17.875ms，查询内未见时钟跳变。已不见此前约1.31秒偏差；网络往返仍约0.58—0.62秒，因此这些是估计值，不能声称认证到毫秒级精度。本轮没有修改系统时间。

新case016沿用case015采样源码原字节、官方环境、147只一次Quote/Trade和0—2000ms分类，只更改外部归档路径、案例标识与一次nonce；26项离线测试及变更复核通过。09:05:25.719202 UTC订阅返回，之后观察30.014秒，真实结果如下：

| 项目 | 校时后结果 |
|---|---|
| Quote | 409条、147只首报价覆盖 |
| Trade | 734条、45只；734条全部符合原时限 |
| 首报价后的Quote | 262条，全部符合原时限 |
| 源时间领先本机 | 0条（上一短窗712条） |
| 较旧Quote | 141条，均为首次快照；不是后续传输超时证据 |
| 成交源时间至接收 | 393—1601ms，中位995ms，含源时间整秒精度影响，不称独立网络延迟 |
| 未处理尾部 | 0条 |

本次校时后相同接收程序未再出现未来时间，支持此前异常与本机时间偏慢有关。SPY收到1条及时成交；QQQ在这30秒只有旧首报价、没有新成交，不能据此判定断流，也不能宣称两只参考标的的Quote/Trade均连续通过。盘前有些标的成交稀少，147首推不等于147只都有及时新成交。只能说本轮收到的新成交和后续报价时间质量通过，尚未验证正式WSL链路、完整开盘窗口或整场稳定性；历史订阅超时根因仍未由本实验证明。

本次连接退出、临时认证清理、原认证及五份正式保护状态不变均已核验；Windows临时case的14个文件全部逐件归档校验后删除。今晚两任务XML和manifest保持不变、Ready且未消耗，继续Windows21:25—21:45后WSL21:50—22:20串行计划。完整交易日通过仍0，正式系统未恢复，未操作账户或订单。原先“用户尚未校时”仅代表上一次检查时的情况，以本节复验结果为准。

安全证据见[校时后复测](../evidence/20260921-premarket-postsync.json)。完整私密事件、NTP四时刻、原字节对照、测试与独立复核、退出及清理记录保存于`/home/hgl/project-archives/Price-Action-Trader/20260921-premarket-postsync/`。

## WSL当前盘前实测（北京时间17:25，case017）

用户明确要求现在测试WSL，而非只等待今晚。已使用主项目实际`.venv-m15`（Python3.12.3、官方Longbridge5.0.0）运行与成功Windows case016逐字节相同的采样源码：官方默认配置、147只一次Quote+Trade、订阅后30秒观察，无分批或自定义网络设置。405项既有文件全部匹配固定清单；新外壳12项退出/期限/互斥离线测试和独立复核通过。开始前确认Windows退出、互斥锁空闲，未启动并行行情。WSL两个只读NTP估计偏差约+67/+74ms，未见此前约1.31秒偏差，受网络往返误差限制不称高精度校时证明。

**结果失败：09:25:21.460085 UTC进入147只订阅，09:25:52.385286返回OpenApiException/request_timeout，约30.925秒。** `subscribed=false`，从未进入`observing`，没有形成盘前事件记录。这是SDK返回请求超时，未触发外部90秒强制期限；不是完成30秒正常观察后没有成交。

采样程序在subscribe返回后才消费回调队列，因此summary的event_count=0只证明未记录事件，不能单凭该字段推断原始回调绝对为0；SDK私密日志已独立复核：09:25:22.329737出现AuthResponse，09:25:22.381993出现UserQuoteProfileResponse（订阅上限500，并有美股LV1套餐记录，非逐股权限证明），09:25:22.382283记录147只SubscribeRequest。没有观察到订阅成功响应，但09:25:52.384578出现1条SDK已解析的SPY Quote推送，距请求日志30.002295秒、紧接着订阅超时。因此不能称“SDK完全没有收到推送”；该条是否进入Python回调/队列尚未知，日志请求也不是独立的网络发送确认。这里只保存脱敏事件类别，不输出认证载荷。失败发生在独立接收的订阅阶段，未进入主项目K线或策略处理，不能把策略没有输入当作另一根因。

当前证据是Windows17:05成功订阅并收到实时更新，而WSL17:25同源采样仍订阅超时。两次不在同一瞬间，操作系统、Python版本和平台SDK二进制也不同，因此不能宣称已证明WSL系统、网络、SDK或长桥服务端的具体根因；也不能说长桥在所有环境都不可用。校时解决了此前Windows的负年龄问题，但没有使本次WSL订阅成功。WSL行情可用性本次未通过，仍需围绕订阅请求/响应阶段定位。

进程exit2、已回收且进程组为空，原认证只读未复制且哈希不变，5份保护状态/固定源码/官方环境/今晚manifest保持。锁空闲、隔离标记已清，两项今晚任务仍Ready、XML不变、未消耗；当晚Windows后WSL计划继续，不能将本次失败伪装成准备通过。没有自动重试、改策略、恢复正式程序或操作订单。完整日通过仍0。

安全结果见[WSL盘前证据](../evidence/20260921-wsl-premarket-check.json)。全部源码、冻结清单、独立复核、失败摘要和原生日志保存在项目外`/home/hgl/project-archives/Price-Action-Trader/20260921-wsl-premarket-check/`，该一次入口已消耗。
