# 2026-09-24 行情接回策略与恢复验收

用户要求把剩余问题一起处理，尽量在今晚恢复整个系统。本轮范围扩展为真实行情接收、原有五分钟聚合、原策略判断的只读连通验证；不包括新的账户或订单授权，不通过删除旧故障记录恢复运行。

## 怎样才算通过

| 层次 | 必须取得的证据 | 通过后能说明什么 |
| --- | --- | --- |
| 接收 | 官方环境、147只实际新鲜行情、参考流连续、时间质量及正常退出 | 本次窗口内行情接收通过 |
| K线 | 原始价格成交量真实传递，原聚合按完整边界生成147条分类记录，无缺失、迟到补造或重复计量 | 本次完整边界的K线链路通过 |
| 策略 | 合格的147×60已完成日日线、真实日内上下文、原策略逐条运行；上下文不足和零信号分别留证 | 实际判断已运行；不足的策略不能宣称具备完整输入 |
| 恢复资格 | 同一正常完整交易日从开盘至收盘，147×78=11,466条分类记录，以上条件全部满足 | 行情及策略链路取得恢复资格；订单仍按独立授权和原风险门槛处理 |
| 稳定 | 连续三个完整交易日通过 | 才称行情稳定 |

无成交延续K线不允许触发开仓；未出现合格信号允许零订单。盘中加入后的缺失开盘区间不能用历史回填冒充实时验证。

## 今晚执行

保留已审case021北京时间21:25—21:45开盘接收任务。此次发现的策略缺口不是另一个行情源问题，而是现有Windows诊断仅记录时间身份，没有提供原策略需要的价格成交量。

实现唯一Windows官方QuoteContext输出完整有序数据，WSL只消费该数据并复用现有FiveMinuteBarBuilder、PipelineProbeEvidence和DiagnosticStrategyPipeline。新的消费者不得启动Linux行情SDK或账户上下文。订阅保持147只一次Quote+Trade，日线使用官方按单标的请求接口；保持股票池、策略合同和风险时限。

新通路在离线组合测试、Windows原生序列化测试及独立复核通过后，安排case021确认退出后的单次有限窗口。若不能在开盘前审查完毕，今晚不能取得完整日资格；应尽量取得剩余时段真实K线和策略判断证据，不把开盘测试与之后的短窗拼成整场。

## 关键检查

- UTC与Decimal精度保留；Windows单调时间不与Linux单调时间直接相减。
- 严格run_id和连续序号，源接收时间不可在跨系统读入时刷新；重复传输拒绝，同一成交时间的不同真实成交保留。
- 有序水位证明此前回调前缀已处理才封K线；文件EOF本身不代表数据齐全。
- 日线只采用最新已完成交易日及之前60个不同交易日，缺失或过期不调用策略。
- 消费者异常立即终止本次Windows连接；退出未知保留阻断和凭证证据。退出、终止序号、尾部队列及保护状态交叉核验。
- 来源、唯一连接、时间质量、外部期限及无订单限制均保持。接收成功不覆盖策略失败，程序退出0不替代验收。

## 官方依据与当前状态

[报价字段说明](https://open.longbridge.com/zh-CN/docs/quote/push/quote)明确timestamp为最新成交时间；[逐笔成交说明](https://open.longbridge.com/zh-CN/docs/quote/push/trade)提供成交批次；[K线接口](https://open.longbridge.com/zh-CN/docs/quote/pull/candlestick)支持按标的请求最近K线。本轮只增加完整字段传输及既有模块适配，不修改官方SDK。

新通路实现及独立复核已完成，case022已冻结并登记Windows一次任务，真实策略窗口尚未通过。case021已实际运行并在开盘旧Quote处理处停止，结果见下节。旧故障、下单防重状态和正式订单开关保持，完整交易日通过仍为0。

## 实施与离线验证记录

新增 `scripts/m15_windows_feed_producer.py` 和 `scripts/m15_windows_feed_consumer.py`。Windows只保留一个官方同步行情上下文；完整数据经严格序号的NDJSON进入WSL，后者实际调用原五分钟聚合与原8条策略router。跨系统原始接收时刻保持，成交同timestamp不去重，未来Quote在更新价格基线前拒绝，日线和实时流完整性分别校验。没有替换策略或降低行情时限。

新监督壳继承唯一连接锁、官方文件检查、Windows Job、临时认证归属与五份生产保护状态校验。增加消费者失败联动退出、两端窗口/结束序号/全部预期边界交叉核验，处理时钟跳变及收盘截止。程序退出0不能单独签发通过。两份外置配置明确关闭旧 `paper_validation_approved`、旧批准日期和formal transition，生产原配置未改。

离线结果：最终35项新数据链测试、65项监督壳测试、93项复用模块回归通过；另有独立反例和11项测量测试，见最终部署记录。不重复累计Windows同套15项或独立复跑。两项147规模组合分别覆盖盘中与收盘最后边界，含8820条日线和原策略真实调用。另作一次剩余时段规模穿透：68边界、9996条分类K线、68次策略边界判断，最终版本耗时31.215秒；全部使用假SDK并禁网，不能作为真实交易日证据。

独审发现并修复初始化心跳/阶段不一致、未来Quote污染基线、只形成少数边界却可能误报全窗通过，以及旧20分钟看护上限残留；新看护上限为20715秒，只有固定剩余时段窗口允许，原600秒日线、45秒其他初始化与行情质量时限保持。

新case022目标北京时间9月24日22:15启动，22:16以后拒绝，9月25日04:00:05截止，覆盖收盘后原封口等待。原case021已于21:30终止；新任务先检查其退出及认证归属，再由原全局锁和双系统标记防并行。最早14:15 UTC之后ready，第一完整区间为14:20—14:25，至收盘最多68个完整边界；初始化较晚会进一步减少，应按实际区间报告。

21:17前后的新只读测时显示较大的网络往返不确定性；此前20:43配置修正和一次质量通过不保证后续采样始终达标，不能凭这批数据断定电脑又慢了。新任务的测量改进已通过独立复核，原0.5秒门槛保持，启动前仍重测；时间质量不通过时总验收不得通过。

## 21:30真实开盘结果与针对性修复

case021于北京时间21:25:03启动，21:25:06.617921订阅完成；21:30:00.402943本地监控以 `event_timestamp_regressed` 停止。原生SDK日志中QQQ从盘前 `Pre`、最后成交13:29:59 UTC、量1,132,819，切换为 `Intraday`、最后成交13:20:00、volume与open/high/low为0，last_done=741.210。这确认了时段转换和旧报价返回；停止点在本地报价检查，不能据此宣布官方行情断流。停止前已收到60只股票112条盘中事件，其中56只58条分类为fresh；不等于147只盘中验收。

新consumer允许Quote可选OHLC为0；回退或超龄报价保留分类，不更新当前价格、K线或策略状态，不刷新参考行情的新鲜时间；新鲜同秒的合法价格变化保留，但不因此刷新成交进展。Trade同秒多笔与原时限不变。已保存真实字段回放及独立反例证据，旧case021结果不追改。其Windows退出已核验，临时认证已清理，五份生产保护状态和原认证未变。

## 自动授时和测量的不确定性

Windows原生命令读回：0x8动态授时仍生效，最近成功同步为21:18:04，闰秒指标0、层级5、最近错误0；因此不能把后续测量未过解释成自动校时根本没有运行。

改进后的只读测量对两个授时源各固定取3个样本，按有效样本的最小网络往返选择，全部6个样本留存，不按是否通过选择。内部12秒、原外部15秒期限及0.5秒门槛保持。依据[RFC5905时钟过滤](https://datatracker.ietf.org/doc/html/rfc5905#section-10)的延迟筛选思路，并非实现完整NTP客户端。

一次实际准备测量的两个保守误差上界分别为0.521904秒、0.526626秒，仍未通过0.5秒门槛。启动时按固定规则再测一次；不反复测到通过，不因能收行情而覆盖时间质量失败。11项离线测量测试和独立复核通过，只说明测量程序通过检查，不能代替真实时间质量。

## 常规时段状态隔离

另补修实时Quote的时段隔离：`Pre/Post/Overnight`保留在原始流并分类，但不进入常规时段的价格、成交量基线、K线与参考活跃状态。缺失或未知时段拒绝，未来时间和处理积压检查仍先执行。`SecurityQuote`初始快照与`PushQuote`不同：官方盘前盘后值为独立对象，生产者只传顶层常规字段，不为缺失时段默认赋予Intraday。原Trade、父聚合和策略算法未修改。

离线重放已覆盖“高成交量Pre→零成交量旧Intraday→较低累计量的新鲜Intraday”，不会再把盘前量误当盘中累计量；未知时段、未来报价及积压反例均验证。此修复仍需要未来真实开盘验证，不能把今晚盘中加入升级为开盘策略通过。[官方快照字段](https://open.longbridge.com/zh-CN/docs/quote/pull/quote)

## 最终部署与剩余验收

一次任务 `PAT-Official-Strategy-Bridge-20260924-Once` 已登记并读回Ready，下一次执行为北京时间2026-09-24 22:15，22:16后不接受启动，2026-09-25 04:00:05停止。执行上限350分钟，Interactive/Limited、IgnoreNew、无重试或错过后补跑，任务过期24小时清理以免在整段运行期间被删除。已读回接电、当前登录会话和中国标准时间。

冻结清单SHA256：`a5885fe4538b7ad5782e7a753a3b0bb14328e73773cea904f873de1b5bd0fec2`。独立复核457项文件绑定，其中425项为消费者代码、配置和解释器依赖；Windows原生核验及真实wsl.exe入口的窗外拒绝均通过，未提前消耗一次窗口或建立SDK连接。最终214项去重离线检查通过：35数据链、65监督壳、93复用模块、10独立反例、11测量程序；Windows同套复跑不重复计数。

新case的程序、配置、准备结果和私有运行证据保存在项目外 `/home/hgl/project-archives/Price-Action-Trader/20260924-strategy-bridge/`；Windows副本为 `C:\Users\hegua\AppData\Local\Temp\pat-official-isolated-20260920\case022-windows-strategy`。仓库只保存实现、测试、本文与脱敏证据摘要，不保存凭证内容。

今晚实际仍须依次验证：147×60最新已完成日日线；147只实时输入与参考流连续；所有预期完整五分钟边界；原8条策略真实判断及缺上下文原因；收盘封口、时间质量、两端退出和认证清理。以 `run-once-result.json`、两端summary及原始流交叉判断，不能只取某个True字段。

当前没有真实完整交易日通过，WSL直接SDK超时的根因仍未证明解决；本路径只让行情接收保留Windows、既有处理和策略运行在WSL。今晚错过了整条策略链的开盘区间，所以即使剩余时段全部通过，也只记本次盘中链路通过。下一步需使用已审实现安排一个开盘前至收盘的真实完整日；通过后方有恢复资格，连续三个完整日通过方称稳定。完整账户程序、正式自动启动与任何订单恢复仍需独立门槛及人工复核；不得通过清除故障或防重状态绕过。
