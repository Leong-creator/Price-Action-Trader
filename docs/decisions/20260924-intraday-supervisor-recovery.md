# 2026-09-24 盘中持续接收与看护生命周期修复

用户要求先修复持续实时获取行情的问题，今晚完成盘中测试。保留长桥官方Windows接收、147只股票、原K线/策略及全部行情时限；不等待下一开盘，不开启账户或订单。

## 已证停止位置

case022在22:15:39至22:20:11持续收到147只Quote与147只Trade；55,717条Quote、55,604批成交、234,031笔Trade。最后源回调22:20:11.747，最后有序输出22:20:11.772；Windows控制器22:20:11.791记录control_pipe_closed并关闭Job。Windows任务退出码0xC000013A，原设定运行期限5小时50分未到，Linux看护和最终收尾记录消失。消费者约5秒后才报RuntimeError，是停源后的次生问题；首个完整区间应22:20—22:25，当时尚未结束。

目前没有证据确定是谁中断原任务。Windows任务详细日志未启用，Linux未见OOM记录，系统journal权限不足，不能将相邻网络事件或操作时间当根因。

## 按已证薄弱环节修复

- 消费端只记录RuntimeError会丢掉真实停止原因，现改为明确本地安全原因码；未知异常不保存可能含私有数据的原文。
- 新增每秒原子更新的live-status.json，分别记录原始接收时刻、处理时刻、成功序号与失败尝试、累计报价/成交覆盖、完整K线与策略判断次数、首个故障。累计覆盖不代表此刻所有股票均新鲜；本地状态写入时间不是行情时间。
- 启动隔离必须覆盖实际Windows子进程，不能仅证明Linux看护仍活。首轮double-fork实验显示Linux看护可在宿主强杀后继续，但宿主退出前创建的Windows假进程仍被连带终止，因此该候选未获部署通过。
- 已通过精确宿主退出交接验证：Windows启动包装器确认本次wsl.exe真正退出，再交付与run_id和清单绑定的证明；后台看护收到证明后才创建Windows程序。单次启动、原全局锁、双系统阻断、Windows Job及已确认退出后才清凭证的规则保持，不增加自动重试。

## 旧case022受控收尾

已持原全局锁核对原Job退出、Windows原PID及匹配行情进程均不存在、Linux同用户进程可见且原看护/消费进程不存在、两份阻断标记确属同次运行。原认证文件和五份生产保护状态哈希一致；两份临时认证的身份与哈希匹配。

按原归档/清理函数保存证据并移除本次临时副本，最后清理精确匹配的阻断标记。另写recovery-completion.json，不补造缺失的原run-once-result或guardian-result。独立读回确认副本/私有根/阻断已清，原认证及五份状态未变；没有新SDK连接。

## 离线验证及真实验收边界

41项数据链组合检查通过，包括24项消费者、15项生产者和2项147规模原聚合/router集成。新增错误与实时状态模块的独立26项复跑不重复累计。另完整离线重放case022真实记录117,150条，234,031笔成交与8,820条日线均处理；上游关闭后明确得到market_data_heartbeat_deadline_exceeded，验证原因码而非实连通过。

新窗口的核心验收是147只实际行情持续进入处理、原时限下无断流/处理积压、每个预期完整区间都有147条分类记录、实际策略完成判断、最终退出与临时认证收尾完整。短窗不能证明完整交易日或三日稳定；时间质量单独记录，不因收流正常覆盖未通过项。没有合格信号允许零订单。

部署清单已冻结为`fc1e34a56e8a94c043830359c184069ebb11b0f6a9113674c3f6ce953aae1384`。最终75项外壳与41项数据链共116项去重离线检查通过；实际当前Windows包装器经UNC写出宿主退出证明、自身退出后，后建Windows假进程仍152次心跳持续30.703秒，随后受控EOF退出0。先前失败对照保持，不据此倒推原case022中断者已确定。

Windows一次任务`PAT-Official-Intraday-Recovery-20260924-Once`已登记读回Ready，2026-09-24北京时间23:15开始、23:16后拒绝新启动，次日00:00:05截止。任务入口为原生Python包装器，上限2分钟只负责交接；行情运行由独立daemon、原全局锁与Windows Job按2705秒窗口/2715秒上限监督。任务入口exit0只表示交接完成，不能代表测试通过。case023已运行至期限并完成收尾，结果如下。

两端固定文件、425消费者依赖、旧case022补充收尾记录和凭证有效期已核验。真实原生入口窗外试跑被拒绝且未消耗reservation、未创建临时认证或SDK。生产原状态与订单保持停止；交流电自动睡眠为0，当前保持接电。程序和运行证据位于项目外`/home/hgl/project-archives/Price-Action-Trader/20260924-intraday-recovery/`。

本次新增终止信号记录和有界收尾；SIGKILL、WSL整体关闭、主机断电仍不可捕捉，未知退出仍保留阻断，不能声称抵抗所有外部中断。没有新增跨系统0.5秒proof门槛；原时间质量检查在交接后独立执行。

## 本轮真实盘中结果（9月25日00:00后核对）

北京时间9月24日23:15启动窗口，实际首次回调23:15:40.258，最后回调次日00:00:04.998，回调覆盖2,664.740秒（44分24.740秒）。00:00:05按期限正常结束，guardian判定completed，生产者、消费者和Windows控制器退出0；daemon退出4表示原时间质量未通过，没有终止信号或再次中断。当前已停止，不是仍在持续接收。

| 检查 | 实测结果 |
| --- | --- |
| 接收覆盖 | Quote147/147、Trade147/147；累计覆盖不等于每只股票每秒都有新成交 |
| 原始流 | 1,061,591条严格连续序号，同一run_id且末尾end完整；503,675条Quote、503,679批Trade，共1,840,919笔成交 |
| 实际消费 | 1,840,693笔成交；窗口截止后才处理的134条尾部事件不进入已完成窗口，不能把原始总数当消费总数 |
| 完整五分钟边界 | 23:25、30、35、40、45、50、55及00:00，共8/8；每边界147条，总1,176条分类记录 |
| 策略可用K线 | 1,175条；BLK 23:40—23:45延续类记录未用于开仓输入，分类记录未缺失。由边界门禁及输入差异联合确认，未单独落盘的blocked子类型不强行确定 |
| 原策略判断 | 8次，均在边界后约2秒完成；日线背景147×60=8,820条，通过原新鲜度检查 |
| 策略信号 | PLTR/FTD在23:35、23:55、00:00分别产生1条本次观察信号，3个不同本日live_event_id；同一策略同一标的的连续观察，非3笔订单或3个独立交易机会 |
| 退出及保护 | child、consumer、Windows控制器退出0，Job ActiveProcesses=0；临时认证清理，原认证和5份保护状态不变，无账户或订单访问 |
| 验收分层 | bounded_pipeline_passed=true、reception_window_passed=true；clock quality=false、diagnostic_window_passed=false，production/full_session/strategy_full_acceptance均false |

结束后一次扫描已封存原始流，SPY/QQQ原始回调最大间隔约1.503/0.617秒；Quote统计包含重复报价，不能称独立fresh-only结果。最大成交时间年龄约2.426秒、回调至有序输出约0.095秒，均仅为原始记录时钟下的度量，不当作独立UTC认证或纯网络延迟。窗口没有触发原行情静默、处理积压或缺失边界故障；没有要求证明SDK内部从未重连。

校时未通过的具体证据：启动选中的两个授时样本，其abs(offset)+RTT/2约0.785秒与0.616秒，均大于原0.5秒界限。样本有效，因此按既定只读规则完成接收；测量不足以证明所需时间精度，不能据此断言长桥断流，也没有放宽门槛。该结果不能以窗口结束后的新测量覆盖。

本次主要问题的结论：已修复本地运行交接薄弱环节，并实证官方Windows行情能持续进入WSL原K线与策略。旧case022具体中断触发者仍未知；WSL直接连接官方SDK仍未取得通过证据。短窗中部分五分钟策略需要20根背景而仅有7—8根，或缺真实开盘上下文，故不声称所有策略已完成业务验收。下一步集中解决可靠时间质量证明，再在已验单连接路径进行147×78完整交易日及后续三日稳定性验证；不需为一般盘中连通性验证等待下一开盘，不用盘后补录冒充实时整场。

终态原始证据、独立扫描和审查位于上述项目外归档：consumer-output/summary.json、guardian-result.json、run-once-result.json、daemon-result.json、finished-wire-review.json、review/。对应可提交摘要见docs/evidence/20260924-intraday-supervisor-recovery.json。大流、认证与运行日志不进入Git。

风险标记：启动及凭证生命周期改动涉及高风险边界，正式生产启用前需人工复核；本轮只读盘中窗口已经完成，不包含账户或订单恢复。

独立终验另确认459项冻结绑定一致、两系统对应进程均已消失、全局连接锁空闲、两处临时阻断与认证私有根已清理。Windows原始日志的已归档副本仍保留，待单独核验后去重；该项不是临时凭证残留，不作为旧系统运行入口。
