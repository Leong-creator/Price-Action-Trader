# 2026-09-24 盘中持续接收与看护生命周期修复

用户要求先修复持续实时获取行情的问题，今晚完成盘中测试。保留长桥官方Windows接收、147只股票、原K线/策略及全部行情时限；不等待下一开盘，不开启账户或订单。

## 已证停止位置

case022在22:15:39至22:20:11持续收到147只Quote与147只Trade；55,717条Quote、55,604批成交、234,031笔Trade。最后源回调22:20:11.747，最后有序输出22:20:11.772；Windows控制器22:20:11.791记录control_pipe_closed并关闭Job。Windows任务退出码0xC000013A，原设定运行期限5小时50分未到，Linux看护和最终收尾记录消失。消费者约5秒后才报RuntimeError，是停源后的次生问题；首个完整区间应22:20—22:25，当时尚未结束。

目前没有证据确定是谁中断原任务。Windows任务详细日志未启用，Linux未见OOM记录，系统journal权限不足，不能将相邻网络事件或操作时间当根因。

## 按已证薄弱环节修复

- 消费端只记录RuntimeError会丢掉真实停止原因，现改为明确本地安全原因码；未知异常不保存可能含私有数据的原文。
- 新增每秒原子更新的live-status.json，分别记录原始接收时刻、处理时刻、成功序号与失败尝试、累计报价/成交覆盖、完整K线与策略判断次数、首个故障。累计覆盖不代表此刻所有股票均新鲜；本地状态写入时间不是行情时间。
- 启动隔离必须覆盖实际Windows子进程，不能仅证明Linux看护仍活。首轮double-fork实验显示Linux看护可在宿主强杀后继续，但宿主退出前创建的Windows假进程仍被连带终止，因此该候选未获部署通过。
- 已通过精确宿主退出交接验证：Windows启动包装器确认本次wsl.exe真正退出，再交付与run_id和清单绑定的证明；后台看护收到证明后才创建Windows程序。单次启动、原全局锁、双系统阻断、Windows Job及已确认退出后才清凭证的规则保持，不增加自动重试。

## 旧case022受控收尾

已持原全局锁核对原Job退出、Windows原PID及匹配行情进程均不存在、Linux同用户进程可见且原看护/消费进程不存在、两份阻断标记确属同次运行。原认证文件和五份生产保护状态哈希一致；两份临时认证的身份与哈希匹配。

按原归档/清理函数保存证据并移除本次临时副本，最后清理精确匹配的阻断标记。另写recovery-completion.json，不补造缺失的原run-once-result或guardian-result。独立读回确认副本/私有根/阻断已清，原认证及五份状态未变；没有新SDK连接。

## 离线验证及真实验收边界

41项数据链组合检查通过，包括24项消费者、15项生产者和2项147规模原聚合/router集成。新增错误与实时状态模块的独立26项复跑不重复累计。另完整离线重放case022真实记录117,150条，234,031笔成交与8,820条日线均处理；上游关闭后明确得到market_data_heartbeat_deadline_exceeded，验证原因码而非实连通过。

新窗口的核心验收是147只实际行情持续进入处理、原时限下无断流/处理积压、每个预期完整区间都有147条分类记录、实际策略完成判断、最终退出与临时认证收尾完整。短窗不能证明完整交易日或三日稳定；时间质量单独记录，不因收流正常覆盖未通过项。没有合格信号允许零订单。

部署清单已冻结为`fc1e34a56e8a94c043830359c184069ebb11b0f6a9113674c3f6ce953aae1384`。最终75项外壳与41项数据链共116项去重离线检查通过；实际当前Windows包装器经UNC写出宿主退出证明、自身退出后，后建Windows假进程仍152次心跳持续30.703秒，随后受控EOF退出0。先前失败对照保持，不据此倒推原case022中断者已确定。

Windows一次任务`PAT-Official-Intraday-Recovery-20260924-Once`已登记读回Ready，2026-09-24北京时间23:15开始、23:16后拒绝新启动，次日00:00:05截止。任务入口为原生Python包装器，上限2分钟只负责交接；行情运行由独立daemon、原全局锁与Windows Job按2705秒窗口/2715秒上限监督。任务入口exit0只表示交接完成，不能代表测试通过。新case023尚未取得真实窗口结果。

两端固定文件、425消费者依赖、旧case022补充收尾记录和凭证有效期已核验。真实原生入口窗外试跑被拒绝且未消耗reservation、未创建临时认证或SDK。生产原状态与订单保持停止；交流电自动睡眠为0，当前保持接电。程序和运行证据位于项目外`/home/hgl/project-archives/Price-Action-Trader/20260924-intraday-recovery/`。

本次新增终止信号记录和有界收尾；SIGKILL、WSL整体关闭、主机断电仍不可捕捉，未知退出仍保留阻断，不能声称抵抗所有外部中断。没有新增跨系统0.5秒proof门槛；原时间质量检查在交接后独立执行。
