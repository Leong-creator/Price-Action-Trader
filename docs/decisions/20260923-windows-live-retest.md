# 2026-09-23 北京时间 Windows 盘中持续接收验证

用户要求现在实连继续测试。case020已完整跑完预定01:12—01:17诊断窗口，接收层通过；订阅后实际观察约293.6秒，并非完整五分钟都处于订阅状态。时间质量门槛未通过，所以外壳总结果仍为observed_not_passed。不能把接收通过扩展成正式策略运行、整场完整或整体恢复。

## 实际结果

- 美股交易日是UTC2026-09-22，北京时间已为9月23日；真实开盘13:30UTC、收盘20:00UTC没有伪改。Windows官方SDK5.0.0、原147只一次Quote/Trade和已审首条旧报价修复保持，实际配置和冻结文件经35项独立绑定核验，外壳定向65项离线验证通过。
- 17:12:06.405972 UTC订阅成功，至17:17:00按期结束。控制器运行294.749秒，正常退出；窗口内165,908条事件进入处理，147只都有新鲜事件。
- 独立逐条重算：44,655条Quote及121,253条Trade，两类均覆盖147只、均有新鲜事件。53,805条判为新鲜，54条旧首报价仅留证，112,048条同时间记录及1条乱序Trade不续静默；没有窗口截止后的补记。实际首末接收相距293.538218秒。54条旧首报价均早于订阅成功时刻，修复在本次真实记录中生效。
- SPY报价/成交参考流最大间隔分别4.786/4.799秒，QQQ为2.405/2.406秒，均小于原30秒限制。尾部各流距离窗口截止约0.318—0.319秒；没有靠窗口结束后新数据补齐末段。
- 最大队列647项，最大处理等待120.241ms，小于原2秒限制；结束队列0。监控读取错误0、原子替换错误0，没有再触发前两次测试中的立即停止。
- 全局行情锁已释放、无残留连接/隔离标志，Windows Job活动进程0、临时认证清理完毕，原认证和5份正式保护状态校验一致。没有账户、下单或正式系统重启。

这是147只实际新鲜事件覆盖以及SPY/QQQ四条参考流的连续性检查，不是其余145只双流每笔完整性证明；仍须逐股票五分钟K线、策略输入和完整日验收。K线与策略本轮均未测试，完整日0。

## 时间质量单列

本次微软授时样本offset约−71.2ms、RTT640.9ms，保守界约391.7ms，符合本地0.5秒前提；另一授时样本offset约+159.5ms、RTT1044.5ms，保守界约681.7ms，未过。后者仅往返不确定性的一半就超过0.5秒，不能据此声称主机一定慢或快超过0.5秒，也不能说是行情断流。

沿用已审盘中诊断方式：至少一个有效授时样本允许只读接收，双源时间证据不足使整体验收保持False。采样summary的diagnostic_window_passed=True只含其局部行情检查；外壳最终reception_window_passed=True、diagnostic_window_passed=False且received_for_diagnosis_only=True才是总口径。0.5秒是本地诊断时间证据门槛，不是长桥官方接入要求。本次不事后调整门槛或用较好样本覆盖不合格样本。

## 上次定时为何没有结果

case018在9月22日21:25触发，但连接前两份授时offset约+0.6245/+0.6543秒，保守界约0.9938/1.0179秒，按该任务严格前提拒绝启动。只有refusal及launch-clock记录，没有run-once-started、临时认证ownership或Windows SDK产物；未创建行情SDK，不能归为长桥测试失败。该拒绝记录外壳只写了RuntimeError，具体clock_alignment_unproven由当时保存的样本和冻结校验代码确定。

## 当前结论与后续

Windows官方路径已取得一次接近五分钟的持续收流证据，上一轮首条旧报价和监控文件问题在本窗口未再次导致停止。仍不宣称所有历史故障已有同一根因或Windows长期稳定。

下一步分别处理时间证据的可靠获取，以及既有K线/策略链路的真实接入验证；保留147股票、策略和风险规则，一个完整交易日、连续三个完整日的标准保持。WSL此前初始化请求超时尚未解决，本轮没有盲目追加相同条件WSL连接；没有自动安排新任务或恢复交易。

原始数据位于 `/home/hgl/project-archives/Price-Action-Trader/20260923-windows-intraday/`，见[安全汇总](../evidence/20260923-windows-live-retest.json)。本轮新的真实行情连接共一次，旧case018/019记录保持。
