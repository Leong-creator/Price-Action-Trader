# 2026-09-24 晚间开盘测试安排与准备核验

今晚已登记一次Windows官方行情接收测试：北京时间21:25启动，覆盖21:30开盘，到21:45截止，21:26后拒绝启动。当前任务实际读回Ready/NextRun为今晚，完整Windows→WSL看护命令形状已试跑，提前调用正确拒绝且未连接SDK、未复制认证、未消耗运行机会。此前没有今晚有效任务，本轮新建，不沿用已过期case018。

## 已准备内容

- case021复用case020已实测的Windows官方5.0.0环境、147只同步一次Quote/Trade、原官方采样源及首条旧报价修复；只适配本次日期、20分钟窗口与开盘订阅截止。实际run-spec验证9月24日、13:30UTC真实开盘、20:00UTC收盘；订阅资格截止恢复为开盘，而非最晚启动时刻。
- 官方解释器/SDK实际文件、helper/sample、配置、认证校验清单及5份正式保护状态逐项校验。认证期限覆盖窗口结束后一小时；目标临时认证目录不存在，无其他行情进程、锁占用或跨系统隔离标记。
- 65项外壳/时钟/看护定向测试通过，独立审查实际日期配置、边界与部署绑定。未新建SDK连接验证，以免占用今晚连接或混淆运行机会。
- 任务 `PAT-Official-Quote-Continuity-20260924-Once`，manifest SHA256 `a3b59cd6bbedc5f1932cd3ccbfc4223c8ab0b4710ea5f790eeed16bff98ba70c`。用户LEONG\hegua、Interactive有限权限、IgnoreNew、无自动重试/补跑，25分钟外部任务上限；无idle/network/battery附加阻止条件，WakeToRun=True。真实行情SDK运行在Windows；WSL仅负责看护和连接锁，不代表WSL行情已恢复。
- 当前Windows用户已登录且存在桌面会话，主机接交流电，交流电睡眠与休眠均为0（不自动进入），唤醒定时器允许。准备时条件不是未来保证；测试期间须保持通电、联网和登录，不关机、不手动睡眠。

## 当前时钟待办与如实验收

准备检查WindowsTime显示未同步（LI3/stratum0），最后成功同步记录为9月24日13:31:45。两份只读NTP估计offset约+0.945/+0.988秒，指向主机偏慢；用户已被请求在Windows设置中立即同步，尚未收到完成确认。本轮没有冒充管理员修复成功，也未更改系统时间/代理/DNS。

今晚继承case020已经使用的接收/时间质量分离规则：启动前记录两源原始测量，至少一个合法样本才允许只读接收；双源保守时间界不满足0.5秒则整体标记未通过，但继续取得行情接收证据。该0.5秒是本地验证前提，不是长桥官方要求。原行情未来时间、两秒新鲜度/队列、30秒参考流静默、五秒监控期限不变。没有合法授时样本、重复进程、文件变化或认证不合格仍会拒绝；不能承诺无条件启动。

校时完成后需再测确认；启动当时会重新测量，准备样本不替代当时判断。不能靠改门槛、回填旧信号或删除故障记录制造通过。

## 今晚能验证什么

今晚是开盘行情持续接收测试：检查订阅成功时刻、147只实际新鲜事件、SPY/QQQ报价和成交四条流、开盘前后最大间隔、处理积压、监控与退出状态。通过后仍不是完整日验收。

只读接口复核发现，当前Windows continuity采样仅保存身份与时间信息，没有复制价格/成交量，所以不能把此前165908条诊断事件当作OHLCV数据使用。项目现有pipeline会自行启动Linux SDK worker，并依赖Linux进程机制；不存在可直接部署的Windows→WSL完整行情桥。今晚不宣称K线/策略已经接通。

下一实施项是Windows唯一行情上下文输出完整精度的Quote/Trade及合格日线上下文，WSL仅消费、复用现有五分钟聚合和原策略router，不再建立第二行情连接；先验证跨平台时间、有界队列、丢序/重复、退出与价格精度，再做真实完整边界和策略判断。最终仍需147只×78边界=11466条分类清晰记录，一个完整日取得恢复资格，连续三日才称稳定；生产和交易未恢复。

## 结果读取与收尾

执行后以新archive的run-once-result.json及case021原始events/summary/run_result联合判定；局部接收通过不能覆盖外壳时间质量不通过。若拒绝或异常，保留具体阶段证据，不自动重试或补跑。独立任务不依赖本次对话保持打开；目前仅确认准备与登记完成，未到窗口、没有真实结果。

文件和安全证据在 `/home/hgl/project-archives/Price-Action-Trader/20260924-windows-opening/`，见[准备证据](../evidence/20260924-opening-readiness.json)。旧case013—020证据不覆盖。
