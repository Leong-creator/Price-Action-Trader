# Windows官方行情：一次开盘连续接收测试

2026-09-21。用户授权按环境对比方案执行并测试。本轮先实施已成功Windows路径的连续接收诊断，正式入口切换与策略接回仍以它的结果为前提。

## 本次验证对象

- 日期：美股2026-09-21，计划北京时间21:25至21:45（UTC13:25至13:45）；覆盖开盘前5分钟、开盘后15分钟。13:26UTC之后拒绝启动，不错过后补跑。
- [NYSE官方交易日历及时间](https://www.nyse.com/trade/hours-calendars)列出的该日为正常交易日，正常开盘09:30纽约时间，即北京时间21:30。此20分钟诊断不代替完整交易日。
- 保留已核验Windows CPython3.14.3、官方Longbridge5.0.0、默认Config、同步QuoteContext与147只一次Quote+Trade。官方[订阅接口](https://open.longbridge.com/docs/quote/subscribe/subscribe)允许该规模单次提交，不增加分批。
- 相比case012，公开API调用方式保持，新增有界持续观察与脱敏事件记录；这是新诊断程序，不能写成与旧sleep(10)样例字节一致。没有项目运行模块、日线初始化、策略、账户或订单调用。

## 实施与判定

工具位于项目外 `/home/hgl/project-archives/Price-Action-Trader/20260921-windows-continuity/`，Windows执行目录为既有隔离根下 `case013-windows-opening-continuity`。旧case012记录保留。部署清单绑定新程序、helper、静态生产阈值副本、测试窗口、运行编号和官方环境文件；不使用旧环境或生产启动脚本。

测试回调只复制标的、接收时间、行情时间等白名单字段到有界队列，外层记录每只股票Quote/Trade的接收数、新鲜数据与重复/旧时间分类。使用现有30秒静默数值，分别检查SPY/QQQ的Quote、Trade四条参考流；首段、更新之间和窗口末尾都检查，报价更新不能掩盖逐笔停止。沿用2秒交付时限、45秒初始化/订阅期限和5秒工作进程心跳期限。重复时间不延长存活资格；2秒以内的乱序Trade仅分类，不误报或续命，过旧数据仍失败。无Trade推送不能直接解释为交易所没有成交。

官方5.0.0 [时间转换源码](https://raw.githubusercontent.com/longbridge/openapi/v5.0.0/python/src/time.rs)将回调时间转换为本机时区的无时区Python datetime。诊断按该转换规则还原为UTC，显式记录解释方式；不把Windows本机时间直接贴UTC标签，也不修改SDK。这是行情时间字段的兼容处理，不是已证明的历史订阅超时原因。

外层Linux guardian持唯一行情锁，Windows控制器以原子Job归属、父管道EOF与绝对截止时间约束子进程。中断、超时或错误保留原因；未确认子进程退出和Job进程数0时保留隔离标记。故障不会触发重试、换源、第二连接或交易恢复。

实际触发时才核验并临时复制认证；已有Windows认证不得覆盖。确认退出后，归档核验本次副本并逐项清理。原凭证和5份正式故障/订单防重状态保持；本轮认证有效期已只读核查可覆盖测试结束及余量，触发时再次检查，不提前跨日放置临时凭证。

`exit0`或Job归零只表示程序退出得到确认；行情通过必须另外检查 `summary.json` 的完整观察窗、147只新鲜盘中数据覆盖、四条参考流独立连续性及无故障。缺少逐笔或覆盖不足即使进程正常退出也不得写通过。其他145只的两种流逐项全程完整性不由本次参考监控证明。所有产物均保持 `production_acceptance=false`，K线与策略验收未执行。

## 验证记录与触发状态

最终冻结副本共86项离线测试通过：Windows Job10项、Windows阶段看护14项、Windows行情健康/fake SDK24项、Linux guardian18项、Linux一次执行/假凭证20项。独立审查及最后四流/乱序规则差异复核通过。官方SDK的5个package文件逐字节与5.0.0 wheel一致，基础Python及DLL与上次已核验来源相同；未导入真实SDK或实连。

Linux/Windows两侧清单校验通过；按计划命令形状实际启动非交互WSL再调用runner，正确返回窗口外拒绝、SDK未启动，且未消耗未来一次执行。当前工具与该命令形状最终Windows子进程的HTTP/HTTPS代理变量值相同；这只是准备时核查，不保证未来代理进程仍存活。

一次任务 **`PAT-Official-Quote-Continuity-20260921-Once` 已注册并读回验证，状态Ready，下次运行2026-09-21 21:25 +08:00**。仅当前用户Interactive/Limited权限、无密码/提权、无重试、无错过补跑、重复触发忽略；诊断自身于21:45结束，外层25分钟仅作异常退出上限。触发窗口21:26截止，任务到期1小时后由系统清理。电脑须开机且该用户保持登录；未满足条件不能当作已运行或行情失败。

固定manifest SHA256为 `45fdd6cfbaa7dd4933fb27a36a485157d382bf87ca2cfc1779e639f1f4313943`，任务动作绑定该值；源文件、配置和环境变化会拒绝执行。本次没有提前复制认证，行情锁空闲，两处活动标记不存在，5份正式保护状态哈希不变。可读证据见[准备及注册结果](../evidence/20260921-windows-opening-continuity-prepared.json)。

人工复核标记：临时凭证管理与跨系统进程退出属于高风险边界。本次诊断已获用户明确授权并完成独立代码复核；该授权不延伸到账户连接、订单或正式系统迁移验收。

**截至准备完成，真实case013尚未启动，完整交易日通过仍为0。** 测试完成时runner会自动将记录归档到上述Linux目录并核验；可能含认证信息的SDK日志仅在私有目录保存。Windows私有证据副本会明确标记待后续逐项清理，临时认证副本则按已审退出流程自动清除，二者不得混称“全部清理完成”。

## 后续处理

执行后首先读取项目外的 `run-once-result.json`、guardian退出记录和case013事件/汇总，区别未能启动、订阅失败、持续接收失败和观察窗通过。任何失败都先给出具体阶段及证据，不重开同条件重试。

如果该窗口通过，再决定唯一正式运行位置及最小迁移，接回原队列、五分钟处理和策略，验证完整交易日。当前没有部署候选、解除故障锁、恢复生产自动启动或下单。若仍需隔离平台原因，再按环境对比报告准备同Python/SDK、相邻时段的有限串行对照，不把一次Windows成功归因于WSL。

本轮另确认一个接回时必须处理的既有缺陷：主线和候选的 `sdk_plain_value` 保留SDK无时区datetime（`scripts/m15_longbridge_sdk_runtime_lib.py:2025`）；Quote回调经入队、`seed_quote`、pending batch及进程队列后，原payload的timestamp仍未归一。主线父进程 `record_live_quote_session_state`（`scripts/run_m15_longbridge_sdk_runtime.py:449`）将其交给 `strict_event_datetime`，后者第813行拒绝无时区值，导致保留旧报价状态。候选对应第463、827行同样如此。`seed_quote`只转换自身局部时间，不改payload；转换为UTC的received_at不是行情timestamp。后续应在SDK专用归一入口转为aware UTC，保留通用严格时间检查。此次独立诊断已正确处理该官方时间类型，但没有改正式代码；该缺陷发生在收到回调后，不能解释此前零回调的订阅初始化超时，也未证明9月8日现场就是因此断流。
