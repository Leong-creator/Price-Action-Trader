# Windows原生隔离环境：官方147只行情样例当次成功

2026-09-20。用户要求建立全新临时隔离环境，核查长桥官方接入。已建立Windows原生环境并完成一次只读测试：**147只股票全部收到报价首推，进程正常退出。** 这证明该账户和官方SDK在这个环境中当次可以工作，不能据此前Linux失败宣称长桥数据源完全不可用；同时不能从一次成功证明Windows已修复长期故障。

## 隔离与来源

- 使用签名有效的Python Software Foundation CPython 3.14.3 AMD64，从Windows本机可信解释器创建全新venv；不进入WSL运行SDK，不覆盖项目或Linux环境。
- 从PyPI取得官方 `longbridge-5.0.0-cp314-cp314-win_amd64.whl`，以pip离线 `--no-index --no-deps` 安装。wheel SHA256 `258c999121b5e844362f66fb2931eacdc9c3b48ea084c27c519425df41c09e2a`；5个package文件与wheel逐字节相同，实际隔离加载pyd SHA256 `b2973af71a96ee2f85a90eadb1adfd916a2ac013eb2a7c6f5d0d8e122ad935ff`。
- 使用前次相同的公开API样例原字节，SHA256 `22b624aafead9302158d16dcfa5cb31cde32b081a44de9b5a7eb9639382243e5`。原147只一次Quote+Trade、默认Config、普通回调与sleep(10)保持；没有项目scripts、队列、K线、策略、账户或订单客户端。
- 同一份有效认证缓存临时复制到原先不存在的官方Windows默认位置；新目录DACL仅当前用户及SYSTEM。没有在代码、对话或公开证据写入凭证，没有修改Linux原凭证；测试结束后副本归档并逐项移除。
- 不配置地域覆盖，不注入DNS/端点，不更改系统代理。Windows当前进程代理环境保留；实际SDK选择 `.cn`。与前次比较同时改变了操作系统、Python版本、原生wheel及进程环境；仍共用物理机器和未独立控制的网络，不能将成功归因于唯一变量。

官方依据：[接入与认证](https://open.longbridge.com/docs/getting-started)、[5.0.0安装文件](https://pypi.org/pypi/longbridge/5.0.0/json)、[订阅接口](https://open.longbridge.com/docs/quote/subscribe/subscribe)。官方允许一次500只，无需为147只增加分批。

## 一次性运行与退出

外部Windows控制器用原子JobList纳管、KILL_ON_JOB_CLOSE、60秒期限与控制管道EOF看护管理本次子进程及子孙。Linux guardian持现有行情锁和持久活动标记；只有本次新结果、源码哈希、控制器退出码、子进程退出及Job剩余进程数0均吻合才解除标记。flock不具备跨Windows内核的原子生命周期约束，异常未确认退出时活动标记保留，禁止继续启动。

执行前完成10项Windows原生离线测试、跨WSL/Windows的dummy EOF回收测试及guardian旧结果/错误退出码拒绝测试，独立审查通过。所有测试均未调用SDK。实测仅case012一次，无自动重试。

## 结果与证据强度

| 项目 | 已核实结果 |
|---|---|
| 官方接入 | 默认选择 `openapi-quote.longbridge.cn`，认证和行情档案查询返回 |
| 订阅调用 | 成功返回；SubscribeRequest日志15:47:42.905741Z至成功unit响应15:47:42.963463Z，相隔57.722毫秒，仅为日志时序 |
| 报价实际交付 | stdout有147条可完整解析的PushQuote，唯一147只与源股票集合完全一致；native日志147条Quote及集合也一致 |
| 逐笔成交 | 0条；周日休市，未验证持续Trade交付 |
| 标准错误 | 0字节 |
| 总进程时间 | 11.082秒，包含样例sleep(10)，不能当作订阅耗时 |
| 退出 | 样例exit0、控制器exit0、Job剩余进程0、两系统活动标记解除、行情锁空闲 |
| 生产影响 | 五份故障/启动/订单防重/epoch状态哈希不变，原始凭证不变；正式候选未部署，未解锁、未恢复自动启动或订单 |

这是实际147只Quote首推证据，强于仅查看SDK本地 `subscriptions()` 列表。成功unit响应并未逐项列出所有Quote/Trade权限，休市Trade=0不签发逐笔或完整日通过。此前Linux也有成功样例，Windows本次成功不是平台因果对照完成，也不能用于判断9月8日开盘断流已解决。

私有记录保存在项目外 `20260920-isolated-windows/`：官方来源、安装校验、输入清单、控制器/测试、case012原始日志及摘要、前后保护状态校验、凭证副本清理清单。原始记录不外发。机器可读脱敏事实见[结果JSON](../evidence/20260920-isolated-windows-result.json)。

## 清理与下一步

测试已停止。2份临时Windows认证文件及2个新建私有认证根目录已归档、核验并删除；Linux原件保持。保留一个停止的Windows诊断venv作为下一交易时段的待验环境，无启动项、定时任务或交易入口；再次使用前须重验来源、连接资格并安全配置认证。它不是部署环境或旧系统回退入口。

Windows端case012的7份重复证据文件也已逐项验证与Linux私有归档一致后移除，包括可能含认证信息的SDK原始日志；后续复核读取私有归档，不在临时环境中继续保留这些副本。

当前累计12项独立样例、3项项目探针，真实完整交易日通过0天。优先在这个已完成初始化的环境中验证真实开盘和连续Quote/Trade，再决定正式运行位置及最小接入改动；不因本次首推成功直接迁移、修改策略或恢复下单。一个正常完整交易日147只×78边界、11,466条分类记录通过作为恢复资格依据，连续三个完整日后才称稳定。数据时效、缺口停止、无成交延续K线不能开仓和不补旧信号规则保持。
