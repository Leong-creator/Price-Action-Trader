# Windows后WSL：当晚测试前巡查

用户确认先Windows、再WSL，并要求提前巡查、修补和测试。本轮从干净已推送main 965b782建立独立分支。窗口保持北京时间2026-09-21 Windows21:25—21:45、WSL21:50—22:20；不增加行情连接、重试、交易或系统网络改动。

## 本轮发现及修补范围

| 检查项 | 确认的问题 | 处理 |
|---|---|---|
| Windows队列与断流判断 | 四条参考流上次在第1秒，新事件30.5秒已入队；31秒先检查会误报30秒断流，实际到达间隔29.5秒、积压0.5秒均合格 | 有限FIFO批次处理后按该批次检查时刻判断；保留实际2秒积压及四流30秒标准 |
| Windows窗口封口 | 截止后首条数据可补足覆盖；正常落盘延迟又可能被计入窗口末尾静默 | 截止后接收只留证，不计覆盖或存活；静默只计算到窗口终点，保留真实记录时间 |
| WSL绝对截止 | Linux单调时钟不含休眠时间，原程序只在启动时换算截止，休眠恢复后可能超窗运行 | 内外层同时检查UTC与单调期限；逐消息检查，时钟中断否决观察通过，外层最多每秒复查 |
| WSL退出错误 | worker捕获错误后可能正常exit0，旧收尾忽略error消息，可凭先前计数误报观察通过 | 退出后也检查有界尾队列，保留错误；首个主故障不被覆盖，退出K线不回填策略 |
| 日线输入门槛 | 诊断仅计总行数，未复用正式程序逐股票去重日期/最新已完成交易日检查 | 复用现有daily_context_is_current，重复、过期、未完成日线不得进入router或报输入齐全 |

这些是可离线复现的本地诊断/验收缺陷，不是已证明的长桥订阅超时或9月8日断流根因。没有改SDK原包、官方调用、147股票、K线算法、策略条件或风险阈值。

## 全链路及主机检查

已核对两项指定任务、实际运行入口、跨系统退出证据、Job/进程组、连接锁、临时认证清理和原认证有效期、官方文件来源、五份故障/防重状态、旧自动启动、时钟/时区、电源及空间。

实查仅发现今晚两项项目一次任务，未发现旧项目登录启动、WSL cron/systemd/profile/boot配置或相关行情进程。交流供电，交流自动睡眠/休眠关闭，唤醒定时器启用；Windows约152GB、Linux约955GB可用。原认证哈希未变，有效期覆盖两场结束及一小时余量；正式保护状态匹配、连接锁空闲。检查只是当前快照，未来触发仍由已审入口重新核验文件、认证、窗口、锁和前序退出。

Windows时间服务显示未同步（Leap=3、Stratum=0），不能写成NTP正常。Windows与WSL时间一致；匿名HTTPS服务器Date与请求中点近似差0.286秒，RTT0.800秒，目前未见明显漂移，但这不是精密授时证明。未改系统时间或强制同步；时间服务状态保留观察，不归责为已证明的SDK失败原因。

生产规模额外离线穿透：147只原生产名单、8820条合成日线，经真实worker/IPC/父报价状态/原五分钟处理/原router，形成441条5分钟记录、3个边界、3次策略判断，8条原可执行合同加载无异常。最终日线门槛版本复验总耗时0.539秒，仅说明这次离线处理无明显规模瓶颈，不是实机行情延迟指标。所有SDK输入为合成，账户和网络禁止，输出在项目外；未满足日内历史的策略如实阻断。

## 验证、部署与结果口径

改动必须在独立复核及针对性测试通过后冻结。Windows source/health发布至原隔离case，重新核验两侧文件及manifest；Linux更新实际代码/runner哈希和前序Windows清单哈希；两项任务只更新绑定的清单校验值，原窗口、身份权限、无重试/无补跑和串行限制保持。旧源码、清单和任务XML已在项目外逐项归档；无旧运行退路。

Linux的bounded_pipeline_observed仅表示曾观测到实际成交、合格K线和原router判断，不证明整窗Trade持续。前一分钟有Trade、后续只有Quote不能据累计数宣布连续通过；实测后须检查原始阶段和事件时间。外层pipeline_window_passed=false，完整日及策略全部验收仍未通过。Windows四参考流的连续性口径独立保留，不混用。

正式故障不解除，账户程序及生产自动启动不恢复。本轮新增真实SDK连接0，真实完整交易日0。窗口后优先核对未启动/订阅/回调/队列/边界/日线/策略/退出各阶段，不能用合成数据、结束后记录、正常进程退出或零订单冒充行情修复。

私有证据目录：`/home/hgl/project-archives/Price-Action-Trader/20260921-pretest-audit/`；主机只读证据：`/home/hgl/project-archives/Price-Action-Trader/20260921-operational-preflight-174635/`。最终测试与任务校验结果在收尾记录。

## 收尾结果

独立复核无未决阻断；按测试ID去重121项通过（仓库相关41、Linux外壳19、Windows采样/健康37、Windows外壳24），保留各冻结阶段日志，不把重复跑的专项相加。最终日线门槛下147只规模复验通过；治理编译、敏感信息/大文件扫描与差异检查通过。两入口以新清单提前试跑均正确exit4、sdk_started=false，未消耗当晚执行，也未复制认证或建立连接。

两侧文件、官方原包、五份保护状态与认证覆盖期再次核验通过。两项任务已实际更新并读回Ready，只变Action中的清单校验值；触发窗口、Settings、Principals逐项保持，仍有限权限、零重试、零错过补跑、重复触发忽略。

- Windows当前manifest SHA256：`efb3d5bc0faaad2e5055e82c9c947946b4f97ef0cde657e2882561e7c59be22b`。
- Linux当前manifest SHA256：`86b317744db4489d7b88072f9c4abb397363470fdbe2b06f127102f025b36bdf`，绑定上述前序Windows清单。
- Linux CLI冻结SHA256：`5b97b92ee787c7f3d536e232199407583a05613d2dc16ab77858401235f466e5`。

[当前准备证据](../evidence/20260921-pretest-audit.json)取代较早准备记录中旧清单哈希的“当前”含义；旧材料保留作历史，不据其重新部署。原Windows/Linux任务登记记录也已同步最新读回。修复合入最新干净已推送main后更新来源部署清单，本轮任务分支随即收尾，保留主工作区及M7历史分支。
