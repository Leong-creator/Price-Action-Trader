# 项目状态

- 2026-08-03：`M10-PA-002-5m` 保持原规则和原独立仓不变；新增 `M10-PA-002-5m-repaired-v1` 作为第 `19` 个长桥 SDK 模拟账户运行单元及独立仓。两者同标的可以各自生成订单，资金、敞口、实际成交批次、退出和成绩均按 `capital_bucket + runtime_id + order_id + trade_id` 分开，不合并为一笔策略成绩。
- 2026-08-03：修复版不接本地模拟，实时突破后要求下一根五分钟K线仍站稳，目标 `1.3R`、最大风险距离 `2.5%`；每两笔长桥实际完整亏损后跳过恰好下一个合格信号；未触发止损/止盈的持仓在下一纽约交易日退出。
- 2026-08-03：PA002 节点评估已并入现有 M15 后台看护，每 `300` 秒检查实际成交归因，5日给技术提示，双版本各自15日且100笔后给最终人工复核建议；只在新节点或新归因异常时标记通知，不自动改策略。
- 2026-08-03：开盘前巡检发现并修复长桥 SDK 报价事件中的 `TradeSession` PyO3 枚举无法跨进程序列化，部分事件此前可能在 multiprocessing feeder 中丢失但进程仍显示存活。SDK 对象现在递归转换为普通 Python 值，并用不可序列化整数子类补回归。修复后重载状态为 SDK 单实例、`300/300` 行情、`18000/18000` 日线上下文、账户快照健康、模拟账户确认、正式编号 active、配置指纹一致、订单派发开启；修复后日志新增区间未再出现 `TradeSession` 序列化错误，开盘验收仅等待常规交易时段。
- 2026-08-03：`M10-PA-002-5m` 深度复核确认近期亏损不是 300 只扩池或近期阈值改动突然造成。长桥当前非故障日完整交易为 `61` 笔，`21` 胜 `40` 负，毛盈亏 `-60.02 USD`，保守估算费用 `244.00 USD`，扣费后 `-304.02 USD`；扩池前 `44` 笔已经是 `14` 胜 `30` 负、扣费后 `-246.36 USD`。旧本地账户的 `+3538.22 USD / 870` 笔来自同时做多做空、修复确认、连续亏损冷却及旧/备用行情的不同执行口径，不能当作长桥当前纯做多突破版的同规则验证。当前规则保持不变，继续采集到至少 `15` 个有效交易日且 `100` 个完整交易后再定策略改动；期间只补运行稳定性，不为短期成绩修改信号门槛。
- 2026-07-31 完成长桥 SDK 行情、选股和分仓整改：正式交易池从已验收的 147 只扩到独立 300 只；五分钟成交量和盘中日线确认改用 Longbridge SDK 累计行情，缺失、倒退或时序异常会安全阻断；半导体候选按本轮 `SPY/QQQ + SMH/SOXX + 个股` 相对强度轻量排序，不增加行情请求或放宽门槛。
- 最终审查已补齐运行时硬门禁：300 只交易池必须携带完整扩展验收配置；异常累计量和倒序时间戳不会覆盖最后可信基线，恢复后的下一笔仍按最后可信累计量计算。
- 长桥看板现分为真实账户、精确虚拟批次和无法归因差额三层。长桥持仓缺当前价时，真实账户、虚拟仓、运行单元、标的集中度和当日持仓的金额与浮盈统一显示等待长桥数据，数量仍独立对账，禁止用零值或开仓价伪造实时估值。
- `PA004-MBF` 与 `PA004-MBF-QC` 已生成 28 个未平批次、21 个标的的精确模拟账户清理计划。清理链路硬性要求 `account_channel=lb_papertrading`；非交易时段保持 `pending_cleanup`，只冻结实际存在待清批次的仓，退出继续允许，常规交易时段由 SDK 常驻进程逐批提交，全部成交后只为实际清理的仓建立新统计基线。
- 2026-07-30 已修复 M15 延迟突然升高的主要原因：长期运行后 `m15_realtime_signal_events.jsonl` 增至 `6621` 行约 `15.4MB`，执行流水增至 `6826` 行约 `31.5MB`，旧热路径在每个五分钟批次重复读取历史、重建成交归因并触发 WSL 路径解析。现改为审计文件增量追加、运行期内存热状态、按交易日压缩无订单阻断行、扩展 153 只在进入策略上下文前过滤，以及成交事实签名缓存。真实上下文无下单回放中，路由中位 `326.6ms`、持仓管理中位 `158.7ms`、合计中位 `487.4ms`、最大 `540.6ms`，较修复前约 `2.3~2.6s` 降低约 `79%`。`429` 项 M15 聚焦回归和编译检查通过；当前正式进程为 SDK、账户为 `lb_papertrading`、配置指纹一致、订阅 `300/300`、交易池 `147/147`、日线 `18000/18000`、账户快照 `2s`，开盘验收 `13` 项通过、`0` 项失败，后台看护健康。
- 2026-07-29 巡检修复 `LCID` 账户清仓归因回归：7 月 23 日人工清仓后实际持仓曾归零，7 月 28 日 `M10-PA-004-long-1d` 又正常买入 13 股；旧实现因当前持仓不再为零而拒绝历史清仓调整，错误保留 353 股旧虚拟残量。现在调整只作用于人工核准的旧开仓订单号，并要求清理后的虚拟净持仓与长桥当前实际持仓精确一致。SDK 慢速对账重算后异常 `0`、持仓不一致 `0`、策略成绩恢复可信；运行层已用原模拟账户配置安全重载，开盘验收 `13` 项通过、`0` 项失败，仅等待常规交易时段。
- 2026-07-29 修复交易 OAuth 失效后的假健康：M15 运行进程现在定期使用 SDK 只读订单接口检查交易上下文，失效时重建 OAuth 和 TradeContext；重建失败期间 `dispatch_enabled=false`，状态显示“交易连接恢复中”，已有失败信号不补交。
- 2026-07-29 修复做空容量误判：此前只读取 `cash_max_qty`，会把实际借券容量误判成零。现在开空只采用 `margin_max_qty`，并把现金数量、融资/借券数量和最终采用口径完整写入审计；普通买入仍保持现金约束。
- 2026-07-29 修复跨日分仓敞口漏算：执行器改用持仓管理器恢复的精确未平成交批次成本，并只额外叠加未成交订单剩余金额。当前仓位超过各仓 `6000 USD` 上限时会明确显示超限、剩余额度为零并停止新增，退出链路不受影响。
- 2026-07-29 修复退出重试和优先级：执行器固定止损先于止盈和开仓；同一批次失败退出进入 `900s` 冷却。只有账户订单读取成功且券商侧按信号编号/请求编号确认没有对应活动或已成交订单时才允许重试，超时但券商已创建订单的情况不会再次卖出。普通 SDK 限频不再触发 OAuth 上下文重建，而是短暂停止新开仓后复查。新增 OAuth、做空容量、跨日成交批次、未成交预留、超限展示、止损优先和重复退出回归；相关 M15 聚焦测试共 `221` 项通过，Python 编译检查通过。
- 2026-07-28 盘中巡检修复了两项 SDK 稳定性问题：下单 `TradeContext` 遇到 OAuth 刷新失败后不再复用失效上下文或补交旧信号，而是在本轮结束后重建供下一条新信号使用；SDK 行情订阅冷启动卡住时会顺序切换到唯一 SDK 批量快照进程，连续 5 轮 `147/147` 且每轮不超过 `1s` 后才标记可交易。当前快照轮询实测约 `0.06~0.08s`，账户快照约 `0.4~0.5s`，模拟账户和 `8820/8820` 交易日线上下文正常。快照模式不计入订阅或 300 只扩展验收，慢统计也不再建立第二个行情连接。
- 2026-07-28 完成长桥统计和稳定性收口，未修改任何策略门槛、仓位、止损、目标价或运行单元名单。最新 SDK 慢速对账按长桥实际 `Filled` 成交重建出 `209` 个开仓批次，其中 `49` 个仍未平、`153` 笔已完成交易、`159` 个退出成交事件；分批退出不再重复计作多笔交易。全量已完成交易毛盈亏 `-707.51 USD`，按保守费用模型估算费用 `616.02 USD`、扣费后 `-1323.53 USD`。其中系统故障日 `106` 笔、正常日 `47` 笔；正常日扣费后 `-508.07 USD`。故障日交易仍保留审计，但已经从当前策略胜率、盈利因子和回撤判断中排除。
- 2026-07-28 三条受限做空仓的诊断已从路由结果扩展到券商执行全链路：当前正式测试累计 `2` 个看跌候选，`1` 个通过路由，`1` 个进入券商可开数量检查并被券商数量门禁阻断，取得订单号和实际成交均为 `0`。可开数量成功结果已加入短时缓存并记录实时/缓存来源、缓存年龄和耗时，失败仍安全阻断；这解释了“有候选但没有做空成交”，不再把它误报为策略完全没有信号。
- 2026-07-28 SDK 稳定性修复完成代码和回归：账户工作进程的关键字段错误不会再覆盖可信快照，会保留原时间戳、重建工作进程并在连续异常后熔断；所有配置共用用户级全局运行锁，订阅失败会在真实失败路径累计；300 只订阅池与原 147 只交易池由独立文件及指纹隔离，重排扩展池不能改变实际交易标的。成交归因、账户状态、SDK 运行、做空仓、订单生命周期、持仓退出、看板和验收的全部 M15 单元测试共 `393` 项通过；盘后实机重载期间捕获一次不可信账户响应，系统按设计保留可信快照、重建工作进程并在下一轮恢复健康，最终综合验收为 `12` 项通过、`0` 项失败、仅等待交易窗口。
- 2026-07-28 对 2026-07-27 纽约交易日完成长桥事实源复盘：策略订单 `70` 张且全部成交，`35` 买/`35` 卖，退出为 `18` 次止损和 `17` 次止盈；长桥 App 对齐口径当日盈亏 `+355.62 USD`，按实际退出成交归因的已实现毛盈亏 `+323.03 USD`，差额来自仍持有仓位的当日价格变化和账户级口径。成交归因异常、持仓数量不一致和越过 147 只交易池下单均为 `0`。信号到 SDK 请求的中位数约 `345ms`、95 分位约 `1298ms`、最大 `1683ms`，没有超过 `5s`，但 95 分位仍未达到 `1s` 优化目标。运行状态现会在同一纽约交易日重启时保留最近 200 个延迟样本和最后一次热路径结果，跨交易日自动清空。300 只扩展池另发现 `BK -> BNY`、`MMC -> MRSH` 两个 2026 年代码变更，已更正并补齐 SDK 日线缓存；当前恢复为订阅 `300/300`、总日线 `18000/18000`、原交易池 `147/147` 与 `8820/8820`、模拟账户快照健康、下单通道已武装等待下一交易时段。
- 2026-07-28 行情池扩展第一阶段已启用：正式 M15 SDK 仍只有一个行情长连接，订阅范围由 147 只改为 300 只；原 147 只是唯一可交易股票池，新增 153 只只读验收，不会生成策略信号或订单。代码新增全局单连接锁、交易股票池路由硬边界、交易/扩展订阅覆盖分栏、交易日线上下文独立门禁和新增 153 只单日只读验收状态。原 147 只任一订阅或日线失败会停止新开仓；新增 153 只失败只标记扩展验收失败，不影响原策略运行。聚焦 SDK、开盘验收和周一验收共 87 项测试通过。
- 2026-07-27 周一开盘前复核确认三条受限做空仓自正式测试以来没有长桥订单或成交。实际五分钟行情重放显示 `M10-PA-002-5m-short` 曾有 `12` 条通过看跌结构和质量门槛的候选，但原 `500 USD` 单标的上限与 `10 USD` 风险上限、`2R` 目标、约 `4 USD` 双边费用和 `12 USD` 净利润门槛组合后，理论最高净利润仅约 `11 USD`；`M10-PA-011-ORB-R1-5m-short` 的理论上限约 `10.06 USD`，同样低于 `15 USD` 门槛，属于配置数学矛盾。三条短仓单标的名义金额上限已提高到 `750 USD`，每笔最大风险仍为 `10 USD`、每仓总敞口仍为 `2000 USD`，结构、质量分、盈亏比和净利润门槛均未降低；真实行情重放后 `12` 条候选中有 `2` 条可通过完整路由。新增按短仓正式测试编号累计的做空候选诊断，看板会直接展示候选数、可提交数和主要阻断原因，不再被下一轮空摘要覆盖。
- 2026-07-27 周一前巡检发现 2026-07-24 后 Windows 到 WSL 的隐藏保活连接已经退出，M15 SDK、后台看护和本地盘后调度器均随之停止，但长桥 SDK 行情、模拟账户、持仓、订单和成交端点仍可正常读取，故障不在授权或数据源。现已通过隐藏登录启动器恢复长期保活，并确认 SDK、看护器和盘后调度器均为单实例；PowerShell `Register-ScheduledTask` 仍被权限拒绝，但当前用户 `schtasks.exe` 路径已成功建立工作日北京时间 20:45 的无窗口唤醒任务，并启用电池供电可启动、错过时间后补启动和无限执行时限。该时间比夏令时开盘提前 45 分钟，给 8820 条日线冷启动和有界重试留出余量。冷启动实测仅 `PYPL` 的日线请求连续三次瞬时失败、第四次单独读取成功，因此日线单标的重试现从 2 次提高为可配置的 5 次；完整日线未就绪前继续禁止新开仓。
- 2026-07-27 长桥看板归因事实源修正：盘中热路径归因文件只在新成交时更新，周末无成交时不能作为慢统计新鲜度依据；看板现读取 SDK 慢速对账每 300 秒重建的 `m15_longbridge_fill_attribution_v2.json`。该文件与热路径归因当前均为 298 个匹配事件、174 个未平批次、0 个异常，但慢速文件具有本轮刷新时间，因此不会再把“周末没有新成交”误报为统计过期。
- 2026-07-24 收盘复盘：M15 SDK 实时链路保持 `sdk_connected=true`、订阅 `147/147`、日线 `8820/8820` 完整，70 个热路径延迟样本全部低于 1 秒，95 分位 `855ms`；2026-07-23 策略订单共 68 张，其中 66 张实际成交、2 张按旧买单超时规则取消，收盘后无未完成订单。长桥 App 对齐公式的账户当日盈亏为 `-322.60 USD`；按实际成交归因的当日已平仓毛盈亏约 `-507.49 USD`，主要损失来自 `M10-PA-002-5m -232.65`（`0胜/11负`）和 `M10-PA-004-MBF-1d -151.72`（`1胜/10负`）。两种盈亏差异来自未平仓持仓当日价格变化和账户级口径，不是把未成交草稿计入成绩。
- 2026-07-24 已修复盘后本地系统在精简守护环境中误报 `longbridge_cli_missing`：报价和历史 K 线均可确定性找到显式 `LONGBRIDGE_CLI_PATH`、系统 `PATH` 或 `~/.local/bin/longbridge`；日线更新改为基于已有缓存增量补齐，日线和五分钟总抓取预算修正为 `294`。修复后昨晚盘后产物已达到报价 `147/147`、日线 `147/147`、五分钟 `147/147`，股票池成员和顺序完全一致，M13 本地账本与 M14 修复评估均完成；相关 33 项单元测试通过。该修复只服务盘后本地研究，不改 M15 SDK 自动交易、策略门槛或代理设置。
- 2026-07-24 盘中巡检确认 M15 SDK、`lb_papertrading`、`147/147` 订阅、`8820/8820` 日线、账户快照和单实例看护均健康；开盘验收 `14/14`、综合验收 `13/13` 通过。当前纽约交易日长桥账户共 `64` 张订单，其中策略订单 `63` 张（`62` 成交、`1` 取消），另有用户人工 `LCID` 清理成交；策略订单含 `22` 笔开仓、`33` 笔止损、`8` 笔止盈，当前挂单为 `0`。修复 SDK 枚举状态 `OrderStatus.Canceled` 未规范化的问题，`USO` 超时撤单现在正确归入取消原因，不再显示“状态未知”；相关 `42` 项测试通过。
- 2026-07-23 盘中巡检已修复 `LCID/GILD` 的历史账户净额与虚拟成交批次不一致：只对人工核准的具体历史开仓订单号、且长桥实际净持仓已经为零的批次执行账户对账调整，不按标的或数量猜测归因；调整事件永久排除在资金池和策略成绩之外，未来同标的新成交不受影响。修复后盘中成交归因 `218` 个匹配事件、`144` 个未平批次、`0` 个异常，持仓管理的净持仓不一致标的由 `LCID/GILD` 降为 `0`。SDK 进程重载后的首个完整五分钟周期继续保持 `lb_papertrading`、订阅 `147/147`、日线 `8820/8820`、账户快照健康，信号到 SDK 请求本地处理耗时 `565ms`；该周期 12 条候选全部因已有同仓同标订单或仓位压力质量门槛被正确阻断，没有制造订单。
- 2026-07-23：已确认 7 月 16 日清仓成功，当前 46 个持仓标的是正式测试启动后产生并跨日持有，不是清仓前遗留。7 月 22 日实际成交 5 笔止损、2 笔止盈，说明卖出链路部分正常；同时发现增量成交归因不完整造成 19 个持仓标的错误冻结，以及 DDOG 止损事件未进入执行器后被永久去重。已完成完整成交归因基线优先、止损/目标元数据补全、非正式成交过滤、未执行退出事件重试和异常去重；固定单轮 10 条退出已改为只限制非紧急退出，所有已核验止损当轮全部进入 SDK 节流队列。LCID 当前长桥实际持仓 56 股因历史超卖无法可靠归因，用户已授权按账户对账清理；一次性纸面账户市价退出请求已登记，等待下一常规交易时段提交，成交后不计入任何策略仓成绩。81 项持仓与 SDK 定向测试通过。
- 2026-07-22 盘中实机巡检通过：M15 实际引擎为 SDK、长桥账户确认为 `lb_papertrading`，订阅 `147/147`、日线 `8820/8820`、账户快照年龄 `9s` 且刷新约 `0.51s`，正式编号和单实例看护正常。当天长桥实际订单 `18` 张，其中 `17` 张成交、`1` 张取消、当前挂单 `0`；全部订单均能映射到资金池和运行单元，信号到 SDK 请求中位数约 `171ms`、最大 `684ms`。另修复周一综合验收把 `account_snapshot_age_seconds=0` 当成缺失值的误判，以及后台慢统计只重试 `request timeout`、不重试同类 `connect timeout` 的恢复缺口；两者均只影响验收/统计，不改交易连接、策略门槛或订单。
- 2026-07-22 跨策略同标的成交归因已改为长桥真实成交批次账本：正式测试的开仓和平仓必须携带长桥订单号、成交编号和原开仓批次，部分成交只核销对应剩余数量；未匹配成交保留为待修复诊断，后续补齐元数据后可自动重新归因。长桥实际净持仓与各策略虚拟批次不一致时，系统冻结该标的新开仓并停止自动策略退出，避免一个策略误卖另一个策略的仓位。运行层和慢统计分别使用独立归因状态文件，互不覆盖。
- 2026-07-22 账户快照 `181s` 过期的根因已定位并修复：旧实现把 SDK 首次建连时间算进常规 `8s` 刷新期限，工作进程会在连接完成前被反复终止；后台看护又因状态指纹互相覆盖累计拉起 5 个 M15 进程。现在首次连接使用独立 `30s` 启动期限，常规刷新仍为 `8s`；第一次常规超时会重建并在同一轮再试一次，连续异常才进入 `15s` 熔断。运行层使用文件锁强制单实例。实机复核账户刷新约 `0.4~0.6s`、超时 `0`、熔断关闭、模拟下单通道开启；慢统计最近两天增量查询约 `1.4s` 完成，聚焦测试 `236` 项通过。
- 2026-07-21 修复后台慢速统计持续超时：看护器此前每约6分钟重新请求全部 `963` 条历史订单，两次SDK读取各等待30秒后仍超时，导致看板统计过期和综合验收反复报警。现改为可信历史缓存加最近两天增量对账，订单号和成交编号去重覆盖；完整看护恢复为约 `1.8s`，其中账户慢统计约 `1.45s`，综合验收恢复 `12 pass / 1 waiting / 0 fail`。
- 2026-07-21 修复长桥看板当日盈亏口径：用户截图中的 App 资产页为 `+19.37 USD`，旧看板误用了纽约交易日 `profit_analysis_by_market=-109.26 USD`。现按长桥官方公式，用实际持仓、实际成交、昨收价和盘后价复算为 `+19.38 USD`；总现金 `88254.34 USD`、持仓市值约 `14239.60 USD`、总资产 `102493.94 USD` 与截图对齐。纽约交易日收益分析保留为独立诊断值，不再冒充 App 当日盈亏。
- 2026-07-21 重启后系统已恢复并补齐三项稳定性缺口：账户轮询和订单执行不再共用 SDK `TradeContext` 或限频锁，账户只读连接的请求超时可重建后重试一次，不影响下单连接；盘后重启会复用已验证覆盖最新完整纽约交易日的 `8820` 条日线缓存；慢速统计对历史订单/成交的偶发请求超时只在新只读连接上重试一次。当前隐藏保活、M15 SDK、后台看护、本地盘后调度器各一个实例，`lb_papertrading`、`147/147`、`8820/8820`、账户快照、正式编号和配置指纹均正常，综合验收为 `12 pass / 1 waiting / 0 fail`。
- 2026-07-20 昨晚长桥事实源复盘：当日长桥有 `63` 张订单、`62` 张实际成交、`1` 张取消，当前持仓标的 `22`、挂单 `0`。28 个实际平仓批次按成交归因得到毛盈亏 `+148.09 USD`、胜率 `35.71%`；长桥 SDK 查询纽约交易日 `2026-07-20` 的美股收益分析为 `-109.26 USD`，该值是收益分析口径，不是 App 资产页当日盈亏。当前完整区间累计美股收益分析为 `-51.98 USD`。延迟方面 63 张订单全部在 `1.640s` 内发起 SDK 请求，57 张不超过 `1s`，无订单超过 `5s`。
- 2026-07-21 盘中巡检修复 SDK 旧买单维护归因：长桥订单备注实际格式为 `PAT-RT <signal_id> <client_request_id>`，旧维护器却把整段备注作为 `signal_id` 精确匹配，导致 `PYPL` 未成交买单超过配置的 `900s` 后仍未撤销。维护器现在从备注 token 中解析已知实时信号编号，继续只处理能归因到 M15 的订单，不触碰人工或未知订单；新增回归使用长桥真实备注格式验证过期买单撤销。
- 2026-07-20 盘中巡检发现并修复账户快照的瞬时误阻断：当 SDK 的资产、持仓或当日订单查询偶发失败时，旧实现会立即用失败结果覆盖上一份健康快照，导致 `14:50 UTC` 的 8 条实时信号全部误报 `blocked_non_paper_account`。协调器现在只在账户通道仍为 `lb_papertrading` 时保留上一份健康快照及其原始时间戳，并记录失败时间和错误；失败持续超过 `45s` 后仍会按旧安全门禁停止新开仓，检测到账户通道变化则立即拒绝保留。盘中综合验收同时接受 canonical `ready_for_longbridge_paper_orders` 状态，不再假报警。修复后首个完整五分钟批次收到 `146/147` 个有实际报价标的，生成 7 条信号，`M10-PA-008-1d` 的 `PYPL 7股` 在本地处理 `37ms`、SDK请求 `142ms` 后取得长桥模拟订单号 `1263874240470351872`；账户、订阅、日线、看板、开盘验收和周一综合验收均恢复正常。
- 2026-07-20 电脑重启后完整巡检确认 Windows 隐藏保活、M15 SDK、后台看护和本地盘后调度器均自动恢复为单实例。启动初期 `profit_analysis_by_market` 超时曾把盘中账户快照拖到 `74s`，超过 `45s` 安全线并使开盘验收阻断；现已把收益分析彻底移出 15 秒账户快照，盘中快照只读取交易必需账户字段。重启加载修复后连续观测账户快照年龄为 `2~13s`，`147/147` 订阅、`8820/8820` 日线、`lb_papertrading`、正式测试编号和配置指纹均正常；M15 全量 `329` 项测试通过，开盘验收 `13 pass / 1 waiting / 0 fail`、周一综合验收 `12 pass / 1 waiting / 0 fail`，唯一等待项是北京时间 `21:30` 常规交易时段。
- 2026-07-18 周一稳定性最终复核已完成：修复做多/做空正式测试编号同时启用时做空成交可能被做多编号过滤、从而无法生成回补的问题；修复后台慢统计在一次跳过后丢失上次成功时间、导致提前重复刷新问题；SDK 统计现在还会核验真实进程 PID、状态年龄和运行配置指纹，进程死亡时禁止刷新统计时间；周一综合验收的 `broker_connection` 由 SDK 连接和 `lb_papertrading` 验证共同决定，账户验证失败时不再出现连接为真的矛盾字段。实际 SDK 进程重启后订阅 `147/147`、日线 `8820/8820`、账户快照健康、模拟账户和正式测试编号均正常。周一综合验收仍为 `12` 项通过、`1` 项等待交易时段、`0` 项失败。
- 2026-07-18 周一前最终巡检已补齐控制面问题：后台看护由两个重复进程收敛为单实例，并增加启动锁/运行锁；健康流水降为状态变化或每 5 分钟记录，避免重复写盘。SDK 慢统计现在要求模拟账户快照不超过 `45s`，主进程离线时不会刷新出伪新鲜数据。独立长桥看板已正确显示“SDK 下单通道已武装”，不再因读取不存在的旧字段误报关闭。
- 2026-07-18 旧验收口径已退役：`m15_monday_refresh_acceptance` 改为只核对 M15 SDK、`lb_papertrading`、`147/147`、`8820/8820`、账户快照、正式测试编号、看护器和长桥看板，不再读取 M12/M13/M14 或旧 CLI 会话。当前验收 `12/1/0`，唯一等待项是周末不在美股常规交易时段。旧目录的 opening readiness 只写 canonical SDK 路径指针，不再保留旧 blocked 结论。
- 2026-07-18 对账统计新增跨测试编号退出隔离：完整区间 `900` 张订单、`439` 张实际成交中识别出 `2` 笔平仓引用旧测试编号开仓信号的历史错误归因。成交继续计入长桥账户事实口径，但 `include_in_bucket_performance/include_in_strategy_performance=false`，不再污染正式分仓、策略胜率和盈亏。
- 2026-07-18 巡检确认 2026-07-17 美股交易时段没有新行情信号或新订单请求：M15 SDK 与后台看护在北京时间 15:18 随 WSL `init` 退出，开盘前已离线，因此不是“没有合格信号”。但长桥把 2026-07-16 收盘瞬间遗留的 `ABBV` 买单和 `GILD` 卖单在 2026-07-17 更新为实际成交；这两笔是隔夜遗留单，不是周五系统生成的新交易。历史接口按原订单创建时间筛选，直接用周五日期查询会漏掉这种跨日成交，SDK 慢速对账现改为拉取完整测试区间并按订单号、状态和成交数量重算。长桥 SDK 行情、资产、持仓、订单、成交和盈亏只读端点当前均可用，账户确认仍为 `lb_papertrading`。Windows 登录启动项已改用 `--keep-alive` 保持一个不可见的 `wsl.exe`，并仅在 WSL 内每 60 秒幂等检查 M15 SDK、后台看护和本地盘后调度器；不再依赖无权限的 21:10 计划任务，也不新增会弹窗的 Windows 轮询。

- 2026-07-18 长桥独立看板的慢统计已从旧 CLI 切换为 SDK-only：每 300 秒后台拉取完整测试区间的长桥实际订单、成交和美股市场盈亏，重新生成账户摘要、订单对账、未成交诊断和盈亏文件；该慢任务不阻塞下单热路径。实测当前区间返回 `900` 张订单、`439` 张实际成交，对账文件与看板均刷新为可信；长桥 App 顶部“当日盈亏”仍因 SDK 未公开对应字段显示“等待长桥字段对齐”，不使用本地估算冒充。

- 2026-07-17：昨晚长桥 SDK 正式测试链路已确认真实运行：长桥模拟账户共返回 `55` 张订单、`53` 张实际成交；正式基线之后有 `39` 张策略订单，其中 `37` 张成交。发现并修复两项问题：纽约时间 `16:00:00` 仍提交日内单，导致 `ABBV` 买单和 `GILD` 卖单停在 `NotReported`；持仓管理未按正式测试编号过滤旧执行流水，导致一张旧 LCID 虚拟仓记录错误触发 `PA002-5m` 卖出。现在收盘时刻禁止提交，且退出管理只消费当前正式测试编号之后的归因记录。账户总盈亏 `-305.59` 是长桥 SDK 当前账户级分析字段，包含旧历史，不作为昨晚正式基线策略成绩；昨晚正式基线的策略成绩必须继续按长桥实际成交和有效归因计算。

- 2026-07-16 盘中正式测试已恢复：长桥 SDK 已完成 16 个旧模拟持仓清理，当前实际持仓、未完成挂单和待确认订单均为零，正式做多/做空测试编号已激活并允许新开仓。本轮发现 formal marker 虽有激活时间，但共享执行 epoch 的 `test_started_at` 为空且状态名为 `activated`，导致清仓后的新信号仍被过滤。修复后统一写入 `status=active` 和同一 `test_started_at`，运行层、执行器、路由器均可安全兼容并自愈旧产物；开盘验收新增实际执行 epoch 一致性硬检查。当前 SDK 进程、看护、模拟账户、`147/147` 订阅、`8820/8820` 日线上下文、账户快照和配置指纹均正常，相关 148 项回归通过。
- 2026-07-16 17:30 正式开盘前最终验收：M15 SDK 运行进程、后台看护和本地盘后调度器均已由 Windows 隐藏启动入口独立拉起并保持存活；当前运行引擎为 `sdk`，模拟账户通道为 `lb_papertrading`，订阅 `147/147`、日线 `8820/8820`、账户快照约每 `15s` 更新，配置指纹一致。账户总资产已修复为 SDK `account_balance.net_assets` 原始口径并同时标注币种，当前约 `798272.12 HKD`；可用美元约 `89752.98 USD`，二者不再混成同一口径。独立看板会在订单/盈亏慢统计过期时显示“交易核心正常、统计待刷新”，不再沿用旧盈亏。正式测试仍按设计处于 `pending_flatten`：今晚 `21:30` 先清空 `16` 个旧持仓，确认持仓、挂单和待确认订单全部为零后自动激活正式测试编号并允许新信号。
- 2026-07-16 策略就绪复核：长桥配置仍包含 `18` 个运行单元，但只有 `13` 个运行单元的静态目标盈亏比能够通过现有硬门槛。`M10-PA-013-5m`、`M10-PA-011-ORB-R1-5m`、`M10-PA-001-1d`、`M10-PA-005-5m`、`M10-PA-012-5m` 的策略目标低于其 `1.5R/2.0R` 最低要求，当前只会记录并阻断，不应描述为“已就绪下单”。本轮没有为了制造交易而降低门槛或强行抬高目标价；其余 `13` 条可以在正式基线激活后按现有仓位和风控进入模拟订单链路。
- 2026-07-16 瘦身复核：盘中只保留 M15 SDK、M15 轻量看护和本地盘后调度三个常驻进程，合计常驻内存约 `100MB`，M12.47/M12.37 和旧 CLI 实时链路均未运行。`reports` 已从约 `1.0GB` 降到约 `901MB`；剩余主要是历史回测、旧策略研究和压缩归档，不在下单热路径。进一步压缩旧 `SF-*`、回测目录和 Git 对象，以及把盘后批处理从兼容调用进一步缩成按运行单元执行，安排在正式交易窗口之后，避免开盘前引入无关风险。
- 2026-07-16 17:10 开盘前二次巡检修复：发现上一组 M15 SDK、后台看护和本地盘后调度进程随旧 WSL 会话退出，历史状态文件已正确改报停止。根因是 Windows 隐藏 VBS 仍使用容易失效的多层 `bash -lc` 引号。现已改为 `wsl.exe --exec bash <bootstrap>` 直接启动，并实际完成“停止三服务 -> VBS 隐藏重启 -> WSL init 托管”验收；SDK、看护和盘后调度器持续存活。当前纸面账户、行情、资产、持仓、订单和成交只读端点均通过，订阅 `147/147`、日线 `8820/8820`、账户快照健康、配置无漂移；正式状态仍为预期的 `pending_flatten`，16 个旧持仓开盘清零前禁止新开仓。额外将旧 CLI 会话/账户/权益流水和 M14 历史流水压缩归档，`reports` 从约 `1.0GB` 降至约 `903MB`，订单、成交和分仓审计未删除。
- 2026-07-16 长桥与本地系统已完成结构性拆分：M15 SDK 独立负责盘中行情、信号、风控、模拟账户订单和账户统计；本地系统统一改名为“本地研究与修复系统”，只在纽约收盘 15 分钟后运行一次，不再启动 M12.47/M12.37 盘中常驻。M15 readiness/watchdog 已移除 M12.47 阻断，开机脚本只启动 M15 SDK、M15 看护和本地盘后调度器。
- 2026-07-16 正式测试仍处于 `pending_flatten`：长桥模拟账户当前实际 `16` 个旧持仓、`0` 个挂单，允许新开仓为 `false`。市价清仓、稳定请求标识、一次可成交限价退路和“持仓/挂单/待确认全部归零后激活正式编号”均已实现；未清零前绝不进入新测试。
- 2026-07-16 独立长桥看板与资源瘦身已落地：长桥板块不再从 M12 大看板、本地账本、旧快速队列或 CLI 备用路径取数；日志/报告工具盘后只压缩已关闭行情事件和旧 M14 大流水，订单/成交/分仓审计长期保留。M15 专属 300 标的候选池已建立但尚未启用，生产仍为 `147/147`。
- 2026-07-16 SDK 验收复盘与清仓恢复：`2026-07-15` 长桥实际历史为 `13` 张订单、`10` 张成交、`3` 张取消；其中 SDK 验收新增 `8` 张买入成交，覆盖 `MSFT/AAPL/AMZN/ORCL/GOOG/GOOGL/META`，另有 `LI` 止盈和 `LRCX` 止损两张卖出成交。滚动热路径 `31/31` 个样本都在 `1s` 内，P95 为 `579ms`，因此昨晚实时下单链路达标。纽约时间 `15:48` 的授权清仓在停止 SDK 与看护后遇到报价端 `connect timeout`，没有生成清仓订单，长桥仍有 `16` 个持仓标的。现已修复清仓只读重试和报价退化：报价短暂不可用时使用券商持仓成本生成更保守的保护限价，不把报价连接失败升级成整批清仓失败；失败后会写入 `pending_flatten`，下个常规交易窗口只允许清仓、禁止新开仓。正式做多/做空测试编号已统一为 `m15-sdk-formal-single-strategy-20260716` / `m15-sdk-formal-short-strategy-20260716`，只有长桥确认持仓和挂单归零后才开始统计。SDK 状态与后台看护状态也已修正，进程死亡时不再显示历史 `running/healthy`。
- 2026-07-16 SDK 盘中验收补强：长桥 SDK 已在真实模拟账户链路取得订单号并完成成交回查，重启后的完整五分钟批次热路径耗时保持在 1 秒目标内。进一步审计确认，订阅覆盖 `147/147` 不等于每个标的每个五分钟区间都有新报价推送；日线确认层现已改为只有本轮实际收到完整五分钟 K 线的标的才可生成当轮日线意图，避免报价静默时把旧价格当成新确认。系统不补造静态 K 线，缺失代码继续显式列出。
- 2026-07-16 SDK 种子池修正：长桥 SDK 直接核对显示旧代码 `PARA` 最近多个交易日日线固定为 `11.04` 且成交量为 `0`，而 `PSKY` 当日有有效报价、最近 60 根日线完整且成交量正常；147 只池现等量以 `PSKY` 替换 `PARA`。M15 运行指纹同时覆盖种子池及旧行情采集兼容模块，后续池代码变更会触发安全重载，不会继续运行旧代码列表。
- 2026-07-16 SDK 成交量修正：长桥官方把报价推送 `current_volume` 定义为相邻推送间增量，但新订阅的首个快照没有本地前值，实测可能携带较大的当日累计量。五分钟聚合器现将每个标的连接后的首个报价只用于定价、成交量记为 `0`，第二次推送起才累加增量，避免重启后的第一根完整 K 线产生虚假放量信号。

- 2026-07-15 M15 SDK 纸面订单全链路已用长桥实际订单和成交验证：当前运行引擎为 `sdk`、账户为 `lb_papertrading`、订阅 `147/147`、日线 `8820/8820`、账户快照健康，开盘值守 `13/13` 通过。`16:10 UTC` 完整 147 标的批次用当前 SDK 五分钟聚合日线生成 `8` 条新信号，`7` 条通过并全部取得长桥订单号，`1` 条因仓位压力下质量不足阻断；其中 `MSFT 2 股`、`ORCL 6 股`、`AMZN 3 股`、`AAPL 2 股` 已实际成交，未成交买单仍由 SDK 原生维护。下一轮同仓同标的重复信号有 `6` 条被正确去重，没有重复买入；同时 `M10-PA-002-5m` 的 `LRCX 3 股` 退出从信号到 SDK 请求 `13ms`、请求响应 `139ms` 并实际成交。此前 `M10-PA-002-1d` 的 `LI 26 股` 止盈单经 SDK 改价后也已按均价 `12.835` 成交。整批热路径实测 `373ms`（单笔退出场景）到 `1110ms`（同轮 7 笔串行 SDK 订单），均低于 `5s` 验收线；延迟口径已修正为真正发起 SDK 请求时记录，最后一笔不会再被错误算成零等待。
- 2026-07-15 SDK 稳定性复核新增六项修复：运行指纹现在同时覆盖实际配置、关联配置和六个实时 Python 模块，代码热更新会由后台看护安全替换旧进程；日线策略输入改为上一交易日完整 SDK 日线加当前交易日五分钟聚合确认，不再拿昨日日线直接当今日信号；`M10-PA-004-MBF-1d` 与 `M10-PA-004-MBF-QC-1d` 已按独立规则进入实时检测；五分钟区间中途重启会抑制第一根残缺 K 线，从下一完整区间恢复；挂单维护已全部改为 SDK 原生撤单/改单，未成交买入 `900s` 后撤销、退出单 `60s` 后按当前价提供 `0.5%` 可成交保护区间；最近一次实际维护动作会长期保留，不被后续“无需操作”覆盖。盘中 `GOOG/GOOGL/META` 三笔未成交买单已在 15 分钟后实际变为 `Canceled`，当前未完成挂单为 `0`。收盘清仓 dry-run 已确认当前 13 个实际持仓均可从 SDK 取得现价、方向和可卖数量且无阻断；只有正式清仓确认持仓和挂单归零后，才会写入 `m15-sdk-formal-single-strategy-20260716` 新测试基线。

## 当前阶段

- 稳定基线：`main`
- 当前支线：`fix/m15-monday-stability`

## 当前 milestone

- 稳定基线：`M8E.2 Longer-Window Daily Validation`（已完成）
- 当前支线 milestone：M14/M15 加速推进整改、runtime 级内部模拟准入与 Longbridge paper preflight
- 当前子阶段：M15 长桥 SDK 是唯一盘中交易主系统，只消费长桥实时行情和长桥模拟账户自身状态。本地研究与修复系统只在盘后运行，不参与长桥启动、风控、下单、持仓或盈亏。做多单策略仓每仓虚拟资金 `10000 USD`、总敞口 `6000 USD`、单标的 `1500 USD`、单笔风险 `20 USD`；仅三条受限短仓允许做空测试，每仓 `10000 USD`、总做空敞口 `2000 USD`、单标的 `500 USD`、单笔风险 `10 USD`。生产股票池继续为独立 `147` 只；300 池只作后续只读验收。旧快速队列、CLI 提交器和 M12.47 退出自动启动。旧版历史净利润继续禁止显示、入账或参与判断。

<!-- strategy_factory_provider_contract={"active_provider_config_path":"config/strategy_factory/active_provider_config.json","primary_provider_runtime_source":"source_order[0]"} -->

## 当前分支

- `fix/m15-monday-stability`

## 当前工具修复

- 2026-07-15 SDK 纸面下单与明日正式基线闭环已完成盘中验证：`M10-PA-002-1d` 对 `LI` 的止盈退出通过持久 SDK 提交，订单号为 `1262072366440157184`，长桥请求返回耗时 `141ms`，信号处理在 `1s` 目标内；该订单当时为 `New` 挂单、未成交，因此没有计入成交成绩，也没有重复卖出。重启加载最新代码后，下一轮 `147/147` 五分钟 K 线全部进入热路径，完整路由和风控耗时 `180ms`。运行状态现保留最近 200 轮 P50/P95/最大耗时和 `1s/5s` 分布，并分别列出每轮缺失 K 线标的。账户盈亏分析等非关键慢接口失败不再把已经确认的纸面账户误判为不可交易；余额、持仓或订单接口异常仍会停止新开仓。普通止损/止盈限价加入 `0.5%` 可成交保护区间，减少原价挂单卡住。收盘清仓现会轮询确认撤单、实际持仓和挂单归零，只有确认完成才预置 `m15-sdk-formal-single-strategy-20260716` 与 `m15-sdk-formal-short-strategy-20260716` 两个明日正式测试基线；未确认时不会补单或伪造清仓成功。

- 2026-07-15 盘中 M15 SDK 信号断流根因已修复并实测：此前五分钟 K 线在区间完成后，错误地按“最后一笔报价距现在的时间”套用 `2s` 时效门槛；正常的低频报价标的会因此把完整 K 线误判过期，策略完全收不到当日事件。现改为完整 SDK 五分钟 K 线按实际完成时间校验，原始报价和非 K 线事件仍保留原有过期保护。修复后 `15:10/15:15/15:20 UTC` 每轮均有 `147` 条实时行情事件进入路由，热路径实测约 `163ms`；最新一轮识别出 `5` 条 `M10-PA-002-5m` 突破候选（AAPL、C、GOOG、MSFT、XLV），但止损距离过小、目标空间不足，扣除双边费用后预期净利润为 `-3.45 ~ -3.92 USD`，因此正确阻断，没有制造订单。

- 2026-07-15 同时修复 SDK 运行重启后的两项稳定性缺口：一是启动时恢复当天、每标的最近 `20` 根 SDK 五分钟 K 线作为策略上下文，不回放任何历史订单；二是 SDK 下单前将裸美股代码转为 `.US`，并将单笔券商异常记录为该笔失败而不让运行进程退出。此前 BABA 的退出意图因提交 `BABA` 而被 SDK 拒绝并触发进程重启；修复后当前运行层已恢复 `147/147` 订阅、`8820` 条日线、`294` 条当日五分钟上下文和健康账户快照。今后若退出请求明确在取得订单号前失败，下一根新 K 线会创建带新编号的重试意图；已提交、待确认或已有卖出挂单时仍不会重发。M15 SDK 与本地模拟继续完全隔离。

- 2026-07-15 开盘验收默认配置已对齐当前 SDK 模拟订单运行层：`run_m15_opening_trade_readiness.py` 省略 `--config` 时不再回退到已退役的 CLI 守护器配置，避免把正常 SDK 进程误报为离线；显式传入历史配置仍只用于兼容性复盘，不能作为当前交易健康结论。

- 2026-07-15 SDK 运行层停止流程已改为优雅停止主进程：先通知主进程关闭行情子进程和进程间队列，再等待退出；避免旧的进程组强制终止留下孤儿行情订阅或资源清理警告。盘前已完成一次停止、清理历史孤儿子进程并自动重启的验证，重启后恢复 `147/147` 订阅、完整日线缓存和健康账户快照。

- 2026-07-15 M15 已完成 SDK-only 实时运行层切换验证：项目虚拟环境固定 `longbridge==4.3.3`，SDK 行情、资产、持仓、订单、成交和组合盈亏预检全部通过，确认账户通道为 `lb_papertrading`。M15 自动链路不再调用 CLI；CLI 只保留人工诊断用途。147 只标的按 `10` 只一批订阅，实测订阅覆盖 `147/147`；日线缓存改为独立可回收 SDK 子进程，实测完整写入 `8820=147×60` 条日线且无失败，不会卡住行情推送。五分钟收盘事件会在 `150ms` 小窗口内合并，避免 147 条事件逐条触发完整策略重算；日线内存窗口已扩至完整 60 日上下文。M15 账户快照每 `15s` 更新，超过 `45s` 自动停止新开仓。Windows 隐藏启动入口现直接拉起 SDK 运行层，不再先启动旧 CLI 守护器；后台看护也只确认 SDK 实际进程与配置。用户已批准今晚开放 SDK 模拟订单，用自然出现的合格信号验收下单、订单号、成交和退出跟踪；两天只读门禁已关闭，但模拟账户、实时数据完整性、账户快照时效和全部现有策略/风控门槛继续强制执行。今晚成交仅归档为 SDK 执行验收；用户已批准收盘前清仓：脚本会先停止新开仓和后台看护、取消未完成订单，再按长桥实际持仓方向卖出多头或回补空头。只有清仓成交确认后，明晚才建立正式测试基线并归零统计。

- 2026-07-15 SDK 启动缓存稳定性补强：完整日线缓存改为原子写入，避免重启期间半成品覆盖旧完整缓存；启动时只有 `147×60` 完整、无失败且覆盖最近一个完整美股交易日的缓存才会复用，否则订单通道保持关闭并重新拉取。常规时段收盘后将重新拉取最终日线，保证下一个交易日使用最新已收盘日线。实测重启后缓存即时复用，M15 当前为 `147/147` 订阅、`8820/8820` 日线、健康账户快照、模拟订单提交已武装，仅等待常规交易时段。

- 2026-07-14 M15 SDK 授权与低延迟整改：SDK OAuth 已完成授权，专用令牌已写入本机受限目录；SDK 账户余额与 CLI `lb_papertrading` 的美元现金、冻结现金和资产读数已交叉确认。当前网络下 `cn` 行情 WebSocket 可用、`global` 行情 WebSocket 超时，因此 SDK 显式固定使用 `cn` 行情端点；交易状态 WebSocket 仍不可用，已改为可选，不再阻塞 SDK HTTP 订单请求或后台只读对账。已完成 SDK 模拟订单通道切换：启动器会互斥停止旧 CLI 实时守护器，SDK 对 147 只标的按每批 10 只订阅，避免一次性订阅超时；CLI 仅保留每 60 秒账户、持仓、挂单和成交对账慢路径，不再位于信号和下单前。SDK 运行状态新增事件到执行热路径的延迟窗口；账户快照超过 `90` 秒时新开仓仍必须阻断。后台看护和开盘验收已改为检查 SDK 进程，当前为 `armed_waiting_regular_session`，`12` 项通过、`1` 项等待交易窗口。
- 2026-07-14 SDK 运行复核补正：五分钟 K 线完成事件的 `received_at` 现固定为实际完成/派发时刻，最后一笔行情时间只保留在 `source_event_at`。此前该字段可能早于 K 线结束，导致延迟统计偏低；已修复，未补交任何旧信号或订单。
- 2026-07-14 SDK 连接稳定性补正：SDK 常驻进程遇到行情网关短暂连接超时后改为按配置退避并在原进程内重连；不再退出后等待后台看护的下一轮拉起。后台看护仍作为第二层存活保护。
- 2026-07-14 SDK 运行回退（历史记录，已由 2026-07-15 SDK-only 修复取代）：当时订阅请求可能长期不返回，因此曾临时回退到 CLI。现已通过可终止订阅子进程、独立日线缓存子进程、订阅批处理和实际运行配置校验解决；当前 M15 自动路径不再使用该 CLI 回退。
- 2026-07-14 M15 SDK 实时运行层开始落地：新增持久 SDK 行情/交易客户端适配、五分钟 K 线推送聚合、订单回执订单号解析，以及热路径“追加写入 + 最近 4096 条行情窗口”处理，避免每轮扫描和重写完整历史行情事件文件。SDK 行情仍保持与本地模拟完全隔离、仅纸面账户；实际切换需先完成项目虚拟环境 SDK 安装和 OAuth client_id 授权，切换时必须关闭旧 CLI 行情采集器，不能双写同一事件流。当前不改变现有模拟订单权限，也不自动提交订单。
- 2026-07-13 受限做空模拟测试整改已完成并通过回归：在不改变既有做多单策略仓、也不提高其 `6000 USD` 敞口上限的前提下，已新增 `M10-PA-002-5m-short`、`M10-PA-013-5m-short`、`M10-PA-011-ORB-R1-5m-short` 的独立短仓配置、实时看跌检测器和 `m15-short-single-strategy-20260713` 基线。三仓分别使用 `10000 USD` 虚拟资金、`2000 USD` 最大做空敞口、`500 USD` 单标的上限、`10 USD` 单笔风险。执行链路把 `sell` 明确标为开空、`buy` 明确标为回补；开空前校验纸面账户、同标的多头/多头买单冲突、整股向下取整、券商 `max-qty --side sell` 和严格价格行为门槛。回补必须携带原开空订单号，并按资金池、运行单元、标的和原订单逐笔核验；同一原开空批次只要有未终态回补订单就持续阻断。只有券商已确认撤销、拒绝或过期且该短仓实际仓位仍在时，才会重建新的 `-retry-N` 回补意图；已成交、待确认或尚未终态的回补绝不重发。同标的不同短仓可以各自回补，不会互相阻断。长桥成功回执但缺订单号时只会等待后台对账，不会重复提交同一信号；执行器和持仓管理器只会使用订单备注精确匹配的对账叠加层补齐其订单号与终态，原始执行流水保持不改写。看板在没有长桥成交对账时只显示请求待对账，不会把本地已提交算成持仓、盈亏、胜率、回撤或敞口；做空表现与做多质量统计分开。短仓只用长桥实时行情生成信号，本地模拟不参与。M12.29 已把自身激活扫描范围传给 M12.12 数据源，缓存计数也按实际范围截断，避免测试范围与 `147` 只历史缓存发生假性不一致。M15 全量回归 `211` 项通过，M12 看板回归 `67` 项通过；本轮运行假客户端/只读验证，未调用长桥下单命令；实际模拟交易日只接受当日新产生的合格短仓信号。
- 2026-07-13 M12 刷新性能整改已完成：`csv_stats`、K 线读取和校验和均按文件路径、修改时间和大小缓存，同一轮不会重复扫描同一份长桥 K 线 CSV；文件刷新后缓存会自动失效。该改动针对全量看板单测和 147 标的刷新中重复 CSV 扫描的热点，不改变行情、策略或订单结果。
- 2026-07-10 盘前巡检修复：长桥账户的全量历史订单/成交读取不再随 M15 的 `5` 秒实时循环重复执行。它现在由后台慢路径每 `300` 秒更新一次，并使用独立 `90` 秒只读超时；盘中只复用最近成功的 `m15_longbridge_last_trustworthy_order_history.json`，不会卡住实时信号到模拟订单的路径。历史接口短暂超时不会再把 `814` 条长桥订单、`357` 笔成交误刷成零，也不会把已匹配的 `154` 条本地归因误报为未确认提交。持仓报价退化时新增显式退化状态：没有可信旧快照即显示“等待长桥持仓行情”，不会把零值冒充真实盈亏；有可信旧快照则保留并标明缓存来源。M15 守护器已重启加载新逻辑，M12.47 仍按设计在北京时间 `21:25` 自动预热，未手动运行 M12.37。
- 2026-07-11 最终稳定性修复已启用：M15 现运行模拟订单启用版配置，状态文件包含实际配置路径、配置指纹、进程号和运行编号；启动入口发现同一 M15 模拟守护器配置漂移时会安全替换旧进程，普通状态检查只报告异常。M12.47 已重启加载子进程进度心跳、最多 3 次过旧恢复和盘后 147 只日线最终刷新逻辑。Windows 当前用户无权注册计划任务，已实际安装无窗口的登录一次性启动项 `PriceActionTrader-M15-Startup.vbs`，它直接隐藏调用 WSL；旧 `300` 秒 PowerShell 循环文件和仍在运行的遗留循环进程均已移除，因此不会再周期性弹出终端。重启/登录后会自动启动，工作日 `21:10` 的额外 Windows 定时唤醒仍受权限限制。当前 M12.47、M15 实时守护器和后台看护器均存活；开盘值守为 `armed_waiting_regular_session`，只等待下个美股常规交易时段。
- 2026-07-11 看板口径与 Windows 自启修正：M12 长桥面板现在把 `当日盈亏 / 持仓今日浮动 / 当前持仓总盈亏 / 已实现估算 / 交易累计盈亏` 继续严格拆开，分仓表的“当日盈亏”不再回退混用累计已实现；账户状态过期或长桥字段退化时，相关盈亏卡片统一显示“等待长桥数据/等待长桥持仓行情”，不再同时保留陈旧数值。长桥面板新增“长桥真实账户敞口”和“实时提交流程”摘要，明确区分真实持仓标的与本地虚拟切片，并展示行情、合格信号、草稿、真实提交、有订单号、成交及阻断原因。Windows 自启安装脚本改为用户级隐藏计划任务，保留“登录时 + 工作日 21:10”两个单次触发器；若权限拒绝注册计划任务，只退化到隐藏的一次性登录启动项，不再回退到 `300` 秒循环。
- 2026-07-07 长桥单策略仓信号收紧与快速排序已落地：M15 实时路由器现在只用本轮候选已有字段做轻量质量排序，不额外请求长桥接口、不读取本地模拟账本；卖出仍优先，买入按质量分优先，避免弱信号先占满单策略仓。`PA004-long单仓（M10-PA-004-long-1d）` 已收紧为收盘位置至少 `0.60`、成交量至少前一日 `1.10x`，跳空守住型信号收盘位置至少 `0.65`；`FTD原版单仓（M12-FTD-001-baseline-1d）` 必须有 `SPY/QQQ` 大盘确认，并要求个股涨幅 `>=1.70%`、成交量 `>=1.15x`、收盘位置 `>=0.70`；`FTD连亏保护单仓（M12-FTD-001-loss-streak-guard-1d）` 要求个股涨幅 `>=2.00%`、成交量 `>=1.25x`、收盘位置 `>=0.75`。M15 执行器新增仓位压力门槛：单仓剩余额度低于 `1500 USD` 时只允许质量分 `>=80`，低于 `750 USD` 时只允许质量分 `>=90`，并继续保留剩余额度压缩为合规整股的逻辑。长桥看板新增质量排序、大盘确认阻断、质量门槛阻断和仓位压力阻断统计。验证已通过 `python -m py_compile` 覆盖 M15 路由/执行/看板，`tests.unit.test_m15_longbridge_realtime_signal_router` 与 `tests.unit.test_m15_longbridge_realtime_execution` 共 `71` 项通过，长桥看板关键口径单测通过；一次包含全量看板测试的组合 unittest 曾卡在不可中断 I/O，已终止后拆分验证。
- 2026-07-07 长桥 App 当日盈亏口径复核：用户截图显示 App 总资产 `102665.98`、现金 `80467.35`、持仓市值 `22198.63` 可用 `portfolio.total_cash + portfolio.market_accounts.US.market_value` 与 CLI 对齐；但截图 App 顶部当日盈亏 `231.54` 与当前 CLI 可见的 `profit-analysis` 同日 `sum_profit=63.45`、`portfolio.total_today_pl=331.454`、`profit-analysis by-market US=79.75`、`cash-flow` 费用口径均不一致。因此活跃看板不再把 `63.45` 标为“长桥账户当日盈亏”，改为“长桥接口净值日内变化”；新增“长桥App当日盈亏=等待长桥字段对齐”，直到找到与 App 完全一致的只读字段。`portfolio.total_today_pl` 继续只显示为“长桥接口持仓今日浮动”，不得冒充账户当日盈亏。同时保留 Longbridge CLI 偶发退化快照保护：当持仓成本价等于现价、`prev_close=0` 且盈亏为 `0` 时，保留上一份可信持仓盈亏快照，避免把看板盈亏刷成 `0`。本次只改只读账户状态和看板口径，不改变下单策略、不接实盘、不做真实资金动作。
- 2026-07-06 开机自启与盘中稳定性巡查修复：由于当前 WSL `systemd --user` 不可用，已新增 Windows 登录启动项 `C:\Users\hegua\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\PriceActionTrader-M15-Startup.cmd`，它会进入 `/home/hgl/projects/Price-Action-Trader` 并运行 `scripts/start_m15_trading_stack_after_boot.sh`，只负责拉起 M12.47、M15 长桥实时守护器和后台看护器，并执行 status/readiness 检查；仍不手动运行 M12.37 once-mode。巡查发现 M12.47 在 147 只全量扫描期间仍可能误杀正在刷新的 M12.37，已把 `m12_29_current_day_scan_dashboard` 刷新中的保活窗口提高到 `28800` 秒，避免 4 小时以上的健康长刷新被误杀。当前 `restart_count=20`、`stale_dashboard_restart_count=20` 是本次修复前的累计值，后续以今晚新会话是否继续增加为准。同时修复 M15 触发限价单被长桥拒绝的问题：当买入触发价已经等于或低于当前价时，执行器会在下单前自动降级为普通限价单，并记录 `trigger_limit_downgraded_to_limit_trigger_already_reached`，避免 “Trigger price cannot be equal to the current price” 导致漏挂。当前 M12.47、M15 实时守护器、后台看护器均存活，非交易时段 M12.37 子会话按设计暂停；M15 开盘值守 `10/11` 通过、`1` 项等待常规交易时段，状态为 `armed_waiting_regular_session`。
- 2026-07-03 长桥截图“融资欠款”核查与硬风控修复：复核当时的长桥模拟账户、订单对账和 M15 执行流水后，未发现融券做空证据：长桥 `short-margin` 返回空列表、当前持仓数量均为正、M15 已提交订单中 `sell_short/short` 为 `0`。截图里的异常是融资/保证金欠款口径，直接原因是模拟账户 `USD` 可用现金为负但 `HKD` 现金和总购买力较高，旧执行链路只看总现金/购买力，可能允许美股买入走融资。现已把长桥账户状态补充为分币种现金快照，并在 M15 实时执行、开盘值守、会话守护器和后台看护器中加入 `margin_financing=false` 硬边界；美股买入必须使用 `USD` 可用现金复核，订单会导致对应币种现金为负时阻断为 `blocked_margin_financing_disabled`。本条保留的是历史核查结论；当前唯一做空例外是本状态文件 2026-07-13 条目列出的三条受限纸面短仓，期权、实盘和真实资金仍未启用。
- 2026-07-02 二次巡检修复：发现 M15 配置已切到 `m15-single-strategy-buckets-20260702` 单策略单仓，但运行摘要仍停留在旧 `m15-dual-bucket-20260613`，导致看板可能继续展示旧 `main / experimental / pa002_main` 分仓。现已修复 M12 长桥看板分仓选择逻辑：当当前启用配置的 `test_epoch_id` 与执行产物旧基线不一致时，看板优先展示配置中的 `8` 个单策略仓和统一实验仓，并明确标记 `waiting_runtime_refresh`，旧基线交易只进入旧测试历史，不混入当前分仓统计。已重启 M15 长桥实时守护器和后台看护器；开盘值守检查为 `10` 项通过、`1` 项等待常规交易时段、`0` 项失败。
- 2026-07-02 长桥单策略单仓正式测试已落地：长桥券商侧仍只有一个 `lb_papertrading` 模拟账户，本地默认改为 `test_epoch_id=m15-single-strategy-buckets-20260702` 的单策略单仓复盘。第一批单仓为 `PA004-long单仓（M10-PA-004-long-1d）`、`PA002-5m单仓（M10-PA-002-5m）`、`FTD原版单仓（M12-FTD-001-baseline-1d）`、`FTD连亏保护单仓（M12-FTD-001-loss-streak-guard-1d）`、`PA004-MBF单仓（M10-PA-004-MBF-1d）`、`PA004-MBF-QC单仓（M10-PA-004-MBF-QC-1d）`、`PA013-5m单仓（M10-PA-013-5m）`、`PA011-ORB-R1单仓（M10-PA-011-ORB-R1-5m）`；每仓虚拟资金 `10000 USD`、总敞口 `6000 USD`、单标的 `1500 USD`、单笔风险 `20 USD`、现金保留 `4000 USD`，统一按 `1.0` 测试且不设所有单仓合计敞口。统一实验仓继续保留低倍率筛选 `M10-PA-002-1d`、`M10-PA-013-1d`、`M10-PA-008-1d`、`M10-PA-005-1d/5m`、`M10-PA-012-5m`、`M10-PA-001-1d`；旧 `main/experimental/pa002_main` 三仓方案仅作历史复盘，不再是默认长桥测试仓。M15 路由和执行配置、模拟订单启用配置、默认资金池兜底、长桥看板单仓复核和相关单测已同步；中文仓位展示必须带完整策略运行单元编号，避免只写 `FTD` 或 `002` 造成误读。
- 2026-07-02 长桥整股化数量处理与 FTD 接入修复：M15 实时信号路由器和实时执行器现在统一使用长桥整股化数量规范，`raw_quantity >= 1` 的小数建议数量会向下取整为 `submitted_quantity` 并重新计算单笔风险、敞口、扣费后利润和收益风险比，只有 `raw_quantity < 1` 才以 `blocked_quantity_below_one_share` 阻断；`blocked_fractional_disabled` 不再用于阻断 `24.0964 -> 24` 这类大于 1 股的小数数量。`M12-FTD-001-loss-streak-guard-1d` 已和 `M12-FTD-001-baseline-1d` 一起接入长桥实验仓，二者保持独立运行单元、独立归因和独立统计；其他 shadow / m14-modify / 非 FTD loss-streak 变体仍留在本地模拟。M12 长桥板块新增整股化取整、不足 1 股阻断、取整后风控阻断和做空未启用统计。
- 2026-07-02 巡查修复：M12.47 盘中熔断的直接原因不是长桥行情源失败，而是 M12.32 主看板复制了长桥未成交诊断里的原始 `order_id` 字段，触发只读看板 forbidden-output 扫描；现已改为主看板只显示“未成交-序号”，真实长桥订单号只保留在独立对账产物中。同步补齐 `config/examples/m15_opening_trade_readiness.paper_orders_enabled.json`，避免开盘值守找不到启用版配置后回退到普通 M15 守护器配置。当前复核：M15 长桥模拟账户值守 `11/11` 通过，`ready_for_longbridge_paper_orders`；M12.47 存活并在美股常规交易时段自动拉起 M12.37，未手动运行 M12.37 once-mode。
- 已完成 M14/M15 加速推进整改：M14 gate 改为 `runtime_id` 主键，`strategy_id` 仅作父策略分组；泛化 `continue_testing / waiting_for_review / 继续观察` 不再作为当前下一步输出，交易 runtime 统一改为 `advance_internal_sim`、`risk_limited_advance`、`repair_now`、`pause_runtime` 或 `paper_candidate`，非独立交易条目统一改为 `auxiliary_module`。
- 已固化辅助模块定位：`M10-PA-003/006/010/014/015/016` 和 `AI-TRADER-EXTERNAL` 现在在 M14 gate、M14 next-step matrix 和 M15 strategy execution preparation 中显示为“辅助模块：启用为某某用途，不作为独立交易策略”，不再作为独立交易 runtime 进入模拟账户。
- 已移除旧历史利润类显示链路：M12 JSON/CSV/HTML/Markdown writer 会递归剔除旧版历史利润类字段，当前活跃看板 artifact 已清理；M14 decision ledger 现在写当前规则快照，避免继续露出旧 `continue_testing / modify / reject / promote` 噪音。
- 已增强 Longbridge paper preflight：`scripts/run_m15_longbridge_paper_preflight.py` 只做 paper-only 前置检查，拒绝 live token、碎股、融券做空、期权、非美股常规时段、关闭 kill switch、非 `longbridge_quote_readonly` 行情来源，以及 M14 未重算到当前看板交易日的旧门禁。当前 M14 已重算到 `2026-06-01`，预演状态为 `ready_for_user_paper_credential_approval`；`11` 个非修复 runtime 可进入长桥模拟账户测试名单，不再只限制 `M10-PA-004 / M10-PA-005 / M10-PA-008`。仍未尝试凭证注入、broker 连接或订单提交，模拟账户令牌和首次提交必须等用户单独批准。
- 已新增 M15 长桥 OAuth 连接审计：`scripts/run_m15_longbridge_paper_connection_check.py` 现在按 OAuth 授权作为模拟账户入口，读取 `longbridge check` 和 `longbridge auth status` 的脱敏状态；当前 `connection_check_status=connected_oauth_user_confirmed_paper_account`，`paper_account_verified=true` 来自用户确认的 OAuth 模拟账户授权，`paper_account_marker_verified=false` 仅表示 CLI 未暴露账户类型字段。连接审计本身不读取资产、不读取持仓、不下单；模拟账户现金、持仓、挂单读取只在 M15 提交器账户状态里执行。
- 已将旧 M15 长桥模拟账户订单提交器降级为遗留路径：`scripts/run_m15_longbridge_paper_order_submitter.py` 仍可用于账户状态刷新、人工诊断和历史兼容，但不再是长桥实时下单热路径。旧提交器此前消费 `m15_longbridge_fast_signal_queue`，这个口径已被废弃，避免继续让本地模拟账本和本地平仓影响长桥模拟账户。
- 已新增长桥实时行情事件采集器：`scripts/run_m15_longbridge_realtime_market_event_ingestor.py` 和 `config/examples/m15_longbridge_realtime_market_event_ingestor.json` 第一版只调用 Longbridge CLI 只读 `kline` 命令，把 `147` 只种子池的 `1d / 5m` K 线落成 `m15_realtime_market_events.jsonl`。该采集器不读取账户、资产、持仓、订单、旧快速队列或本地模拟账本；CLI 缺失或 K 线失败时只写 deferred，不生成假行情。为降低延迟，实时轮询新增热名单优先和游标分批：总池仍是 `147` 只，但每轮默认只拉热名单加一段轮询子集。
- 已给长桥实时行情事件文件增加归档上限：`m15_realtime_market_events.jsonl` 超过配置上限后会把旧行情事件移入 `archive/`，当前文件只保留最近事件，避免长期运行后每轮路由解析百 MB 历史行情。2026-06-30 巡检时已把该文件从约 `115MB` 降到约 `9.7MB`；执行流水暂不截断，因为它参与订单去重、分仓归因和持仓恢复。
- 已修复 2026-06-16 交易时段长桥无订单的链路问题：现场确认 M15 守护器曾停在旧的等待交易窗口状态，重新拉起后又暴露出单个 `longbridge kline` 请求可能拖慢整轮的问题。现在 M15 行情采集器对每个长桥只读 K 线请求执行硬超时，超时后杀掉子进程组并跳过该标的；配置改为每轮 `8` 个标的（`6` 个热名单 + `2` 个轮询标的）、单次超时 `2` 秒、重试 `1` 次，避免一个慢行情请求卡死信号和下单链路。`run_m15_longbridge_realtime_session_supervisor.py` 的 watch loop 也增加顶层异常保护，异常会写状态并继续下一轮，不再直接退出。
- 已新增长桥模拟账户实时信号路由器：`scripts/run_m15_longbridge_realtime_signal_router.py` 和 `config/examples/m15_longbridge_realtime_signal_router.json` 只消费实时行情/新 K 线事件，输出 `m15_realtime_signal_events.jsonl` 给实时执行链路。该路由器明确写入 `local_simulation_isolated=true`、`local_ledger_input_ref=""`，不读取 `m12_46_account_trade_ledger.jsonl`、不读取旧快速队列、不使用本地平仓。修复策略、辅助模块和影子变体在信号生成阶段直接隔离，不再生成长桥待提交信号。第一版支持行情事件内嵌策略意图、`M10-PA-004-long-1d` 日线强势跟进检测器，以及 `price_action_realtime_v1` 通用实时价格行为检测器；后者已覆盖 `M10-PA-001-1d/5m`、`M10-PA-002-1d/5m`、`M10-PA-005-1d/5m`、`M10-PA-008-1d`、`M10-PA-012-5m`、`M10-PA-013-1d/5m`、`M12-FTD-001-baseline-1d`、`M12-FTD-001-loss-streak-guard-1d` 的白名单信号生成，FTD 两个版本在实验仓分开统计。同标的同方向多运行单元共振时只保留一条主信号，支持信号进入合并审计并提高主信号仓位上限，避免重复下单；跨资金池同标的信号不合并。当前 `M10-PA-002-1d` 原主力仓信号保持不变，同时复制到 `002专项主力仓` 做对照；`M10-PA-002-5m` 直接进入 `002专项主力仓` 做正常测试。
- 已优化 M15 实时信号路由热路径：路由器不再每轮读取 6 月以来全部历史行情做实时判断，而是只处理当前交易会话收到的行情事件、同标的/周期最多 20 条历史 K 线上下文，以及旧嵌入信号的阻断审计行。这样保留价格行为策略需要的前序 K 线，同时避免无关旧行情长期堆积拖慢下单链路。
- 已新增长桥模拟账户实时账户状态读取器：`scripts/run_m15_longbridge_realtime_account_state.py` 和 `config/examples/m15_longbridge_realtime_account_state.json` 只读取长桥模拟账户通道、现金、购买力、持仓和未完成挂单，输出 `m15_longbridge_realtime_account_state.json` 给实时执行链路；不提交、不撤单、不读取本地模拟账户。
- 已新增长桥模拟账户旧买入挂单清理器：`scripts/run_m15_longbridge_realtime_stale_order_cleanup.py` 和 `config/examples/m15_longbridge_realtime_stale_order_cleanup.json` 只处理上一交易窗口遗留的长桥模拟账户买入挂单；它必须先确认 `lb_papertrading`、模拟账户已验证、实盘和真实资金边界关闭，且禁止撤卖出保护单。M15 实时守护器现在按“账户状态读取 -> 旧买入挂单清理 -> 账户状态复读 -> 持仓退出管理 -> 新信号执行”的顺序运行，避免昨晚遗留买入挂单继续占用今晚新信号额度或被误解为新订单。
- 已新增长桥模拟账户实时持仓退出管理：`scripts/run_m15_longbridge_realtime_position_manager.py` 和 `config/examples/m15_longbridge_realtime_position_manager.json` 只看长桥账户持仓、长桥实时行情和长桥实时执行记录里的止损/目标价，触发后生成卖出已有多头的平仓信号；本地模拟平仓不会触发长桥平仓，卖出不会变成做空。长桥账户已有但没有本轮 M15 开仓元数据的持仓现在按“只接管退出”管理：不迁移成本地策略、不参与新开仓或加仓，但可以按长桥账户自身成本价和实时价格生成止损/止盈退出计划。
- 已新增长桥模拟账户实时执行链路：`scripts/run_m15_longbridge_realtime_execution.py` 和 `config/examples/m15_longbridge_realtime_execution.json` 只消费实时信号事件与长桥模拟账户自身状态，输出 `m15_longbridge_realtime_execution.json/jsonl/md`。该链路明确写入 `local_simulation_isolated=true`、`local_ledger_input_ref=""`，不读取 `m12_46_account_trade_ledger.jsonl`，不使用本地平仓阻断长桥开仓，不把 M13/M14 完整重算作为每笔下单前置。实时执行会检查长桥账户已有持仓、挂单、现金和敞口，新开仓不能重复买已有持仓或已有挂单标的；平仓只能卖出已有多头。实时信号到本地发起下单请求的目标为 `1` 秒内，第一版 `5` 秒内可接受；超过 `5` 秒会记录延迟异常并重新校验当前价格、风控和扣费后利润，但超过 `max_delayed_signal_age_seconds` 或信号自身有效期的旧信号会被阻断，不再停机后补交旧信号。真实提交适配器已存在但默认关闭，必须显式模拟账户批准后才调用长桥模拟订单命令。
- 已补强 M15 实时执行热路径：执行器现在只处理未处理过的实时 `signal_id`，不会每轮重新扫描并追加同一批旧信号的阻断流水；如果条件后来仍然成立，必须由新的长桥行情事件生成新的实时信号。当前现场复核显示旧 609 条实时信号被跳过，执行流水没有继续增长。
- 已补强长桥实时执行的重复提交保护：实时执行链路现在不仅看长桥账户持仓和未完成挂单，也会读取自己的实时提交账本，把本交易日已提交但账户状态尚未刷新出来的买单计入待成交敞口；同一标的在账户状态刷新前不会重复买入，避免接口状态延迟造成重复开仓。若已提交订单已经体现在长桥账户持仓或挂单里，则不再被提交账本重复计入敞口，避免过度阻断。执行层还新增同轮同标的保护：同一轮已经选择了某个标的的买单后，后续同标的买入会被阻断，防止不同运行单元在同一轮重复下单。
- 已修复长桥虚拟资金池占用误算：M15 实时执行现在会用长桥账户 `orders / historical_orders / open_orders` 的最新订单状态校正提交账本，已取消、拒绝或过期且未成交的买单不再占用主力仓/实验仓；已成交且长桥仍有持仓的买单继续占用对应资金池；卖出成交会按数量释放本地虚拟敞口。修复后主力仓占用从旧口径 `7411.89` 收敛到 `6031.58`，剩余略超 `6000` 是当前实际持仓占用，不是取消单误算；因此主力仓新增买入会继续受总敞口上限保护，实验仓正常。
- 已修复长桥实时风控只看日内敞口的缺口：现场发现主力仓已有隔夜持仓占用时，执行器仍只按本交易日提交记录做总敞口检查，导致 `RIVN / LCID` 两笔主力仓模拟买单在主力仓已经接近/超过 `6000` 上限时被提交并成交。现在实时风控和看板摘要统一按当前测试基线全量持仓/挂单计算总敞口；新增回归测试覆盖“前一交易日成交且仍持有的仓位必须计入今日总敞口”。当前这两笔已是长桥模拟账户持仓，后续由持仓退出管理处理；修复后主力仓在释放敞口前会阻断新增买入，实验仓仍按自身上限独立运行。
- 已新增长桥模拟账户实时会话守护器：`scripts/run_m15_longbridge_realtime_session_supervisor.py` 和 `config/examples/m15_longbridge_realtime_session_supervisor.json` 只在美股常规交易时段按顺序运行实时行情采集、实时信号路由、实时账户状态、旧买入挂单清理、清理后账户状态复读、实时持仓退出和实时执行链路；非交易窗口只写等待状态。该守护器输出 `m15_longbridge_realtime_session_supervisor.json/jsonl/md`，明确记录 `legacy_fast_queue_used=false`、`manual_m12_37_once_used=false`、`local_ledger_input_ref=""`，并在连续失败达到阈值后熔断，避免异常时反复提交或反复拉起。
- 已给 M15 实时会话守护器流水增加保留上限：守护器状态流水是审计日志，不参与下单去重、持仓归因或风控恢复；超过上限时自动归档旧行并保留最近 `5000` 行。2026-06-25 巡检时，`m15_longbridge_realtime_session_supervisor_ledger.jsonl` 已从约 `332MB` 降到约 `16MB`，旧流水归档到运行产物目录，未改变执行流水、信号事件或账户状态语义。
- 已给 M15 实时会话守护器文本日志增加保留上限和等待状态静默输出：守护器启动前会把超过上限的 `m15_longbridge_realtime_session_supervisor.log` 归档到运行产物 `archive/` 目录；非交易窗口如果只是重复等待常规交易时段，不再每轮写相同日志。2026-06-26 巡检发现该文本日志已增长到约 `37MB`，该修复只影响人工排查日志体积，不改变长桥实时执行、风控、下单去重、账户状态或信号事件语义。
- 已优化 M15 非交易窗口巡检：长桥实时守护器在非美股常规交易时段仍不运行交易循环、不提交订单，但会只读刷新长桥模拟账户状态、盈亏对账和 M12.32 长桥面板，避免账户总盈亏、今日盈亏、持仓和挂单停留在昨晚旧快照。若只读刷新失败，会保留上一有效账户状态并写明 `account_state_refresh_failed`，不把本地模拟数据拿来补算。
- 已新增开盘值守配置与验收入口：默认实时执行配置继续保持安全只读演练；`config/examples/m15_longbridge_realtime_execution.paper_orders_enabled.json` 与 `config/examples/m15_longbridge_realtime_session_supervisor.paper_orders_enabled.json` 是本轮用户批准后的长桥模拟账户订单启用版，只允许 `lb_papertrading`、美股常规交易时段、整股、只做多、普通限价/突破触发限价、日内单，不允许碎股、融券做空、期权、实盘或真实资金。`scripts/run_m15_opening_trade_readiness.py` 会输出开盘值守验收结果，确认 M12.47、M15 实时守护器、模拟账户、模拟下单开关、安全边界和修复策略隔离状态。
- 已修复 M15 开盘值守和状态输出误导：`scripts/run_m15_opening_trade_readiness.py` 现在会明确显示 M15 实时守护器 PID 是否存活、状态文件生成时间和状态年龄；`scripts/run_m15_longbridge_realtime_session_supervisor.py --status` 也会同时显示进程存活、状态文件年龄、状态内市场窗口和当前真实市场窗口。2026-06-12 盘中排查确认，6 月 10-12 日无新长桥订单的直接原因是 M15 实时守护器在 6 月 9 日盘后停止后未被重新拉起；已用模拟订单启用版重新启动，开盘值守当前为 `ready_for_longbridge_paper_orders`。重新拉起后的首轮链路新增 `100` 条长桥只读行情事件，但实时信号路由新信号为 `0`，因此没有提交新订单；这属于本轮信号/风控结果，不是长桥账户连接或下单接口故障。
- 已修复 M12.47 读取热更新看板时的半写入崩溃问题：2026-06-12 盘中现场发现 M12.47 在 M12.37 重写 `m12_32_minute_readonly_dashboard_data.json` 时读到半写 JSON，触发 `JSONDecodeError` 并退出，导致看板后续不刷新；现在 `read_json_if_exists` 会把读取中的 `ValueError` 视为临时不可读，返回空并等待下一轮，守护器不会被半写文件打死。同步把 M12.28 旧交易时段看板 JSON/HTML/Markdown 输出改为原子写，降低读到半文件的概率。已重新启动 M12.47 与 M15 模拟订单启用版守护器；当前 M12.47 存活、M12.37 子会话由守护器托管刷新，M15 开盘值守 `11/11` 通过。
- 已优化 M12.37 交易时段刷新节奏：同一个 5 分钟 K 线桶内不再每分钟重跑本地完整 M12.29 看板，而是写入 `light_heartbeat_waiting_next_5m_bar` 轻量心跳；新的 5 分钟 K 线桶到来后才重新执行重型本地看板刷新。M12.47 看板覆盖已识别轻量心跳并用中文显示“等待下一根 5 分钟 K 线再重算本地完整看板”，避免把正常轻量等待误判为看板不刷新。长桥 M15 实时链路继续独立运行，不依赖 M12/M13/M14 完整重算。
- 已修复 M12.47 对 M12.37 长刷新误杀的问题：现场巡检发现 147 只扫描 + M13/M14 后置重算时，M12.47 仍按 `600` 秒核心看板旧阈值重启 M12.37，导致 `stale_dashboard_restart_count=2`。现在 `m12_29_current_day_scan_dashboard` 刷新中的宽限提高到 `5400` 秒，并且只有 manifest 明确处于 `refresh_in_progress` 且属于当前子会话时才用近期 artifact 活动作为健康证据；普通 cache 写入不会掩盖真正卡死。已重启 M12.47，当前 M12.47 PID `950385`、M12.37 子会话 PID `950387`，`restart_count=0`、`stale_dashboard_restart_count=0`。
- 已修正 M12.47 等待开盘前预热的看板文案：交易日前等待预热时不再显示“非交易日审计快照”，而是显示“等待开盘前预热：保留上一有效快照，未进入新交易日刷新窗口”，避免把正常等待误判为数据源降级或非交易日。
- 当前开盘值守状态：北京时间 `2026-06-16 22:23` 复核时，M12.47 守护器已重新拉起且 M12.37 子会话由守护器自动刷新，M15 长桥实时守护器 PID `26248` 存活，当前处于美股常规交易时段。开盘值守 `11/11` 通过，状态为 `ready_for_longbridge_paper_orders`；长桥模拟账户确认 `lb_papertrading`、当前持仓 `0`、未完成挂单 `0`。最新 M15 交易轮完成，新增长桥只读行情事件 `19` 条，信号路由新增可执行信号 `0`，执行器 ready `0`、submitted `0`；当前无订单的原因是没有合格新信号，而不是账户连接、模拟下单开关或守护器链路故障。
- 已把 M15 长桥模拟账户快速新信号队列降级为审计/历史兼容产物：`scripts/run_m15_longbridge_fast_signal_queue.py` 仍可用于分析本地模拟账本里的重复信号、共振、费用门槛和旧快照问题，但不再给长桥实时链路提供待提交订单。本地账本同一运行单元、同一标的的后续平仓只影响旧队列审计，不影响长桥模拟账户实时开仓。
- 已把长桥模拟账户状态接入 M12.32 主看板：`m12_32_minute_readonly_dashboard_data.json/html` 现在新增“长桥模拟账户”面板，读取 M15 实时模拟账户状态、实时守护器、实时行情、实时信号、实时执行和 OAuth 连接审计，展示模拟账户是否连接、可用购买力、持仓数、挂单数、实时链路状态、资金模型和接口延迟。旧快速队列不再作为主看板交易状态展示，只保留为本地历史审计产物，避免误读为长桥当前待下单来源。M12.47 状态覆盖重写 HTML 时也会同步刷新该面板，避免交易时段回写成旧看板结构。该面板只做状态展示，仍按 `10000 USD` 共享资金模型控仓，不迁移本地旧模拟持仓，不展示敏感凭证，不启用实盘或真实资金动作。
- 已把 M15 长桥实时守护器接入 M12 主看板同步：每轮 M15 实时链路完成后，会只更新 M12 看板里的“长桥模拟账户”面板和顶部两个长桥指标，不触发 M12.37、不读取本地模拟账本、不改变 M13/M14 结果。实时链路可用时，看板不再显示旧提交器的 `not_us_regular_session` 等遗留阻断，并会按 M15 实时提交流水统计当天已提交模拟订单数。
- 已修复 M12 主看板里长桥板块“看起来没刷新”的误导：长桥模拟账户面板现在写入独立 `generated_at`、账户状态刷新时间和盈亏对账刷新时间，并在状态表显示“长桥面板刷新时间”。非交易窗口文案从“上一轮审计快照”改为“账户状态已只读刷新，交易循环等待下一交易日自动运行”，避免把本地模拟大看板时间和长桥账户状态时间混在一起。
- 已修复长桥看板非交易窗口误判过期：当 M15 实时守护器处于等待下一交易日/非交易窗口时，M12.32 长桥面板会保留上一轮有效的长桥实时审计快照，继续显示模拟账户自身的持仓、挂单、实时行情采集、实时信号和持仓退出状态，不再把它误写成“账户状态待刷新”。旧顶部指标“长桥可提交订单”已清除，统一改为“长桥本轮可新开仓”。非交易窗口即使有上一交易窗口提交记录，也会明确写成“上一轮长桥审计快照”，避免误解为当前正在提交。
- 已修复 M15 提交后账户状态滞后：提交器在成功向长桥模拟账户提交订单后，会立刻重新读取模拟账户资产、持仓和挂单，并把本轮已提交信号指纹写入 `m15_longbridge_paper_account_state.json`。这样看板会把未成交订单显示为挂单，而不会继续显示提交前的 `0` 持仓、`0` 挂单状态。
- 已修复长桥模拟账户看板成交后显示旧挂单的问题：M12.32 长桥面板现在会检查 M15 账户状态和提交器状态的交易日及生成时间，超过 `300` 秒未刷新的状态会显示“账户状态待刷新”，不再继续展示旧的持仓/挂单数字。当前只读刷新后，长桥模拟账户面板已显示 `2` 个持仓（`AAPL / XLU`）和 `0` 个挂单。
- 已修复长桥实时链路提交口径和看板盈亏口径：长桥面板不再使用“长桥今日盈亏 / 长桥总盈亏”这类模糊字段，统一拆成“长桥App当日盈亏”“长桥接口净值日内变化”“长桥接口持仓今日浮动”“长桥当前持仓总盈亏”“长桥交易累计盈亏”；`Longbridge CLI` 返回空 JSON 或无订单号时会立刻反查长桥订单列表，能匹配到同标的、同方向、同数量、同价格订单就确认提交，找不到才记为未确认请求。`TEAM / TSLA` 这类可卖数量为 `0` 的重复平仓请求已被阻断，平仓信号 ID 也改为稳定指纹，避免每轮重复生成卖单。
- 已固定长桥模拟账户盈亏主口径：M12.32 长桥面板优先读取长桥只读 `profit-analysis / portfolio / positions / executions / orders` 对账结果；“长桥App当日盈亏”只有找到与 App 完全一致的接口字段才显示数值，否则显示“等待长桥字段对齐”；“长桥接口净值日内变化”用同日 `profit-analysis` 的 `sum_profit`；“长桥接口持仓今日浮动”只用 `portfolio.total_today_pl`；“长桥当前持仓总盈亏”只用当前持仓成本价/数量/当前价计算；“长桥交易累计盈亏”只用美股交易盈亏汇总。长桥接口缺数据时显示“无法计算/等待长桥数据”，禁止用 `100000`、旧本地模拟账本或旧 `historical_net_profit` 硬算。看板新增逐标的盈亏和逐策略成交盈亏归因；同一标的多策略参与时写成分摊估算。
- 已把长桥模拟账户交易质量指标加入 M12.32 长桥面板并修正成交胜率口径：顶部卡片和“账户与执行”详情现在显示“长桥逐标的胜率”“长桥交易胜率”“长桥盈亏比”“长桥回撤代理”。逐标的胜率来自长桥只读 `profit-analysis` 的逐标的盈亏，按盈利标的数 / 有盈亏标的数计算；长桥交易胜率来自 `longbridge order executions --history` 历史成交明细，按 FIFO 配对后的已平仓买入批次计算，默认今日订单列表只用于当前挂单/状态，不再当作总成交。当前核对区间 `2026-06-01 ~ 2026-06-09` 读取到历史订单 `503` 条、历史成交 `59` 条、可配对已完成买入批次 `24` 个，胜率 `12.50%`，配对盈亏 `-159.17`；看板已清理旧“长桥配对成交胜率/已配对成交胜率”显示。盈亏比按平均盈利 / 平均亏损计算。单标的亏损率仍显示为“回撤代理”，不得解释成完整资金曲线峰值回撤。该口径不读取本地模拟账本，不使用旧历史净利润字段。
- 已把本地模拟历史质量固定进 M12.32 本地模拟看板：新增 `local_simulation_history_quality`，只统计 `m12_46_account_trade_ledger.jsonl` 中已平仓本地模拟记录，按总体、主线策略、实验策略、修复/救援策略、`1d/5m` 和运行单元输出胜率、平均盈利、平均亏损、盈亏比、盈利因子和累计已实现盈亏；该板块只服务本地模拟复盘，不参与长桥实时下单链路。
- 已新增长桥模拟账户独立权益曲线与完整最大回撤口径：`scripts/run_m15_longbridge_realtime_account_state.py` 每次只读账户状态时会追加 `m15_longbridge_realtime_equity_curve.jsonl`，记录长桥模拟账户总净值、现金、持仓市值、挂单金额和账户通道；M12.32 长桥面板新增“长桥完整最大回撤”，按这条权益曲线逐点计算峰值到谷值回撤。当前权益曲线已有 `2` 个真实快照，账户总净值均为 `102307.19`，所以完整最大回撤为 `0.00%`；后续交易时段会随着 M15 账户状态刷新持续累计。单标的亏损率仍保留为“回撤代理”，但不再替代完整最大回撤。
- 已提高长桥实时信号质量门槛：扣费后预计净利润低于当前运行单元门槛的实时信号只记录不提交；全局最低 `5 USD`，普通可交易信号最低 `8 USD`，`M10-PA-001` 等弱策略最低 `12 USD`。目标利润/止损风险普通信号必须 `>= 1.5`，弱策略必须 `>= 2.0`；不足时直接阻断，不强行拉高目标价。M15 实时路由器和执行器都写入低利润阻断、盈亏比阻断和对应看板计数，避免把这些阻断误解成漏单。
- 已落地长桥弱策略限权：`M10-PA-004-long-1d` 作为主交易策略，`M10-PA-013`、`M10-PA-002-1d`、`M10-PA-005`、`M12-FTD-001-baseline-1d` 等为严格小仓位测试，`M10-PA-002-5m` 已提升到 `002专项主力仓` 做正常测试，`M10-PA-001-5m` 回到本地修复，不再生成长桥买入订单。长桥看板新增“策略质量与限权”，按长桥历史成交和实时提交流水匹配运行单元，默认展示修复前/修复后样本，不读取本地模拟账本。
- 已补强长桥实时延迟、冷却和退出状态机：超过 `5` 秒的旧实时信号不再直接补交，必须由当前行情重建新的实时信号意图；同一标的已有长桥持仓、买入挂单、卖出挂单、本交易日已提交买单或当天亏损卖出后都会阻断重复开仓；`M10-PA-001` 先限制为每天最多 `1` 个新标的。卖出请求新增 `exit_state`，只允许卖长桥账户当前可卖数量，已有同标的卖出挂单或已提交卖出流水时不重复提交。
- 已修复长桥实时守护器等待窗口状态误报：非交易窗口不运行交易循环时，M15 守护器会复用最近一次长桥模拟账户状态，只读显示 `paper_account_verified`、持仓和挂单数量，不再误写成账户未验证。M12.32 长桥面板也会把旧产物中的非系统元数据持仓统一显示为“只接管退出待实时复核”，当前面板为 `10` 个系统管理持仓、`3` 个只接管退出持仓、`0` 个未接管退出持仓。
- 已补强长桥卖出防重复的执行层兜底：M15 实时执行器现在对卖出信号额外检查长桥账户自己的可卖数量和同标的已有卖出挂单；如果 `available=0` 或已经有 `Sell` 挂单，直接写入 `blocked_close_quantity_not_available` / `blocked_existing_sell_open_order_same_symbol`，不再向长桥模拟账户重复提交卖出订单。该修复只影响长桥模拟账户执行链路，不读取本地模拟平仓，也不启用实盘。
- 已修复看板半写文件导致“不刷新/读坏 JSON”的稳定性问题：M12.32 核心 JSON/JSONL、M12.47 HTML overlay、M15 实时状态 JSON/JSONL 和 M15 写回 M12 看板都改为临时文件原子替换；看板读取 JSONL 时会跳过最后一条半写入行，不再因为守护器并发写入而整页失败。
- 已修复长桥数据源偶发降级的主要路径：M12.28 长桥 quote 与 M15 实时 K 线采集都增加短重试，M12.47 读取 M12.37 正在写入的 dashboard/manifest 时做安全重试；当前自检显示 M12.32 `quote_source=longbridge_quote_readonly`、`quote_count=147`、`quote_error=""`。
- 已修复看板“数据源降级”误报：M12.32 现在区分“真实备用行情 / 禁止抓取 / no-fetch”和“长桥行情正常但严格全量扫描口径未完成”。当 `quote_source=longbridge_quote_readonly` 且没有 quote error 时，不再把日线缓存沿用或非交易窗口审计快照写成 fallback/no-fetch。M12.47 非交易窗口也会把旧 `refresh_in_progress` manifest 覆盖成 `idle_waiting_market_window`，避免看板误显示 M12.37 仍在刷新。
- 已修复 M12.47 status 旧 pid 误报：停止旧守护器并启动新守护器后，`--status` 现在优先使用仍存活的 pid 文件，而不是旧 status JSON 里的旧 pid，避免出现“后台进程明明存在但看板/状态显示自动调度器没有运行”的误判。
- 已增强长桥模拟账户旧买入挂单清理：除上一交易窗口遗留买入挂单外，当前交易日超过 `900` 秒仍未成交的模拟账户买入挂单也会撤销以释放项目资金模型敞口；卖出保护单仍禁止撤销。最新现场复核已撤销 `CMG` 超时买入挂单，随后 `KMB` 买入挂单成交，长桥面板收敛为 `12` 个持仓、`2` 个挂单，剩余挂单均为 `TEAM / TSLA` 卖出保护单。
- 已修复今晚模拟账户提交前的 M14 盘中复核卡点：`config/examples/m12_37_intraday_auto_loop.json` 的 `m14_finalize_policy` 已从 `postmarket_only` 改为 `postmarket_or_runtime_ready`。现在 M12.47 拉起 M12.37 后，只要当天行情和账户化运行数据准备好，M13/M14 可以在常规交易时段完成当天重算，M15 才能用当天新开仓信号进入长桥模拟账户提交链路；仍禁止手动 M12.37 once-mode，仍不迁移旧本地模拟订单。
- 已增强 M15 strategy execution preparation：`scripts/run_m15_strategy_execution_preparation.py` 生成周一前准备计划，第一批内部模拟固定为 `M10-PA-004 / M10-PA-005 / M10-PA-008`，第二批候选、修复队列、辅助模块和 Longbridge paper 进入条件均已写成可审计 JSON/Markdown。修复队列现在逐条写明 `M10-PA-001-1d/5m`、`M10-PA-004-MBF-QC-1d`、`M10-PA-007-1d`、`M10-PA-009-1d`、`M10-PA-011-5m`、`M12-FTD-001 baseline/loss-streak-guard` 的修复动作、验收条件、仓位倍率和长桥模拟账户阻断状态；`M10-PA-002-5m` 当前已从修复队列提升到 002 专项长桥测试。
- 已把 M15 修复计划落到 M12 账户化实际运行链路：`scripts/m12_29_current_day_scan_dashboard_lib.py` 现在按 runtime 写入仓位倍率、收益风险比、止损距离、假突破确认、图形上下文和连续亏损暂停。`M10-PA-001-1d/5m`、`M10-PA-004-MBF-QC-1d`、`M10-PA-005-1d/5m`、`M10-PA-007-1d`、`M10-PA-008-1d`、`M10-PA-009-1d`、`M10-PA-011-5m/ORB-R1-5m`、`M10-PA-012-5m`、`M10-PA-013-1d/5m`、`M12-FTD-001 baseline/loss-streak-guard` 已不再只是报告里的修复队列；`M10-PA-002-5m` 已单独提升到 002 专项长桥测试。下一次 M12.47 fresh refresh 时会直接用这些规则决定开仓、跳过或风控标记。周一前不能生成新 M13/M14 行情账本，验收仍等待 M12.47 在美股常规交易时段自动拉起 fresh refresh。
- 已新增 M15 Monday refresh acceptance：`scripts/run_m15_monday_refresh_acceptance.py` 只读 M12.47/M12.37/M13/M14/M15 产物，输出交易窗口验收清单；验收口径已从固定 50 只改为当前 active universe，默认要求 `147` 只种子池日线与当日 5m 完整。该验收只表示“本地只读预演链路可继续推进”，不等于允许实盘或真实资金动作。
- 已新增 M15 今日利润质量审计：`scripts/run_m15_profit_quality_audit.py` 只读 M12.32 看板与 M12.46 账户运行状态，输出 `m15_profit_quality_audit.json/md`。最新审计显示 `2026-06-01` 合计内部模拟今日盈亏 `10023.48`，其中今日平仓 `8658.66`、当前未平仓浮盈浮亏 `1364.82`；利润来源是长桥只读行情估值下的内部模拟，不是账户现金利润、不是长桥模拟账户成交、不是实盘。大额收益主要来自旧持仓今天平仓，尤其 `SNOW / ORCL / INTC`，因此不能把今日高利润理解为今天新信号直接赚到；进入长桥模拟账户仍必须等 M14 当天正式重算和用户单独批准。
- 已新增 M15 全策略长桥模拟账户订单预演：`scripts/run_m15_all_strategy_order_preview.py` 只读 M12 账户化开平仓、M13 当天账本和 M14 gate，把所有交易运行单元的本地模拟开仓/平仓转成长桥模拟账户格式的订单草稿，并输出 `m15_all_strategy_order_preview.json/md/jsonl`。辅助模块不生成订单，修复运行单元继续生成本地草稿但标记为不得提交。当前按 `10000 USD` 共享模拟资金、整股、只做多、普通限价/突破触发限价、单日最多 `6` 笔规则筛选；做空开仓、无对应模拟账户持仓的平仓、数量不足 `1` 股、超仓位或超风险订单只保留本地草稿。该预演只用于本地记录和稳定性对比，不连接长桥账户、不读取凭证、不提交订单。
- 已修复 M14/M15 推进口径回归：`scripts/m14_strategy_next_step_readiness_matrix_lib.py` 现在会保留来自 M14 gate 的 `risk_limited_advance`，不会因为还存在影子参数/风控复核就把盈利但高风险 runtime 改写成 `repair_now`；`scripts/m14_strategy_challenge_gate_lib.py` 现在不会让旧的 audit-only degraded quote day 阻断当前已满足有效 10 日窗口的 paper preflight 候选。已补单元测试覆盖这两个口径。
- 已修复 M12.37/M12.47 面板假日误判：`M12.29` 和 `M12.37` 现在共用配置化美股休市日历，`2026-05-25` Memorial Day 不再显示为常规交易时段，也不会在休市日误跑 M13 ledger；M12.47 `--status` 会把现有静态看板 overlay 成“非交易日等待 / audit-only 快照”。
- 已修复 M12.37/M12.47 盘中看板刷新状态误导：M12.37 每轮开始会先写 `refresh_in_progress` 进度 manifest，完成后写 `refresh_complete`，失败时写明错误类型；M12.47 主看板 overlay 现在明确区分“核心看板生成时间”和“守护器心跳时间”，刷新中会显示 `refreshing_stale / M12.37 正在刷新中`，不再把心跳时间伪装成核心数据刷新。M12.47 的滞后重启保护也会识别本轮有效刷新进度，避免 147 只股票全量刷新过程中被 600 秒旧阈值反复误杀；只有刷新进度本身超时停住才会重启子会话。
- 已修复 M14 gate 与券商式主面板同步滞后：M12.47 `--status` 现在会刷新主面板里的 M14 进度、内部模拟准入数量和每条策略账户的 gate/decision；M14 `goal_status` 也直接写入 `challenge_progress_label`、`paper_trial_gate_approved_count` 和 approved ids，避免心跳汇报继续读到旧的 `approved 0`。
- 已修复券商式主面板 PA004 对照区同步漏网：M12.47 overlay 现在同时刷新 `strategy_accounts` 与 `pa004_comparison.rows`，避免 PA004 在账户列表显示 `approved_internal_sim_only`、但对照表仍显示旧 `not_approved_challenge_incomplete`。同时修正自选股信号数/策略列表错位，以及空旧版利润字段显示成裸 `%` 的表格问题。
- 已修复券商式主面板非交易日误报：M12.47 audit-only overlay 不再把休市/等待窗口渲染成“数据刷新告警”，改为“非交易日审计快照”；顶部指标补充守护器状态时间和数据快照状态，自选股表格补回“总PnL”列。
- 已补强 M12.47 非交易日面板摘要：audit-only overlay 现在会覆盖旧的 `plain_language_result`，避免休市日继续展示“50 只股票 0/50 缺口”并误导为行情刷新故障；M13 状态也会标记为 `audit_only_snapshot`，不把假日旧账本当作新的交易日进展。
- 已新增 M14 策略救援计划入口：`scripts/run_m14_strategy_rescue_plan.py` 会从 M14 summary/gate/decision ledger 生成 `m14_strategy_rescue_plan.json/md`，把获批策略、亏损待救策略、零信号 detector 重建、plugin/research 策略分 lane 管理；AI-Trader 与 TradingAgents 只作为影子信号/多角色诊断参考，不允许 copy-trading 或绕过本地 M13/M14 gate。
- 已把首批 M14 救援变体登记进 M13 runtime registry：`M10-PA-001/002/007/009/012/013`、`M12-FTD-001`、`PA004-MBF-QC` 和 `M10-PA-011-ORB-R1` 现在会进入每日 ledger；未接线变体保持 `not_connected`、`lane=rescue`、`required_for_goal=false`，不会覆盖 baseline，也不会伪装成已测试通过。
- 已接上 M14 rescue 最小输入流：`M10-PA-001-m14-modify-20260522`、`M10-PA-002-m14-modify-20260522`、`M10-PA-004-MBF-QC-m14-modify-20260522`、`M10-PA-007-m14-modify-20260522`、`M10-PA-008-broker-risk-cap-shadow`、`M10-PA-009-m14-modify-20260522`、`M10-PA-012-m14-modify-20260522`、`M10-PA-013-m14-modify-20260522`、`M12-FTD-001-m14-modify-20260522`、`M10-PA-011-ORB-R1` 现在进入 M12 accountized runtime 的 `rescue` lane；其中 PA004-MBF-QC rescue 使用更严格的风险压缩 adapter，PA008 broker-risk-cap shadow 只测试单笔最大风险 `100.00` 的数量上限，ORB-R1 使用 5m 失败开盘区间突破后回测确认 adapter，其余复用父策略扫描器并增加质量过滤；当前已登记 rescue runtime 均已接上输入流，后续不得伪装为已通过，只能继续用 10 日 A/B 数据判断。
- 已新增 M14 rescue runtime 覆盖审计：`scripts/run_m14_rescue_runtime_coverage.py` 会只读 M13 registry、M14 rescue plan 与 M12 account specs，生成 `m14_rescue_runtime_coverage.json/md`；当前登记 rescue runtime 为 `11/11` 策略、`12` 个账户全部接线，计划 rescue/rebuild 动作为 `10/10` 覆盖，但状态明确是 `connected_not_promoted`，仍需 10 个交易日 A/B ledger 才能 promote / modify / reject。
- 已补齐 M14.2 broker readiness dry-run artifact 生成器：`scripts/run_m14_2_broker_readiness_scaffold.py` 现在只读 `m14_paper_trial_gate.json` 与 `m14_internal_paper_execution_ledger.jsonl`，把内部模拟 `risk_check` 事件转成 `broker_readiness_plan.json` 与 `broker_readiness_audit.jsonl`；当前真实样本去重后为 `8` 条内部模拟风控检查、`5` 条 dry-run ready、`3` 条被风控阻断，仍未连接 broker、未读取凭证、未生成真实订单。
- 已新增 M14.2 broker blocker diagnostics：`scripts/run_m14_2_broker_blocker_diagnostics.py` 会只读 broker readiness plan、M14 challenge 风控配置和内部模拟执行账本，生成 `broker_blocker_diagnostics.json/md`；当前 `3` 条 blocked dry-run rows 分布在 `M10-PA-005 / M10-PA-008`，其中 `M10-PA-008` 属于单笔风险略超 `100` 的 quantity cap 影子修复候选，`M10-PA-005` 属于总敞口排序与连续亏损 cooldown/quality veto 影子修复候选。诊断不改变 readiness status，不把 blocked row 升级为 ready，也不允许 broker/live。
- 已新增 M14.2 broker blocker shadow repair plan：`scripts/run_m14_2_broker_blocker_shadow_repair.py` 会只读 blocker diagnostics，生成 `broker_blocker_shadow_repair.json/md`；当前 `M10-PA-008 / ADBE` 是唯一 quantity-cap 影子 A/B 候选，数量从 `5.2469` 降至 `5.2083` 后风险从 `100.74` 压回 `100`，而 `M10-PA-005 / XLY` 应延后等待敞口或由排序器让路，`M10-PA-005 / XLV` 必须保留连续亏损暂停并只测试 cooldown/quality veto。原 dry-run rows 全部仍保持 blocked，不允许 broker/live。
- 已把 M14.2 broker blocker shadow repair 接入 M14 goal readiness：`m14_goal_readiness_report.json/md` 现在在单一验收视图中显示 `1` 个 quantity-cap 候选、`1` 个敞口延后、`1` 个 cooldown 暂停，并把“只作为内部模拟 A/B 准备、原 readiness rows 仍保持 blocked”写入 next actions；当前 `readiness_status_mutation=false`，不允许 broker/live。
- 已新增 M14.2 broker blocker shadow A/B prep：`scripts/run_m14_2_broker_blocker_shadow_ab_prep.py` 会只读 `broker_blocker_shadow_repair.json` 生成 `broker_blocker_shadow_ab_prep.json/md`；当前 `3` 条 repair rows 被拆成 `1` 个 PA008 quantity-cap 影子 runtime 登记候选，以及 `2` 个 PA005 rule-only 影子候选（敞口排序、cooldown/quality veto）。该 prep 已接入 `m14_goal_readiness_report.json/md`，并明确 `m13_registry_mutation_count=0`、`m12_account_specs_mutation_count=0`、`broker_readiness_status_mutation_count=0`，原 blocked rows 仍保持 blocked，不允许 broker/live。
- 已新增 M14.2 PA005 broker blocker rule-only shadow evidence：`scripts/run_m14_2_broker_blocker_rule_shadow_evidence.py` 只读 `broker_blocker_shadow_ab_prep.json`，生成 `broker_blocker_rule_shadow_evidence.json/md`；当前仅打开 `2` 条 PA005 rule-only evidence（`1` 条组合敞口排序、`1` 条 cooldown/quality veto），不创建 runtime、不登记 M13、不修改 M12 account specs、不把原 broker readiness blocked rows 改成 ready。该 evidence 已接入 `m14_goal_readiness_report.json/md`，后续只等 M12.47 fresh refresh 后对比新的内部模拟规则表现，不允许 broker/live。
- 已把 PA008 quantity-cap 影子候选接成独立 simulated rescue runtime：`M10-PA-008-broker-risk-cap-shadow-1d` 进入 M12 account specs 与 M13 registry，父策略仍为 `M10-PA-008`，只在影子账户里允许多/空父策略扫描结果并把单笔风险预算封顶为 `100.00`；M10-PA-008 baseline、broker readiness blocked rows 和 broker/live 边界均不覆盖。该 runtime 当前 `required_for_goal=false`、`connected_not_promoted`，下一步等 M12.47 fresh refresh 生成首条 M13 ledger 后重新累计 10 个 rescue A/B 交易日证据。
- 已新增 M14 goal readiness 总览：`scripts/run_m14_goal_readiness_report.py` 汇总 M14 challenge、paper gate、rescue plan、rescue coverage 和 M14.2 broker dry-run；当前结论为项目处于 `M14 stable strategy testing + M14.2 broker readiness dry-run scaffold`，10 日 challenge `10/10`，`M10-PA-004 / M10-PA-005 / M10-PA-008` 可继续内部模拟账户测试，rescue runtime 只进入 10 日 A/B 取证，不进入 broker/live。
- 已新增 M14 internal simulated-account launch readiness：`scripts/run_m14_internal_sim_launch_readiness.py` 只读 M14 gate、M13 runtime registry、M12 account input audit、M13 scorecard 与 M14.2 broker dry-run plan，生成 `m14_internal_sim_launch_readiness.json/md` 并接入 goal readiness；当前 `3/3` 个获批策略、`4/4` 个 approved runtime 输入已接线，可继续内部模拟账户测试。`M10-PA-005 / M10-PA-008` 仍有 broker dry-run blocker watch，`can_start_broker_paper=false`，broker/live/real order/paper approval 继续全禁用。
- 已新增 M14 internal simulated-account next-session plan：`scripts/run_m14_internal_sim_next_session_plan.py` 把 launch readiness、goal readiness、rescue next-refresh readiness 与 rescue A/B evidence 合成下一次 M12.47 fresh refresh 的操作计划；当前 `3/3` 个 approved 策略、`4/4` 个 approved runtime 可进入下一次内部模拟刷新，`M10-PA-004` 直接继续，`M10-PA-005 / M10-PA-008` 继续但保留 broker dry-run blocker watch。计划明确 `manual_m12_37_once_allowed=false`，下一次刷新只能由 M12.47 守护器拉起，且 broker paper/live/real order/paper approval 继续全禁用。
- 已把 M14 strategy pre-refresh review packet/audit 接入 project stage assessment：`scripts/run_m14_project_stage_assessment.py` 现在把 goal readiness、next-session plan、rescue plan、rescue backlog、post-refresh outcome、evidence gap matrix、evidence gap burndown、pre-refresh review packet、pre-refresh review audit 与 post-fresh checklist 合成项目阶段和策略路线图；当前阶段为 `M14 stable strategy testing + M14.2 broker readiness dry-run scaffold`，10 日 challenge 已 `10/10`，`M10-PA-004 / M10-PA-005 / M10-PA-008` 继续内部模拟，救援侧为 `9/11` 条已有 M13 ledger、`2` 条等首个 ledger、promotion allowed `0`。post-refresh 当前 `fresh_refresh_observed=false`、`quote_source=fallback_quotes_only`、`13/13` watch rows 仍等待 M12.47 fresh refresh、`passed_count=0`、`failed_count=0`；burndown 当前 `20` 条 open gap 排成 `P0/P1/P2=12/1/7`，其中 `3` 条 approved refresh、`2` 条 first-ledger watch、`10` 条 rescue A/B、`11` 条 shadow-review；pre-refresh review packet 当前有 `19` 条 review rows、`1` 条 held row，`12` 条仍依赖 fresh refresh、`7` 条只允许 artifact/source review；pre-refresh review audit 当前 `19` 条 audit rows 中 `7` 条可 artifact-only review、`12` 条等待 M12.47 fresh evidence、`0` 条缺 supporting artifact，external/shadow readiness 均为 `11/11`，close/promote/discard/mutation allowed 均为 `0`；仍不允许手动 M12.37 once-mode、broker paper/live、真实订单或 paper approval。
- 已新增 M14 rescue A/B evidence tracker：`scripts/run_m14_rescue_ab_evidence_tracker.py` 会只读 rescue coverage、M14 gate、M13 scorecard、M13 signal ledger 和 account ledger，生成 `m14_rescue_ab_evidence_tracker.json/md`；当前结果为 rescue runtime coverage 完整，既有 9 条 rescue 变体已有 M13 ledger 证据，新接入的 PA008 broker-risk-cap shadow 与 PA012 target/stop 影子 runtime 尚待下一次 M12.47/M13 刷新生成首条 ledger，因此当前为 `9/11` observed、`2` 条 no-ledger、promotion allowed 仍为 `0`。
- 已新增 M14 rescue optimization backlog：`scripts/run_m14_rescue_optimization_backlog.py` 会把 rescue plan、rescue A/B evidence 与 M14.2 broker dry-run blocker 合成提前优化队列；M13 已改为优先使用 M12 trade ledger 的显式 `trading_date`，旧 open 记录则回退到 `signal_time` 归属交易日，因此 `M10-PA-011-ORB-R1` 的 signal-to-account no-op 已清零并记录 `2` 条 open operation。当前显示 `11` 条 rescue 变体均有队列行，其中 `8` 条是已接线但零信号，`2` 条新影子 runtime（PA008 broker-risk-cap、PA012 target/stop）等待下一次 M12.47-owned fresh run 生成首条 M13 ledger，`0` 条是有信号但未形成 account operation，另有 `3` 个 broker dry-run 风控阻断事件分布在 `M10-PA-005 / M10-PA-008`。该队列只用于优化/诊断，不计作 promotion，也不允许 broker/live。
- 已新增 M14 rescue next-refresh readiness：`scripts/run_m14_rescue_next_refresh_readiness.py` 会把 rescue backlog、zero-signal diagnostics、A/B evidence、PA012 target/stop shadow normalization 与 PA005 rule-only broker shadow evidence 合成下一次 M12.47 fresh refresh 后的验收矩阵；当前跟踪 `13` 条 watch rows，其中 `7` 条 fresh quote 复核、`2` 条首个 M13 ledger watch、`2` 条 PA005 broker-rule shadow 复核、`1` 条 PA012 target/stop shadow 对比、`1` 条 PA013 parent-detector 同周期等待。该矩阵明确当前 `parameter_change_allowed_now_count=0`，下一步先等 fresh evidence 再决定是否调参，仍不允许 broker/live。
- 已新增 M14 rescue post-refresh outcome review：`scripts/run_m14_rescue_post_refresh_outcome_review.py` 会在下一次 M12.47 fresh refresh 后只读 M12/M13/M14 artifact 并判定 `13` 条 watch rows 是否通过；当前快照 `quote_source=fallback_quotes_only`，因此 `fresh_refresh_observed=false`、`13/13` 条均为 `waiting_for_m12_47_fresh_refresh`、`passed_count=0`、`failed_count=0`。该评审器只做证据归因，不触发 M12.37 once-mode，不改参数、不改 registry、不改 account specs、不改 broker readiness，也不允许 broker/live/real order/paper approval。
- 已新增 M14 rescue external reference map：`scripts/run_m14_rescue_external_reference_map.py` 会把 AI-Trader 与 TradingAgents 的可参考工程模式映射为本地只读复核清单；当前覆盖 `11` 条 rescue rows 和 `2` 条 broker-blocker rows，输出 shadow signal scoreboard、技术证据复核、bull/bear objection、risk/portfolio review 与 decision-log audit lane。该 artifact 已接入 project stage assessment；它只作为架构参考，不允许 copy trading、外部 agent 决策覆盖本地 M13/M14 gate、broker sync、真实订单、live execution、paper approval 或手动 M12.37 once-mode。
- 已新增 M14 rescue parameter experiment queue：`scripts/run_m14_rescue_parameter_experiment_queue.py` 会把当前 rescue backlog 与 next-refresh/zero-signal/target-stop/external-reference 证据合成只读参数实验队列；当前共 `14` 条候选，`allowed_now_count=0`，`10` 条仍等 fresh refresh、`1` 条等 parent detector 证据、`2` 条等首个 M13 ledger、`1` 条继续 A/B 取证。候选家族覆盖 fresh quote gate、target/stop reward geometry、ledger path mapping、PA013 parent detector、PA005 exposure/cooldown 和 PA008 quantity cap；该队列只做下一轮验收准备，不改 M13 registry、不改 M12 account specs、不改 broker readiness，也不允许 broker/live 或手动 M12.37 once-mode。
- 已新增 M14 rescue parameter activation gate：`scripts/run_m14_rescue_parameter_activation_gate.py` 会把参数实验队列和 post-refresh outcome review 合成下一层验收门；当前 `14` 条 gate rows 中 `13` 条仍等待 M12.47 fresh refresh、`1` 条继续 A/B 取证，`shadow_review_candidate_count=0`、`implementation_mutation_allowed_count=0`、`parameter_mutation_allowed_count=0`。该 gate 只在 fresh evidence 通过后把候选标记为人工 M14 shadow-review，不直接调参、不改 M13 registry、不改 M12 account specs、不改 broker readiness，也不允许 broker/live 或手动 M12.37 once-mode。
- 已新增 M14 rescue parameter shadow spec：`scripts/run_m14_rescue_parameter_shadow_spec.py` 会把参数实验队列、activation gate、PA012 target/stop normalization、PA008/PA005 broker-blocker shadow prep/evidence 与外部参考映射合成只读影子规格；当前 `14` 条 spec rows、`14` 个 candidate variants、`12` 条仍等待 M12.47 fresh evidence，覆盖 `1` 个 PA012 `risk_normalized_1_0r` target/stop 影子变体、`1` 个 PA008 quantity-cap 变体、`2` 个 PA005 broker-rule 变体，以及 fresh quote recheck、ledger mapping、parent detector 和继续 A/B 的验收口径。该 spec 只做 review/spec，不触发参数实现、不改 registry/account specs/broker readiness，也不允许 broker/live、真实订单、paper approval 或手动 M12.37 once-mode，并已接入 project stage assessment。
- 已新增 M14 strategy decision ladder：`scripts/run_m14_strategy_decision_ladder.py` 会把 goal readiness、next-session、rescue A/B evidence、parameter shadow spec、external reference map 与 objective execution plan 合成策略级晋级/救援/放弃决策阶梯；当前 `20` 条策略路线中，`3` 条 approved 策略只能进入下一次 M12.47-supervised 内部模拟刷新，`10` 条继续 rescue/detector/参数影子取证，`7` 条保持 shadow/plugin/research coverage，`final_discard_allowed_count=0`、`promotion_candidate_count=0`。该 ladder 明确最终放弃必须等 rescue route、shadow spec、首个 ledger、10 日 A/B 证据和人工 M14 review 全部完成，不允许 broker/live、真实订单、paper approval、参数 mutation、registry/account-spec/broker readiness mutation 或手动 M12.37 once-mode，并已接入 project stage assessment。
- 已补强 M14 objective completion audit：`scripts/run_m14_objective_completion_audit.py` 会把当前用户目标拆成 `13` 项需求审计，并直接读取 parameter shadow spec、strategy decision ladder、strategy evidence gap matrix 与 strategy source recheck triage 作为弱策略救援/调参/原始资料再提取路径证据；当前 `proven=4`、`guardrail=3`、`in_progress=3`、`blocked=3`、`objective_complete=false`。已证明项目阶段、10 日 challenge、approved internal-sim 和外部参考映射；审计已显示 `14` 个参数影子变体、`10` 条 rescue-continuation ladder rows、`7` 条 source recheck rows 和 `2` 条 future source-reextract candidates，且 `final_discard_allowed_count=0`。strategy evidence gap matrix 仍有 `20` 条 open gap、`12` 条 fresh-refresh wait、`2` 条 first-ledger gap、`10` 条 rescue 10 日 A/B gap、`11` 条 shadow-review gap；仍未完成的是 rescue promotion 证据、M12.47 fresh refresh 前置和整体目标完成。该审计已接入 project stage assessment，且继续禁止 broker/live、真实订单、paper approval、参数 mutation、registry/account-spec/broker readiness mutation 和手动 M12.37 once-mode。
- 已新增 M14 objective execution plan：`scripts/run_m14_objective_execution_plan.py` 会把 objective completion audit 的 blocked/in_progress 项拆成 `7` 条执行动作；当前 `P0=5`，`5` 条动作仍需要 M12.47-owned fresh refresh，覆盖 approved internal-sim 下一次刷新、2 条 no-ledger rescue 首账本 watch、11 条 rescue A/B 继续取证、14 条参数 activation gate fresh-evidence 后 shadow-review、broker dry-run watch-only、外部参考 review-only 和目标完成复核；目标完成复核现在直接显示 `open evidence gaps=20`。该计划不触发手动 M12.37 once-mode、不改参数、不改 M13 registry/M12 account specs/broker readiness，不允许 broker/live/真实订单/paper approval。
- 已新增 M14 post-fresh-refresh recompute checklist：`scripts/run_m14_post_fresh_refresh_recompute_checklist.py` 会把下一次 M12.47 fresh refresh 后的重算顺序、验收门和边界固化成 `m14_post_fresh_refresh_recompute_checklist.json/md`；当前清单含 `29` 步、`28` 个只读 M14 script steps、`9` 个 acceptance gates，并要求 objective audit / objective execution / strategy decision ladder / strategy evidence gap matrix / strategy evidence gap burndown / source visual confirmation response gate / future source-reextract spec prep / objective blocker burndown / strategy next-step matrix / internal-sim trial acceptance gate / strategy pre-refresh review packet / strategy pre-refresh review audit 稳定化后再刷新 project stage assessment。当前仍 `fresh_refresh_observed=false`、`quote_source=fallback_quotes_only`、`13` 条 post-refresh watch rows 等待；该 checklist 不触发 M12.37 once-mode、不接 broker、不改参数、不改 registry/account specs/broker readiness，并已接入 project stage assessment。
- 已新增 M14 strategy evidence gap matrix：`scripts/run_m14_strategy_evidence_gap_matrix.py` 会把 strategy decision ladder、rescue A/B evidence、parameter shadow spec、objective audit/execution 与 post-fresh checklist 合成策略级证据缺口矩阵；当前 `20/20` 条策略路线仍有 open evidence gap，`12` 条需要下一次 M12.47 fresh refresh，`2` 条缺首个 rescue-specific M13 ledger，`10` 条缺 rescue 10 日 A/B 证据，`11` 条缺 shadow-review 证据；`final_discard_allowed_count=0`、`promotion_candidate_count=0`、`parameter_mutation_allowed_count=0`。该矩阵已接入 project stage assessment，继续禁止 broker/live、真实订单、paper approval、参数 mutation、registry/account-spec/broker readiness mutation 和手动 M12.37 once-mode。
- 已新增 M14 strategy evidence gap burndown：`scripts/run_m14_strategy_evidence_gap_burndown.py` 会把 gap matrix、objective execution plan、parameter experiment queue、activation gate 与 external reference map 合成缺口消化队列；当前 `20` 条 burndown rows，`P0/P1/P2=12/1/7`，lane 计数为 approved internal-sim refresh `3`、first rescue ledger `1`、rescue shadow parameter review `7`、detector rebuild A/B `1`、rescue A/B collection `1`、shadow/plugin research `7`，并汇总 `3` 条 approved refresh、`2` 条 first-ledger watch、`10` 条 rescue A/B、`11` 条 shadow-review、`19` 条 pre-refresh review available。该队列只排序证据收集和人工复核，不允许 promotion、final discard、参数 mutation、broker/live、真实订单、paper approval 或手动 M12.37 once-mode，并已接入 post-fresh checklist 和 project stage assessment。
- 已新增 M14 strategy pre-refresh review packet：`scripts/run_m14_strategy_pre_refresh_review_packet.py` 会把 burndown 里的 `19` 条 pre-refresh-available rows 转成只读复核包，另有 `1` 条 `M10-PA-004-MBF` held row；review rows 的 `P0/P1/P2=12/0/7`，其中 `12` 条仍依赖 M12.47 fresh refresh，`7` 条只能做 artifact/source review，`11` 条带外部参考复核清单。该 packet 只能准备人工复核、影子参数验收和外部架构参考检查，不能 close gap、不能 promote、不能 discard、不能改参数、不能手动运行 M12.37 once-mode，也不能启用 broker/live；已接入 post-fresh checklist 和 project stage assessment。
- 已新增 M14 strategy pre-refresh review audit：`scripts/run_m14_strategy_pre_refresh_review_audit.py` 会把 pre-refresh review packet 的 `19` 条 review rows 对照 burndown、decision ladder、external reference map、parameter shadow spec 和 activation gate 做 supporting artifact 审计；当前 `7` 条可做 artifact-only review，`12` 条 supporting artifact 齐全但仍等 M12.47 fresh evidence，`0` 条需要 backfill，external reference readiness `11/11`、shadow parameter readiness `11/11`、decision ladder present `19/19`。该 audit 只判断复核准备度，不 close gap、不 promote、不 discard、不改参数、不手动运行 M12.37 once-mode，也不启用 broker/live；已接入 post-fresh checklist 和 project stage assessment。
- 已新增 M14 strategy source recheck triage：`scripts/run_m14_strategy_source_recheck_triage.py` 会把 pre-refresh audit 中 `7` 条 artifact-only review rows 转成源资料复核队列；当前 `2` 条为 source/visual recheck candidates（`M10-PA-003`、`M10-PA-010`），`2` 条保持 research-only 风险定义 hold（`M10-PA-006`、`M10-PA-016`），`2` 条只能作为 parent setup 的 supporting rule（`M10-PA-014`、`M10-PA-015`），`1` 条为外部参考 hold（`AI-TRADER-EXTERNAL`），可供 future source-reextract 候选 `2` 条。该 triage 只排序源资料/视觉复核，不创建策略、不 close gap、不 promote、不 discard、不改参数、不手动运行 M12.37 once-mode，也不启用 broker/live；已接入 project stage assessment。
- 已新增 M14 strategy source reextract plan：`scripts/run_m14_strategy_source_reextract_plan.py` 会把 source recheck triage 固化成原始资料再提取计划；当前 `7` 条 plan rows 中 `2` 条可作为 future source-reextract candidates（`M10-PA-003` tight channel、`M10-PA-010` climax/final-flag TBTL），`2` 条 research-only hold、`2` 条 supporting-only attachment、`1` 条 external-reference hold，共 `16` 个 source-review tasks 和 `16` 个 review questions。该 plan 只准备后续源资料/视觉复核，不创建策略、不 close gap、不 promote、不 discard、不改参数、不手动运行 M12.37 once-mode，也不启用 broker/live；已接入 objective completion audit 和 project stage assessment。
- 已新增 M14 strategy source reextract review：`scripts/run_m14_strategy_source_reextract_review.py` 会只读 source reextract plan 和 Brooks 原始 markdown，为 `M10-PA-003`、`M10-PA-010` 生成源资料复核 packet；当前 `2` 条 review packets、`10` 个 source-backed atoms、`6` 个 source-review answers，`2` 条 future specs 仅允许在视觉对齐后 draft，`2` 条仍需 visual review。该 review 只准备 future spec，不创建策略、不 close gap、不 promote、不 discard、不改参数、不手动运行 M12.37 once-mode，也不启用 broker/live；已接入 objective completion audit 和 project stage assessment。
- 已新增 M14 strategy source visual alignment gate：`scripts/run_m14_strategy_source_visual_alignment_gate.py` 会只读 source reextract review 与 M10 visual golden case pack，校验 `M10-PA-003`、`M10-PA-010` 的视觉对齐准备度；当前 `2` 条 gate rows、`10` 个 visual cases，case mix 为 `6/2/2`（positive/counterexample/boundary），`10/10` checksum match，素材均在旧 M10 worktree local-only 路径可读，`2` 条均 ready for manual visual alignment，但 `2` 条仍需要人工视觉确认后才允许 future spec draft。该 gate 不 draft spec、不创建策略、不 close gap、不 promote、不 discard、不改参数、不手动运行 M12.37 once-mode，也不启用 broker/live；已接入 objective completion audit 和 project stage assessment。
- 已新增 M14 strategy source visual confirmation packet：`scripts/run_m14_strategy_source_visual_confirmation_packet.py` 会只读 visual alignment gate，把 `M10-PA-003`、`M10-PA-010` 的 `10` 个 visual cases 整理成 `2` 条人工确认 packet rows、`6` 个 confirmation questions 和 `10` 条 case confirmation rows。当前 `2` 条 packet 均 ready for manual review，但 recorded confirmations 为 `0`、future spec unblocked 为 `0`；该 packet 只准备人工视觉确认清单，不记录确认、不 draft spec、不创建策略、不 close gap、不 promote、不 discard、不改参数、不手动运行 M12.37 once-mode，也不启用 broker/live；已接入 objective completion audit 和 project stage assessment。
- 已新增 M14 strategy source visual confirmation response gate：`scripts/run_m14_strategy_source_visual_confirmation_response_gate.py` 会只读 visual confirmation packet，并在缺少人工回复时生成 `m14_strategy_source_visual_confirmation_response.json` pending scaffold；当前 `2` 条 response gate rows、`6` 个 question responses 和 `10` 个 case responses 均为 pending，manual confirmation complete 为 `0`、future spec unblocked 为 `0`。该 gate 只校验人工视觉确认回复，不替代人工确认、不 draft spec、不创建策略、不 close gap、不 promote、不 discard、不改参数、不手动运行 M12.37 once-mode，也不启用 broker/live；已接入 objective completion audit 和 project stage assessment。
- 已补强 M14 strategy source visual confirmation review pack：同一个 response gate 现在额外输出 `m14_strategy_source_visual_confirmation_review_pack.md/html`，把 `M10-PA-003`、`M10-PA-010` 的 `6` 个问题和 `10` 个 case 图片资产整理成可人工复核的静态包；当前 `10/10` 个本地图片资产存在，人工回复仍全是 pending，future spec unblocked 仍为 `0`。该 review pack 只方便人工看图，不记录确认、不 draft spec、不创建策略、不 close gap、不 promote、不 discard、不改参数、不手动运行 M12.37 once-mode，也不启用 broker/live；已接入 objective completion audit 和 project stage assessment。
- 已新增 M14 strategy future source-reextract spec prep：`scripts/run_m14_strategy_future_source_reextract_spec_prep.py` 会只读 source reextract review 与 visual confirmation response gate，把 `M10-PA-003`、`M10-PA-010` 整理为 `2` 条条件式 future spec 草案准备行，包含 `10` 个 source-backed atoms、`6` 个 source-review answers、`2` 个 conditional drafts；当前人工视觉确认未完成，`0` 条 unblocked、`2` 条 blocked_until_manual_visual_confirmation、`16` 个待确认项。该 prep 明确写入旧版利润规划输入为 `false`，账户下钻旧版利润字段不得进入策略规划、创建、晋级、淘汰、参数激活或 broker readiness；已接入 objective completion audit、objective blocker burndown、strategy next-step matrix、post-fresh checklist 和 project stage assessment。
- 已新增 M14 objective blocker burndown：`scripts/run_m14_objective_blocker_burndown.py` 会只读 objective audit、project stage、evidence burndown、source visual response gate 与 future source-reextract spec prep，生成 `m14_objective_blocker_burndown.json/md`；当前 `7` 条 blocker rows，`P0/P1/P2=4/3/0`，其中明确把账户下钻里的旧版利润字段列为污染风险并排除在策略 promotion、rescue priority、参数 activation、broker readiness 和 objective completion 决策之外，旧版利润规划输入数量为 `0`。当前仍是 `10/10` challenge、`3` 个 approved internal-sim 策略可继续内部模拟，但 `2` 个 rescue runtime 等首个 M13 ledger、`10` 条 rescue A/B gap、`6` 个视觉问题、`10` 个 case 回复和 `16` 个 future spec prep 待确认项仍未完成；不允许 broker/live、真实订单、paper approval、参数 mutation 或手动 M12.37 once-mode。
- 已把 M12.46 券商式看板里的旧版历史指标从展示层移除：`m12_32_minute_readonly_dashboard_data.json/html` 下一次由 M12.47/M12.37 正常刷新时会继续执行旧版利润字段清理；这些字段不得参与 M14 策略晋级、救援排序、参数激活、broker readiness 或 objective completion。
- 已新增 M14 strategy next-step readiness matrix：`scripts/run_m14_strategy_next_step_readiness_matrix.py` 会只读 strategy decision ladder、evidence gap matrix/burndown、objective blocker、source visual response gate、future source-reextract spec prep、rescue A/B evidence 和 parameter shadow spec，生成 `m14_strategy_next_step_readiness_matrix.json/md`；当前 `20` 条策略路线中，`3` 条只能继续下一次 M12.47-supervised 内部模拟刷新，`10` 条进入 rescue/影子参数复核，`7` 条保持 source/plugin research review-only；future source-reextract spec prep 仍为 `2` 条条件稿、`0` 条 unblocked、`16` 个待确认项。该矩阵把旧版利润规划输入数量固定为 `0`，并保持 promotion、final discard、parameter activation、broker paper start 全部为 `0`；不允许 broker/live、真实订单、paper approval、参数 mutation、registry/account-spec/broker readiness mutation 或手动 M12.37 once-mode。
- 已把 M14 strategy next-step readiness matrix 与 internal-sim trial acceptance gate 接入 post-fresh recompute checklist 和 project stage assessment：`m14_post_fresh_refresh_recompute_checklist.json/md` 现在把 source visual response gate、future source-reextract spec prep、objective blocker burndown、next-step matrix 与 trial acceptance gate 纳入 M12.47 fresh refresh 后重算顺序，清单为 `29` 步、`28` 个只读 M14 script steps、`9` 个 acceptance gates，包含 legacy-history exclusion gate 与 internal-sim trial acceptance gate；`m14_project_stage_assessment.json/md` 现在直接汇总 next-step matrix 的 `20` 条策略桶位、`3/10/7` 路线分布，以及 trial gate 的 `3/3` start-ready、`3` 条 fresh-refresh-required、post-fresh recompute `29` 步、global gates `pass=3 / waiting=2`、`legacy_historical_profit_planning_input_count=0`，顶层阶段判断不再只依赖孤立报告。
- 已把旧版利润字段隔离下沉到 M14 internal simulated-account next-session plan：`scripts/run_m14_internal_sim_next_session_plan.py` 现在把旧版利润规划输入禁用写入硬边界、策略会话行、acceptance checks 和 P0 planning boundary watch，配置若打开该字段会直接失败；最新 `m14_internal_sim_next_session_plan.json/md` 显示旧版利润规划输入数量为 `0`、`global_watch_row_count=5`，因此下一次内部模拟会话计划也不会把账户下钻旧版利润字段用于策略规划、晋级、救援排序、参数激活或 broker readiness。
- 已新增 M14 internal simulated-account trial acceptance gate：`scripts/run_m14_internal_sim_trial_acceptance_gate.py` 会只读 next-session plan、next-step matrix、project stage assessment、post-fresh checklist、objective blocker 和 post-refresh outcome，生成 `m14_internal_sim_trial_acceptance_gate.json/md`；当前 `3/3` 个 approved 策略可进入下一次 M12.47-supervised 内部模拟试运行，`3/3` 都仍需 fresh refresh 后验收，post-fresh recompute step count=`29`、acceptance gates=`9`，global gates 为 `pass=3 / waiting=2`，`M10-PA-005 / M10-PA-008` 只能带 broker dry-run blocker watch 继续，`legacy_historical_profit_planning_input_count=0`、broker paper/live/manual M12.37/参数 mutation 全部继续禁用。
- 已新增并纠偏 M14 rescue zero-signal diagnostics：`scripts/run_m14_rescue_zero_signal_diagnostics.py` 会只读 M12 dashboard 与 rescue coverage，拆解零信号 runtime 的父策略输入和过滤失败原因；当前 `9` 个零信号 runtime 中，`7` 个主要是当前 recompute/audit 快照报价源非 `longbridge_quote_readonly`，应等 M12.47 下一次 fresh quote 刷新后复核；`M10-PA-012-m14-modify-20260522-5m` 是 `reward_r_below_min` 阻断，但 shadow-only `1.0R / 1.1R / 1.2R` 质量门计数均为 `0`，下一步应先检查 target/stop 生成而不是简单降 min-R；`M10-PA-013-m14-modify-20260522-1d` 是父策略同周期也零信号，需保持同周期映射并等待父 detector 证据，不跨周期硬映射。该诊断已接入 M14 goal readiness，不改变 broker/live 边界。
- 已新增 M14 rescue target/stop diagnostics：`scripts/run_m14_rescue_target_stop_diagnostics.py` 会只读 M12 dashboard 与 zero-signal artifact，专门检查 reward/R 阻断 runtime 的 entry/stop/target 几何；当前只纳入 `M10-PA-012-m14-modify-20260522-5m`，父源行 `47` 条、非杠杆看涨有效行 `15` 条、风险门槛通过 `12` 条，但 `1.0R / 1.1R / 1.2R` 通过数均为 `0`，reward/R 区间为 `0.7159 ~ 0.9819`。下一步应影子测试 ORB target/stop 生成（开盘区间高度、measured move 或 normalized 1.0R target），不要直接降低 frozen rescue runtime 阈值；该诊断已接入 M14 goal readiness，不改变 broker/live 边界。
- 已新增 M14 rescue target/stop shadow normalization：`scripts/run_m14_rescue_target_stop_shadow_normalization.py` 会只读 M12 dashboard 与 target/stop diagnostics，生成 PA012 影子目标价修复候选；当前 `risk_normalized_1_0r` 可让 `12/12` 条已过方向、杠杆、价格几何与风险门槛的候选达到 `1.0R`，中位目标价调整约 `0.0410%`，而 `opening_range_height_30m` 仍未突破原始低于 `1.0R` 的问题。
- 已把 PA012 `risk_normalized_1_0r` 从 artifact-only 设计输入接成独立 rescue shadow runtime：`M10-PA-012-m14-modify-20260522-target-stop-risk_normalized_1_0r-shadow-5m` 进入 M12 account specs 与 M13 registry，父策略仍为 `M10-PA-012`，只在影子账户里把目标价规范化为 `entry + 1.0R`；PA012 baseline 和 frozen `M10-PA-012-m14-modify-20260522` 均不覆盖。该 runtime 仍是 simulated-only、`required_for_goal=false`、不算 promotion、不允许 broker/live，下一步必须等 M12.47 fresh quote 后由 M13 生成首条 ledger，再重新累计 10 个 rescue A/B 交易日证据。
- 已修复 M14 内部模拟桥接的平仓同步缺口：`run_internal_paper_bridge` 现在按时间处理 `open/close`，同一交易日平仓会释放内部模拟敞口；同时把 M14 内部模拟总敞口从孤立的 `1000` 对齐到本项目账户化 runtime 的 `25000` 量级，保留单笔风险 `100` 不变。修复后 M14.2 dry-run ready 从旧口径 `1/8` 提升为 `5/8`，剩余阻断来自真实 `max_risk_per_order / max_total_exposure / consecutive_losses_limit`。

## 已完成

- Git 仓库初始化
- V2 目录结构落盘
- Codex agents 配置写入
- 核心规则文档写入
- 测试数据样本写入
- KB 校验与索引脚本写入
- GitHub 登录与推送链路打通
- 正式 `plans/active-plan.md` 已写入
- M1 代表性 wiki 页面已补齐（concept / setup / source）
- M1 schema、frontmatter、KB validate、wiki index 契约已对齐
- M1 验收条目已补齐并通过 reviewer / qa
- M2 数据契约已统一到 `src/data/schema.py`
- M2 本地 CSV/JSON loader 与 deterministic replay 已完成
- M2 单测已覆盖正向样本、重复键、非法 market、非法 severity、非法 timezone 与时间归一化
- M2 已通过 reviewer / qa 技术验收
- M3 已新增 research-only 的知识引用包与最小结构化索引
- M3 已新增 `src/strategy/` 最小 contracts / knowledge / context / signal 原型
- M3 单测已覆盖无信号、单信号、traceability、placeholder 低置信度、news 风险附着、缺失 source_refs 早失败、invalidation、双信号稳定性
- M3 已通过 reviewer / qa 正式复审，确认 strategy 仅消费 M2 数据契约，未越界到 execution / risk / broker / 外部 API
- M4 已新增 `src/backtest/` 最小 contracts / engine / reporting 基线
- M4 已覆盖零交易、单交易、多交易、same-bar stop/target、end_of_data、news 不改收益、profit_factor 空值路径
- M4 已通过 reviewer / qa 正式复审，确认回测层只消费本地 replay / bars 与结构化 signal，未越界到 execution / risk / broker / 外部 API
- M5 已新增 `src/risk/` 最小 contracts / engine 与 `src/execution/` 最小 paper adapter / state / logging
- M5 已覆盖 allow、risk_block、market_closed、duplicate_signal、loss-streak halt、manual recovery、config_error、invalid_request、mismatched / stale / direction-mismatch risk decision
- M5 已通过 reviewer / qa 正式复审，确认风控先于执行、paper-only 边界清晰、close-path 审计日志可复盘，未越界到真实 broker / live execution
- M6 已新增 `src/news/` 最小 contracts / filtering 与 `src/review/` 最小 contracts / reporting
- M6 已补 `knowledge/wiki/rules/m6-news-review-evidence-pack.md`，并同步 `knowledge/wiki/index.md` 与 `knowledge/wiki/log.md`
- M6 已覆盖无新闻、caution、block、future-event leakage、防止缺失 reference timestamp、review 中 filter / explanation / risk_hint 角色透传、以及新闻不改写 signal 主字段
- M6 已通过 reviewer / qa 正式复审，确认新闻仍只作 filter / explanation / risk_hint，未越界到 signal / order / execution，且复盘输出保留结构化 `news_review_notes`
- M7 已新增 `src/broker/` assessment-only contract draft 与 `tests/unit/test_broker_contract_assessment.py`
- M7 已新增 `docs/broker-readiness-assessment.md` 与 `docs/broker-approval-checklist.md`，明确 readiness 边界、approval gates、rollback boundary 与当前 `no-go` 结论
- M7 已通过 reviewer / qa 正式复审，确认 broker 层仍停留在 contract draft / readiness artifact，未越界到真实 broker SDK、真实账户联通或 live execution
- M8A 已完成主线与门禁文档切换：`plans/active-plan.md`、`docs/acceptance.md`、`docs/status.md`、`docs/roadmap.md`、`docs/decisions.md`
- M8A 已新增 `docs/testing-reliability.md` 与 `docs/eval-rubric.md`，冻结 M8 的测试目标、输入边界、评分维度与硬门禁
- M8A 已新增 `docs/test-dataset-curation.md`，冻结 M8 可接受数据层级、样本元数据、脱敏与离线边界
- M8A 已落盘 `tests/golden_cases/`、`tests/integration/`、`tests/reliability/`、`reports/reliability/` 的 discoverable 骨架目录
- M8A 已新增 `scripts/run_reliability_suite.py`，默认运行当前 baseline unit suites，并对空目录或缺真实历史样本显式 skipped / deferred
- M8B 已新增 `src/strategy/alignment.py`，提供最小 knowledge alignment 评估入口
- M8B 已收紧 `src/strategy/knowledge.py` 的 `source_refs` 校验：缺失或不存在的 wiki/raw refs 现在会硬失败
- M8B 已在 `tests/golden_cases/cases/` 落盘 5 个 golden cases，覆盖 placeholder setup、news role conflict、insufficient evidence、not_applicable hard gate、missing/fake refs hard fail
- M8B 已新增 `tests/reliability/test_kb_alignment.py`、`tests/reliability/test_no_hallucinated_kb_refs.py`、`tests/reliability/test_no_trade_when_insufficient_evidence.py`
- M8B 已验证 reliability suite 7 项通过，且原有 `tests/unit/` 57 项无回归
- M8B 已于 2026-04-17 通过 merge commit `0047100` 从 `integration/m8-reliability-validation` 整合进稳定基线 `feature/m7-broker-api-assessment`
- M8C 已新增 `tests/integration/test_offline_e2e_pipeline.py`，覆盖 `src/data -> src/strategy -> src/backtest -> src/risk -> src/execution -> src/news -> src/review` 的离线端到端集成链路
- M8C 已新增 `tests/reliability/test_replay_determinism.py`、`tests/reliability/test_no_future_leakage.py`、`tests/reliability/test_audit_traceability.py`、`tests/reliability/test_forbidden_paths.py`
- M8C 已覆盖 deterministic replay 一致性、bars / news future leakage fail-fast、forbidden paths、audit/review traceability、`end_of_data`、缺 bar gap 与重复 bar 稳健性
- M8C 已验证 `tests/reliability` 18 项、`tests/integration` 4 项、`tests/unit` 57 项通过；`python scripts/run_reliability_suite.py` 当前合计执行 79 项本地离线测试
- M8C 保持 `paper / simulated` 与 `no-go` 边界，未新增真实 broker、真实账户或 live execution 路径
- M8D 已新增 `docs/shadow-mode-runbook.md` 与 `scripts/run_shadow_session.py`，固定 manifest 驱动的只读 shadow/paper 验证路径
- M8D 已新增 repo-safe 小样本 manifest：`tests/test_data/real_history_small/sample_us_5m_recorded_session/dataset.manifest.json`
- M8D 已新增 `tests/reliability/test_regime_robustness.py`、`tests/reliability/test_shadow_paper_consistency.py`、`tests/reliability/test_dataset_manifest_contract.py`
- M8D 已补齐 `docs/test-dataset-curation.md` 的 small/large dataset、realtime recording 与受控标签规范，并更新 `reports/reliability/README.md` 的报告最小字段
- M8D 当前已具备：manifest 校验、shadow/paper dry-run、报告 traceability、无样本 deferred 语义；尚未实际运行用户真实历史样本或录制型实时样本
- 已新增 `scripts/download_public_history.py`，public-history 入口当前通过 repo config 中的 `source_order[0]` 解析 `primary_provider`；provider-specific 历史路径仍保留为兼容或对照事实
- 已新增 `scripts/run_public_backtest_demo.py` 与 `scripts/run_public_backtest_demo.sh`，可在本地缓存基础上直接生成用户可读的历史回测演示报告
- 已新增 `config/examples/public_history_backtest_demo.json` 与 `docs/user-backtest-guide.md`，固定第一轮演示范围为 `NVDA / TSLA / SPY`、`2024-01-01 ~ 2024-06-30`、`1d`
- 已完成一轮公共历史数据演示回测：`yfinance` 下载的本地缓存已落在 `local_data/public_history/`，示例 run `demo_public_2024h1` 生成于 `reports/backtests/demo_public_2024h1/`
- 该示例 run 在当前 demo 风控参数下输出：总收益率 `1.9923%`、最大回撤 `1.5157%`、交易 `16` 笔、胜率 `43.75%`；仍明确保持 `paper / simulated`
- M8B.1 已完成知识源接入诊断与最小补齐：确认 transcript / Brooks PPT 先前缺席的根因是 raw-only、无 `source` 页、未入 active rule pack，且默认 strategy bundle 未读取 rule pack；现已新增真实存在文件的 `source` 页、补齐 rule-pack/index 接线，并让默认 signal 链能带出这些 `source` 页引用
- M8B.1 已新增 `docs/knowledge-source-trace.md` 与 `tests/reliability/test_knowledge_source_trace.py`，验证“已接线的 transcript/PPT 来源能进入 `source_refs`，未接线时不会被编造”
- M8B.2 已拆成两个子阶段：
  - `M8B.2a`：Source Registry + Chunk Registry + Knowledge Atom Schema + Builders/Validators + Callable Index
  - `M8B.2b`：仅在 `2a` 全部测试通过、未触发熔断且 reviewer / qa 通过后，才允许接 strategy / explanation / review / report
- M8B.2 的长期边界保持不变：`statement` 是中间层 callable atom，不是 executable rule，不得参与 trigger 判定
- M8B.2a 已完成：
  - 已新增 `knowledge/schema/source-registry-schema.md`、`chunk-registry-schema.md`、`knowledge-atom-schema.md`、`callable-access-contract.md`
  - 已新增 `knowledge/indices/source_manifest.json`、`chunk_manifest.jsonl`、`knowledge_atoms.jsonl`、`knowledge_callable_index.json`
  - 已新增 `scripts/build_source_manifest.py`、`build_chunk_registry.py`、`build_knowledge_atoms.py`、`build_callable_index.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`
  - 已补齐 5 份方方土笔记 source page 与 2 份 Brooks PPT per-file source page
  - 当前 source coverage 结果为 `parsed=9 / partial=1 / blocked=0`，未触发 `blocked >= 4` 熔断
  - 当前 partial source 为 `方方土视频笔记 - 楔形.pdf`，原因为 `1 page(s) produced no stable text`
  - 已落盘 `statement` atom，当前原子统计为：`statement=11171`、`source_note=5492`、`open_question=24`、`concept=1`、`setup=1`、`rule=1`
  - 关键 curated atoms 已形成 evidence-backed atom：`market-cycle-overview`、`signal-bar-entry-placeholder`、`m3-research-reference-pack`
  - 已新增 `tests/reliability/test_kb_coverage.py`、`test_knowledge_atoms.py`、`test_callable_access.py`
  - 已通过 `tests/reliability` 39 项与 `tests/unit` 60 项回归
  - M8B.2a.1 已完成 statement 质量审计与 merge gate，审计结论为 `pass_with_small_fixes`
  - 已完成最小修复：收紧 statement 提取条件，去除明显页眉页脚 / 时间轴 / 起始标点 / 未完成碎片，并对同一 source 内的 statement 做保守去重
  - 当前 statement 分布为：`al_brooks_ppt=11042`、`fangfangtu_transcript=88`、`fangfangtu_notes=41`
  - 当前重复/噪音摘要为：`exact_dup_extra=13`、`normalized_dup_extra=16`、`trailing_open=0`、`datey=0`、`start_punct=0`
  - 已于 2026-04-17 通过 merge commit `23755c0` 从 `feature/m8b2-knowledge-atomization-callable-access` 整合进稳定基线 `feature/m7-broker-api-assessment`
- M8B.2b 已完成：
  - 已新增 `src/strategy/knowledge_access.py`，提供 callable atom query、curated-first trace resolver、transitional view 与 legacy `source_refs` 聚合 helper。
  - `Signal` 已新增 `knowledge_trace`，`ReviewItem` 已新增 `kb_trace`，并保持 legacy `source_refs` / `kb_source_refs` 兼容。
  - `knowledge_trace` 当前只接入 trace / explanation / review / report；`statement`、`source_note`、`contradiction`、`open_question` 仍不得进入 trigger。
  - 已新增 source family 失衡保护：curated atom 优先、statement/source-note trace 去重、限量、按 source family 做多样性控制，不允许用 atom 数量当作 confidence 或 trigger 权重。
  - public demo 已新增 machine-readable `knowledge_trace.json`，Markdown `report.md` 只展示精简 trace 摘要，每笔代表性交易最多 3 条。
  - 已通过 `tests/reliability/test_strategy_atom_trace.py`、`tests/unit/test_strategy_signal_pipeline.py`、`tests/unit/test_news_review_pipeline.py`、`tests/unit/test_public_backtest_demo.py`、`python -m unittest discover -s tests/reliability -v`、`python -m unittest discover -s tests/unit -v`。
  - M8B.2b 已从 `feature/m8b2b-knowledge-trace-integration` 整合回稳定基线 `feature/m7-broker-api-assessment`。
- M8C.1 已完成：
  - 已新增 `config/examples/public_history_backtest_long_horizon.json`，把 daily public history demo 扩展到 `2020-01-01 ~ 2025-12-31`，覆盖 `NVDA / TSLA / SPY` 三个 equity/ETF 标的。
  - 已新增 walk-forward 风格 split：`in_sample`、`validation`、`out_of_sample`，并补 regime 分层窗口摘要。
  - 已新增结构化产物：
    - `summary.json`
    - `split_summary.json`
    - `regime_breakdown.json`
    - `knowledge_trace_coverage.json`
    - `no_trade_wait.jsonl`
    - `trades.csv`
    - `knowledge_trace.json`
    - `report.md`
    - `equity_curve.png`
  - 已新增 `no-trade / wait` 结构化持久化，覆盖 `context_not_trend`、`duplicate_direction_suppressed`、`insufficient_evidence`、`risk_blocked_before_fill`。
  - 已补齐 per-symbol breakdown、regime 摘要、blocked signals 汇总、knowledge trace 覆盖率摘要，以及 curated vs statement trace 占比摘要。
  - 当前示例 run `m8c1_long_horizon_daily_validation` 在 `2020-01-01 ~ 2025-12-31`、`NVDA / TSLA / SPY`、`1d` 条件下输出：总收益率 `-0.0915%`、最大回撤 `1.5697%`、交易 `12` 笔、blocked signals `396`、`no_trade_wait` `4486`；仍明确保持 `paper / simulated`。
  - 已通过新增 `tests/reliability/test_long_horizon_daily_validation.py`、`tests/unit/test_public_backtest_demo.py` 回归，以及现有 `tests/reliability` / `tests/unit` 套件。
- M8C.2 已完成：
  - 已新增 `config/examples/intraday_pilot_spy_15m.json`，固定首轮 intraday pilot 为 `SPY / 15m / America/New_York / 2026-03-30 ~ 2026-04-16`。
  - 已新增 `scripts/intraday_pilot_lib.py` 与 `scripts/run_intraday_pilot.py`，在不改 trigger 的前提下，复用既有 replay / strategy / backtest / risk / execution / review 能力完成单标的 intraday paper validation。
  - 已把真实 `SPY 15m` regular-session 数据缓存到 `local_data/public_intraday/`，并生成独立 run `reports/backtests/m8c2_intraday_pilot_spy_15m/`。
  - 已落盘结构化产物：
    - `summary.json`
    - `session_summary.json`
    - `session_quality.json`
    - `knowledge_trace.json`
    - `knowledge_trace_coverage.json`
    - `no_trade_wait.jsonl`
    - `trades.csv`
    - `report.md`
    - `equity_curve.png`
  - 已验证的 intraday 能力包括：
    - session open/close
    - market hours / timezone
    - 日内风险重置
    - duplicate signal protection
    - slippage / fee 最小配置模型
    - `no-trade / wait` 结构化输出
    - intraday 下的 `knowledge_trace` 与 legacy `source_refs` 兼容
    - source family 失衡保护继续生效，Brooks statement 数量不影响 trigger 或权重
  - 当前示例 run `m8c2_intraday_pilot_spy_15m` 在 `SPY / 15m / 2026-03-30 ~ 2026-04-16` 条件下输出：总收益率 `0.0832%`、最大回撤 `0.9789%`、交易 `25` 笔、blocked signals `8`、`no_trade_wait` `285`；仍明确保持 `paper / simulated`。
  - 已新增 `tests/unit/test_intraday_pilot.py` 与 `tests/reliability/test_intraday_pilot_validation.py`，并通过现有 `tests/reliability` / `tests/unit` / public demo smoke 回归。
  - 已完成第二标的扩展验证：新增 `config/examples/intraday_pilot_nvda_15m.json`，并生成 `reports/backtests/m8c2_intraday_pilot_nvda_15m/`。
  - 当前示例 run `m8c2_intraday_pilot_nvda_15m` 在 `NVDA / 15m / 2026-03-30 ~ 2026-04-16` 条件下输出：总收益率 `0.8793%`、最大回撤 `0.0000%`、交易 `2` 笔、blocked signals `27`、`no_trade_wait` `310`；仍明确保持 `paper / simulated`。
  - 第二标的扩展未改 trigger，仍保持 session open/close、market hours / timezone、日内风险重置、duplicate signal protection、slippage / fee、`no-trade / wait` 与 `knowledge_trace` 兼容边界不变。
- M8D.1 Artifact & Trace Unification 已完成：
  - 已重算并落盘 `reports/backtests/m8c1_long_horizon_daily_validation/`，统一 `summary.json`、`report.md`、`knowledge_trace.json`、`knowledge_trace_coverage.json`、`no_trade_wait.jsonl`、`trades.csv`、`split_summary.json`、`regime_breakdown.json` 与 `equity_curve.png`。
  - 已把 long-horizon daily run 与当前 intraday trace contract 对齐：`actual hit`、`actual evidence`、`bundle support` 在 `summary / report / coverage` 中都已清晰分层。
  - user-facing artifact 中的 `source_refs` 现只代表 actual refs；`bundle_support_refs` 单独存在；`legacy_source_refs` 只作为兼容/追溯字段保留。
  - `m3-research-reference-pack` 不再在 long-horizon daily run 的 visible actual refs 中伪装成实际命中；当前只保留在 bundle support / legacy trace 层。
  - 已新增/补强 `tests/reliability/test_long_horizon_daily_validation.py`、`tests/reliability/test_intraday_pilot_validation.py`、`tests/unit/test_public_backtest_demo.py`，并通过 `tests/reliability`、`tests/unit` 与 `scripts/run_reliability_suite.py`。
- M8D.2 Curated Promotion Minimal Expansion 已完成：
  - 已新增第二轮最小 promoted theme：`breakout_follow_through_failed_breakout`、`tight_channel_trend_resumption`，并新增对应 rule 页 `knowledge/wiki/rules/breakout-follow-through-failed-breakout-minimal.md` 与 `knowledge/wiki/rules/tight-channel-trend-resumption-minimal.md`。
  - transcript / Brooks 现在会通过更具体的 promoted curated rule theme 进入 actual trace 的 `evidence_refs / evidence_locator_summary`；visible trace 不再只停留在 `market_cycle_context`、`signal_bar_entry` 与单一 `trend_vs_range_filter`。
  - 新 promoted theme 只进入 actual trace、signal explanation、`no-trade / wait`、report / review 层；仍保持 `draft / low confidence / research-only`，不进入 trigger。
  - 已重算并落盘 `reports/backtests/m8c1_long_horizon_daily_validation/`，使 checked-in daily artifact 与第二轮最小 promoted theme 保持一致。
  - 已更新 `tests/reliability/test_curated_promotion_minimal_set.py`、`tests/reliability/test_strategy_atom_trace.py`、`tests/reliability/test_long_horizon_daily_validation.py`、`tests/unit/test_strategy_signal_pipeline.py`，并继续通过 `python -m unittest discover -s tests/reliability -v` 与 `python -m unittest discover -s tests/unit -v`。
- M8D.3 Repository State Consistency 已完成：
  - 已把 `README.md`、`docs/status.md`、`plans/active-plan.md`、`docs/acceptance.md`、`docs/decisions.md`、`docs/roadmap.md` 的主线状态统一到 `main`。
  - 已把 `docs/testing-reliability.md`、`docs/eval-rubric.md`、`docs/shadow-mode-runbook.md`、`tests/reliability/README.md`、`tests/golden_cases/README.md`、`tests/integration/README.md`、`tests/test_data/real_history_small/*.md`、`reports/reliability/README.md` 的阶段口径统一到 `M8 shadow/paper baseline + M8D.1/.2/.3`。
  - 已明确：`main` 是唯一长期稳定基线；`feature/m7-broker-api-assessment` 只保留为历史阶段分支；当前主线已进入 `M8`，不是停留在 `M7`。
  - 本阶段未改 trigger，未改 `knowledge/raw`，未进入 broker/live/real-money，也未新增新的验证窗口或标的。
- M8E.1 Validation Gap Closure 已完成：
  - 已把 checked-in `m8c1_long_horizon_daily_validation` 的 `summary.json` 与 `report.md` 路径元数据统一为 repo-relative logical path，不再保留旧 worktree 绝对路径。
  - 已在 `reports/backtests/m8c1_long_horizon_daily_validation/summary.json` 新增 `sample_adequacy`，并在 `report.md` 新增“样本充分性”小节；当前规则固定为每个 split `executed_trade_count >= 5` 才视为 `adequate`。
  - 当前 `validation` 与 `out_of_sample` split 仍为 `insufficient_sample`，口径明确为“验证诚实但样本不足”，而不是伪装成已充分验证。
  - 已补齐 `tests/golden_cases/test_catalog_smoke.py`，使 `scripts/run_reliability_suite.py` 的 `golden` suite 不再长期处于无测试可执行状态。
  - 已补强 `tests/unit/test_public_backtest_demo.py`、`tests/reliability/test_long_horizon_daily_validation.py`、`tests/reliability/test_intraday_pilot_validation.py`，并固定 artifact `trades.csv` 使用 LF 写出以减少无意义字节漂移。
  - 本阶段未改 trigger，未改 `knowledge_trace` contract，未改 `knowledge/raw`，未进入 broker/live/real-money。
- M8E.2 Longer-Window Daily Validation 已完成：
  - 已新增 `config/examples/public_history_backtest_longer_window.json`，固定验证范围为 `NVDA / TSLA / SPY`、`1d`、`2018-01-01 ~ 2026-04-17`。
  - 已新增 checked-in run `reports/backtests/m8e2_longer_window_daily_validation/`，并落盘 `summary.json`、`report.md`、`knowledge_trace.json`、`knowledge_trace_coverage.json`、`no_trade_wait.jsonl`、`trades.csv`、`split_summary.json`、`regime_breakdown.json` 与 `equity_curve.png`。
  - 当前更长窗口 daily run 输出：总收益率 `1.5599%`、最大回撤 `1.9237%`、交易 `14` 笔、blocked signals `562`、`no_trade_wait` `6210`；仍明确保持 `paper / simulated`。
  - 新 run 继续使用 repo-relative artifact path，并继续携带 `sample_adequacy`；当前 `in_sample` 为 `adequate`，`validation` / `out_of_sample` 仍为 `insufficient_sample`。
  - 已新增 `tests/reliability/test_longer_window_daily_validation.py`，覆盖 artifact 完整性、split / regime / no-trade / trace coverage 回归与 `sample_adequacy` 落盘。
  - 本阶段未改 trigger，未改 `knowledge/raw`，未进入 broker/live/real-money，也未启动 intraday 更长窗口。
- M9A 项目保护与支线建立已完成：
  - 已从 `main` 新建 `feature/m9-price-action-strategy-lab`，不在 `main` 直接开发。
  - 已落盘 `reports/strategy_lab/m9_initial_project_snapshot.md`，固定记录起始分支、commit、`git status`、M8 进度摘要、目录概览与安全边界。
  - 已明确 `paper / simulated`、`no-go`、不得接真实账户、不得自动实盘下单的边界在 M9 继续有效。
- M9B 来源盘点已完成：
  - 已落盘 `reports/strategy_lab/m9_source_inventory.md`，并固定来源优先级为 `fangfangtu_transcript > al_brooks_ppt > fangfangtu_notes`。
  - 已把方方土 YouTube 转录补入第一优先来源，Brooks PPT 为第二优先，方方土 notes 为第三优先。
- M9C 首批策略卡提炼已完成：
  - 已新增 `knowledge/wiki/strategy_cards/` 目录、模板与索引。
  - 已落盘首批 `10` 张 Markdown 策略卡，覆盖趋势恢复、突破 follow-through、失败突破、区间边缘反转、H2/L2、tight channel、楔形、开盘区间、趋势日过滤、高波动风险过滤。
  - `PA-SC-007`、`PA-SC-008`、`PA-SC-010` 已明确保留 `draft`、`chart_dependency: high`、`needs_visual_review: true`。
- M9D 测试计划首轮已完成：
  - 已落盘 `reports/strategy_lab/m9_strategy_test_plan_index.md`。
  - `PA-SC-002`、`PA-SC-003`、`PA-SC-005` 已写成详细测试计划，其余卡片已写简版测试计划。
- M9F 面向非技术用户的总结已完成：
  - 已落盘 `reports/strategy_lab/m9_strategy_lab_summary.md`，汇总来源、策略卡、优先级、测试方案、当前不能证明的内容与下一步建议。
- M9E `PA-SC-002` 第一轮最小闭环已完成：
  - 已落盘 `knowledge/wiki/strategy_cards/combined/PA-SC-002-executable-v0.1.md`、`reports/strategy_lab/pa_sc_002_minimum_experiment_v0.1.md`、`reports/strategy_lab/pa_sc_002_first_backtest_report.md`。
  - 已在 `SPY / 5m / regular session only / yfinance / 2026-02-20 ~ 2026-04-17` 范围内完成第一轮最小回测，结果为 `98` 笔交易、Expectancy `-0.1066R`、净盈亏 `-$374.65`、期末权益 `$24,625.35`。该结果只保留为首轮历史记录；后续 Strategy Factory 的 provider 选择以 repo config contract 为准。
  - 已明确本轮资本口径：默认起始本金 `$25,000`、单笔风险预算 `$100`，后续实验固定同步报告美元盈亏、权益变化与美元回撤。
- M9E `PA-SC-002` 诊断型变体测试已完成：
  - 已落盘 `reports/strategy_lab/pa_sc_002_diagnostic_analysis.md` 与 `reports/strategy_lab/pa_sc_002_variant_suite.md`，并生成 `reports/strategy_lab/pa_sc_002_variant_suite_artifacts/`。
  - 当前诊断结论为：`Midday Block` 是唯一既改善明显、又仍达到最小 probe 门槛的单因素变体；`Stronger Negative Veto` 单独使用改善不足；`Late Only` 仅可视为后验诊断上限，不可直接升格为正式版本。
  - 当前 `PA-SC-002` 的业务结论仍为“修改后重测”，尚未达到保留进入第二轮改进的门槛。
- M9 provider-specific 历史数据接入与长窗口验证已完成：
  - 已新增 `scripts/longbridge_history_lib.py`，通过 `longbridge kline history --format json` 接入只读历史数据 adapter，并支持 `1m / 5m / 15m / 30m / 1h / 1d / 1w / 1mo / 1y` 的分段抓取。
  - 已新增 `config/examples/public_history_backtest_long_horizon_longbridge.json` 与 `config/examples/intraday_pilot_spy_5m_longbridge.json`，分别用于 `NVDA / TSLA / SPY 1d` 长周期 daily validation 与 `SPY 5m` intraday pilot。
  - 已完成模拟账户授权与真实下载验证：`NVDA / TSLA / SPY 1d` 已成功拉到 `2020-01-02 ~ 2025-12-31`，`SPY 5m` 已确认在当前账户权限下可稳定拉到 `2024-04-01 ~ 2026-04-17`。
  - 已新增 vendor anomaly 隔离：若供应商返回的 OHLC 明显违反 `high/low` 约束，会被单独落到 `.vendor_anomalies.json`，不混入正式回测 CSV；当前已记录 `SPY 1d 2020-11-18` 一根异常 bar。
  - 已把 daily public-history demo 的 risk session 从单一 `historical-demo` 改为 `session_key reset per trading day`，避免 `consecutive_losses_limit` / `daily_loss_limit` 跨多年窗口持续冻结后续样本。
  - 已修复 intraday session audit 的 timeframe 识别：`5m` regular session 现可正确接受 `15:50`、`15:55` 两根尾盘 bar，不再误判为 out-of-hours。
  - 当前 longbridge daily run `reports/backtests/m9_longbridge_daily_validation_reset/` 在 `2020-01-01 ~ 2025-12-31`、`NVDA / TSLA / SPY`、`1d` 条件下输出：总收益率 `14.2321%`、最大回撤 `7.3702%`、交易 `370` 笔；`in_sample / validation / out_of_sample` 三个 split 当前均为 `adequate`。
  - 当前 longbridge intraday run `reports/backtests/m9_longbridge_intraday_spy_5m_full_window/` 在 `2024-04-01 ~ 2026-04-17`、`SPY / 5m` 条件下输出：总收益率 `-62.6007%`、最大回撤 `62.6647%`、交易 `2869` 笔、可用 session `507 / 514`；说明数据覆盖已足够，但当前策略在该日内口径下表现较差。
  - 上述 provider-specific run 只保留为历史事实；当前 Strategy Factory 的 provider 口径已切到 config contract，而不是把某个 provider 名称写成长期默认值。
- M9 配套契约与门禁已同步：
  - 已更新 `plans/active-plan.md`、`docs/acceptance.md`、`docs/decisions.md`、`docs/requirements.md`、`docs/architecture.md`、`docs/pa-strategy-spec.md`、`docs/knowledge-base-design.md`、`docs/knowledge-atomization.md`、`docs/roadmap.md`、`README.md`。
  - 已更新 `knowledge/schema/*.md`、`scripts/validate_kb.py`、`scripts/build_kb_index.py`，并新增 `tests/unit/test_kb_scripts.py` 以支持 strategy cards frontmatter 与 `templates/` 跳过逻辑。
- M9G.0 Strategy Factory Contract Freeze & Legacy Boundary Reset 已完成：
  - 已新增 `config/strategy_factory/active_provider_config.json`，并把 Strategy Factory 的 `primary_provider` 固定为 `active_provider_config_path -> source_order[0]` 的运行时 contract。
  - 已新增 `docs/strategy-factory.md`、`reports/strategy_lab/strategy_factory_plan.md` 与 `knowledge/wiki/strategy_factory/` 目录骨架。
  - 已新增 `reports/strategy_lab/strategy_factory/coverage_ledger.json`、`extraction_queue.json`、`catalog_ledger.json`、`backtest_queue.json`、`triage_ledger.json`、`run_state.json`、`final_summary.md`。
  - `coverage_ledger.json` 当前已完整入账 `3 family / 10 source`，并显式保留 `fangfangtu-beginner-note`、`fangfangtu-wedge-note(partial)`、`al-brooks-price-action-ppt-37-52-units`。
  - legacy `PA-SC-*` 目录与 `PA-SC-002` 历史报告当前只作为 legacy / historical baseline；新 catalog 不再以其编号、聚类或结论作为先验。
  - 已新增 `scripts/validate_strategy_factory_contract.py` 与 `tests/unit/test_strategy_factory_contract.py`，用于校验 repo config、contract docs 与 `run_state.json` 的一致性。
- M9G Full Extraction Completeness Audit v4 已完成：
  - 已新增并冻结 `reports/strategy_lab/strategy_catalog.json`、`strategy_dedup_map.json`、`chunk_adjudication.jsonl`、`source_family_completeness_report.json`、`source_theme_coverage.json`、`cross_chunk_synthesis.json`、`cross_source_corroboration.json`、`cross_source_corroboration_final.json`、`overmerge_review.json`、`saturation_report.json`、`unresolved_strategy_extraction_gaps.json`、`full_extraction_audit.json` 与 `factory_summary.md`。
  - 已生成 `reports/strategy_lab/cards/SF-001.md` ~ `SF-005.md` 与对应 `reports/strategy_lab/specs/SF-001.yaml` ~ `SF-005.yaml`。
  - 新 catalog 当前冻结为 `5` 张 `SF-*` strategy cards：`SF-001 Trend Pullback Second Entry`、`SF-002 Breakout Follow-Through Continuation`、`SF-003 Failed Breakout Range-Edge Reversal`、`SF-004 Tight Channel Trend Continuation`、`SF-005 Gap Continuation Versus Exhaustion`。
  - `full_extraction_audit.json` 当前结论为：`text_extractable_closure=true`、`full_source_closure=false`；原因是 visual-heavy / partial source gaps 仍存在，并已逐条写入 `unresolved_strategy_extraction_gaps.json`。
  - `saturation_report.json` 当前已完成双轮连续零增量收敛检查，`closure_reached=true`。
  - `cross_source_corroboration_final.json` 已在 catalog freeze 之后重算，后续文档与汇总只允许引用 final 版本。
  - 当前 `ready_for_backtest=true` 只表示 text-extractable catalog 已具备进入下一阶段的条件，不代表本轮已启动任何 batch backtest。
- M9H Controlled Batch Backtest + Strategy Triage 已完成 wave2 扩样本回测：
  - 已新增并落盘：
    - `reports/strategy_lab/backtest_eligibility_matrix.json`
    - `reports/strategy_lab/backtest_dataset_inventory.json`
    - `reports/strategy_lab/executable_spec_queue.json`
    - `reports/strategy_lab/backtest_queue.json`
    - `reports/strategy_lab/backtest_batch_summary.json`
    - `reports/strategy_lab/strategy_triage_matrix.json`
    - `reports/strategy_lab/final_strategy_factory_report.md`
    - `reports/strategy_lab/final_strategy_factory_trade_report.md`
    - `reports/strategy_lab/final_strategy_factory_cash_report.md`
  - 已为 `SF-001 ~ SF-005` 生成 `reports/strategy_lab/<strategy_id>/executable_spec.md`、`test_plan.md`、`summary.json`、`candidate_events.csv`、`skip_summary.json`、`diagnostics.md`；`SF-001 ~ SF-004` 另生成 baseline / quality_filter 两个 variant artifact 与按数据集拆分的 `variants/<variant_id>/datasets/<symbol>/` 产物。
  - wave2 让 `SF-001 ~ SF-004` 进入 `SPY / QQQ / NVDA / TSLA`、`5m`、`2025-04-01 ~ 2026-04-21` 的 primary-provider 聚合回测；`SF-005` 当前明确保持 `deferred_single_source_risk`。
  - 当前 triage 结果为：
    - `SF-001 = modify_and_retest`
    - `SF-002 = modify_and_retest`
    - `SF-003 = modify_and_retest`
    - `SF-004 = modify_and_retest`
    - `SF-005 = deferred_single_source_risk`
  - 当前样本门槛结论为：
    - `SF-001 ~ SF-004 = robust_candidate`
    - `SF-005 = not_run`
- 当前 `automation_state.json`、`heartbeat.jsonl`、`reports/strategy_lab/strategy_factory/backtest_queue.json`、`triage_ledger.json`、`run_state.json` 已同步到 `M9H.wave2_batch_backtest_triage_completed`。
- 当前回测结论仍严格保持 `paper / simulated`；不构成 broker/live/real-money 能力，不构成稳定盈利结论。
- `quality_filter` 当前只允许被解释为 `diagnostic_selected_variant`；`SF-003 baseline`、`SF-003 quality_filter`、`SF-004 quality_filter` 的 R/cash 异号必须作为 cash sizing 差异解读，不能直接视为策略转正。
- M9I.1 Freeze v0.2 Candidate Specs 已完成：
  - 已新增 `reports/strategy_lab/specs/SF-001-v0.2-candidate.yaml`、`SF-002-v0.2-candidate.yaml`、`SF-003-v0.2-candidate.yaml`、`SF-004-v0.2-candidate.yaml`。
  - 以上 4 份文件都保持为完整自包含的候选包装层，绑定 `Wave2` 证据路径，并明确 `v0.1 -> v0.2-candidate` 的收紧项、预期改善和残余风险。
  - 现有 `reports/strategy_lab/specs/SF-001.yaml ~ SF-004.yaml` 继续保留为 `v0.1` 基线；`strategy_catalog.json` 继续保持 `frozen`。
  - `SF-005` 当前仍保持 `deferred_single_source_risk`，未进入本轮 `v0.2-candidate` freeze。
  - 已新增 `reports/strategy_lab/v0_2_spec_freeze_summary.md` 汇总本轮冻结结果与下一步验证建议。
- M9I.2 Wave3 Holdout / Walk-forward Robustness Validation 已完成：
  - 已新增 `reports/strategy_lab/wave3_robustness_summary.md` 与 `wave3_robustness_summary.json`，并在 `reports/strategy_lab/SF-001~004/wave3/` 下落盘 holdout / walk-forward / symbol / regime / time-of-day / cost-stress / conversion 产物。
  - 本轮只加载冻结后的 `SF-001 ~ SF-004 v0.2-candidate`，未修改 specs，未新增过滤器，`SF-005` 继续保持 `deferred_single_source_risk`。
  - 当前 Wave3 实际公共窗口为 `2025-04-01 ~ 2026-04-21`、`SPY / QQQ / NVDA / TSLA`、`5m`；由于没有 `strict post-freeze holdout`，本轮不允许输出 `retain_candidate`。
  - 当前 Wave3 triage 结果为：
    - `SF-001 = modify_and_retest`
    - `SF-002 = modify_and_retest`
    - `SF-003 = modify_and_retest`
    - `SF-004 = insufficient_sample`
  - `strategy_triage_matrix.json` 当前已保留 Wave2 历史，并追加 Wave3 history；`run_state.json` 已同步到 `M9I.2.wave3_robustness_validation_completed`。
- M10 Price Action Strategy Refresh 已启动：
  - 已从 `main` 创建独立 worktree/branch：`codex/m10-price-action-strategy-refresh`。
  - 已落盘 `reports/strategy_lab/m10_price_action_strategy_refresh/workspace_audit_legacy_inventory_m10.md`，把现有 M8/M9 worktrees、`PA-SC-*`、`SF-*`、旧 catalog/specs/triage 全部登记为 legacy-only。
  - 已导入 Brooks v2 manual transcript 到 `knowledge/raw/brooks/transcribed_v2/al_brooks_price_action_course_v2/`，当前包含 `191` 个 `units/*.md`、`191` 个 `evidence/*.md` 和本地 `assets/evidence/` 图片资产；图片按 `.gitignore` local-only 管理，校验和写入 `assets_evidence_checksums.sha256`。
  - 已新增 Brooks v2、ChatGPT share、Codex thread 的 source page；ChatGPT share 和 Codex thread 均标记为 reference-only，不作为策略 source of truth。
  - 已更新 source/chunk/atom/callable builder，使 `brooks_v2_manual_transcript`、`fangfangtu_youtube_transcript`、`fangfangtu_notes` 可独立检索；当前 source coverage 为 `total_registered=11 / parsed=10 / partial=1 / blocked=0`。
  - 已生成 clean-room `M10-PA-*` 初版 catalog，共 `16` 个策略/规则条目，输出到 `reports/strategy_lab/m10_price_action_strategy_refresh/strategy_catalog_m10.json`。
  - 已生成 `source_support_matrix_m10.json`、`chatgpt_bpa_comparison.json`、`legacy_comparison_m10.json`、`visual_gap_ledger.json`、`backtest_eligibility_matrix.json` 与 `m10_test_plan.md`。
  - 已完成 M10.1 目录复审与测试承接，新增 `m10_catalog_review.md`、`strategy_catalog_m10_frozen.json`、`m10_strategy_test_queue.json`。
  - M10.1 当前冻结 `16` 条策略/规则：`M10-PA-001/002/005/012` 进入 Backtest Wave A，`M10-PA-013` 进入 Wave B candidate，`M10-PA-003/004/007/008/009/010/011` 进入 Visual golden case first，`M10-PA-014/015` 为 supporting rules，`M10-PA-006/016` 为 research-only。
  - Visual golden case 不是所有策略的统一门槛，只针对上述强图形依赖策略；supporting / research 条目不走普通 visual review，也不作为独立 trigger。
  - 已完成 M10.2 Visual Golden Case Pack，新增 `visual_golden_cases/`、`m10_visual_golden_case_index.json`、`m10_visual_case_selection_ledger.json`、`m10_2_visual_review_summary.md`、`m10_3_backtest_spec_handoff.md`。
  - M10.2 当前 `ready_count=7 / blocked_count=0`；每个 visual strategy 均有 `3` 个 Brooks v2 正例、`1` 个反例、`1` 个边界例，且 evidence image logical path / checksum 可解析。
  - `visual_pack_ready` 只表示图例包证据完整，仍需人工 visual review；不代表策略有效、盈利或可进入实盘。
  - 已完成 M10.3 Backtest Spec Freeze，新增 `backtest_specs/`、`m10_backtest_spec_index.json`、`m10_event_definition_ledger.json`、`m10_skip_rule_ledger.json`、`m10_cost_sample_gate_policy.json`、`m10_3_backtest_spec_freeze_summary.md`。
  - M10.3 当前只覆盖 Wave A：`M10-PA-001/002/005` 测 `1d / 1h / 15m / 5m`，`M10-PA-012` 测 `15m / 5m`；未把 visual-first、supporting、research-only 条目放入 Wave A spec。
  - M10.3 只冻结事件识别、entry/stop/target、skip 规则、成本敏感性和样本门槛；未运行 historical backtest，未输出收益、`retain/promote` 或实盘能力结论。
  - 已完成 M10.4 Historical Backtest Pilot，新增 `scripts/m10_historical_pilot_lib.py`、`scripts/run_m10_historical_pilot.py`、`config/examples/m10_wave_a_historical_pilot.json` 与 `reports/strategy_lab/m10_price_action_strategy_refresh/historical_pilot/m10_4_wave_a_pilot/`。
  - M10.4 使用 sibling main worktree 的本地 Longbridge cache：`1d` 固定长窗口为 `2010-06-29 ~ 2026-04-21`，`5m` 使用 `2024-04-01 ~ 2026-04-21` intraday cache，`15m / 1h` 从 `5m` 聚合并记录 `derived_from_5m`。
  - M10.4 当前 `available_count=16 / deferred_count=0`，覆盖 `SPY / QQQ / NVDA / TSLA`、Wave A 共 `14` 个 strategy/timeframe 组合；每个组合都输出 candidate events、skip/no-trade ledger、source ledger、成本敏感性、per-symbol、per-regime 与 failure-mode notes。
  - M10.4 当前所有组合 outcome 均为 `continue_testing`；这只表示样本门槛和链路检查继续可测，不代表策略有效、盈利、可保留、可推广或可实盘。
  - M10.4 已通过 `validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、M10.1/M10.3/M10.4 单测、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.5 Read-only Observation Plan，新增 `m10_5_pilot_quality_review.md`、`m10_5_observation_candidate_queue.json`、`m10_5_read_only_observation_plan.md`、`m10_5_observation_event_schema.json`、`m10_5_paper_gate_handoff.md`。
  - M10.5 只把 Wave A 转成只读观察候选队列：`M10-PA-001/002/005` 保留 `1d / 1h / 15m / 5m`，`M10-PA-012` 只保留 `15m / 5m`；visual-first、Wave B、supporting-only、research-only 均未进入队列。
  - M10.5 当前实时只读输入状态为 `observation_input_deferred`，未启动 observation runner；`1d` 只允许规划为收盘后观察，`1h / 15m / 5m` 只允许规划为 regular-session bar close 后记录。
  - M10.5 已把 `M10-PA-005` 的 `1h / 15m / 5m` 标记为 `definition_breadth_review`，该标记只代表定义宽度需复核，不代表 PnL 结论。
  - M10.5 已通过 `validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、M10.1/M10.3/M10.4/M10.5 单测、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.6 Read-only Observation Input / Ledger Prototype，新增 `config/examples/m10_read_only_observation_replay.json`、`scripts/m10_read_only_observation_lib.py`、`scripts/run_m10_read_only_observation.py` 与 `reports/strategy_lab/m10_price_action_strategy_refresh/read_only_observation/m10_6_replay/`。
  - M10.6 使用本地 cached OHLCV recorded replay，当前 `deferred_input_count=0`，`lineage_counts={"native_cache": 8, "derived_from_5m": 8}`；未接实时行情订阅、broker、真实账户或订单通道。
  - M10.6 当前生成 `108640` 条 observation ledger row：`73619` 条 candidate event、`35021` 条 skip/no-trade；ledger 只用于检查输入、schema、bar-close 记录和人工复核流程。
  - M10.6 未输出策略有效、盈利、保留、推广、paper gate 或实盘能力结论；M11 paper gate 继续关闭。
  - M10.6 已通过 `validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、M10.1/M10.3/M10.4/M10.5/M10.6 单测、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.7 Business Metric Policy，新增 `m10_7_business_metric_policy.md`、`m10_7_capital_model.json`、`m10_7_client_report_template.md`。
  - M10.7 固定后续甲方报告默认口径：初始本金 `100,000 USD`、单笔风险当前权益 `0.5%`、不使用杠杆、每条 strategy/timeframe/symbol 先独立账户测试、成本压力 `1 / 2 / 5 bps`。
  - M10.7 只冻结后续模拟资金曲线和成绩单口径；未运行新回测，未批准 paper trading，未接 broker、真实账户或订单通道。
  - M10.7 已通过新增 business metric policy 单测、`validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.8 Wave A Capital Backtest，新增 `scripts/m10_capital_backtest_lib.py`、`scripts/run_m10_wave_a_capital_backtest.py`、`capital_backtest/m10_8_wave_a/` 与 M10.8 单测。
  - M10.8 使用 M10.4 candidate events 重新按 M10.7 资本模型计算仓位、成本、权益、胜率和回撤；未重新发明信号，未读取旧 `PA-SC-*` / `SF-*` 作为策略来源。
  - M10.8 当前生成 baseline trade ledger `73619` 行、all-cost simulated rows `220857` 行、metrics `222` 行，并输出 baseline equity curve CSV/SVG。
  - M10.8 baseline strategy results：`M10-PA-001` 净利润 `395723.86` / 收益 `24.7327%` / 胜率 `0.3584`；`M10-PA-002` 净利润 `52496.28` / 收益 `3.2810%` / 胜率 `0.3492`；`M10-PA-005` 净利润 `-147665.89` / 收益 `-9.2291%` / 胜率 `0.3401`；`M10-PA-012` 净利润 `215141.29` / 收益 `26.8927%` / 胜率 `0.3815`。
  - M10.8 继续把 `M10-PA-005` 标记为 `needs_definition_fix`，资本结果不得覆盖其 `definition_breadth_review` 问题。
  - M10.8 已通过新增 Wave A capital backtest 单测、M10.7 policy 单测、`validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.9 Definition Tightening，新增 `scripts/m10_definition_tightening_lib.py`、`scripts/run_m10_definition_tightening.py`、`definition_tightening/m10_9_pa_005/` 与 M10.9 单测。
  - M10.9 只处理 `M10-PA-005`：先移除同一确认 bar 的重复候选，再对 `1h / 15m / 5m` 同标的同方向失败突破施加 20-bar 冷却；该逻辑不读取 PnL、资金曲线、胜率或 profit factor。
  - M10.9 baseline retest：`1d` 交易数 `1481 -> 1188`、净利润 `-34130.96 -> -22855.77`；`1h` `1469 -> 950`、`-613.09 -> 4527.65`；`15m` `7511 -> 2966`、`50820.21 -> 14707.57`；`5m` `23881 -> 8007`、`-163742.05 -> -107134.41`。
  - M10.9 明确记录上游缺口：M10.4 candidate events 未持久化 `range_high/range_low/range_midpoint/breakout_extreme/reentry_confirmation_index`；因此 `M10-PA-005` 仍保持 `needs_definition_fix`，不能升级为已修好策略。
  - M10.9 已通过新增 definition tightening 单测、M10.7/M10.8/M10.9 组合单测、`validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.10 Visual Wave B Gate，新增 `scripts/m10_visual_wave_b_gate_lib.py`、`scripts/run_m10_visual_wave_b_gate.py`、`visual_wave_b_gate/m10_10/` 与 M10.10 单测。
  - M10.10 当前 Wave B queue 为 `M10-PA-013/003/008/009/011`；其中 `M10-PA-003/008/009/011` 来自 visual gate，`M10-PA-013` 为既有 Wave B candidate。
  - M10.10 暂不进入自动回测的视觉策略：`M10-PA-004` 与 `M10-PA-007` 标记 `needs_definition_fix`，`M10-PA-010` 标记 `visual_only_not_backtestable`。
  - M10.10 已通过新增 visual Wave B gate 单测、M10 strategy refresh 单测、`validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.11 Wave B Capital Backtest，新增 `scripts/m10_wave_b_capital_backtest_lib.py`、`scripts/run_m10_wave_b_capital_backtest.py`、`capital_backtest/m10_11_wave_b/` 与 M10.11 单测。
  - M10.11 覆盖 `M10-PA-013/003/008/009/011`，当前生成 baseline trade ledger `20055` 行、metrics `270` 行、candidate events `20055` 行，并输出 baseline equity curve CSV/SVG。
  - M10.11 baseline strategy results：`M10-PA-003` 净利润 `-54431.35` / 收益 `-4.5359%` / 胜率 `0.3391`；`M10-PA-008` 净利润 `64057.47` / 收益 `4.0036%` / 胜率 `0.3541`；`M10-PA-009` 净利润 `16277.18` / 收益 `1.0173%` / 胜率 `0.3395`；`M10-PA-011` 净利润 `-13014.18` / 收益 `-1.6268%` / 胜率 `0.2881`；`M10-PA-013` 净利润 `-127027.54` / 收益 `-7.9392%` / 胜率 `0.3255`。
  - M10.11 视觉策略结果来自 OHLCV 近似规则，仍需人工图形复核；不得解释为策略批准、paper trading 准入或实盘能力。
  - M10.11 已通过新增 Wave B capital backtest 单测、M10.10 visual gate 单测、`validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.12 All Strategy Scorecard，新增 `scripts/m10_all_strategy_scorecard_lib.py`、`scripts/run_m10_all_strategy_scorecard.py`、`all_strategy_scorecard/m10_12/` 与 M10.12 单测。
  - M10.12 当前覆盖全部 `16` 条 `M10-PA-*`：`8` 条完成资金测试、`3` 条需要定义修正、`1` 条图形复核保留、`2` 条只能辅助、`2` 条 research-only。
  - M10.12 portfolio proxy 使用 `100,000 USD` 初始本金、最大同时风险 `4%`、最多同时持仓 `8`，只纳入 `completed_capital_test` 策略，并排除仍需定义修正的 `M10-PA-005`；当前 proxy final equity 为 `105728.18 USD`、proxy net profit 为 `5728.18 USD`，该结果不是按真实时间戳合并订单的可执行组合回测。
  - M10.12 输出 `m10_12_all_strategy_metrics.csv`、`m10_12_strategy_decision_matrix.json`、`m10_12_portfolio_simulation_report.md`、`m10_12_client_final_report.md` 与结构化 summary；仍不批准 paper trading，不接 broker、不下单。
  - M10.12 已通过新增 all-strategy scorecard 单测、`validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M10.13 Read-only Observation Runbook，新增 `scripts/m10_read_only_runbook_lib.py`、`scripts/run_m10_read_only_runbook.py`、`read_only_observation/m10_13/` 与 M10.13 单测。
  - M10.13 主观察队列为 `M10-PA-001/002/012/008/009`，覆盖 `13` 个策略周期；筛选规则为 completed capital test、整体收益为正、且进入观察的 timeframe 收益为正；其中 `M10-PA-008/009` 来自 M10.11/M10.12 后续筛选，必须保留人工图形语境复核。
  - M10.13 明确排除 `M10-PA-005`，直到 range-geometry 定义修正完成；`M10-PA-003/011/013` 保留 watchlist/deferred，不进入主观察队列。
  - M10.13 输出 `m10_13_observation_candidate_queue.json`、`m10_13_read_only_observation_runbook.md`、`m10_13_weekly_observation_template.md` 与结构化 summary；仍不启动 observation runner，不接 broker、不下单。
  - M10.13 已通过新增 read-only observation runbook 单测、`validate_kb.py`、`validate_kb_coverage.py`、`validate_knowledge_atoms.py`、完整 `tests/unit`、完整 `tests/reliability` 与 `git diff --check`。
  - 已完成 M11 Paper Gate，新增 `scripts/m11_paper_gate_lib.py`、`scripts/run_m11_paper_gate.py`、`paper_gate/m11/` 与 M11 单测。
  - M11 候选池来自 M10.13 主观察队列：`M10-PA-001/002/012/008/009`；其中 `M10-PA-001/002/012` 是 Tier A 核心观察候选，`M10-PA-008/009` 是 Tier B 视觉条件候选，必须先关闭人工图形语境复核。
  - M11 gate decision 为 `not_approved`，所有候选当前都标记为 `counts_as_gate_evidence_now=false`；当前没有任何策略可作为 paper trading approval evidence。
  - M11 明确阻塞项：尚无真实只读观察周报闭环、缺少人工业务审批、视觉候选仍需人工图形复核、broker/order 路径必须继续禁用。
  - M11 输出 `m11_paper_gate_report.md`、`m11_candidate_strategy_list.json`、`m11_risk_and_pause_policy.md` 与结构化 summary；仍不启动 observation runner，不接 broker、不接真实账户、不下单、不批准 paper trading。
  - M10/M11 明确保持 `paper / simulated`；未接 broker、未接真实账户、未进入实盘或自动下单。
  - 已完成 M12.0 Longbridge Read-only Auth Preflight，新增 `scripts/m12_readonly_auth_preflight_lib.py`、`scripts/run_m12_readonly_auth_preflight.py`、`m12_read_only_pipeline/m12_0_auth_preflight/` 与 M12.0 单测。
  - M12.0 当前 auth status 为 `valid_readonly_market_data`，说明本地 Longbridge CLI、token、quote snapshot 与 latest K-line 只读探针可用；下一步可进入 M12.1 只读 bar-close feed。
  - M12.0 只允许 `check / quote / kline / subscriptions` 行情检查命令；交易、账户、资产、持仓、现金、融资、订单相关命令均未调用，且继续保持 `broker_connection=false / real_orders=false / live_execution=false / paper_trading_approval=false`。
  - 已完成 M12.1 Longbridge Read-only Feed，新增 `config/examples/m12_longbridge_readonly_feed.json`、`scripts/m12_readonly_feed_lib.py`、`scripts/run_m12_readonly_feed.py`、`m12_read_only_pipeline/m12_1_readonly_feed/` 与 M12.1 单测。
  - M12.1 当前生成 `16` 条只读 feed ledger，覆盖 `SPY / QQQ / NVDA / TSLA` 与 `1d / 1h / 15m / 5m`；quote snapshot 只作为健康检查，K 线轮询作为主输入，subscriptions 只作为诊断。
  - M12.1 strategy scope 只包含 Tier A：`M10-PA-001/002/012`；未纳入 `M10-PA-008/009` 视觉候选、definition-fix、supporting-only 或 research-only 条目。
  - M12.1 不运行策略、不生成交易/账户字段、不输出盈亏结论，继续保持 `paper_simulated_only=true / broker_connection=false / real_orders=false / live_execution=false`。
  - 已完成 M12.2 Core Strategy Daily Observation，新增 `config/examples/m12_core_strategy_daily_observation.json`、`scripts/m12_core_daily_observation_lib.py`、`scripts/run_m12_core_daily_observation.py`、`m12_read_only_pipeline/m12_2_daily_observation/` 与 M12.2 单测。
  - M12.2 只消费 M12.1 feed ledger、M10.13 observation queue、M11 Tier A 候选与 M10.3 backtest specs；自动观察范围严格限制为 `M10-PA-001/002/012`，未纳入 `M10-PA-008/009` 视觉条件候选。
  - M12.2 当前生成 `32` 条只读观察记录：`M10-PA-001` 12 条、`M10-PA-002` 12 条、`M10-PA-012` 8 条；由于输入只有单根 latest bar，本阶段全部诚实标记为 `skip_no_trade`，不从单根 bar 编造完整策略触发。
  - M12.2 输出 `m12_2_daily_observation_report.md`、`m12_2_observation_events.csv`、`m12_2_observation_events.jsonl` 与 `m12_2_strategy_status_matrix.json`；继续保持 `paper_simulated_only=true / broker_connection=false / real_orders=false / live_execution=false / paper_trading_approval=false`。
  - 已完成 M12.3 Visual Review Precheck，新增 `config/examples/m12_visual_review_precheck.json`、`scripts/m12_visual_review_precheck_lib.py`、`scripts/run_m12_visual_review_precheck.py`、`visual_review/m12_3_precheck/` 与 M12.3 单测。
  - M12.3 覆盖 `M10-PA-008/009` 优先复核、`M10-PA-003/011/013` 观察名单、`M10-PA-004/007` 定义问题辅助证据；其中 `M10-PA-013` 明确为无 visual pack 的既有 Wave B candidate。
  - M12.3 当前生成 `7` 条 strategy rows 与 `30` 条 case rows；当前 main worktree 缺 local-only Brooks 图片，但旧 M10 worktree 中 30/30 个本阶段所需 case 图片存在且 checksum 全部匹配。
  - M12.3 只是 agent 预审和人工复核包，不关闭人工 visual judgment，不批准 paper trading，不接 broker、不下单。
  - 已完成 M12.4 Definition Fix and Retest，新增 `config/examples/m12_definition_fix_and_retest.json`、`scripts/m12_definition_fix_and_retest_lib.py`、`scripts/run_m12_definition_fix_and_retest.py`、`definition_fix/m12_4_definition_fix_and_retest/` 与 M12.4 单测。
  - M12.4 只覆盖 `M10-PA-005/004/007`：`M10-PA-005` 复用 M10.9 结构性复测，`M10-PA-004/007` 只登记 M12.3/M10.10 支撑的定义字段缺口，不伪造 before/after 交易结果。
  - M12.4 当前结论为三条策略继续保持 `needs_definition_fix`；本阶段没有根据收益曲线调参，不批准 paper trading，不接 broker、不下单。
  - 已完成 M12.5 Liquid Universe Scanner，新增 `config/examples/m12_liquid_universe_scanner.json`、`scripts/m12_liquid_universe_scanner_lib.py`、`scripts/run_m12_liquid_universe_scanner.py`、`scanner/m12_5_liquid_universe_scanner/` 与 M12.5 单测。
  - M12.5 第一版高流动性 US 股票/ETF seed 共 `147` 只，自动 scanner 只启用 Tier A：`M10-PA-001/002/012`；当前本地 cache 实际完成 `SPY / QQQ / NVDA / TSLA` 扫描并输出 `12` 条候选，其余 `143` 只缺少本地数据的 seed 全部 deferred，不补假候选。
  - 已完成 M12.6 Weekly Client Scorecard，新增 `config/examples/m12_weekly_client_scorecard.json`、`scripts/m12_weekly_client_scorecard_lib.py`、`scripts/run_m12_weekly_client_scorecard.py`、`weekly_scorecard/m12_6/` 与 M12.6 单测。
  - M12.6 汇总 M10.12 历史资金测试、M12.2 每日只读观察、M12.3 图形预审、M12.4 定义修正和 M12.5 scanner，输出 `16` 条策略 dashboard、本周客户周报和下周行动计划；当前交易状态仍为 `closed_not_authorized`。
  - 已完成 M11.5 Paper Gate Recheck，新增 `config/examples/m11_5_paper_gate_recheck.json`、`scripts/m11_5_paper_gate_recheck_lib.py`、`scripts/run_m11_5_paper_gate_recheck.py`、`paper_gate/m11_5_recheck/` 与 M11.5 单测。
  - M11.5 基于 M12.2-M12.6 的实际 artifact 复查 `M10-PA-001/002/012/008/009`，当前 gate decision 仍为 `not_approved`；`paper_trading_approval=false`，`broker_connection=false`，`real_orders=false`，`live_execution=false`。
  - M11.5 当前阻塞项为：没有完成真实只读观察窗口、M12.2 没有 completed candidate events、`M10-PA-008/009` 人工图形复核未关闭、`M10-PA-005/004/007` 定义 blocker 未关闭或未正式降级、scanner universe cache 覆盖不完整、缺少人工业务审批。
  - 已完成 M12.7 Daily Trend Benchmark Reuse，新增 `config/examples/m12_daily_trend_benchmark.json`、`scripts/m12_daily_trend_benchmark_lib.py`、`scripts/run_m12_daily_trend_benchmark.py`、`benchmark/m12_7_daily_trend_benchmark/` 与 M12.7 单测。
  - M12.7 把早期截图中的 `signal_bar_entry_placeholder` 日线逻辑固定为 `M12-BENCH-001 Daily Trend Momentum Baseline`，只作为 benchmark / scanner 因子候选，不作为 M10 clean-room strategy，也不作为准入证据。
  - M12.7 使用本地 Longbridge `1d` cache 重跑 `SPY / QQQ / NVDA / TSLA` 的 `2010-06-29 ~ 2026-04-21` 长窗口；在 `100,000 USD` 模拟初始资金、`500 USD` 单位预算口径下，输出 `1165` 条 benchmark 模拟事件、模拟最终权益 `$149,786.19`、模拟净结果 `$49,786.19`、模拟收益率 `49.7862%`、胜率 `36.2232%`、profit factor `1.1357`、模拟峰谷回落 `12.1939%`。
  - M12.7 当前决策为 `scanner_factor_candidate`，可供后续 M12.8/M12.11 的 scanner 排名因子设计参考；仍明确保持历史模拟 benchmark 边界，不作为准入证据。
  - 已完成 M12.8 Universe Kline Cache Completion，新增 `config/examples/m12_universe_kline_cache.json`、`scripts/m12_universe_kline_cache_lib.py`、`scripts/run_m12_universe_kline_cache.py`、`kline_cache/m12_8_universe_kline_cache/` 与 M12.8 单测。
  - M12.8 读取 M12.5 的 `147` 只 seed，生成 `588` 条 `symbol x timeframe` cache manifest row：`1d / 5m` 为 native cache，`15m / 1h` 明确为 `derived_from_5m`。
  - M12.8 当前有任一 native cache 的标的是 `4` 只：`NVDA / QQQ / SPY / TSLA`；因目标窗口更新到 `2026-04-27`，完整覆盖目标窗口的标的是 `0` 只，缺口全部进入 deferred/error ledger。
  - M12.8 已生成 `294` 个 native cache 只读 fetch plan request，估算 `23373` 个 Longbridge chunk；默认 checked-in run 不执行批量下载、不写 `local_data/`，避免把大规模外部请求伪装成已完成缓存。
  - 已完成 M12.9 Visual Review Closure，新增 `config/examples/m12_visual_review_closure.json`、`scripts/m12_visual_review_closure_lib.py`、`scripts/run_m12_visual_review_closure.py`、`visual_review/m12_9_closure/` 与 M12.9 单测。
  - M12.9 覆盖 `M10-PA-008/009/003/011/004/007` 共 `6` 条策略、`30` 个 Brooks v2 图例 case；所有 case 保留 logical image path、checksum、Brooks unit ref、decision points、disqualifiers 与 OHLCV approximation risk。
  - M12.9 当前把 `M10-PA-008/009` 标记为 `visual_review_closed` 的 agent-side closure，但 `user_confirmation_required_before_paper_gate=true`，因此当前仍不能作为 paper gate evidence。
  - M12.9 当前把 `M10-PA-004/007` 保持为 `needs_definition_fix` / definition evidence only，不生成交易复测、不进入 paper gate。
  - 已完成 M12.10 Definition Fix and Retest，新增 `config/examples/m12_10_definition_fix_and_retest.json`、`scripts/m12_10_definition_fix_retest_lib.py`、`scripts/run_m12_10_definition_fix_retest.py`、`definition_fix/m12_10_definition_fix_and_retest/` 与 M12.10 单测。
  - M12.10 为 `M10-PA-005` 重新从本地 K 线持久化 `34651` 条带 `range_high/range_low/range_height/breakout_edge/reentry_close/failed_breakout_extreme` 的几何事件记录；复测指标仍引用 M10.9 可追溯 before/after metrics，结论为 `reject_for_now_after_geometry_review`。
  - M12.10 将 `M10-PA-004/007` 正式降级为 `visual_only_not_backtestable_without_manual_labels`，不生成 before/after trade、PnL、胜率或回撤数字。
  - M12.10 继续保留 `M10-PA-008/009` 的用户图形确认阻塞，未确认前不得计入 paper gate evidence。
  - 已完成 M12.11 Read-only Trading Dashboard，新增 `config/examples/m12_readonly_trading_dashboard.json`、`scripts/m12_readonly_trading_dashboard_lib.py`、`scripts/run_m12_readonly_trading_dashboard.py`、`dashboard/m12_11_readonly_trading_dashboard/` 与 M12.11 单测。
  - M12.11 看板汇总 `12` 条 scanner 候选、`32` 条只读观察事件、`4` 个只读标的 latest bar close、`10` 个模拟资金曲线引用、M12.10 definition 决策和 M11.5 gate 状态。
  - M12.11 输出字段使用 `readonly_*`、`hypothetical_*`、`simulated_*` 语义；当前仍是 `paper_trading_approval=false`、`trading_connection=false`、`real_money_actions=false`、`live_execution=false`。
  - 已完成 M12.12 Daily Observation Loop，新增 `config/examples/m12_12_daily_observation_loop.json`、`scripts/m12_12_daily_observation_loop_lib.py`、`scripts/run_m12_12_daily_observation_loop.py`、`daily_observation/m12_12_loop/` 与 M12.12 单测。
  - M12.12 已按 M12.5 static seed 顺序选出第一批 `50` 只股票/ETF，并通过 Longbridge 只读 K 线把 `1d` 长窗口补到 `50/50`、当前交易日 `5m` 补到 `50/50`；长历史 `5m` 全窗口仍为 `0/50`，不得宣称已完成两年日内历史。
  - M12.12 当前每日只读循环生成 `141` 条候选：`M10-PA-001` 29 条、`M10-PA-002` 25 条、`M10-PA-012` 76 条、`M12-FTD-001` 11 条；覆盖 `45` 只出现候选的标的。
  - M12.12 已把早期日线截图逻辑重新正式化为 `M12-FTD-001 方方土日线趋势顺势信号K` 并在第一批 50 只上重测；模拟初始资金 `100,000`、最终权益 `845,131.37`、模拟净盈利 `745,131.37`、收益率 `745.13%`、胜率 `36.74%`、最大回撤 `49.04%`、交易 `41,030` 笔。因回撤过大且 profit factor 仅 `1.06`，当前只作为对照/选股因子，不直接作为 paper gate 证据。
  - M12.12 已生成中文只读看板 `m12_12_readonly_daily_dashboard.html`，首页优先展示今日机会、机会估算盈亏（未成交）、早期日线历史盈利/胜率/最大回撤、第一批可测股票和策略状态；页面明确候选不是成交，不是模拟买卖试运行，也不接真实交易。
  - M12.12 已整理 `M10-PA-008/009` 的 10 个关键图形确认 case，并把 `M10-PA-004/007` 继续标为需人工标签/定义闭环；这两类图形/定义问题不再阻塞 `M10-PA-001/002/012 + M12-FTD-001` 的每日只读主线。
  - 已完成 M12.14 Source Strategy Closure，新增 `scripts/m12_14_source_strategy_closure_lib.py`、`scripts/run_m12_14_source_strategy_closure.py`、`source_revisit/m12_14_source_strategy_closure/` 与 M12.14 单测。
  - M12.14 已把 `M12-FTD-001` 从“对照/benchmark”升级为“重点增强测试候选”：保留当前历史模拟收益 `745.13%`、胜率 `36.74%`、最大回撤 `49.04%`，并补入方方土 notes、方方土 YouTube transcript、Brooks v2 手工转录和 Brooks/PPT 支撑来源，下一步做 A/B 重测以降低回撤。
  - M12.14 回看早期策略来源后新增 `6` 条测试/过滤候选：`M12-SRC-001` 日线趋势顺势信号K增强版、`M12-SRC-002` 趋势回调二次入场、`M12-SRC-003` 突破后跟进、`M12-SRC-004` 紧密通道筛选、`M12-SRC-005` 长回调保护、`M12-SRC-006` 主要趋势反转观察。
  - M12.14 已直接关闭 `M10-PA-008/009` 的关键图例确认口径：`M10-PA-008-positive-001` 通过，`M10-PA-008-boundary-001` 剔除正例并保留为排除证据，`M10-PA-009-counterexample-001` 保留为反例，`M10-PA-009-boundary-001` 通过；当前不再需要用户逐图确认。
  - M12.14 已把定义问题明确收口：`M10-PA-005` 已补字段但复测弱，暂不进每日主线；`M10-PA-004/007` 正式降级为图形研究/新检测器任务，不再挂“等用户确认”。
  - 已完成 M12.15 `M12-FTD-001 v0.2` A/B 重测，新增 `scripts/m12_15_ftd_v02_ab_retest_lib.py`、`scripts/run_m12_15_ftd_v02_ab_retest.py`、`ftd_v02_ab_retest/m12_15/` 与 M12.15 单测。
  - M12.15 对早期强日线策略跑了 `5` 个版本：baseline、长回调保护、1-2 根跟进确认、行情背景+信号K质量、完整增强版；结果显示 baseline 收益最高但回撤仍大，最佳折中为 `pullback_guard`。
  - M12.15 关键结果：baseline 收益 `745.13%`、胜率 `36.74%`、最大回撤 `49.04%`、交易 `41030` 笔、最大连续亏损 `65`；`pullback_guard` 收益 `610.44%`、胜率 `37.36%`、最大回撤 `48.38%`、交易 `22184` 笔、最大连续亏损 `56`；`follow_through_confirm` 收益 `-179.50%`、最大回撤 `154.82%`；`context_signal_quality` 收益 `288.22%`、最大回撤 `64.08%`；`full_v02` 收益 `-76.03%`、最大回撤 `99.94%`。
  - M12.15 结论：`M12-FTD-001 pullback_guard` 进入 M12.17 每日只读测试候选；它不是实盘策略批准，也不是 paper trading 准入证据，仍需连续每日观察验证。
  - 已完成 M12.16 Source Candidate Test Plan，新增 `scripts/m12_16_source_candidate_test_plan_lib.py`、`scripts/run_m12_16_source_candidate_test_plan.py`、`source_candidate_test_plan/m12_16/` 与 M12.16 单测。
  - M12.16 已把 `6` 条来源回看候选排清楚：`M12-SRC-001/002/003` 进入每日只读测试队列，`M12-SRC-004/005` 先做选股排名/风险过滤器，`M12-SRC-006` 进入严格反转观察队列。
  - M12.16 明确 `M12-SRC-004/005` 不是独立交易策略，不能拿它们单独统计盈利；`M12-SRC-006` 与 `M10-PA-008` 合并观察，但不进入自动触发主线。
  - 已完成 M12.17 Daily Observation Continuity，新增 `scripts/m12_17_daily_observation_continuity_lib.py`、`scripts/run_m12_17_daily_observation_continuity.py`、`daily_observation/m12_17_continuity/` 与 M12.17 单测。
  - M12.17 当前写入第 `1/10` 个连续交易日记录：今日机会 `141` 条，今日估算盈亏 `-8482.20`，今日估算收益率 `-8.48%`，当前浮盈机会占比 `23.40%`。
  - M12.17 看板首页已优先展示今日机会、今日估算盈亏、估算收益率、FTD增强版账本表现/胜率/最大回撤、连续记录天数；交易明细展示股票、策略、周期、方向、假设入场/止损/目标、当前价和模拟盈亏。
  - M12.17 仍是只读观察，不是真实成交；模拟买卖试运行复查至少还差 `9` 个交易日连续记录。
  - 已完成 M12.18 Visual Strategy Observation，新增 `scripts/m12_18_visual_strategy_observation_lib.py`、`scripts/run_m12_18_visual_strategy_observation.py`、`visual_observation/m12_18/` 与 M12.18 单测。
  - M12.18 将 `M10-PA-008/009` 转入严格图形观察，不再等人工逐图确认，也不混入每日主线自动触发；当前整理出 `8631` 条观察候选，其中 `M10-PA-008` `4375` 条、`M10-PA-009` `4256` 条。
  - M12.18 严格规则：`M10-PA-008` 必须有趋势破坏、二次测试、反转确认；`M10-PA-009` 必须有三次推动、通道/三角结构、反转确认。两者都不是模拟买卖准入证据。
  - 已完成 M12.19 Visual Detector Prototypes，新增 `scripts/m12_19_visual_detector_prototypes_lib.py`、`scripts/run_m12_19_visual_detector_prototypes.py`、`visual_detectors/m12_19/` 与 M12.19 单测。
  - M12.19 将 `M10-PA-004/007` 从“等图例/等定义”改为检测器原型任务，整理出 `10` 个候选图例：`M10-PA-004` `5` 个，`M10-PA-007` `5` 个。
  - M12.19 结论：两条策略当前都是 `detector_prototype_ready_not_backtest_ready`，也就是“知道机器要识别什么，但还不能拿来跑盈利回测”。后续若继续推进，需要单独实现 swing/leg/channel 检测器并补单测。
  - 已完成 M12.20 Visual Detector Implementation，新增 `scripts/m12_20_visual_detector_implementation_lib.py`、`scripts/run_m12_20_visual_detector_implementation.py`、`visual_detectors/m12_20/` 与 M12.20 单测。
  - M12.20 已用第一批 `50` 只股票/ETF 的 `1d` 长窗口缓存实际运行 `M10-PA-004/007` 机器识别检测器，`daily_cache_ready_symbols=50`、`deferred_input_count=0`。
  - M12.20 共识别出 `4801` 个候选图形：`M10-PA-004` 宽通道边界反转 `2301` 个，`M10-PA-007` 第二腿陷阱反转 `2500` 个；这些是候选图形，不能直接用于买卖动作，不统计盈利/胜率/回撤。
  - M12.20 已落地统一策略队列：`M10-PA-001/002/012 + M12-FTD-001 pullback_guard` 进入每日测试；`M10-PA-003` 与 `M12-FTD-001-filter` 是过滤器；`M10-PA-008/009` 是严格观察；`M10-PA-004/007` 是机器检测器观察；辅助/研究策略不再混进每日测试。
  - 已完成 M12.21 Detector Quality Review，新增 `scripts/m12_21_detector_quality_review_lib.py`、`scripts/run_m12_21_detector_quality_review.py`、`visual_detectors/m12_21/` 与 M12.21 单测。
  - M12.21 对 M12.20 已保留的 `4801` 条机器识别候选做了全量结构复核：结构复核未拒绝 `4801` 条，通过率 `100.00%`，其中高质量 `2785` 条、中等质量 `1645` 条、建议抽样看图 `371` 条、自动拒绝 `0` 条。
  - M12.21 分策略结果：`M10-PA-004` 高质量 `780` 条、中等 `1165` 条、建议看图 `356` 条；`M10-PA-007` 高质量 `2005` 条、中等 `480` 条、建议看图 `15` 条。
  - M12.21 已生成 `m12_21_review_packet.html` 与 `m12_21_review_sample.csv`，用于后续快速抽查机器识别是否把普通震荡误判成策略图形；本阶段不输出盈利、胜率、回撤或模拟买卖准入结论。
  - M12.21 限制：M12.20 每个 strategy/symbol 最多保留最近 `50` 条候选，因此本阶段是对 checked-in retained candidates 的全量结构复核，不代表 raw detector 全历史分布。
  - 已完成 M12.22 Detector Sample Visual Review，新增 `scripts/m12_22_detector_sample_visual_review_lib.py`、`scripts/run_m12_22_detector_sample_visual_review.py`、`visual_detectors/m12_22/` 与 M12.22 单测。
  - M12.22 对 M12.21 retained `4801` 条候选做全量严格复核，并生成 `467` 条标注图包，覆盖全部 `371` 条 needs_spot_check，同时加入通过样例对照组。
  - M12.22 全量严格复核结果：清晰像目标图形 `3275` 条、边界样例 `1357` 条、疑似误判 `169` 条；仅看 needs_spot_check，清晰像目标图形 `156` 条、边界 `187` 条、疑似误判 `28` 条。
  - M12.22 分策略结果：`M10-PA-004` 清晰像目标图形 `1868` 条、边界 `390` 条、疑似误判 `43` 条；`M10-PA-007` 清晰像目标图形 `1407` 条、边界 `967` 条、疑似误判 `126` 条。
  - M12.22 结论：`M10-PA-004/007` 不能直接进入完整历史回测；下一步先收紧宽通道普通震荡过滤、第二腿确认延迟和两腿偏离上限，再重跑检测器。
  - 已完成 M12.23 Detector Tightening Rerun，新增 `scripts/m12_23_detector_tightening_rerun_lib.py`、`scripts/run_m12_23_detector_tightening_rerun.py`、`visual_detectors/m12_23/` 与 M12.23 单测。
  - M12.23 收紧后保留 `3990` 条候选图形：清晰像目标图形 `3917` 条、边界样例 `73` 条、疑似误判 `0` 条。
  - M12.23 对比 M12.22：边界样例从 `1357` 降到 `73`，疑似误判从 `169` 降到 `0`；`M10-PA-004` 收紧后清晰 `1587`、边界 `0`、疑似误判 `0`；`M10-PA-007` 收紧后清晰 `2330`、边界 `73`、疑似误判 `0`。
  - M12.23 raw/capped 审计：`M10-PA-004` 原始候选 `4582`、收紧后原始 `1996`、cap 后 `1587`；`M10-PA-007` 原始候选 `20190`、收紧后原始 `4984`、cap 后 `2403`。
  - M12.23 结论：检测器质量已足够进入 M12.24 `1d` 小范围历史测试准备，但仍不是盈利结论，也不进入模拟买卖试运行。
  - 已完成 M12.24 PA004/PA007 Small Historical Pilot，新增 `scripts/m12_24_pa004_pa007_small_pilot_lib.py`、`scripts/run_m12_24_pa004_pa007_small_pilot.py`、`visual_detectors/m12_24_small_pilot/` 与 M12.24 单测。
  - M12.24 将收紧后候选转成可测试交易 `3795` 条，跳过 `195` 条；测试周期只用 `1d`，不使用尚未完整补齐的长历史 `5m`。
  - M12.24 结果：`M10-PA-004` 收益 `-0.9580%`、胜率 `34.72%`、最大回撤 `4.0220%`、交易 `1535` 笔，结论为保留图形研究；`M10-PA-007` 收益 `0.6473%`、胜率 `41.15%`、最大回撤 `3.8796%`、交易 `2260` 笔，结论为进入每日观察。
  - M12.24 不批准模拟买卖试运行；`M10-PA-007` 只能进入 M12.25 每日观察队列，`M10-PA-004` 不进入每日观察。
  - 已完成 M12.25 Daily Observation Continuity，新增 `scripts/m12_25_daily_observation_continuity_lib.py`、`scripts/run_m12_25_daily_observation_continuity.py`、`daily_observation/m12_25_continuity/` 与 M12.25 单测。
  - M12.25 当前主线每日只读机会仍为 `141` 条，今日估算盈亏 `-8482.20`、估算收益率 `-8.48%`、连续记录 `1/10`；因为没有新的交易日数据，本阶段没有硬凑第 2 天。
  - M12.25 队列结果：`M10-PA-001/002/012 + M12-FTD-001` 继续是每日测试主线；`M10-PA-007` 新增为观察队列，最近小范围测试收益 `0.6473%`、胜率 `41.15%`、最大回撤 `3.8796%`；`M10-PA-004` 保留图形研究，不进入每日观察。
  - M12.25 不批准模拟买卖试运行；`M10-PA-007` 也不进入 paper gate evidence 或真实交易路径。
  - 已完成 M12.26 Cache Scanner Expansion，新增 `scripts/m12_26_cache_scanner_expansion_lib.py`、`scripts/run_m12_26_cache_scanner_expansion.py`、`scanner/m12_26_cache_scanner_expansion/` 与 M12.26 单测。
  - M12.26 数据结论：第一批 `50` 只日线可用 `50` 只、当日 `5m` 可用 `50` 只，长历史 `5m` 完整覆盖仍为 `0/50`；147 只扩展还有 `97` 只缺缓存，不进入 scanner 可用候选。
  - M12.26 scanner 结论：当前可交付候选 `141` 条，覆盖 `45` 只股票；策略命中为 `M10-PA-001` `29` 条、`M10-PA-002` `25` 条、`M10-PA-012` `76` 条、`M12-FTD-001` `11` 条。
  - M12.26 策略边界：自动 scanner 只包含 `M10-PA-001/002/012 + M12-FTD-001`；`M10-PA-007` 仍是观察队列，不自动进入 scanner；`M10-PA-004` 保留图形研究。
  - 已完成 M12.27 PA004 Expanded Retest + Live Read-only Snapshot，新增 `scripts/m12_27_pa004_retest_live_snapshot_lib.py`、`scripts/run_m12_27_pa004_retest_live_snapshot.py`、`visual_detectors/m12_27_pa004_retest_live_snapshot/` 与 M12.27 单测。
  - M12.27 开盘时段只读快照已跑通：北京时间 `2026-04-28 21:45` 左右使用 Longbridge 只读 quote/K 线获取 `SPY/QQQ/NVDA/TSLA x 1d/1h/15m/5m` 共 `16` 条记录，缺口 `0`；这说明行情读取可用，但当前还不是常驻自动刷新。
  - M12.27 PA004 诊断复测结论：整体混合版本仍弱，收益 `-0.9580%`、胜率 `34.72%`、最大回撤 `4.0220%`；但 PA004 只做多分支转正，收益 `0.5530%`、胜率 `40.24%`、最大回撤 `2.0479%`、交易 `574` 笔，结论改为“PA004 做多版进入下一轮观察候选”。
  - M12.27 PA004 做空分支暂不进入主线：收益 `-1.5005%`、胜率 `31.43%`、最大回撤 `3.1993%`、交易 `961` 笔；下一轮不应把 PA004 做多/做空混在一起解释。
  - 已完成 M12.28 PA004 Long Dashboard Refresh，新增 `scripts/m12_28_trading_session_dashboard_lib.py`、`scripts/run_m12_28_trading_session_dashboard.py`、`config/examples/m12_28_trading_session_dashboard.json`、`dashboard/m12_28_trading_session_dashboard/` 与 M12.28 单测。
  - M12.28 已把 PA004 做多版接入盘中只读模拟看板：当前看板显示 `160` 条机会/观察项，其中主线策略机会 `141` 条、PA004 做多观察 `19` 条；PA004 做多观察当前快照模拟盈亏 `14499.10`。
  - M12.28 使用 Longbridge 只读 quote 获取第一批 `50` 只股票当前价格；当前市场状态为美股常规交易时段，纽约时间 `2026-04-28 10:59:47 EDT`。
  - M12.28 当前盘中模拟盈亏快照为 `-69888.72`，其中主线机会 `-84387.82`、PA004 做多观察 `14499.10`；这只是按当前只读价估算，不是成交、不是实盘、不是策略最终结论。
  - M12.28 已显式标记数据对齐限制：主线候选日期仍是 `2026-04-27`，报价日期是 `2026-04-28`，当前属于 `live_quote_overlay_on_prior_candidate_set`，下一步必须把 50 只股票滚动到当前交易日重新扫描。
  - 已完成 M12.29 Current-day Scan + Minute Read-only Dashboard，新增 `scripts/m12_29_current_day_scan_dashboard_lib.py`、`scripts/run_m12_29_current_day_scan_dashboard.py`、`config/examples/m12_29_current_day_scan_dashboard.json`、`daily_observation/m12_29_current_day_scan_dashboard/` 与 M12.29 单测。
  - M12.29 已把扫描日期滚动到当前美股交易日 `2026-04-28`：第一批 `50` 只股票日线可用 `50/50`、当日 `5m` 可用 `50/50`，Longbridge 只读 quote 覆盖 `50` 只。
  - M12.29 当前今日新机会 `122` 条，策略命中为 `M10-PA-001` `27`、`M10-PA-002` `9`、`M10-PA-012` `84`、`M12-FTD-001` `2`；旧候选数量为 `0`，不再把 `2026-04-27` 旧机会伪装成今日机会。
  - M12.29 当前看板共显示 `141` 条机会/观察项，其中主线今日机会 `122` 条、PA004 做多观察 `19` 条；当前模拟盈亏快照为 `843.92`，其中主线 `-13574.22`、PA004 做多观察 `14418.14`。
  - M12.30/M12.31 已把 M10 16 条、M12-FTD-001 与 6 条来源候选合并成一张中文收口表：`M10-PA-001/002/012/M12-FTD-001` 进入每日实时只读测试，`M10-PA-004` 仅做多观察，`M10-PA-007/008/009` 观察，`M10-PA-003` 过滤器/排名因子，其余辅助/研究/暂不继续，不再挂“等待用户确认”阻塞主线。
  - M12.32 已优化中文分钟级只读模拟看板：首屏改为共享模拟账户总览，策略成绩单只展示真正每日测试/观察策略，来源回看候选只保留在收口表说明中；单策略下钻保留每条机会的来源、数据路径、风险等级和复盘字段。
  - M12.33/M11.6 当前准入结论：连续记录为 `1/10` 个交易日，模拟交易试运行仍未批准；原因是观察天数还不够，不是当前策略测试失败。
  - M12.34 已把观察策略变成真实每日测试 lane：`M10-PA-004` 做多版今日有 `19` 条观察机会，`M10-PA-007/008/009` 今日无触发但已记录为“当日无符合条件机会”，不再只是挂“观察中”状态。
  - M12.35 已把看板按周期重排为 `1d / 1h / 15m / 5m` 四组；当前 `1d` 机会 `34` 条、`15m` 机会 `44` 条、`5m` 机会 `63` 条、`1h` 暂无触发。信号更新语义固定为各自 K 线收盘后确认，盘中 60 秒只刷新当前价和模拟盈亏。
  - M12.36 已新增 `M12-FTD-001` 重点监控卡：账本表现 `610.44%`、胜率 `37.36%`、最大回撤 `48.38%`；今日触发 `2` 条，当前模拟盈亏 `-117.86`，风险标记为“最大回撤高 / 盈利因子偏薄 / 今日暂时亏损 / 信号集中度偏高”。
  - M12.37 已新增自动盘中只读模拟运行器 `scripts/run_m12_37_intraday_auto_loop.py` 与 systemd/cron 示例配置；当前生产节奏以 M12.47 守护器拉起 `--session` 为准：可从盘前预热开始，进入美股常规交易时段后每 60 秒刷新，收盘后自动退出。
  - M12.38 已新增 Codex 观察员文件层：每次刷新会写入 `m12_38_codex_observer_latest.json` 和 `m12_38_codex_observer_inbox.jsonl`，内容是中文人话摘要，包含当前模拟权益、今日模拟盈亏、活跃周期、FTD001 风险和数据异常提醒；当前不再由心跳手动运行 M12.37 once-mode，交易窗口刷新由 M12.47 守护器负责。
  - M12.39 当前继续累计真实交易日观察；本轮没有硬凑交易日，仍是 `1/10`，后续需要实际连续运行到 `10/10` 后再做 M11.6 模拟交易试运行准入复查。
  - 已完成 M12.40 News/Earnings Event Tags，新增新闻/财报 sidecar 脚本和报告；当前基于 2026-04-30 只读候选共标注 `61` 条候选，其中 `2` 条带 Google/Alphabet 财报事件标签，`GOOG/GOOGL` 均已命中；新闻层不改策略定义、不触发真实交易。
  - M12.40 Google 结论：Alphabet 2026-04-29 财报盘后跳涨被识别为正向事件，但 2026-04-30 次日 `GOOG/GOOGL` 的可测候选是 `M10-PA-012` 的 `5m` 看跌开盘区间信号，和财报利好方向相反，因此当前标签为 `earnings_gap_opposite`。
  - 已完成 M12.41 News Impact Simulation：当前事件组样本 `2` 条，模拟盈亏 `-764.64`、胜率 `0.00%`，新闻影响结论为 `risk_filter_only`；这说明当前 Google 案例更适合作为风险过滤/方向冲突提醒，而不是直接加分机会。
  - 已完成 M12.42 FTD001 News/Risk A/B：FTD001 baseline 收益 `610.44%`、最大回撤 `48.65%`；`loss_streak_guard` 版本收益 `1136.15%`、最大回撤 `25.07%`、胜率 `39.48%`，当前是最佳折中候选，但仍只进入增强观察，不批准模拟交易。
  - 已完成 M12.43 Earnings Gap Continuation 原型：Google 财报跳空后次日存在价格确认候选，策略原型结论为 `enter_observation_queue`；该策略仍只做观察，不进入模拟交易准入。
  - 已完成 M12.44 Data Scanner News Expansion：第一批 `50` 只股票池保留，当前候选 `61` 条，其中 `2` 条带新闻/财报字段；scanner 增加 `news_priority`、`high_risk_pause` 等字段，缺新闻数据不伪造“无风险”。
  - 已完成 M12.45 Unified Strategy Portfolio Scorecard：统一策略表覆盖 `23` 条策略/候选，当前每日测试 `4` 条、观察测试 `4` 条；共用 `100,000 USD` 模拟本金组合代理模拟盈亏 `1031.71`、胜率 `46.67%`、最大回撤 `2.83%`。
  - 已完成 M11.7 Paper Trial Recheck 第一版：当前仍未批准模拟交易试运行，原因是只读观察还没满 `10` 个真实交易日，当前约 `1/10`；未批准不是策略失败，而是观察期和业务审批条件未满足。
- M12.46 当前已修复两类核心口径错误：盘后 UTC 跨日平仓现在按纽约交易日记入当日已平仓/当日盈亏；同一纽约交易日先成功入账、后续降级重跑时，不再把 `observed_trading_days` 回写倒退。
- M12.46 当前已把 `M10-PA-004 long-only` 纳入正式主线账户，`M10-PA-005/007/008/009/011/013` 纳入实验账户，`M12-FTD-001 baseline / loss_streak_guard` 保持双账户并行；所有独立账户起始本金统一为 `20,000 USD`。
- M12.46 当前已在当前用户 `crontab` 安装工作日 `21:25` 自动启动的 `--session` 会话：北京时间开盘前预热，盘中每分钟刷新，收盘后自动退出；若运行环境缺少 `systemd --user`，默认用该 `cron` 路径接管自动运行。
- M12.46 当前已把盘中自动会话进一步收紧为“开盘前预热补数 + 盘中每分钟刷报价 + 仅在 `5m` 收盘点补 K 线信号”，并给 Longbridge `quote` / `kline history` 增加硬超时；目标是让交易时段自动跑更像模拟实盘，而不是为了补数据把整轮刷新卡死。
- M12.46 当前已修复 `PA004` 反复“名义上进主线、实际上还吃观察样例”的老问题：根因不是账户壳没建，而是 `PA004` 仍复用了 M12.28 的历史观察样例流；现在已拆成“正式账户信号”与“历史参考样例”两条流，`M10-PA-004 long-only` 主线账户只消费正式 detector entry，历史样例不再混进主线收益、机会统计或账户开仓。
- M12.46 当前已新增 `m12_46_account_input_audit.json` 与面板内“账户输入审计”区，用来强制检查所有已进入测试的主线/实验账户是否都在吃正式输入流；这次也把面板头部改成了“北京时间最后更新 + 纽约时间 + 市场状态 + 运行状态”，方便直接判断交易时段自动会话有没有在实时跑。
- M12.47 当前已补上用户态常驻调度器 `scripts/run_m12_47_session_supervisor.py` 与 `config/examples/m12_47_session_supervisor.json`，不再依赖当前环境里缺失的 `systemd/cron` 守护进程；现在由该守护器在后台常驻，负责等待开盘、拉起 `M12.37 --session`、盯住盘中子会话是否活着、异常退出后重启，并持续写 `m12_47_session_supervisor_status.json`。
- M12.47 当前已把系统 `crontab` 的 M12 条目切到“启动守护器”而不是直接跑 `M12.37 --session`；即使未来 `cron` 守护恢复，也只会尝试拉起单例守护器，不会重复叠多个盘中会话。
- M12.47 当前已把看板头部补成“北京时间最后更新 + 自动会话活跃状态 + 上次心跳（北京时间） + 心跳延迟秒数”；当前离线快照显示守护器 PID 已存在、下次会话计划为北京时间 `21:25` 启动，说明后续不再只是落到 `01:15` 的最后快照后静止不动。
- M12.48 当前已把 Longbridge 原始 `pre_market_quote / post_market_quote` 接入分钟级账户看板、观察摘要和单次 runner 输出，新增 `m12_48_extended_session_monitor.json`，可直接看到盘前异动榜、盘后异动榜和 AMD / 谷歌 / 半导体 / 存储芯片重点关注股命中。
- M12.48 当前已把 `M12.37` 的盘前 quote 刷新打开，并把 `M12.47` 的 post-close 运行窗口扩到 `16:00 ~ 20:00 ET`，用于盘后异动监控；当前真实只读输出已抓到 `AMD / QCOM / MU / SOXX / SMH` 的盘前大幅上涨，并同步进 Codex observer 摘要。
- M12.49 当前已处理运行层硬化：`run_m12_29_current_day_scan_dashboard.py` 与 `run_m12_37_intraday_auto_loop.py` 会拒绝未来 `generated_at` 写正式 artifact；库层 `run_m12_29_current_day_scan_dashboard()` 也同步拒绝未来时间，避免再把测试时间写成正式看板时间。
- M12.49 当前已把看板头部补成“北京时间最后更新 + 当前电脑时间 + 看板新鲜度 + 看板延迟秒数 + 自动会话 + 守护器进程 + 心跳延迟”；当前真实自检显示 `fresh / alive`，守护器 PID 与 M12.37 子会话 PID 均存活。
- M12.49 当前已给 M12.47 加入连续失败熔断：M12.37 子会话连续 `3` 次非零退出后停止无限重启，并输出 `m12_47_session_failure_dossier.json`；避免坏进程反复刷半成品看板。
- M12.49 当前已修复 PA004 正式账户方向字段兼容问题：PA004 检测器输出为中文 `看涨 / 看跌`，正式账户入口现在同时接受 `long / 看涨`，避免未来 PA004 做多信号被误过滤；2026-05-06 当天 PA004 做多仍为 `0` 条，原因是当天命中的 PA004 事件为做空方向，而 PA004 做空版仍冻结。
- M12.49/M14.1 当前已修复看板数据刷新误报与补数节奏：M12.37 常规盘中 K 线补数预算从 `20` 提高到 `100`，并改为按 5 分钟 bucket 触发一次，而不是要求进程刚好命中 `minute % 5 == 0`；`5m_current` 可在 bucket 刷新时强制重拉，即使同日缓存已存在也会更新当日 5m bars。M12.29 freshness 阈值改为至少 `600` 秒，避免 first50 全量补数期间被误报 stale。真实刷新样本 `2026-05-14T15:27:25Z` 已恢复 `first50_daily_ready_symbols=50`、`first50_current_5m_ready_symbols=50`、`current_day_runtime_ready=true`、`data_freshness_warning=''`。
- M12.49 当前已修复三类看板口径失真：`codex observer / auto runner manifest` 现在会按 `盘前 / 盘中 / 盘后 / 休市` 输出对应文案，不再把盘后快照说成“盘中只读模拟”；盘前/盘后异动 headline 只展示当前时段数据，非当前时段快照会显式隐藏；`m12_29_current_day_scan_summary.json` 新增 `current_day_runtime_ready` 与 `runtime_readiness_note`，把“严格 50/50 全齐”和“当前可运行”区分开来。
- M12.49 当前已修复实验账户误报推进：`m12_46_account_input_audit.json` 新增 `current_scanner_connected / input_status`，未接入正式当日扫描输入的实验策略不再被写成 `formal_input_stream=true`；看板“实验账户”说明和 `m12_33_observation_run_status.json` 也同步改成真实口径，避免再把“0 开仓空转账户”误报成已完成实时测试。
- M13 当前已完成 Step 0 工作区整理：在 `codex/m12-49-dashboard-runtime-hardening` 提交并推送 `d6c730f m12.49: archive latest runtime artifacts`，纳入 58 个运行产物文件和 `2026-05-01` 至 `2026-05-07` 的盘后新闻复盘证据。
- M13 当前已新增 `config/examples/m13_strategy_runtime_registry.json`，把 `M10-PA-001/002/004/005/007/008/009/011/012/013/M12-FTD-001` 定义为独立 runtime，把 `M10-PA-003/006/014/015/016` 定义为 plugin/filter A/B ledger，把 `M10-PA-010` 保持 research-only，并把 `AI-TRADER-EXTERNAL` 固定为外部研究源。
- M13 当前已新增 `scripts/run_m13_daily_strategy_test_runner.py` 与 `scripts/m13_daily_strategy_test_runner_lib.py`，只读消费 M12.46 的 `account_input_audit / account_trade_ledger / scorecards`，输出 `m13_strategy_signal_ledger.jsonl`、`m13_account_operation_ledger.jsonl`、`m13_daily_strategy_scorecard.*` 与 `m13_goal_status.json`。
- M13 当前已修复 M12.46 scorecard 语义：`today_signal_count / today_opened_count / today_closed_count` 改为从当日交易账本统计，避免“今日开仓后又平仓”的账户被当前持仓快照误报为 0 信号。
- M13 首次 runner 样本使用 `2026-05-07` 纽约交易日，输出 `24` 条策略/账户信号账本事件、`44` 条账户操作账本事件、`12` 次 open、`12` 次 close；该样本保留为接线前对照，当时 `M10-PA-005/007/008/009/011/013` 仍为 `not_connected` blocker。
- M13 当前已接入实验策略最小 adapter：`M10-PA-005` 复用失败突破 range adapter，`M10-PA-007` 复用 M12.23 第二腿检测器，`M10-PA-008/009/013` 复用 M10.11 Wave B proxy detector，`M10-PA-011` 只保留 `5m` 开盘反转账户，不再用日线伪装开盘策略测试。
- M13 最新盘中 runner 样本使用 `2026-05-20` 纽约交易日，输出所有 required 策略/模块的当日账本状态，`blocked_strategy_ids=[]`，并写入 `119` 条账户操作事件。该结果只表示真实每日测试闭环继续正常，不代表 paper/live 批准。
- M13 当前已明确 AI-Trader 边界：表现优秀的 AI-Trader agent/signal 只能作为公开或用户授权的外部候选信号源进入只读影子测试，不得 copy-trading、不得直接执行、不得把外部信号包装成自有策略。
- M14 当前已新增 `config/examples/m14_strategy_challenge_gate.json`、`scripts/run_m14_strategy_challenge_gate.py` 与 `scripts/m14_strategy_challenge_gate_lib.py`，把 M13 当日账本滚入 append-only `m14_challenge_day_ledger.jsonl`，再输出 `m14_strategy_decision_ledger.jsonl`、`m14_paper_trial_gate.json`、`m14_strategy_challenge_summary.json`、`m14_goal_status.json` 与 `m14_strategy_challenge_dashboard.html`。
- M14 当前已把策略决策固定为 `promote / modify / reject / continue_testing`；默认不满 `10` 个纽约交易日不得调策略定义，只有至少 `3` 个 signal days 后触发 `-2R`、`>3%` 最大回撤、风险拦截占比过高或重复数据缺口时才提前冻结/改版。
- M14 当前已实现内部模拟桥接：只有 `paper_trial_gate=approved_internal_sim_only` 的策略才会把 ledger open event 转成 `ExecutionRequest`，并按 `src.risk.evaluate_order_request` -> `src.execution.PaperBrokerAdapter` 的顺序写入模拟执行账本；当前样本无策略获批，因此 `m14_internal_paper_execution_ledger.jsonl` 为空。
- M12.29/M12.46 看板 HTML 当前已新增“数据刷新告警”，当 `fallback_quotes_only`、`--no-fetch`、`--no-refresh-quotes` 或 `current_day_runtime_ready=false` 时，会在页面顶部明确显示“看板数据未刷新 / fallback quotes / no-fetch”，避免把降级快照误判为完整策略测试。
- M14 当前已新增单测与集成测试，覆盖 append-only challenge、fallback/no-fetch 不批准、亏损 baseline 创建新 variant、风险检查先于模拟成交、M13 daily ledger 滚入 M14 summary 与 dashboard 聚合一致。
- M12.37 自动运行器当前已接入 M13/M14 后置链路：由 M12.47 守护器拉起的 `--session` 每次刷新后运行 M13 更新当日最新账本，M14 默认 `postmarket_only` 只在盘后固化 challenge/gate；所有输出仍保持只读模拟边界，当前不手动运行 once-mode。
- M14.1 当前已修复账户 runtime 的统一平仓语义：所有策略只有 `longbridge_quote_readonly` fresh quote 才能触发平仓，fallback/cached/no-refresh quote 只允许标记 `行情过期，暂停平仓`；止损/止盈成交按 stop/target 结算，不再使用穿透 stop/target 的最新价扩大亏损或盈利。旧账本已生成 `m12_46_account_trade_ledger_repair_audit.json`，其中 `2026-05-12T04:10:35Z` 的 `6` 条 fallback close 已作废，`58` 条旧版 stop/target close 已按 stop/target 纠偏；`M10-PA-004-MBF-1d` 当前恢复为 `3` 个 open position、`0` 笔 closed trade、总盈亏 `0.00`。
- M14.1 当前已修复账户 runtime 的全量持仓估值缺口：既有持仓不再只依赖“当天新信号行”拿报价，而是同时消费第一批 `50` 只 readonly quote pool；因此没有新信号的旧持仓也会参与 fresh quote 估值、止损/止盈检查。`2026-05-12` 盘后重算后，M12 open position 状态全部为 `观察中`，不再有错误的 `行情过期，暂停平仓`；M13 当日账本更新为 `8` 次 open、`7` 次 close。M14 保持原始 challenge append-only 不覆盖，新增 `m14_challenge_day_correction_ledger.jsonl` 记录 `9` 条 5/12 修正行，并让 M14 聚合、decision 与 gate 使用修正后的有效 challenge rows；`strategy_decision_ledger` 的去重键也已纳入 PnL / R / drawdown / open-close counts，保证后续指标修正会追加新 decision 行而不是静默复用旧行。
- M14.1 当前已按用户口径新增 `M10-PA-004-MBF-QC-1d` 并行改进账户：QC 版本要求更强日线涨幅、更强收盘位置、过滤过宽风险，并使用更近的 `1.5R` 目标；M14 circuit-breaker 对原 `M10-PA-004-MBF` 记录 `modify_candidate=true` 和 `next_variant_id=M10-PA-004-MBF-QC`，但不冻结原 MBF，原策略继续跑满 10 个交易日后再最终判断。
- M14.1 当前已修复 M14 challenge 计数口径：`fallback/no-fetch/current_day_runtime_ready=false` 的降级日继续保留在 append-only ledger 做审计，但不再计入有效 `10` 个交易日 challenge，也不再永久污染后续 paper trial gate。`2026-05-08` 当前被保留为 audit-only degraded day；最新 M14 gate 已基于既有 fully-ready challenge 固化为 `10/10`，但这只批准 `M10-PA-004 / M10-PA-005 / M10-PA-008` 继续内部模拟，不批准 broker paper/live。
- M12.47 当前已修复 `--status` 状态回写：手动或自动执行 status 时会把真实 `supervisor_process_alive / child_running / market_status` 写回 `m12_47_session_supervisor_status.json`，避免周末继续显示上一次交易日的 alive 状态。当前北京时间 `2026-05-17 13:46:47` 状态为 `非交易日等待`，下次自动启动窗口为 `2026-05-18 21:25:00 CST`。
- M12.47 最新自检时间为北京时间 `2026-05-21 02:47:30`：守护器 PID `2418` 存活，当前纽约时间 `2026-05-20 14:47:30 EDT`，市场状态为 `美股常规交易时段`，盘中子会话 PID `22357` 正在运行；最近看板为 `2026-05-20T18:45:57Z`，安全边界仍为 `paper_simulated_only=true / trading_connection=false / real_money_actions=false / live_execution=false / paper_trading_approval=false`。
- M12.47/M15 当前已修复 Longbridge 区域接口互相覆盖导致看板、纸账户状态或实时行情只能单边工作的运行问题：账户、资产、订单和成交命令默认使用 `LONGBRIDGE_REGION=global` 与 `.com` endpoint；只读 `quote/kline` 行情命令使用独立的 quote 环境并默认固定 `LONGBRIDGE_REGION=cn` 与 `.cn` endpoint。若 Longbridge 行情仍连续超时，则看板只允许切换到公开 Yahoo chart 只读行情兜底并明确标记 `yahoo_chart_readonly_after_longbridge_error`；长桥实时下单链路不得把 fallback 行情当作可执行实时事件。
- 2026-07-10 巡检确认 `2026-07-09` 长桥模拟账户交易日没有新订单或新成交。根因不是策略质量门槛：M15 守护器整场循环，但 `m15_realtime_market_events.jsonl` 最后事件停在 `2026-07-08`，末轮 `16/16` 个日线/五分钟 K 线请求均因 global `.com` 行情端连接超时而 deferred，导致路由器 `current_session_market_event_count=0`。同机实测 global K 线约 `7~9` 秒后连接超时，`.cn` 日线和五分钟 K 线通常约 `0.65` 秒正常返回，但偶发响应超过旧 `2` 秒门槛；现已按用途拆分 CLI 环境，把实时 K 线单请求门槛调整为 `6` 秒并允许 `2` 次尝试，然后重启 M15 守护器。长桥账户/订单接口仍保持 global，行情入口改走 cn，相关 `py_compile`、聚焦单测和 `git diff --check` 已通过。
- 2026-07-10 北京时间约 `10:41`，WSL 内核日志记录 `Init has exited. Terminating distribution` 并卸载发行版文件系统，M12.47、M15 实时守护器和后台看护器因此一起退出；这不是策略或长桥下单异常。巡检已重新启动三者。当前 Windows 自启策略已收敛为用户级隐藏计划任务，带“登录时 + 工作日 21:10”两个单次触发器；若本机权限仍拒绝注册计划任务，只保留隐藏的一次性登录启动项，后续不再使用 `300` 秒循环 fallback。
- Codex App 自动化已在 `2026-05-21` 调整：`m14` 从每小时 cron 改为每个美股交易日前北京时间 `21:10` 只做一次守护器检查，避免反复新建“策略测试闭环守护”会话；盘中真实刷新继续由 M12.47 supervisor 和 M12.37 child session 承担。
- M14.2 预备方向：可以提前做“模拟账户/券商接入 readiness scaffold”，但只能在单独高风险分支中实现 `broker_paper_ready / live_dry_run_ready` 的接口、配置、审批 gate、凭证隔离、dry-run contract、审计日志和 kill switch；不得接真实账户、不得提交真实订单、不得引入默认 live 开关。实盘交易必须在 M14 10 日 challenge、内部模拟准入、paper dry-run、人工审批和凭证隔离全部通过后再单独批准。
- M14.2 当前已从 M14 分支切出 `codex/m14-2-broker-readiness-scaffold` 做工程准备：新增 `BrokerReadinessConfig`、`BrokerOrderPreview`、`BrokerReadinessPlan` 与 `build_broker_readiness_plan`，只把已通过风控和 `approved_internal_sim_only` gate 的 `ExecutionRequest` 转成 dry-run 订单预演；默认 `broker_connection_enabled=false`、`real_order_enabled=false`、`live_execution_enabled=false`、`paper_trading_approval=false`。
- M14.2 已新增 `scripts/run_m14_2_broker_readiness_scaffold.py` 与 `scripts/m14_2_broker_readiness_scaffold_lib.py`，把真实内部模拟执行账本里的 `risk_check` 事件批量生成 dry-run readiness plan/audit。当前 artifact 位于 `reports/strategy_lab/m10_price_action_strategy_refresh/daily_observation/m14_2_broker_readiness/`，结果显示去重后 8 条内部模拟风控检查中 5 条可形成 dry-run preview，3 条被现有风控阻断；下一步应优先降低弱信号/超敞口触发，而不是放宽风控。
- M12.47/M15 当前已修复网页看板“看起来不刷新”的主要误导：状态页刷新时会先做一次长桥模拟账户只读状态刷新，超过 60 秒或内容生成时间过期就重新读取纸账户认证、资产、持仓和挂单；M12.32 HTML 增加 no-cache 与带时间戳的自动刷新，避免浏览器继续展示旧静态页；长桥账户状态、提交器状态、快速队列状态现在分开判定，旧交易日快速队列只作审计，不再显示成今天可提交订单。北京时间 `2026-06-04 16:02:10` 自检显示守护器存活、当前等待开盘前预热、子会话未运行属正常；长桥模拟账户只读状态为 `2` 个持仓（`AAPL / XLU`）、`0` 个挂单，今天可提交订单 `0`。
- M15 当前已新增长桥模拟账户盘中交易报告：`m15_longbridge_paper_session_report.*` 只读汇总今日累计提交、开盘首小时提交、当前持仓/挂单、最新风控阻断和订单对账；账户状态产物现在保留长桥今日订单列表，报告可用信号编号、标的、方向、数量和价格把提交流水对到长桥订单号。北京时间 `2026-06-04 23:15` 自检显示今日累计提交 `12` 笔、首小时 `9` 笔、当前 `9` 个持仓、`2` 个挂单，`12/12` 笔均已对上长桥订单号，未匹配订单为 `0`；旧本地模拟账本仍不作为长桥下单或对账来源。
- M15 当前已把长桥模拟账户总资产和拆分盈亏接入 M12.32 网页看板，并使用长桥只读 `profit-analysis / portfolio / order history / executions / positions` 对账口径，不再用 `100000 USD` 硬减估算。早期历史对账曾把账户区间净值盈亏、交易累计盈亏和持仓浮动混在“总盈亏”里展示；该旧叫法已经废弃，当前活跃看板只展示“账户当日盈亏 / 接口持仓今日浮动 / 当前持仓总盈亏 / 交易累计盈亏 / 项目资金占用”。项目下单风控仍按 `10000 USD` 模型，不用长桥账户余额放大仓位。
- M15 当前已修复长桥盈亏几天不动的问题：`run_m15_longbridge_realtime_account_state.py` 每次只读账户刷新会同步读取 Longbridge `portfolio`、同日 `profit-analysis` 和 `profit-analysis by-market US`，生成新的 `m15_longbridge_account_pnl_reconciliation.json`；M12.32 看板只接受不早于账户状态的新对账，旧对账不再覆盖当前账户当日盈亏、当前持仓总盈亏或交易累计盈亏。历史记录里的“今日组合盈亏/总盈亏”旧叫法不再作为当前字段名。
- M15 当前已落地长桥模拟账户全新分仓测试框架：长桥券商侧仍是一个 `lb_papertrading` 模拟账户，本地按 `主力仓 10000 USD`、`实验仓 10000 USD`、`002专项主力仓 10000 USD` 三个完全独立虚拟资金池记录和控仓；各资金池不设合计总敞口，同一标的允许不同资金池分别持有。新配置写入 `test_epoch_id=m15-dual-bucket-20260613`，启用 `pending_flatten -> active` 新基线状态：新基线激活前会优先对长桥当前持仓生成清仓卖出信号，清仓成交确认后才开始统计新测试的胜率、盈亏、回撤和策略准入；旧订单/旧持仓/旧盈亏只归档为旧测试历史，不参与新看板默认统计。`M10-PA-004-long-1d` 与 `M10-PA-002-1d` 仍属于原主力仓并正常推进；`M10-PA-002-1d` 还会额外复制到 `002专项主力仓` 做对照；`M10-PA-002-5m` 进入 `002专项主力仓` 做正常测试，不进入原主力仓；`M10-PA-013-1d/5m`、`M10-PA-005-1d/5m`、`M10-PA-008-1d`、`M10-PA-012-5m`、`M12-FTD-001-baseline-1d`、`M12-FTD-001-loss-streak-guard-1d`、`M10-PA-001-1d` 与 `M10-PA-011-ORB-R1-5m` 属于实验仓；其他修复、影子和辅助模块继续只留本地模拟。
- M15 当前已修复新分仓基线清仓卡住问题：长桥持仓缺少当前价时，清仓卖出不再用成本价原价挂单，而是用成本价折扣保护限价；旧卖单提交记录只在短时间内防重复，不再永久阻断已取消卖单后的重新清仓。北京时间 `2026-06-16 00:24` 自检显示旧持仓已全部卖出，长桥模拟账户持仓 `0`、挂单 `0`，`test_epoch_id=m15-dual-bucket-20260613` 已从 `pending_flatten` 切换为 `active`，新测试开始时间为 `2026-06-15T16:23:41Z`；M15 守护器 PID `82124` 存活，继续等待新的实时信号。
- M15 当前已修复新基线清仓窗口导致“昨晚 0 单”的路由缺口：`2026-06-15` 交易时段内，16:05~16:23Z 正在清空旧持仓，这段时间生成的强信号被新基线规则排除；16:23Z 基线激活后，后续路由候选全部因扣费后利润或收益风险比不足被挡住，因此最终没有新买入订单。本轮已让信号路由器读取当前 `test_epoch`，当清仓窗口内的信号在基线激活后仍被当前行情识别为有效机会时，会重建为带 `test_epoch_id` 的新实时信号，不再因为旧信号编号去重而错过仍成立的机会。北京时间 `2026-06-16 09:14` 已重启 M15 守护器加载新逻辑，当前处于等待下一交易日状态。
- M12.32 当前已把长桥模拟账户的实时盈亏从普通状态表中抽出来，新增顶部“实时盈亏”卡片区，直接展示今日盈亏、账户总盈亏、交易总盈亏、已实现估算、持仓浮动和项目资金占用；底层仍使用长桥只读账户/持仓/profit-analysis 对账口径，缺数据时显示等待或无法计算，不使用假本金或旧本地模拟账本硬算。
- M15 当前已修复非交易窗口空转问题：长桥实时链路守护器在美股常规交易时段仍按 `check_interval_seconds=5` 秒巡检，非交易窗口改用 `idle_check_interval_seconds=60` 秒低频巡检，并在距离下一次常规交易时段不足 `120` 秒时自动回到快频检查，避免周末/休市期间每 5 秒重写大看板导致 CPU 偏高。北京时间 `2026-06-20 23:41` 巡检显示 M12.47 存活、M15 守护器 PID `2436057` 存活、当前为非交易日等待，开盘前验收 `10` 项通过、`1` 项等待交易窗口；M12.32 长桥“实时盈亏”卡片已恢复显示 `6` 项实时盈亏/占用指标。
- M15 当前已修复长桥盈亏对账报告不同步问题：账户状态只读刷新现在会同时写入 `m15_longbridge_account_pnl_reconciliation.json` 和 `m15_longbridge_account_pnl_reconciliation.md`，避免 JSON 已是最新但人工 Markdown 报告仍停在旧日期。北京时间 `2026-06-29 14:30` 巡检已重启 M12.47 与 M15 守护器；开盘值守 `10` 项通过、`1` 项等待交易时段，长桥模拟账户为 `lb_papertrading`，当前现金/购买力 `99301.43`，持仓 `16` 条，未完成挂单 `0`，最新长桥账户区间净值盈亏 `-120.34 USD`，股票交易合并盈亏 `-102.46 USD`。
- M15 当前已修复长桥看板盈亏口径混乱问题：M12 顶部不再显示模糊的“长桥总盈亏 / 长桥今日盈亏 / 长桥交易总盈亏”旧字段，统一改成“长桥App当日盈亏”“长桥接口净值日内变化”“长桥接口持仓今日浮动”“长桥当前持仓总盈亏”和“长桥交易累计盈亏”。其中 `portfolio.total_pl` 只代表当前持仓总浮盈浮亏，`portfolio.total_today_pl` 只代表当前持仓今日浮动，不能再叫账户总盈亏或当日总盈亏；`profit-analysis` 同日 `sum_profit` 只代表接口净值日内变化，不能冒充 App 顶部当日盈亏。本地分仓看板同步按相同口径展示主力仓、实验仓、002 专项主力仓的当日盈亏、持仓今日浮动、当前持仓总盈亏、已实现盈亏和交易累计盈亏；旧基线缺少资金池字段的成交仍显示为“未归属资金池”，只作历史归因说明，后续新订单按资金池写入。
- M15 当前已新增长桥后台守护巡检：`scripts/run_m15_background_watchdog.py` 和 `config/examples/m15_background_watchdog.json` 只允许安全地确认 M12.47 守护器、M15 长桥实时守护器和开盘前验收状态，显式拒绝 M12.37 once-mode、`--no-fetch`、`--no-refresh-quotes` 以及任何 Longbridge 下单/撤单命令形状。北京时间 `2026-06-30 10:48` 前后已启动 watchdog PID `32325`，状态为 healthy；该守护只负责后台稳定性巡检，不提交订单、不接实盘。
- M15 当前已纠正 002 专项仓运行单元：`M10-PA-002-1d` 原主力仓和专项仓对照保持不变；`M10-PA-002-5m` 从本地修复仓提升到 `002专项主力仓`，按 `1.0x` 正常测试、只占专项仓资金，不进入原主力仓。M12 长桥看板新增“002专项仓运行单元复核”，把 `002-1d` 与 `002-5m` 的本地净盈亏、胜率、盈亏比、长桥资金池归属和当前动作分开展示，避免以后只按父策略 `M10-PA-002` 抽错。
- 2026-06-30 开盘前巡检已修复长桥看板叠加层：非交易窗口 M15 只读刷新时，`m12_32` 长桥板块现在也会读取当前启用版执行配置，展示 `longbridge_panel_generated_at`、`position_count` 和“002专项仓运行单元复核”。最新巡检显示 `M10-PA-002-1d` 在主力仓 + 002 专项仓，`M10-PA-002-5m` 在 002 专项仓，两者均为“已按运行单元复核”。
- 2026-07-01 盘中巡检修复 M12.47 状态误报：M12.47/M15 守护器均存活，长桥开盘值守 `11/11` 通过；M12.47 状态文件现在只在本轮确实触发重启时显示 `last_restart_reason`，健康刷新时不再把上一轮 `dashboard_stale_*` 旧原因挂在看板上。本地完整看板刷新中但仍处于该步骤允许时长内时，状态显示为 `refreshing`，不再误报 `refreshing_stale`；长桥叠加层会继续补回实时盈亏和 `002专项仓运行单元复核`。当前长桥模拟账户为纸账户，持仓 `13`、挂单 `0`；主力仓已用敞口约 `3246.84 / 6000`，实验仓约 `1685.85 / 6000`，`002专项主力仓` 为 `0 / 6000`，等待 `M10-PA-002-1d/5m` 的合格实时信号。
- 2026-07-01 盘后巡检修复 M15 非交易窗口空快照覆盖问题：长桥账户主状态、账户摘要和盈亏对账三份产物现在会一起保留上一份有效 `lb_papertrading` 模拟账户快照；若只读刷新临时返回空/非模拟账户摘要，守护器状态显示 `account_state_refresh_failed` 与 `failed_using_cached`，但不再把看板现金、持仓、账户当日盈亏和交易累计盈亏清零。最新只读账户快照为 `2026-07-01T03:09:56Z`，长桥模拟账户现金 `98481.28`、购买力 `100270.09`、持仓 `11`、挂单 `0`；账户总资产 `102683.41`，旧字段 `portfolio.total_pl=112.315` 已降级为“当前持仓总盈亏”，旧字段 `portfolio.total_today_pl=-8.023` 已降级为“接口持仓今日浮动”，交易累计盈亏 `165.87`、已实现估算 `53.56`、持仓浮动 `112.32`。`2026-06-30` 美股交易日长桥实际成交 `11` 笔，和 M15 实时提交流水提交 `11` 笔一致；另有 `8` 条买入信号被主力仓敞口或同仓同标的重复规则阻断，未送到长桥。
- 2026-07-01 已落地长桥实际账户口径统一与未成交订单诊断：`scripts/run_m15_longbridge_realtime_account_state.py` 会同时生成 `m15_longbridge_order_reconciliation.json` 和 `m15_longbridge_unfilled_order_diagnostics.json`。最新只读刷新显示长桥历史订单 `635` 条、本地实时提交归因流水 `632` 条、实际成交 `183` 条、未成交 `452` 条，其中拒绝 `436`、取消 `8`、过期 `8`；`3` 条长桥旧路径/未归因订单只影响账户真实总口径，不混入新分仓策略成绩。本地 submitted 但未 `Filled` 的订单不再进入主力仓、实验仓、002 专项仓、策略盈亏、胜率、盈亏比、回撤或占用，只进入未成交诊断。M12 长桥板块已改为读取长桥持仓成本价/当前价计算持仓浮动，不再用本地实时行情价格覆盖长桥持仓价；逐策略盈亏归因也只使用长桥实际成交后的本地归因映射。
- 2026-07-01 用户截图核对显示长桥 App 资产页总资产 `102,688.90`、现金 `98,481.28`、持仓市值 `4,207.62`、顶部当日盈亏 `+94.61`、持仓总盈亏 `+344.66`。CLI 当前 `portfolio.overview.total_today_pl=-8.023` 与 App 顶部当日盈亏不一致，因此已把该字段降级标注为“接口持仓今日浮动”，不再叫“长桥当日盈亏”；App 当日盈亏在看板中显示“等待长桥字段对齐”，直到找到与 App 页面完全一致的接口字段。总资产/持仓市值已改为优先使用 `portfolio.total_cash + portfolio.market_accounts.US.market_value`，当前得到 `102,688.91 / 4,207.627`，与 App 展示基本对齐。订单详情回查已启用缓存和每轮上限，首轮回查 `120` 个未成交订单详情，其中 `112` 条明确为 `Insufficient holdings, please check if there are pending orders occupying available positions`，诊断为“可卖数量不足或已有卖单占用”；剩余未查历史订单继续在后续刷新中补详情。
- 2026-07-02 盘后巡检修复长桥订单对账漏读当前订单的问题：`m15_longbridge_order_reconciliation.json` 现在合并长桥当前订单列表与历史订单列表后再按订单号去重，避免当天订单尚未进入 `order --history` 时被误判为“本地未确认提交”。修复后已重启 M15 长桥实时守护器加载新代码，最新只读刷新显示长桥订单 `660` 条、本地提交归因 `657` 条、实际成交 `205` 条、未成交 `455` 条、本地未确认 `0`。`2026-07-01` 美股交易日长桥实际订单 `25` 条，其中 `22` 条成交、`3` 条取消；取消的 `PEP/MNST/META` 已从 `m15_longbridge_realtime_stale_order_cleanup_ledger.jsonl` 诊断为系统买单超时撤单，不计入分仓盈亏、胜率或回撤。
- 2026-07-03 盘中巡检修复 M15 长桥实时执行流水膨胀问题：`m15_longbridge_realtime_execution_ledger.jsonl` 之前累积了 `106055` 行、约 `126MB`，其中绝大多数是旧测试基线或无基线历史行，导致每轮执行器都读取并重写大文件。当前执行器已改为先把非当前 `test_epoch_id=m15-single-strategy-buckets-20260702` 的旧行压缩归档，再只追加本轮新流水；热文件已降到约 `250KB`，旧流水压缩归档约 `8MB`。该修复不改变长桥信号、风控、下单或分仓口径，只减少热路径 I/O 和后续推送风险。

## 当前阻塞

- 远端推送大文件阻塞已处理：M10.6 原始大 JSONL 已用 gzip archive manifest 保留可追溯，原始文件不再进入可推送历史；后续推送 `main` 不应再被该文件阻塞。
- 当前 M14 已把“是否真的测试过”升级为 challenge/gate 口径；有效 `10` 个纽约真实交易日 challenge 已固化为 `10/10`，且 `2026-06-01` fresh refresh 已完成重算。当前 blocker 不再是 fallback/no-fetch 或旧 M14 日期，而是模拟账户凭证和首笔模拟订单尚未获得用户单独批准；修复队列、救援 A/B 和影子参数证据仍需继续滚动补账本。任何单日 open/close、zero_signal 或 M13 goal complete 都不能直接解释成可投入使用或可盈利。
- M14 当前需要重点处理的策略 blocker：`M10-PA-001/002/007/009/011/M12-FTD-001` 等修复运行单元继续按 `repair_now` 修复并保留本地模拟/本地订单草稿；`M10-PA-012/013` 等盈利但高风险运行单元按 `risk_limited_advance` 降仓推进并补风控证据；辅助模块只服务筛选、目标价、止损、仓位、加仓和图形识别，不伪装成独立交易账户。所有非修复 runtime 已进入长桥模拟账户测试名单，但实际提交必须满足 `10000 USD` 共享资金、整股、只做多、扣费后预计净利润为正、单日订单数和风险/敞口上限；连接账户、读取凭证和提交订单仍需用户单独批准。
- M14/M15 当前策略 blocker 已改为分仓口径：主力仓、实验仓和 `002专项主力仓` 分别满足自身资金、单标的、总敞口、利润门槛、收益风险比、整股、只做多和纸账户边界即可；不再使用资金池合计总敞口，也不因另一个资金池持有同一标的而阻断。修复仓策略不生成长桥买入信号，继续留在本地模拟修复。
- M12.49 运行层 blocker 和 PA004 方向兼容 bug 已修复；M12.47 守护器可自动拉起 M12.37，盘前/盘中/盘后刷新看板；M12.37 已自动接入 M13 runner，M14 只在盘后固化 challenge/gate。
- M11.7 旧模拟交易试运行口径已被 M14 challenge/gate 取代：当前已有 `10/10` challenge 和 `approved_internal_sim_only` 策略，但用户尚未批准 broker paper/live，且 rescue/调参证据仍未完成。
- 新闻/财报当前仍是 sidecar 第一版：Google 案例已覆盖，但生产级新闻/财报历史事件集尚未建立，不能把当前 `2` 条事件样本解释成完整新闻感知回测。
- 147 只种子池的长历史 `5m` 全窗口尚未补齐；当前只要求当日 `1d` 与当前交易日 `5m` 可用于每日只读观察，不把它解释成两年日内历史完整回测。
- 147 只种子池已进入当前日扫描配置；若某交易日仍有标的缺当日长桥只读日线或 5m，则该标的不得进入长桥提交队列。
- `M10-PA-004` 做多分支已进入主推进链路，混合变体继续对照；`M10-PA-007` 归入第二腿图形识别修复队列，未补足稳定图形证据前不进入模拟账户提交链路。
- 真实 broker / live 交易仍冻结，直到用户另行批准；但可以提前准备不联网、不下单的 broker readiness scaffold、paper dry-run contract、审批 gate 与回退机制，避免策略通过后才临时补工程。

## 下一步

- 下一步：让 M12.47 守护器每个工作日自动拉起 `python scripts/run_m12_37_intraday_auto_loop.py --session --config config/examples/m12_37_intraday_auto_loop.json`；该入口已自动触发 M13，并在盘后自动触发 M14，只有故障补跑时才需要手工运行 M13/M14。
- 同步连续运行 M12.47/M12.37/M13/M14 每日只读循环，在 `10/10` challenge 之后继续收集 approved internal-sim 刷新、rescue 首账本、rescue 10 日 A/B、shadow-review 和 broker dry-run blocker watch 证据；若 M12 数据仍是 fallback/no-fetch，当天只能记为数据质量 blocker，不能算作 fresh refresh 通过。
- 每日 challenge 结束后按收益、回撤、胜率、期望、交易频率、滑点敏感性输出可执行动作：`advance_internal_sim`、`risk_limited_advance`、`repair_now`、`pause_runtime`、`auxiliary_module` 或 `paper_candidate`。高回撤、低胜率的盈利策略先降仓推进并加风控提醒，不再用旧的泛化“继续观察/不批准”口径挡住下一步；只有通过内部模拟和长桥预演的运行单元，且用户单独批准后，才允许进入长桥模拟账户，真实账户、真实下单和 live 仍冻结。
- 新增下一步：设计 M14.2 / M15 `broker readiness scaffold`，先做模拟账户与未来券商接入的工程准备，包括 `BrokerExecutionRequest`、`BrokerExecutionPlan`、`credential_policy`、`approval_token`、`paper_dry_run_only`、`live_disabled_by_default`、只读账户探针接口、订单预演日志、熔断/kill switch、回放校验和审计报表；默认不开真实连接，所有真实 broker/paper broker/live 调用都必须等待用户单独审批。
- M14.2 验收下一步：在不引入真实 broker SDK / HTTP / WebSocket 的前提下，把 dry-run preview 与 M14 gate artifact 连接成只读审计报告；之后才讨论 broker paper 凭证注入和人工审批流程。
- 不手动运行 `scripts/run_m12_37_intraday_auto_loop.py --once`；下一次 fresh refresh 只能由 M12.47 守护器在交易窗口拉起 `--session`，所有输出仍是只读模拟，不接账户、不下单。
- `M10-PA-008/009` 不再等图例确认，继续严格定义观察；`M10-PA-004` 做多版现已进入正式主线账户，历史样例只留作参考不入账，PA004 做空版暂不进入主线；`M10-PA-007` 进入每日观察候选。
- 继续分批补齐 `147` 只种子池的长历史 `5m` 全窗口；当前长桥模拟账户快速通道只要求当天新信号的当日日线、当前 5m、长桥只读 quote 和 M14 当天策略名单完整。
- 满足 `147` 只当日数据稳定、`10` 个交易日看板连续输出、重点策略 A/B 重测可解释、扣费后预计净利润为正和用户业务审批后，非修复运行单元才能进入长桥模拟账户提交链路。
- 继续扩展新闻/财报事件样本，优先覆盖 `147` 只种子池里发生财报、盘前/盘后大幅波动、宏观事件日的股票；把 M12.40-M12.45 的 sidecar 输出接入每日看板新闻区域，但不改主策略触发逻辑。
- FTD001 下一步优先把 `loss_streak_guard` 作为增强观察候选加入日报比较：保留 baseline 与增强版并排展示，观察是否真能降低实时回撤。
- M10.6 不得被解释为真实实时观察或盈利证明；M11 paper gate 报告只是准入草案，不是交易许可。
- M10.4/M10.5/M10.6 仍只允许输出 `needs_definition_fix / needs_visual_review / continue_testing / reject_for_now / continue_observation`，不得输出 `retain/promote/live-ready`。
- M9 `SF-*`、`PA-SC-*` 和历史回测结果只允许用于 comparison，不得反向修改 M10 clean-room catalog。
- 若 repo config、`plans/active-plan.md`、`docs/status.md`、`docs/acceptance.md` 的 provider contract 不一致，必须先修正文档/配置一致性，停止任何后续提炼或回测。
- heartbeat / 自动推进若继续扩展，恢复状态只允许依赖 ledgers + `run_state.json.resume_cursor`。
- M10 期间继续保持 trigger 逻辑不变，`statement` 仍不进入 trigger；Brooks v2 raw 导入只用于知识来源登记和策略提炼，不进入 live execution。
- 当前长期稳定基线仍是 `main`，继续保持 `paper / simulated` 与 `no-go` 边界。
- `feature/m7-broker-api-assessment` 继续保留为历史阶段/里程碑分支，不再作为未来默认合并目标。
- M10 不是 broker/live 扩展；当前仍不重新评估真实 broker、真实账户、live execution 或付费 API。
- `2026-07-28` 盘中 SDK 巡检继续定位到行情推送误降级根因：147 个分批 `subscribe` 请求已经全部成功并上报 `147/147`，但随后重复调用 `quote.subscriptions()` 和第二次替换行情回调时卡死，父进程因 20 秒无进度误判订阅失败。实时行情工作进程已改为直接信任逐批确认结果，并在订阅前只注册一次带内存开关的回调；快照轮询仅保留为真正订阅失败后的安全退路。
- 推送工作进程收到 `ready` 后新增 20 秒启动缓冲，用于排空 SDK 初始行情突发；缓冲后恢复 5 秒严格心跳。启动缓冲内不因暂时拿不到 Python 心跳误杀进程，缓冲结束后真实失联仍会停止新开仓并重建行情进程。
- 当前长桥推送订阅在同一会话内仍会不定期卡住，因此已把 SDK 快照安全退路改为“子进程只取快照、主进程持有五分钟 K 线状态”。单次 `quote(147)` 正常约 `60~80ms`；请求卡住时父进程可杀掉并重建子进程，但不会再丢失已经累积的当前五分钟 K 线。快照按轮询接收时间落入 K 线，SDK 最后成交时间继续保留为来源证据，未成交标的形成零成交量平盘而不是误落到旧 K 线。
- 快照间隔从 1 秒调整为 3 秒，降低长桥行情连接持续压力，同时仍满足当前 5 秒内处理验收。快照连续三次失败后只打开行情熔断并停止新开仓，冷却 15 秒后由同一主进程自动重连；不再让整个 M15 永久退出并等待人工巡检。
- 修复订阅超时切换快照时遗漏父级 K 线聚合器初始化的问题。聚合器现在在任何快照工作进程启动前统一创建，快照子进程重建只替换 SDK 请求上下文，不再重置或绕过五分钟 K 线状态。
- `2026-07-28T15:55Z` 真实盘中验证生成 `21` 条信号并提交 `19` 笔长桥模拟订单，全部取得订单号且长桥状态均为 `Filled`；该轮本地处理耗时 `3628ms`。此前同日 `29` 次无订单号拒绝已确认由 OAuth 访问令牌过期且刷新失败造成，不计入成交或策略成绩。
- `2026-07-28T16:10Z` 跨行情子进程重建验证继续成功：`147/147` 个五分钟柱完整收线，生成 `19` 条信号，提交 `2` 笔，耗时 `1295ms`；订单 `1266789716460617728`（`NOW.US` 3 股）与 `1266789717089771520`（`GM.US` 3 股）均为长桥实际 `Filled`。运行层为 SDK、账户为 `lb_papertrading`，账户快照保持在 `15s` 内。
- M15 后台看护已修复业务状态假健康：即使脚本进程退出码为 `0`，只要 SDK 状态仍为连接中、账户快照不健康、开盘验收被阻断或 SDK 综合验收被阻断，看护就会输出 `needs_attention`。解除循环依赖后的当前重新验收结果为开盘 `14/14`、SDK 综合验收 `12/12`、后台看护 `healthy`。
- 已解除 SDK 综合验收与后台看护之间的循环依赖：综合验收不再要求“上一轮看护必须健康”，后台看护仍单向检查综合验收。一次行情子进程瞬时重连只会让当轮看护报警，恢复后下一轮可自动回到健康，不会永久卡死。
# 2026-07-31 M15 行情重连稳定性

- 长桥 SDK 主进程、模拟账户、订单和成交链路正常；盘中 `53/53` 张订单实际成交。
- 300 只快照轮询偶发接近 `1s` 时触发了过严的子进程重建阈值，造成看护与看板在 `running/connecting` 间抖动。快照健康上限已改为 `2500ms`，信号处理 `1s` 目标不变；看护增加 `20s` 有界恢复复查。
- 看板现在区分直接订阅覆盖和实际快照行情覆盖；`sdk_snapshot_poll` 下以 `market_data_coverage/trading_market_data_coverage` 说明实际可用行情。
