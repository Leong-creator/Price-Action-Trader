# 项目状态

## 最新进展：主项目Linux修补与串行测试准备（2026-09-21）

- 已集成候选至本轮修复分支，主项目官方环境升级5.0.0并完成wheel/加载文件校验；保留Python3.12.3。默认入口、SDK时间字段及诊断序列化缺陷已修，正在完成综合复核与合并。
- 旧异步行情桥接、订阅分批和手工行情端点退出当前修补路径；账户仍使用的工具保留。已确认本地错误不等于已经查明历史零回调订阅超时根因。
- 计划Windows21:25—21:45后，WSL真实行情/K线/策略诊断21:50—22:20；短窗口无法替代整场或完整日内策略输入。原策略/147股票/风险不变，无账户或订单调用。
- 本轮尚无新SDK实连，完整日仍0，原故障未解除。最终测试、注册、合并与清理结果见[实施记录](decisions/20260921-linux-runtime-repair.md)。本节更新下文日期记录中的“正式环境未升级、候选未部署”等旧状态，不改旧实验结果。

## 最新状态：Windows开盘测试准备完成，已安排一次执行（2026-09-21）

- 86项离线测试通过，独立审查通过，官方Windows环境及部署文件哈希重新核验。一项Windows有限权限任务已注册并读回，计划北京时间今晚21:25—21:45；仅行情诊断，不是生产自动启动，迟到拒绝、不补跑、不重试。
- 当前尚未到测试窗口，case013未启动，本轮真实SDK连接0、完整交易日通过0。命令形状试跑正确拒绝提前连接，未提前复制认证或占行情锁；5份正式故障/订单防重状态哈希保持。
- 执行后按 `run-once-result.json` 和case013记录判断订阅、四条参考流连续性及147只覆盖；进程退出成功不等于行情通过。正式入口切换、K线/策略接回仍待盘中结果；时间字段归一的既有缺陷已记为接回修复项，未混同为订阅超时根因。见[完整测试安排及验证](decisions/20260921-windows-opening-continuity.md)。

## 最新核查：环境差异与正式入口尚未切换（2026-09-21）

- 只读对比确认：主线默认启动仍引用已清除的 `.venv/bin/python`；已核验官方4.5.0安装在 `.venv-m15`，来源校验、默认行情Config、同步全量订阅等候选尚未部署。不能说正式系统已完全采用Windows成功样例的方式，也不能说旧修改包仍在运行。
- case011与case012源码字节、官方5.0.0版本、认证输入、147只一次Quote+Trade及实际 `.cn` 域名相同；平台、Python、原生包、网络上下文和运行时间不同。Linux独立样例无项目包装仍超时，Windows147只首推成功；尚不能归因到WSL、代理或某个SDK构建。
- 当前代理配置比较不是两次历史环境重建；本轮无SDK连接、运行代码或故障状态修改。既定Windows真实时段连续性验证优先，必要时再设计有控制的有限串行平台对照；正式切换、整场及策略验收仍待完成。详见[完整环境对比](decisions/20260921-environment-comparison.md)。

## 最新状态：Windows隔离订阅及147只首推成功，尚未盘中验收（2026-09-20）

- case012在Windows原生官方签名CPython3.14.3、新venv、官方Longbridge5.0.0 wheel中成功退出。样例原字节、147只一次Quote+Trade及Config默认保持，SDK实际选择 `.cn`；总11.082秒包含sleep(10)，认证/档案之后SubscribeRequest至成功unit响应日志间隔57.722毫秒，不是独立网络耗时测量。
- stdout有完整147行Quote，唯一147只与source集合完全相同，原生日志147条Quote一致；Trade 0、stderr 0。这是休市初始化及首推覆盖证据，不能证明真实开盘后的持续行情、完整K线或策略已恢复。
- Job ActiveProcesses=0，两处隔离标记已清、连接锁空闲；主线5份保护状态和原认证资料哈希不变。临时Windows认证副本2文件及2个新建私有根已归档删除，保留一个停止的Windows诊断venv供后续真实时段复核；无自动启动，复用须核验并重新安全配置认证。
- 累计12项独立样例、3项项目探针，真实完整交易日通过0天。候选未部署，正式环境未升级，原故障未解锁，自动启动和交易未恢复。旧Linux也有成功样本，不能据此认定Windows已修复或WSL为根因。
- 下一步优先在此环境验证真实开盘持续Quote/Trade，然后决定正式运行位置及最小迁移；当前无需以“长桥完全不可用”为由直接换源，不凭周末首推改策略或下单。仍不向外反馈、不自动重试；详见[Windows隔离实测结果](decisions/20260920-isolated-windows-result.md)。
- case011已有美股 `LV1 Real-time Quotes` 套餐记录，但档案查询不证明147只逐项权限；SDK `subscriptions()`为本地记录，不是独立服务端确认。case012实际响应及首推证据不扩大为所有权益或连续性保证，原计划未新增SIP要求。

## 前阶段：官方接入点与当前发布版仍失败，换源待访问资格（2026-09-19）

- 用户明确不向外反馈；已停止供应商提交方案，转为官方方法验证后按条件更换唯一来源。
- case010：官方4.5.0原包、相同147只单次Quote/Trade示例，仅使用官方 `LONGBRIDGE_REGION=hk`。SDK实际选择 `.com`；公开subscribe内部建连超时，总6.208秒、exit1，无认证/订阅协议请求日志、0回调。不能表述为QuoteContext构造失败，也不能据此归责长桥服务端。
- case011：从PyPI下载并核验官方5.0.0原包，隔离安装文件逐字节与wheel一致；相同示例、默认配置选 `.cn`，认证及行情权限返回成功后订阅request timeout，总30.833秒、exit1、0回调。未升级正式环境。
- 累计11项独立示例、3项项目探针。官方方法仍未解决当前环境的接入问题；具体SDK/网络/服务端根因及与9月8日开盘故障的关系未证。停止本阶段长桥实连，不再同条件重试。
- 两进程均退出、连接锁已释放、主线5份保护状态校验不变。候选未部署、未解锁、未恢复自动启动，真实完整交易日通过0天。替代源尚未接入；已询问现有平台/套餐，凭证不得发到对话中。
- 替代源没有凭空增加SIP全市场要求；免费Alpaca的30只限制无法承接147，Tradier零月费须正式账户与实时权益且有条件费用。详见[官方验证结果](decisions/20260919-official-route-result.md)与[来源选择](decisions/20260919-marketdata-source-options.md)。

## 前阶段：官方请求系统观测仍超时（2026-09-19）

- 用户要求继续后，核实此前官方样例调用无误，官方5.0.0虽已发布但未找到本次订阅超时的定向修复依据；未升级、未修改SDK或官方样例。
- 新增一次有不同观测的case009：官方默认配置、147只一次Quote/Trade订阅，总31.034秒后SDK request timeout、0回调，子进程已退出。155次系统采样中，唯一持续443连接末段150次、29.830260秒计数不增长，全部152次收发队列为0。认证/行情档案/订阅共处一个200ms采样窗口，不能把TCP字节或ACK直接认领为订阅响应。
- 源码确认Core等待订阅期间不消费推送队列，旧“无parsed push日志”不等于网络未收到数据。线程等待分类不能证明GIL死锁。需官方核对command6请求/响应时序，未对外提交材料，无新增SDK重试。
- 累计9项独立样例、3项项目探针；修复仍未成立、候选未部署、真实完整交易日0天。主线5份保护状态不变，连接锁空闲；环境、股票池、策略、风控、自动启动和故障锁不变。详见 [新增系统观测](decisions/20260919-sdk-request-completion-observation.md)。

## 当前状态：2026-09-19同步单次147只再失败，修复未成立

- 用户已授权按官方v4.5.0独立async最小示例重新建立基线。官方原包安装已核验，但此前探针存在项目封装和自定义配置，不能把“官方原包”写成“官方最小示例已经验证”。
- 本次示例进程不导入项目 `scripts`，使用官方 `Config` 默认；排他锁和总运行期限在外部执行。已完成SPY/QQQ Quote（11.056秒，exit0，各1条首推），仅改symbols至147只Quote后30.630秒OpenApiException/request timeout，traceback位于await ctx.subscribe，回调0，非60秒外部期限，两次均退出。异步路径未进入Trade。
- 2026-09-19周六非交易日；上述各1条Quote属于首推，不是持续行情。147只初始化失败已可绕过项目代码复现，不能归责策略或只归责旧封装；具体原因及与9月8日开盘断流的关系未证。官方同步147只Quote对照已在10.804秒完成、147条首推覆盖147只、无stderr；只证明这次可行。相同异步147只源码A-B-A交叉复核再次30.730秒订阅失败；同步Quote+Trade公开接口扩展10.904秒完成、147Quote首推、周末Trade 0。仅换同步回调仍30.632秒订阅失败，三次异步147只路径已熔断；不能认定SDK内部根因。当前真实完整日通过仍为0。
- 同步Quote+Trade基础上加一次订阅核对和一次147只快照，11.005秒完成，147条订阅均含Quote+Trade、快照147，无stderr；Quote对象repr147，但stdout交错，不按行认领回调股票。只证明本次查询未卡死，盘中回调连续性仍待验。
- 接回项目raw后，同步默认Config保留50只分批首请求仍超时（30.865秒，0回调）；私有配置仅改一次147只，30.124秒采样完成，订阅0.731秒，147/147，raw/normalized/enqueued/dequeued Quote各147。该次成功曾支持同步默认＋单次全量组合候选，后续失败已否定它足以证明修复；不证明仅API或仅批次为唯一根因，pipeline未跑并暂停。
- 独立审查import运行模块意外触发CLI，已中断回收：实际完成模拟账户只读快照，日线49/147、订阅0/147；dispatch未请求/未启用、提交日志0字节、主树5个受保护文件哈希不变。该事件不算授权诊断/验收，不能概括本轮零账户访问；主入口限定与真实import哨兵测试已补，单独留证，详见 [事件记录](decisions/20260919-runtime-import-start-incident.md)。
- 最新反证：project-final-raw147-003使用候选同步默认＋单次147只，SDK订阅30.659秒timeout、总30.774秒、回调0、worker退出。此前成功样本保留，但该组合不是已证明修复，不能说只差交易日；pipeline未跑并暂停，部署/解锁/自动启动全部暂停。
- 最后独立同步示例仅启用官方LOG_PATH，30.831秒订阅超时、0回调并退出。认证及行情档案响应成功、额度500；SubscribeRequest后无成功订阅响应/推送记录。结合官方源码只能定位到已过本地限流、进入SDK内部发送队列后等待请求完成超时，不能证明上网发送或服务端无回复。8项独立示例与3项项目探针现已停止，见 [支持说明](decisions/20260919-official-sdk-support-brief.md)。
- 当前候选离线组合结果795通过、2跳过：移除1条过时批次源码断言并加强行为检查后，相关147项重跑通过；未重新完整运行797项。2项跳过需显式原包环境/wheel路径，详情见官方基线记录；不是实时行情验收。下文805通过/2跳过为上一轮隔离集成的历史结果。
- 根据同步147只Quote及Quote+Trade当次完成证据，实施唯一官方同步QuoteContext、行情默认Config和一次全量订阅候选修复，删除旧异步行情桥接、批次配置及分批循环；保留队列/K线/策略，父进程监督阶段期限及子进程退出。账户异步和仍被账户/历史使用的helper不改。候选仅供审查，最新实机订阅再失败，未部署。
- 原147只任务的三次熔断继续有效；新授权只执行不同验证对象的官方示例实验，不是自动重试。原故障锁、订单防重状态和历史资料保留；不下单、不清仓、不恢复自动启动或交易。
- 本轮已归档并移除旧integration工作区及本地/远端分支，收敛为main与official-baseline两工作区，长期保留main、M7和当前候选共3个本地/远端分支，暂存0；事实文档临时分支合入后清理。下文前阶段“两工作区”中的integration已退役。详见 [本轮收尾](20260919-official-baseline-closeout.md)；候选未部署，主线正式来源receipt尚未部署，不补签以制造通过。

## 前一阶段：历史清理完成，项目封装诊断未解决

- 本轮已完成归档后的33个旧工作区、191个本地分支、106个远端分支、2份暂存清理，PR #1关闭；9,041个独有知识资料assets文件保全且哈希通过。另有4个本轮主题工作区及分支完成集成归档后收尾，最终保留main与integration两工作区、本地及远端各3分支、暂存0。逐项证据见 [清理、故障和验收结果](20260919-cleanup-and-marketdata-repair.md)。
- 8项旧运行环境/工具/DNS注入残留、额外6项旧SDK源码及wheel目录、3份旧临时实验环境均已归档核验后删除；旧启动入口及退役调度已撤销。关键故障、订单提交日志和编号状态与归档哈希一致，未清除故障锁、未恢复自动启动。
- 主树及隔离集成 `.venv-m15` 已安装并核验官方4.5.0原包，staging环境来源签发及验证通过。主线仅合入纠正文档，运行程序源码仍为 `a413cb8`；程序修复在隔离集成中，尚未正式部署。
- SPY/QQQ两只股票120.028秒只读诊断通过，2/2订阅，记录556条报价及554条逐笔回调；未访问账户/订单，未验K线或策略。随后147只接入连续三次失败：30.034秒原始超时、30.134秒subscribe阶段超时、60.062秒管道对照（真实日线147只/8820条通过后首批50只订阅失败）。回调、订阅进度及K线未取得成功证据；范围收窄到订阅初始化，尚未到策略。已触发熔断并冻结 [Failure Dossier](decisions/20260919-subscription-initialization-failure-dossier.md)，不再同条件连接。
- 今日官方原包复现的是订阅初始化失败；9月8日是初始化后开盘断流，两者尚未证明同根因。今日策略/K线不在失败前路径，不等于历史唯一根因已找到。最新生产故障仍是9月8日21:30:30的参考行情静默，完整五分钟边界0。旧安装包来源错误已纠正，但与该次断流的因果关系未证明；之后故障锁定也不能计作每日重新测试失败。历史证据见 [9月8日现场记录](decisions/20260908-opening-marketdata-failure.md)。
- 原任务熔断后，复核的小批次区分实验每批2只，前30批60只成功、446条原始回调；offset60的CMCSA/WBD批次30.755秒OpenApiException失败，非45秒外层超时，worker已退出。随后将CMCSA/WBD放首批单次60.164秒成功，订阅0.702秒、174条回调，排除该标的对必然不能订阅；最后旋转相同147只列表，仅顺序改变，前40批80只成功、581条回调，第41批MRVL/ADI返回OpenApiException（Other、无code；request_timeout只是消息分类）。排除固定该标的对失败或固定60只上限解释，仍未147只通过；本轮连接实验已停止。
- 隔离集成最终离线回归807项：805通过、2跳过；治理8项及扫描通过。完整旧看板仍有7项已复现基线失败。离线整场回放不作为实时验收，程序尚未部署。
- 本轮真实完整交易日通过0天；目标仍为147只、78个边界、11466条分类明确实时K线，一个完整日提供恢复资格依据，连续三个完整日才称稳定。
- 9月8日临时订单授权已到期，本轮不延续、不下单、不清仓。策略保持8条可执行、3条影子，股票池和风控不变。实际数据连续、及时、完整是验收依据，SDK内部不可观测事件仍记未知；项目不自动行情重试、不换源、不自动恢复交易。

以下日期条目保留为历史证据；其中“当前”“下一”“已部署”和当日授权仅针对条目日期，不代表当前部署、授权或验收状态。

## 2026-09-08 当晚订单联调修复

- 用户明确把今晚目标从只读行情改为模拟订单全流程联调。新增纽约2026-09-08日期限定授权；完整交易日验收记录仍独立保留，未通过不得伪装通过。
- 重启后发现旧运行状态过期造成启动锁定，已保存证据并人工恢复；跨系统身份识别修复单独提交。盘前官方异步行情147/147、8820条最新已完成日线及模拟账户读取通过。
- 订单热路径已接入官方 `AsyncTradeContext`，单次请求限时2秒，父循环共享5秒I/O预算；耗尽前不再发起查询或订单，不通过等待队列补旧单。提交前使用真实当前时间重查信号、账户和交易时段。
- 提交意图在SDK调用前持久化；超时、缺订单号、记录失败均待确认，跨进程重启也不得重复发送。只有唯一匹配的长桥订单号可完成对账；写盘失败仍保留已观测订单号供诊断，不计作确认成交。
- 历史订单和成交查询改为官方异步只读接口。账户实测分别1.119秒和0.392秒，未制造订单；失败不发布伪新缓存，1000行饱和响应不宣称完整。原盈亏接口继续在受外层超时保护的慢进程，不进入下单热路径。
- 跨电脑重启、旧PID复用、嵌套交易故障、初始化失败资源回收均补齐保护。隐藏Windows启动脚本保留单次执行，等待真实退出码；未新增计划任务或周期弹窗。
- 集成M15回归768项、治理6项、改动Python编译及差异检查通过。完整147只/78边界/11466根离线回放通过，只是功能证据，不能作为真实盘中推送、成交或P95延迟证明。
- 当前名单仍为8条可执行合同（5条做多、3条做空），3条视觉合同保持影子验证；策略门槛与仓位未改。实际派发、连接及账户状态以最新 `m15_longbridge_sdk_runtime.json` 和开盘验收产物为准，不能仅凭静态授权声称运行正常。
- 本轮高风险变更须经独立复核后部署；不改策略、仓位、盈亏算法、代理，不制造订单，不自动清理旧持仓。21:35单次心跳已建立，检查首根完整K线、请求/订单号/成交/退出及阻断。

## 2026-09-08 日线输入修复

- 日线 router 按行情时间保留最多60个去重历史时点，不再因20行截短阻断 PA001 的22行或 FTD 的24行输入；策略门槛、风控和配置未改。
- 父进程分别缓存封口前日线 OHLCV 与下一根首个观测报价，拒绝未来时点、跨日和旧信号重放；报价缺失不补造。缓存不能恢复已被 worker 合并丢弃的原始 tick。
- 禁止 socket 连接的相关回归216/216通过（新增17项），随后已合入上方768项集成回归。仅离线样例与回归验证，不新增完整日验收证据。范围见 `docs/handoffs/m15-daily-inputs-20260908.md`。

## 2026-09-07 实机只读部署验收

- 修复已分主题提交、推送并合并到 `main`，代码基线 `d611e21`；生产策略、仓位与配置文件未改。部署清单含官方异步行情桥接器，无源码或配置漂移。
- 北京时间11:40停止原实例后，使用唯一官方 `AsyncQuoteContext` 重新初始化。旧缓存备份在 `/tmp/pat-preopen-runtime-backup-20260907/`，本轮明确不复用缓存，实际重新读取8820条日线并完成147/147订阅；另有9/9已有持仓标的仅作监控。
- 11:41只读账户检查确认 `lb_papertrading`；资产、组合、持仓、当日订单和成交接口无错误。当前持仓24条、当日订单0条；本轮未提交、撤销或制造订单，未读取本地模拟账本。
- 账户快照多次核对为6至18秒，低于45秒；运行进程和看护各一个。行情子进程观测到3个Python线程、20个文件描述符、约59MiB内存。原运行与行情子进程退出已由PID消失确认。
- 开盘准备13项通过、3项等待、0项失败；综合验收10项通过、4项等待、0项失败。等待项是非交易时段、完整交易日证据和未请求派发，不是授权或初始化失败。
- M15组合587项单测、编译、仓库治理检查通过。假SDK整场回放不能计入真实会话，当前真实完整日仍为0/1，新开仓保持关闭。
- 下一真实会话为9月8日北京时间21:30。当前已部署的是只读验收运行，不是已允许正式模拟下单。官方SDK内部重连不可由Python关闭的限制仍须明确解决，不能把一次初始化或单个进程代次视作零重连证明。

## 2026-09-07 开盘前验证

- 用模拟SDK推送连续回放147只、78个五分钟边界、11466根K线，逐项核对OHLC与成交量；不制造长桥订单，不生成真实会话验收通过记录。
- 修正官方美股逐笔成交类型处理、同桶乱序、封口后迟到、节假日占位、边界后报价污染旧平K线、订阅类型覆盖和关键消息丢失。
- 日线在同一个行情连接中盘后后台更新；慢请求不阻塞心跳，截止超时不发布结果，不重试。初始化跨16:10时不能把旧缓存误标成当日已更新。
- 合并父层会话证据、审计门禁和异步接口后，M15组合测试587项通过；包括连续两个完整离线会话、假心跳、77/78边界、审计丢失、超时过滤和线程回收。
- SDK只读账户预检已确认 `lb_papertrading`，资产、持仓、订单、成交及组合接口通过；当前24条持仓、当日订单0条。本次不处理旧持仓，不下测试单。
- 尚未解决：官方SDK内部重连没有Python关闭开关或可靠通知。当前仅项目级自动重试已关闭，不能宣称整个SDK零重连；因此仍只读且不武装派发。休市初始化成功不能替代下一完整交易日真实推送证明。

## 2026-09-07 官方异步行情接口

- 新增 `OfficialAsyncQuoteBridge`：一个后台 asyncio loop、一次官方 `AsyncQuoteContext.create(config)`；同步调用方等待 Future，SDK I/O 在 loop 内 await，无同步 SDK fallback。
- 请求超时或错误后实例永久失效、不重试；清理回调目标、取消请求、释放上下文引用并限时回收线程。官方未公开 native close 确认/禁止内部重连接口，该缺口明确保留。
- `check_health()` 仅检查本地故障及 loop/线程存活，不发 SDK I/O；worker 集成后须每 loop 调用，以立即发现已记录的回调异常。
- 已集成到唯一行情worker、每轮健康检查和部署清单；同步行情上下文不再创建，订单权限和策略冻结不变。只读实机验证已通过（见顶部证据），启用订单派发前仍需人工复核。
- 用法、初始化/超时/关闭契约与官方依据见 `docs/m15-official-async-quote-bridge.md`。
- 离线验证：18 项适配器测试及 132 项原有相关回归共 150 项通过；适配器禁 socket 连接连续 20 轮共 360 次通过，每轮线程数恢复基线。

## 2026-09-07 关机后巡查

- 已保存旧运行状态并经人工确认恢复只读运行；147/147订阅、8820条日线、SDK模拟账户预检通过。9月7日美国劳动节休市，下一交易日为9月8日。
- 综合验收将未请求下单的主动只读启动明确标记为等待行情验收，不再误报下单开关和开盘验收两项失败；账户不新鲜等真实异常仍阻断。
- 尚无新增完整交易日验收证据，模拟新开仓保持关闭，策略与风控参数未改。
- 日线缓存有效期统一使用生产节假日日历：劳动节当晚和9月8日盘前接受完整9月4日缓存，9月8日收盘后要求新日线；不完整缓存仍拒绝。

## 2026-09-08之前的历史结论（官方原包部署声明已更正）

- 官方 SDK 行情整改、看护阶段判断、人工停止状态和完整日线缓存复用修复均已合并并推送到 `main`。
- 账户边界：只允许 `lb_papertrading`；实盘、真实资金、期权和融资未启用。
- 行情传输：生产只允许官方 `longbridge==4.5.0` SDK 的一个长期 `AsyncQuoteContext`，不再使用 `longbridge serve` 或行情CLI。
- 数据流：官方 SDK 报价和逐笔成交推送进入有界内存队列，随后顺序聚合五分钟K线并触发冻结策略合同。
- 行情故障处理：立即 `fault_halted` 并停止新开仓，项目不自动重试、重建连接或回退其他行情源。官方SDK内部重连限制见上文，不得继续沿用“已经禁止所有重连”的结论。
- 本地研究系统与长桥行情、信号、订单、持仓和盈亏完全隔离。

## 2026-08-28 整场结果

- 8月28日完整交易日验收未通过。正式进程在纽约时间09:30:22之后不再取得新的SPY/QQQ参考活动，并于09:30:53以 `reference_market_data_stalled` 安全停机。
- 本场形成0个完整五分钟边界、0根可交易实时K线、0笔新订单；不得把开盘前147/147订阅和8820条日线初始化当成整场成功。
- 运行层的父进程已具备读取 `raw_reference_activity` 的逻辑，但官方SDK子进程重构时漏发该心跳字段；修复后原始SPY/QQQ回调活动会独立于报价批次合并传给父进程。
- 故障证据新增原始通知总数、回调队列深度、参考活动来源、工作线程心跳年龄和资源占用。若再次停机，可以明确区分长桥推送断流与本地队列阻塞。
- 隐藏开机启动入口改为只读启动，不再自动附带 `--dispatch`。完整行情验收通过前，重启电脑不会自动取得新开仓资格。
- 本机串行实测：`cn` 端点3只标的订阅3/3并持续收到SPY/QQQ报价和逐笔成交；默认全球端点连接超时。因此保持 `quote_region=cn`，不修改系统代理，也不自动切换端点。
- 周五盘后发现本地研究调度器与M15日线初始化同时访问长桥：M15在300秒内完成137/147，剩余10只被截止；待本地任务结束后，这10只通过同一SDK逐只复查均在0.08至0.87秒返回60根日线。自动启动现已移除本地盘后调度器，M15成为唯一自动行情连接持有者。
- 停止本地调度器后的独占复测在300秒内完成144/147，最后3只仍是截止时尚未处理而非接口失败。按长桥SDK自动节流的实测结果，日线初始化硬期限改为600秒，并把“截止未处理”与“接口返回失败”分成不同故障原因；仍不重试、不回退。

## 2026-08-28 初始化证据

- 官方 SDK 147只只读探针连续三次完成147/147订阅。
- 最近一次探针三批订阅耗时约811毫秒、42毫秒、41毫秒；30秒收到104条报价和104条逐笔成交，覆盖38个非交易时段仍活跃的标的。
- 正式运行入口只读冒烟完成8820/8820条日线和147/147初始快照。
- SDK账户、组合、持仓、订单与成交端点均通过只读预检，账户确认为模拟账户。
- 聚焦 M15 回归517项通过；看护阶段判断、人工停止状态和完整日线缓存复用均有聚焦测试覆盖。
- 实机只读冒烟没有提交、撤销或制造订单，当前长桥已有持仓保持不变。
- 当日开盘前的干净主线已签发并验证部署清单；正式只读初始化完成147/147订阅、8820/8820日线、147/147初始快照和9/9已有持仓额外监控。
- 看护器现在把“非交易时段等待完整交易日行情门禁”显示为正常等待，连接、账户或订阅真实失败仍单独报警。
- 运行层只复用完整、属于最新交易日且覆盖147只标的的官方 SDK 日线缓存；不完整或过期缓存不允许启动交易链路，也不自动重试。
- 人工停止写入独立的 `operator_stopped` 状态，不再被下次状态检查误判为行情故障；真实 `fault_halted` 仍保持锁定并要求查明原因。

## 已删除的生产路径

- `longbridge serve` 行情适配器。
- 旧实时行情事件采集器和会话守护器。
- 旧纸面会话报告自动路径。
- 旧 SDK 示例、合同兼容、扩展只读、开盘验收和看护配置。
- 清仓中的行情重试、成本价替代行情和市价失败后自动限价退路。

## 截至2026-09-08之前记录的完整日证据缺口

- 2026-08-28完整交易日已经明确失败，尚无78/78个五分钟边界和11466/11466根实时K线证据。
- 2026-09-08允许按顶部用户当日授权进行模拟订单联调；它不等同于整场验收通过。其他未授权日期仍需完整日证据才能新开仓。

## 当前下一步

1. 冻结8项独立官方示例、3项项目raw探针以及单独误启动事件证据，保留成功与失败记录；异步三次失败路径继续熔断，本轮连接已全部停止。
2. 官方日志观测已复现同步147只订阅超时，整理 [脱敏支持说明](decisions/20260919-official-sdk-support-brief.md) 请长桥核对请求接收、订阅响应及SDK处理时序；尚未对外发送。不再盲目试连接，pipeline、部署、解锁和自动启动继续暂停。
3. 根据官方示例与项目封装的真实差异决定修复层；原因未明不部署、不解除故障锁。之后仍需完整交易日数据进入策略，一个完整日提供恢复资格、连续三个完整日才称稳定。

旧架构和失败方案只保留在 `docs/archive/` 与 `docs/decisions/`，不参与当前运行。
