# 2026-09-19 官方独立示例基线与未解决结果

## 目标与当前结论

**当前仍未修复：官方同步示例与项目单次147只探针曾成功，但最终候选代码再次在同样单次147只订阅时超时。同步默认Config＋单次订阅不能视为已证明的解决方案，pipeline、部署、故障解锁及自动启动全部暂停。** 官方4.5.0原包来源已经核验，但此前诊断仍带项目桥接、批次或配置，不能据此声称官方最小示例已验证。

本次用户已明确授权，先验证官方示例再逐层接回项目。前一轮147只同条件任务的熔断和原证据保持不变；9月8日初始化后开盘断流与前一轮订阅初始化失败仍不能认定同根因。

## 固定边界与实验顺序

- 固定官方v4.5.0 async示例和已核验原包，保留官方 `Config` 默认及调用顺序。示例进程零 `scripts` 导入，不运行项目桥接、worker、日线、K线、策略、账户或订单。认证只用已有官方支持资料，不写入公开记录。
- 外部唯一连接锁与总运行期限负责隔离；不在示例内部增加SDK单请求超时、重试或替换订阅逻辑。每次结束确认进程退出，不创建并行行情连接。
- 第一步SPY/QQQ仅Quote；通过后第二步仅扩大至147只Quote；通过后第三步固定147只及其他条件，仅增加Trade订阅和官方Trade回调，保留Quote。失败立即留证停止，不自动换端点或缩减生产目标。
- 2026-09-19为周六非交易日。休市可核查创建与订阅响应，回调数量如实记录；没有盘中更新不能自动解释为断流，首推或订阅成功不能解释为连续行情/整场通过。

## 来源与实际执行登记

| 项目 | 当前证据 |
|---|---|
| 官方示例版本、路径及源文件哈希 | v4.5.0 `examples/python/subscribe_quote_async.py`，SHA256 `4ba4ac4b0be05e8ffc1c1a5d7997af95ee03e552f2f95a9d979d84d11690d062` |
| 实际运行示例与来源差异 | 仅用已有client id替换官方占位（增加标准库Path读取，不公开内容），将官方示例标的替换为SPY/QQQ；147只版本随后只改变symbols列表。保留原示例async调用顺序与订阅后10秒等待 |
| 官方wheel及加载模块校验 | 本轮 `environment-verification.json` verified=true，issues为空；使用已核验隔离集成 `.venv-m15` 解释器 |
| 默认Config与隔离边界 | 官方 `Config.from_oauth(oauth)` 默认，无region/endpoint覆盖；python隔离模式，示例无项目imports。外部deadline为60秒，两次子进程均已退出 |
| 标的有序列表、订阅类型、开始/结束/退出 | 两次输入及结果存私有归档 `20260919-official-baseline/default-two-quote/` 与 `default-147-quote/`；后者inputs.json固定147只原有序列表、变化仅symbols |

## 实测结果

| 阶段 | 唯一变化 | 结果与可证明范围 |
|---|---|---|
| SPY/QQQ Quote | 官方最小基线 | UTC09:30:19.366—09:30:30.422，11.056秒exit0；SPY/QQQ各1条首推。休市首推不是连续行情 |
| 147只Quote | 仅标的数量 | UTC09:31:18.399—09:31:49.029，30.630秒exit1；OpenApiException/request timeout，traceback在await ctx.subscribe，回调0，非60秒外部deadline |
| 官方同步147只Quote对照 | 仅改官方同步/异步接口用法，保持默认配置、147只列表及Quote类型 | UTC09:34:05.545—09:34:16.349，10.804秒exit0；147只Quote各1条首推，无stderr，非连续行情证明 |
| 异步147只Quote交叉复核 | 与首次异步147只源码逐字节一致，组成A-B-A对照 | UTC09:35:33.460—09:36:04.189，30.730秒订阅request timeout，回调0；子进程已退出 |
| 同步147只Quote+Trade扩展 | 以上一步成功同步Quote为基线，仅新增Trade订阅及对应公开接口回调；明确是官方公开接口扩展，不是官方示例原文 | UTC09:37:22.951—09:37:33.856，10.904秒exit0；147条Quote首推、Trade 0、stderr 0。周末无Trade不判失败，也未证明逐笔连续接收 |
| 异步context配同步回调 | 相对异步147只基线仅将async def on_quote改为def on_quote，区分回调调度与context路径 | UTC09:38:05.790—09:38:36.422，30.632秒订阅request timeout，Quote回调0；子进程已退出 |
| 同步初始化后查询扩展 | 在成功同步Quote+Trade基线上新增一次subscriptions及一次quote(147)，不是官方示例原文 | UTC09:42:18.055—09:42:29.060，11.005秒exit0；147条订阅每条含Quote+Trade，完整覆盖147只，快照147；Quote对象输出147、Trade 0、stderr 0 |

官方异步示例来源：[v4.5.0 subscribe_quote_async.py](https://github.com/longbridge/openapi/blob/v4.5.0/examples/python/subscribe_quote_async.py)。私有证据根目录为 `/home/hgl/project-archives/Price-Action-Trader/20260919-official-baseline/`，源记录为source-provenance.json，实际源码及run_result.json各随实验独立保存，不包含公开凭证。

前7项未给官方示例加入逐阶段日志，只有外部开始/结束、输出回调、异常栈和退出证据；第8项仅打开官方原生日志，见后文。前7项的创建耗时、成功订阅耗时及各自精确时间戳未单独测量，不声称已取得。示例保留订阅后sleep(10)，11.056秒及10.804秒是进程总耗时，不是订阅本身耗时。无结果不填0或成功；未知SDK内部事件继续记未知，不把一次进程或上下文当作零内部重连证明。

初始化后查询这次未卡死，但stdout多参数输出与回调交错；147条订阅记录与147只覆盖可准确核验，Quote只按对象repr计数147，不按交错行归属股票。不能把查询成功等同同步SDK在高频盘中不会阻塞回调。可提交的脱敏结果索引由主代理维护于 [官方基线证据](evidence/20260919-official-baseline-results.json)。

## 官方原生日志观测与本轮收口

第8项 `default-147-sync-official-logging-008` 复用同步Quote+Trade扩展源码，仅启用官方 `LOG_PATH`：UTC10:05:46.198—10:06:17.029，总30.831秒，SDK订阅超时，Quote/Trade回调均0，子进程退出。默认SDK实际连接 `.cn` 行情主机，没有手工覆盖端点。日志中认证及UserQuoteProfile响应成功，订阅额度500；10:05:46.979897Z出现SubscribeRequest，之后未记录成功订阅响应或原生推送。

结合官方v4.5源码，RequestTimeout表明请求已过本地限流并进入SDK内部发送队列，等待请求完成超时；这不证明数据已发到网络，更不证明服务端没有响应。SDK内部、网络与服务端仍需区分。本轮8项独立示例及3项项目探针全部收口，不再连接，pipeline、部署、解锁及自动启动保持暂停。官方文档上限500，当前147只一次订阅足够，不再引入无依据分批。脱敏时间线、运行命令形态及源码哈希见 [官方支持说明](decisions/20260919-official-sdk-support-brief.md)，未发送外部消息。

## 离线验证与边界

最终组合验证为795通过、2跳过，并非重新完整运行797项：先前798项包含1条绑定旧批次变量的过时源码断言，移除该冗余断言并加强假SDK行为检查后，相关147项重跑全部通过。跳过项为需要显式官方环境/wheel路径的2项原包兼容性验证；环境来源核验另有本轮证据。依据私有 `offline-regression-consolidated-summary.json`；完整旧看板7项历史基线失败仍未解决，不称全仓测试全绿。离线147只/78边界/11466条回放不计真实交易日，当前真实完整日仍0。

## 接回项目原始接收器的单变量对照

这三次使用项目raw探针，不是官方示例原文；同为同步QuoteContext及行情Config默认，单一连接，子进程结束已确认，无账户/订单访问。

| 实验 | 结果 |
|---|---|
| `project-raw-sync-147-001`，保留旧每批50只 | UTC09:51:48.180—09:52:19.083，总30.978秒；首批50只请求30.865秒SDK request timeout，回调0。说明仅更换同步API尚不足以令旧分批方式通过 |
| `project-raw-sync-single147-002`，私有配置只改50→147 | UTC09:54:17.840—09:54:47.891，总30.124秒；单次订阅0.731秒完成，覆盖147/147，raw/normalized/enqueued/dequeued Quote各147；周末未证明持续Trade或K线 |
| `project-final-raw147-003`，候选正式单次147只实现 | UTC10:01:32.608—10:02:03.310，总30.774秒；单请求30.659秒再次SDK request timeout，0回调，worker已退出。证明此前单次成功不足以保证该组合解决问题 |

早先成功曾作为实施“官方同步Config默认＋一次提交147只”候选的依据；最终同组合失败构成反证，不能再将其称为已解决、只差交易日验证或将原因锁定为API/分批。候选删除旧批次配置与循环、单次订阅≤500（生产仍147）的代码仅供审查，pipeline尚未启动且已暂停；不扩容、不宣称500实测。

## 独立审查误启动事件（不计入示例结果）

只读import运行模块时缺少主入口限定导致CLI误启动，已中断回收。实际完成模拟账户只读快照，行情日线49/147、订阅0/147；dispatch未请求/未开启，提交日志0字节，主树5个受保护文件哈希不变。此事不属于授权诊断或验收，不得把整个本轮说成未访问账户；上述官方独立示例本身未访问账户。入口限定及真实import哨兵测试已补，验收范围及复核记录见独立事件说明，详见 [独立事件记录](decisions/20260919-runtime-import-start-incident.md)。

## 后续判断与验收

本次147只官方异步示例在零项目导入、默认Config下也失败，排除策略/K线作为此次初始化失败前置起因，也不能只归责旧项目封装。尚未判定SDK、网络、权限或服务端原因；与9月8日开盘断流同根因仍未证明。A-B-A交叉复核中异步再次失败，同步Quote及Quote+Trade均能完成当次订阅；仍不能把该路径差异直接等同异步SDK缺陷。仅把异步context的回调改为普通def仍失败，无法用“只有async回调写法不对”解释。该异步路径三次失败已形成 [Failure Dossier](decisions/20260919-official-async-baseline-failure-dossier.md)，不再连接尝试。无新证据不重复同条件连接，不自动换源。

最后一项独立同步示例只启用官方LOG_PATH，仍在订阅阶段超时；8项独立示例与3项项目探针现已收口，停止连接。下一步依据 [脱敏官方支持说明](decisions/20260919-official-sdk-support-brief.md) 请长桥核对订阅响应时序；材料未对外发送。无新的区分依据不继续同条件连接、不改策略或新写法试错。

当前候选实现为官方同步 `QuoteContext`、行情默认Config、一次提交整个订阅集合（≤500，生产池仍147），移除旧异步行情桥接、批次配置及分批循环；队列、K线、策略不改，父进程增加/保留阶段期限与强制退出确认，账户异步客户端及其helper不动。不是同步/异步自动回退，不声称全项目零手工端点。候选离线测试可继续验证代码安全性，但真实订阅初始化仍未通过；pipeline、正式部署、解锁和自动启动保持暂停，观测证据补齐前不称修复完成。

正式程序未因本实验获得部署或启动资格，原故障锁及订单防重保留，官方示例无订单操作；另有上述误启动的模拟账户只读访问事件，其运行记录无派发且提交日志为空，不能与示例结果混算。未清仓、未恢复正式自动启动。仍须在真实交易日证明147只、78个五分钟边界、11466条分类明确实时K线进入策略；一个完整日提供恢复资格依据，连续三个完整日才称稳定。休市示例验证不取代上述门禁。
