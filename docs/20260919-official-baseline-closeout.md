# 官方接入核验及工作区收尾（2026-09-19）

**行情问题仍未解决，候选代码未部署，真实完整交易日通过0天。** 本文补充本轮清理结果，不覆盖原始实验记录。官方允许单次最多500只，当前147只一次订阅；不再凭经验分批，也不将单次成功解释为修复成功。

## 已执行与保留

| 对象 | 结果与依据 |
|---|---|
| 主线 `main` | 仅合入最新事实、计划和证据；生产运行源码仍冻结，不恢复启动 |
| `codex/fix-official-example-baseline-20260919` | 保留为当前唯一修复候选，已推送供审查；删除旧行情异步桥接、批次参数和分批循环，使用官方默认Config和单次订阅；不是已通过部署的修复 |
| `feature/m7-broker-api-assessment` | 按仓库约定保留历史里程碑 |
| 旧 `integration` 工作区 | 完整归档3561个文件，882344374字节；逐文件和归档包读取核验通过。收尾后变化的3个忽略文件另存增量归档 |
| 旧集成分支 | 原提交 `c62f4e8ffde78658f56ec859ab31fe6fa5e9283d` 是候选祖先；删除前复查远端一致，按该SHA限定删除。对应本地分支已删除 |
| 工作区/暂存 | 收敛至主工作区与官方基线工作区；长期保留本地/远端各3分支，暂存0；本次事实文档临时分支合入后收尾 |

移除工作区时，先逐一校验并清理清单中的1165个忽略文件，再执行普通 `git worktree remove`，没有全目录强制清空。历史提交另有完整Git bundle且验证可读。旧集成环境随工作区退役，独立实验当时使用的解释器路径保留为历史执行记录，不作为后续启动入口。

私有归档：`/home/hgl/project-archives/Price-Action-Trader/20260919-official-baseline/`。其中 `integration-retirement/manifest.json`、`verification.json`、`final-ignored-manifest.json`、`completion.json` 可核对内容、校验及删除原因；`pre-convergence.bundle` 保存收尾前Git历史。此前大规模清理和9041份独有资料保全仍见 [原清理记录](20260919-cleanup-and-marketdata-repair.md)。所有离线归档只保存成果和证据，不提供旧运行方式回退。

## 验证及未通过项

- 官方独立示例8项、项目原始接收探针3项均已结束。最终官方同步示例及最终项目单次147只版本仍在订阅阶段超时；所有成功、失败记录均保留。SDK内部请求入队后等待完成超时，不等于已发上网络或服务端未回复。完整分析见 [实验记录](20260919-official-example-baseline.md)。
- 候选离线组合验证795通过、2跳过，相关147项在最终测试改动后重跑通过；不是完整797项重新跑过。候选治理8项及扫描通过；主线事实文档治理6项及扫描通过。旧看板7项历史基线失败仍单列，不宣称全仓全绿。
- 主线5份故障、启动审计及订单防重状态哈希不变；没有通过删除故障文件恢复运行。Windows旧计划任务及4个启动文件均不存在，16个退役systemd路径不存在。系统服务状态查询不可用；进程扫描仅覆盖当前可见 `/proc`，不能将有限扫描写成整台宿主无进程。排他行情锁检查空闲。
- 主线安装包只读完整性核对通过：5个包文件及METADATA共6项与已固定官方wheel逐项相同，解释器和native模块校验一致，无额外源码或补丁标记。主线正式来源核验程序、trust config及receipt尚未部署，原receipt缺失记录保留，不为通过检查临时补签。安装包完整性和正式启动资格分开核对；候选来源核验通过，也不构成生产运行许可。原终检及补充结果分别存于私有 `final-runtime-state-verification.json` 和 `main-package-integrity-supplement.json`，应联合解读。
- 本轮曾发生审查import意外启动并读取模拟账户，已停止、留证并修正入口限定；没有派发订单，主线保护状态不变。不可把整轮说成零账户访问，见 [事件说明](decisions/20260919-runtime-import-start-incident.md)。

## 下一步

1. 以 [脱敏支持说明](decisions/20260919-official-sdk-support-brief.md) 及可复现材料核对官方SDK请求完成、网络传输和服务端订阅响应；材料仅本地整理，尚未对外发送。没有新的区分依据，不重复同条件连接。
2. 取得能区分原因的证据后，只处理对应故障点，再验证独立接收和项目接收；继续沿用官方支持的接口和默认方式，不引入分批、换源或运行回退。
3. 订阅问题解决后才恢复完整管道验证，证明147只、78个边界、11466条分类明确的实时K线进入策略。一个完整交易日提供恢复资格依据，连续三个完整交易日才称稳定；订单恢复另核有效授权。

当前主线事实与候选实现已明确分离。主线冻结的旧源码不是备用运行路径；未完成真实接收验证前，不把候选部署到生产，不解除故障锁或恢复自动启动。
