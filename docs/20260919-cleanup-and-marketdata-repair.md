# 2026-09-19 清理与行情修复记录

## 当前结论

**历史工作区、旧环境已按清单清理，官方环境已建立并核验；行情中断尚未解决，真实完整日通过数仍为0。** 干净官方环境的SPY/QQQ两只股票120秒只读接收成功，原147只接入连续三次失败后已熔断同条件尝试；另行复核的小批次区分实验成功订阅前60只并收到446条原始回调，但在CMCSA/WBD批次失败；该标的对单独可成功，旋转相同147只列表后又在累计80只后的另一批失败。问题已独立复现于官方SDK订阅阶段，供应商/SDK/网络根因仍未确定，本轮连接实验已停止。仍未完成147只订阅或进入生产K线/策略验收。

主线已合入事实纠正文档，运行程序源码仍为原 `a413cb8` 基线。程序修复只在隔离集成工作区；正式程序尚未部署，自动启动未恢复，原故障锁未解除。环境建立、文档纠正、程序部署和行情整场通过是不同状态。

策略保持8条可执行、3条影子合同，生产池147只。9月8日临时订单授权已到期，本轮未下单、未撤单、未清仓；不改系统代理，不增加数据源回退。

## 一、清理结果

私有证据根目录为 `/home/hgl/project-archives/Price-Action-Trader/20260919-cleanup/`。以下引用为该目录内文件名，仅记录核验结果，不将凭证、账户记录或归档内容提交到仓库。

| 对象 | 已核验结果 | 证据 |
|---|---|---|
| 原34个工作区及旧主环境 | 工作区文件与元数据校验完成，旧 `.venv` 单独归档；Git bundle独立克隆和完整性检查通过，332个冻结引用可解析 | `archive-verification.json`、`verified-bundle.json` |
| 旧工作区 | 删除33个，跳过/失败0；保留主工作区及本轮必要工作区 | `worktree-removal-results.json` |
| 本地历史分支 | 归档后删除191个，全部成功；保留main、M7历史及当前任务分支 | `local-branch-removal-results.json` |
| 远端历史分支 | 提交复核后原子删除106个，跳过0；保留main与M7历史 | `remote-removal-result.json` |
| 暂存 | 2份已归档后清除，剩余0 | `stash-cleanup-result.json` |
| 历史草稿PR #1 | 成果与讨论归档后关闭，状态CLOSED | `pr-1/manifest.json`、`pr-1/closed-result.json` |
| 独有知识资料 | 9,041个assets文件恢复到主工作区原知识库层级，文件哈希及新文件元数据均通过；源归档未改 | `unique-assets-migration-result.json` |
| 旧运行残留 | 删除8项：本地修改版 `.venv`、3个旧工具目录、3个DNS注入配置及1个注入动态库，跳过0 | `legacy-runtime-cleanup-results.json` |
| 旧SDK源码与构建wheel | 额外6项全部归档校验后删除：1份旧SDK源码目录、5份wheel目录；跳过0，不含已核验官方wheel与 `.venv-m15` | `sdk-build-retirement/results.json` |
| 旧临时实验环境 | 另外3份旧临时环境归档核验后删除；不把它们都归类为修改包，保留主树及必要集成树的 `.venv-m15` 与正式wheel库 | `legacy-experiment-environment-retirement.json` |
| 本轮任务收尾 | 4个已集成主题工作区及对应本地分支归档后删除；最终仅主树和integration两树，本地及远端各3分支（main、M7历史、当前integration），暂存0 | `task-worktree-retirement.json`、`cleanup-worktree-retirement.json`、`final-cleanup-inventory.json` |
| 旧自动启动 | 旧登录入口、退役M12调度单元已移除，看护进程已停止；尚未恢复正式启动 | `production-writers-frozen.json` |
| 关键保留状态 | 提交日志、原故障运行状态、启动审计、正式测试编号及虚拟账户编号文件均存在且与冻结归档哈希一致；凭证和其他未列入清理的运行目录保留 | `legacy-runtime-cleanup-results.json`的清理后检查 |

归档排除可重建的Python/测试缓存；保留文件内容与修改时间等已列明元数据，硬链接保存为内容相同的独立副本，不宣称保存inode关系或访问时间。归档仅保存历史成果和证据，不作为旧行情运行退路。最终保留的integration分支已推送远端，保存已独立审查的实现和诊断；因行情仍未通过而保留，不能将其删除或当作生产已部署。4个本轮主题工作区的收尾数量不与33个旧工作区混算。

核验限制：4个root系统进程的部分进程信息不可读，已在清单逐项记录；旧Windows任务XML未成功保存，已保留旧入口脚本及删除后独立核验，不宣称保存全部任务元数据。最终两个工作区均干净，9,041份资料与5份关键运行状态再次通过哈希复核。

## 二、故障分析与环境结果

**今日干净官方SDK复现的是订阅初始化失败；9月8日则是完成初始化后的开盘断流。尚未证明两者同根因。** 今日原始接收程序独立失败排除了策略/K线处理作为今日初始化失败的起因，不等于已证明历史断流唯一根因。

| 检查项 | 已知结果与限制 |
|---|---|
| 原始故障 | 9月8日21:30:30北京时间因参考推送停止更新而停机，完整五分钟边界0；详见 [现场记录](decisions/20260908-opening-marketdata-failure.md) |
| 原软件来源 | 原 `.venv` 是本地构建包；仅查4.5.0版本和项目源码不足以证明官方原包。该差异本身仍不能证明断流原因 |
| 官方安装文件 | 已从PyPI分发源取得4.5.0官方wheel并核验SHA256；主树及隔离集成 `.venv-m15` 的SDK二进制哈希均与已核验原包一致 |
| 隔离环境签发 | `official-sdk/staging-receipt-issuance.json`及`staging-receipt-verification.json`均verified=true、issues为空；包含解释器、官方wheel及已安装文件校验。只证明staging来源，不代表正式部署获准 |
| 两只股票诊断 | 纽约9月18日12:36:45至12:38:45（北京时间9月19日00:36:45至00:38:45），SPY/QQQ 2/2订阅，120.028秒完成；原始报价556条、逐笔554条，无账户或订单访问 |
| 147只诊断 | 三次失败：原始探针30.034秒TimeoutError；阶段探针30.134秒明确停在subscribe；生产管道对照60.062秒，真实日线147只/8820条通过后首批50只订阅AsyncQuoteBridgeError。三次回调记录均0，第三次订阅进度0、K线0，后两次worker退出已确认。已熔断，见 [Failure Dossier](decisions/20260919-subscription-initialization-failure-dossier.md) |
| 小批次区分实验 | `raw-147-batch2-003`于16:49:22—16:49:59 UTC运行36.784秒；每批2只，前30批60只成功，记录446条原始回调。offset60的CMCSA.US/WBD.US批次耗时30.755秒后返回OpenApiException，类别sdk_or_runtime_error，并非45秒外层等待超时；worker已正常退出，未强制清理 |
| 失败标的对放首批 | `raw-pair-first-004`于16:55:50—16:56:50 UTC成功完成60.164秒；CMCSA/WBD首批订阅0.702秒、覆盖2/2、174条原始回调，worker已正常退出。排除这对标的必然无法订阅的解释，不证明容量根因或147只恢复 |
| 旋转顺序规模对照 | `raw-147-rotated-005`保持同147只集合、每批2只及45秒单请求期限，仅从原offset60旋转顺序；16:58:35—16:59:13 UTC运行38.759秒，前40批80只成功、581条原始回调，第41批offset80 MRVL/ADI耗时30.736秒后OpenApiException失败，worker已退出 |
| 根因结论 | 两只实时推送与147只历史读取可工作，排除完全不可达；故障范围为订阅初始化；小批次实验进一步定位到累计60只之后的CMCSA/WBD请求，该标的对单独放首批成功，旋转顺序又在不同标的及80只后失败，排除固定CMCSA/WBD必然失败和固定60只上限的解释。尚不能区分SDK、网络或上游请求处理。具体权限、SDK、网络或上游首推批处理原因未定。官方数量上限500只，147只不超此上限，不等于账户或请求必然正常 |

官方wheel SHA256为 `5387db07db2d29dd52ff6abcd5e888f1442cd0581ad38dc5e51182a0a9cd8daa`，已安装SDK二进制SHA256为 `4af34d03b87402e680630e2e0f5e11612d84ce22ac12a8dcd3a18886d094d185`；对应私有记录 `official-sdk/verified-artifact.json` 与staging校验记录。成功两只及三次147失败摘要保存在各自 `live-diagnostics/` 子目录；三次失败核心证据SHA256已冻结在Failure Dossier，不覆盖原记录。

最后异常的安全记录为kind=Other、code=null；request_timeout仅按消息关键词分类（message_keyword_not_root_cause），不能据此认定网络或供应商根因。可交长桥的无密钥摘要见 [订阅诊断摘要](decisions/20260919-longbridge-subscription-diagnostic-summary.md)，仅备好材料，尚未发送外部消息。

已将摘要、精确有序股票输入、官方安装文件信息及脱敏错误整理为私有归档中的 `longbridge-support-package.zip`（12个文件，SHA256 `58f5abe63dba8f9e17b96678b3e1a5b20051cd0274d8d5e46d894616a2819758`）。ZIP完整性、内部11项哈希和脱敏内容经独立复核；它是协查证据包，不是独立安装程序，尚未外发。

这些原始回调探针未评估K线生成或策略消费，均标记 `production_acceptance=false`；SDK原生接收任务及内部重连状态仍为unknown，不虚构零重连。

## 三、运行验收结果与下一步

| 验收项 | 当前结果 |
|---|---|
| 历史清理、资料保全、官方环境来源 | 上述清单及本轮4个主题工作区收尾已核验；只保留main与必要integration工作区 |
| 离线代码验证 | 集成回归807项：805通过、2跳过；治理8项及敏感信息/大文件/差异扫描通过。离线147/78/11466回放不替代实时验收；完整旧看板仍有7项基线失败 |
| 程序修复及正式部署 | 隔离集成中，尚未部署；自动启动未恢复，故障锁未解除 |
| 官方两只股票短时只读接收 | 120秒通过，仅为原始回调证据 |
| 官方147只只读接收 | 原任务三次失败后熔断，独立区分实验最终仍失败；原序最多60只、旋转序最多80只成功，未完成147只，本轮连接实验已停止 |
| 147只、78边界、11466条分类明确实时K线 | 未完成；本轮真实整场通过0天 |
| 数据持续进入策略、账户<45秒、信号P95<1秒 | 尚无本轮整场验收证据 |
| 连续三个完整交易日稳定 | 未完成；不得称行情稳定 |

三次原任务证据保持冻结，不覆盖Failure Dossier。本轮后续区分实验已结束，不再继续更换参数连接。下一步用已备好的无密钥摘要核对供应商订阅响应时序：核查对应UTC时间请求是否收到、Quote/Trade及首推是否发出、权限/请求节流/连接占用是否有服务器记录；同时要求确认官方SDK对成功回复和超时的处理。尚未发送外部消息，不把供应商协查写成已完成。

只有新增服务器记录或可区分假设，才制定下一项单次实验；不任意放宽生产超时、不删股票绕过147目标、不切换SDK私有补丁或行情来源。离线验证日志为私有归档 `integration-m15-verified-tests.log` 与 `integration-final-governance.log`；全看板基线限制见退役路径交接。统一验收快照 `runtime-acceptance-result.json` 保存7份诊断摘要哈希，明确 `status=not_resolved`、生产未部署/未解锁/未恢复启动、真实整场0天。

确定并修复原因、通过针对性测试及独立复核后，才按正式流程签发部署、恢复唯一入口和重新启动。随后验证数据接入策略并覆盖完整交易日；一个完整日通过只提供恢复资格依据，连续三个完整日才称稳定，不延续9月8日订单授权。

项目仍不自动重试行情、不重建连接、不切换来源、不自动恢复交易。原始故障和待确认订单防重记录保留，不靠删除状态解除锁定；无成交延续K线不得开仓，没有合格信号允许零订单。
