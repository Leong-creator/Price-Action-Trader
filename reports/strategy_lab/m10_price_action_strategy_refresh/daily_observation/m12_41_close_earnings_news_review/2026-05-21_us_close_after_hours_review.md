# 2026-05-21（美东）收盘/盘后 财报与新闻回顾（M12）

> 说明：本报告只引用公开新闻/公告 + 本项目只读扫描与模拟盈亏产物；不接账户、不下单、不提供实盘建议。带“待确认”的条目表示信息源在当前运行时点未能完全核对或需要二次核验。

## 1）今天模拟测试结果（一句话）

- 扫描交易日 `2026-05-21`：主线模拟当日盈亏 `+622.66`（权益 `157,516.60`），实验模拟当日盈亏 `-327.73`（权益 `201,280.83`）；只读盘后状态，夜盘不做正式信号确认。

## 2）盘后财报/新闻大波动（重点）

### 2.1 盘后财报日历核对（Nasdaq earnings calendar）

- 以 `2026-05-21` 的公开财报日历核对：盘后（after-hours）披露名单 **未包含** 本项目 First50 股票池中的标的；因此本轮“财报导致的盘后大波动”，对 First50 的直接覆盖压力较小。
  - 备注：该日盘后披露的公司包含 `ROST/TTWO/CPRT/WDAY/ZM` 等（均非 First50；也未出现在今日候选列表中）。

### 2.2 盘后/夜盘大幅波动（阈值监控：>=3%）

- 项目只读的盘前/盘后异动监控（阈值 `3%`）显示：
  - 盘前异动：`USO` `+3.47%`（监控命中）
  - 盘后异动：`0` 条（可能是窗口不完整或数据源限制；见“待确认”）
- 待确认：公开“盘后 top movers”页面抓取在近期存在不稳定/403 的情况，本次运行未做逐条外部页面对账；若你在券商/行情软件看到 First50 内其他标的大幅夜盘跳动，建议手工二次核对并标记到复盘（但仍只做观察，不扩池、不接实盘）。

## 3）这些股票是否被今天策略候选检测到

- `GOOG/GOOGL`：均在今日候选中（各 `3` 条）。
- `SPY/QQQ/IWM`：均在今日候选中（各 `2` 条）。
- `USO`：在今日候选中（`2` 条），且与盘前异动监控一致（同为 `USO`）。

## 4）哪些机会漏掉了以及原因（只谈只读与覆盖，不做交易建议）

- 本次“财报盘后披露名单”与 First50 **几乎无交集**：如果你观察到 First50 内盘后大波动，更可能来自宏观/行业消息、二次解读、或隔夜流动性，而非“当晚披露财报”这一单点原因；需要靠次日 regular session 才能更稳定归因（本项目也以 regular session 为主做结构确认）。
- 盘后异动监控为 `>=3%` 阈值：小于阈值的波动不会出现在监控 headline 里，但仍可能影响次日开盘结构；这属于 **阈值与窗口设计**，不是扫描漏检。
- `M12-FTD-001`（原版/连亏保护）当日为小幅回撤：更适合作为“风控与连亏保护是否按预期触发”的样本，而不是追求事件日表现。

## 5）明天盘前需要重点盯的股票（只读风险清单）

- `USO`：既出现盘前 >=3% 异动、又在候选中；优先核对“异动是否延续到盘前/开盘”以及对应的形态解释是否一致。
- `GOOG/GOOGL`：保持常规必查（候选已覆盖），重点看是否出现非预期 gap/放量（若出现则标记为待确认事件驱动）。
- `SPY/QQQ/IWM`：作为大盘 ETF 必查，重点看是否出现“隔夜跳空导致的形态切换”，避免把隔夜跳变误当作盘内结构。
- `PANW/TSLA/SNOW/NVDA/QCOM`：今日候选频次较高的标的（更可能被你每天复盘反复遇到），盘前若有扩散波动，优先做“候选解释一致性”检查。

## 6）对策略测试的影响（只谈测试与归因）

- 本日更像“隔夜波动/阈值监控 + 候选覆盖一致性”的回归样本：重点验证
  - 候选是否覆盖了你规定的必看标的（`GOOG/GOOGL`、`SPY/QQQ/IWM`、First50）；
  - 异动监控与候选是否对同一标的一致命中（本日 `USO` 一致命中）；
  - 事件窗口（盘前/盘后）不做正式信号确认的边界是否被正确执行与记录。
