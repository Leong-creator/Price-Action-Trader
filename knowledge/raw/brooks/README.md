# Brooks Sources

目录：

- `ppt/`：放 Brooks PPT、导出 PDF 或图片资料。
- `extracted_text/`：放从 Brooks 资料提取的文字。
- `transcribed_v2/`：放 Brooks v2 手工转录知识库副本。`units/` 与 `evidence/` 文本文件可进入 Git；`assets/evidence/` 图片资产按 local-only + checksum 管理。

原始资料只归档，不在本目录做改写。
