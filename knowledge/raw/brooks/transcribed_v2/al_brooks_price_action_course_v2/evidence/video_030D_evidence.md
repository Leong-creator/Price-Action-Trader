# Evidence: Video 30D Trader's Equation and Probability - 50% Pullbacks and Improving Math After Breakouts

来源范围：`part2 p958-p982`。

本文件列出本章使用的证据页。正文没有插入标题页、目录页、重复递进页、回顾页和结束页。

## Used In Chapter

- `part2 p960`: `assets/evidence/video_030D/part2_p0960.webp`，强趋势中 `50% PB` 风险回报比中性但高概率；多头趋势中 `50% PB` 至少 `60%` 概率先测试高点再触发低点下方止损；多头在 `50% PB` 限价买或向上反转 `stop` 买，入场在目标与止损中点，风险约等于回报。
- `part2 p962`: `assets/evidence/video_030D/part2_p0962.webp`，空头趋势中 `50% PB` 至少 `60%` 概率先测试低点再触发高点上方止损；反弹动能常强到让人怀疑是反转；空头在 `50% PB` 限价卖或向下反转 `stop` 卖，风险约等于回报。
- `part2 p963`: `assets/evidence/video_030D/part2_p0963.webp`，Emini 空头趋势中卖 `50% PB`；`bear leg` 为 `9.75 points`，`50% PB` 约 `4.50`；约 `60%` 概率赚 `4.50 points`，`40%` 概率亏 `4.75 points`，高点上方 `1 tick` 止损；`60%+` 概率亏 `10 ticks` 前赚 `10 ticks`，甚至可赚 `30 ticks`。
- `part2 p964`: `assets/evidence/video_030D/part2_p0964.webp`，高概率通常意味着坏 `RR`，小回报、大风险或两者兼有；即使 `scalp` 也可有正 `Trader's Equation`，`scalp` 指 `reward` 少于 `2x risk`；多数交易者仍应 `swing`，`reward` 至少 `2x risk`。
- `part2 p965`: `assets/evidence/video_030D/part2_p0965.webp`，EURUSD 多头趋势中买 `50% PB`；`bull leg 64 pips`，`50% PB` 为 `32 pips`；约 `60%` 概率赚 `32 pips`，`40%` 概率亏 `33 pips`；`60%+` 概率亏 `33 pips` 前赚 `32 pips`，甚至可赚 `50 pips`。
- `part2 p966`: `assets/evidence/video_030D/part2_p0966.webp`，EURUSD 多头腿 `52 pips`、`PB 28 pips`；买 `50% PB` 约 `60%` 概率赚 `26 pips`，`40%` 概率亏 `27 pips`；高概率通常意味着坏 `RR`，但多数交易者仍应 `swing`，`reward` 至少 `2x risk`。
- `part2 p968`: `assets/evidence/video_030D/part2_p0968.webp`，强 `BO`、好背景所以高概率；Emini 强 `bull BO` 收盘高于 `TR`，`TR` 表示 `BO Mode`；`60%+` 概率获得至少等于风险的回报；买收盘，止损在 `bull BO` 下方 `1 tick`，即入场下方 `8 ticks`，最低目标 `8 ticks`。
- `part2 p969`: `assets/evidence/video_030D/part2_p0969.webp`，随着 `BO` 增长，盈利目标增加；第二次买入新最低目标 `14 ticks`，止损距新入场 `14 ticks`；`60%` 概率先达 `14 ticks` 目标；对第一次买入者，止损仍只低 `8 ticks`，目标高 `20 ticks`，因此 `60%` 概率赚 `20 ticks`、风险 `8 ticks`。
- `part2 p971`: `assets/evidence/video_030D/part2_p0971.webp`，突破和回报增长但风险相同；最新买入新最低目标 `23 ticks`，止损距最新入场 `23 ticks`；对第一次买入者，止损仍只低 `8 ticks`，目标高 `38 ticks`，因此 `60%` 概率赚 `38 ticks`、风险 `8 ticks`。
- `part2 p972`: `assets/evidence/video_030D/part2_p0972.webp`，测量移动目标处有获利了结但仍处于多头趋势；第一次收盘买入者风险从未超过 `8 ticks`，随着 `BO` 发展，至少 `60%` 概率获得 `38 ticks` 利润。
- `part2 p973`: `assets/evidence/video_030D/part2_p0973.webp`，USDCAD 强 `bear BO` 收盘低于 `TR`，趋势背景良好；`60%+` 概率获得至少等于风险的回报；强 `BO` 高概率但可能低回报/高风险；`scalp` 方程式可正，但多数仍应 `swing`，`reward` 至少 `2x risk`。
- `part2 p974`: `assets/evidence/video_030D/part2_p0974.webp`，USDCAD 高概率做空；卖 `BO bar` 收盘，止损在 `bear BO` 上方 `1 pip`，即入场上方 `19 pips`；承担 `19 pips` 风险，因此 `60%` 概率获得 `19 pips` 回报，最小目标 `19 pips`。
- `part2 p976`: `assets/evidence/video_030D/part2_p0976.webp`，早期卖家目标增加但风险不增加；`BO` 增长，空头持续 `sell closes`；新目标 `34 pips`，止损距新入场 `34 pips`；对第一次卖家，止损仍只高 `19 pips`，目标低 `49 pips`，因此 `60%` 概率赚 `49 pips`、风险 `19 pips`。
- `part2 p978`: `assets/evidence/video_030D/part2_p0978.webp`，利润目标随 `BO` 增长；新目标 `59 pips`，止损距新入场 `59 pips`；对第一次卖家，止损仍只高 `19 pips`，目标低 `99 pips`，因此 `60%` 概率赚 `99 pips`、风险 `19 pips`。
- `part2 p980`: `assets/evidence/video_030D/part2_p0980.webp`，在测量移动目标获利；最早高概率做空者风险从未超过 `19 pips`，第三次卖出信号时至少 `60%` 概率获得 `99 pips` 利润；向上反转显示空头在 `Sell Climax` 后 `MM target` 部分或全部获利。

## Boundary And Non-Inserted Pages

- `part2 p958`: 视频标题页。
- `part2 p959`: 目录页，列出 `50% pullback` 与 `Math can improve after BO`。
- `part2 p961`: 纯图/重复说明页，内容由 `p960` 和 `p962` 吸收。
- `part2 p967`: 中段目录页，进入突破后数学改善主题。
- `part2 p970`: 重复递进页，内容由 `p969` 与 `p971` 吸收。
- `part2 p975`: 重复说明页，内容由 `p973-p974` 吸收。
- `part2 p977`: 重复递进页，内容由 `p976` 吸收。
- `part2 p979`: 重复递进页，内容由 `p978` 吸收。
- `part2 p981`: 回顾页。
- `part2 p982`: 结束页。
