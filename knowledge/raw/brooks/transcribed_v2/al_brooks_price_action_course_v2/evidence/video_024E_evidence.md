# Evidence: Video 24E Wedges - Failed Wedges, Breakouts, and Measured Moves

来源范围：`part2 p653-p666`。

本文件列出本章使用的证据页。正文没有插入标题页、目录页、回顾页和结束页。

## Used In Chapter

- `part2 p655`: `assets/evidence/video_024E/part2_p0655.webp`，失败楔形反转；空头楔形原本更可能 `bull BO`，但市场反而在 `bull flag` 下方 `bear BO`，走较低概率路径后预期快速突破、两腿和 `measured move`。
- `part2 p656`: `assets/evidence/video_024E/part2_p0656.webp`，失败楔形反转的镜像；多头楔形原本更可能 `bear BO`，但市场反而大 `bull BO`，预计两腿上涨，空头在第一回调回补，未进场多头在第一回调买入。
- `part2 p657`: `assets/evidence/video_024E/part2_p0657.webp`，失败楔形看跌旗形上方出现 `bull BO`；多头楔形默认更可能 `bear BO`，但形态小且有 `bear bars` 时，多为 `minor BO` 和 `TR`，多数交易者 `scalp`。
- `part2 p658`: `assets/evidence/video_024E/part2_p0658.webp`，小型突破下方的小型突破可构成楔形；`TR` 内许多大 `bear bars` 表明卖压强，`LH/LL` 形成早期 `bear trend`，三次下推是 `wedge bull flag`，`bear BO` 后预期两腿横向至向下和 `MM down`。
- `part2 p659`: `assets/evidence/video_024E/part2_p0659.webp`，楔形顶部上方 `BO` 有 `50%` 失败；良好 `wedge top` 和卖压默认 `75% bear BO`，但上方 `BO` 是意外且可能二次顶部尝试，多空平衡，`50%` 成功两腿 `MM up`，`50%` 失败反转。
- `part2 p661`: `assets/evidence/video_024E/part2_p0661.webp`，楔形顶部失败突破；因上方 `BO` 有 `50%` 概率失败反转，一些空头不在 `wedge` 上方退出，而是用宽止损并加仓卖出。
- `part2 p663`: `assets/evidence/video_024E/part2_p0663.webp`，楔形底部下方 `BO` 有 `50%` 失败；良好 `wedge bottom` 和买压默认 `75% bull BO`，但下方 `BO` 是意外且可能二次筑底，多空中性，`50%` 成功 `MM down`，`50%` 失败反转向上。
- `part2 p664`: `assets/evidence/video_024E/part2_p0664.webp`，楔形底部失败突破；因下方 `BO` 有 `50%` 概率失败反转，一些多头不在 `wedge` 下方退出，而是用宽止损并加仓，止损在 `MM down` 下方不远。

## Boundary And Non-Inserted Pages

- `part2 p653`: 视频标题页。
- `part2 p654`: 目录页，列出失败楔形。
- `part2 p660`: `part2 p659` 的文字展开页，正文已并入对应段落。
- `part2 p662`: 楔形底部下方突破导入页，正文并入 `part2 p663`。
- `part2 p665`: 回顾页。
- `part2 p666`: 结束页。
