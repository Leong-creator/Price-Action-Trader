# Fangfangtu YouTube Sources

用户已将方方土视频转录成文档。

目录：

- `transcripts/`：放视频转录文档。
- `metadata/`：放来源链接、标题、发布时间、主题标签、优先级。
- `supplements/`：放后续补充的图表截图、手工标注、关键画面说明。

缺少视频图表不阻塞入库，但 kb_curator 必须在 wiki 或 metadata 中标记 `missing_visuals`。
